{
  "name": "@revnix/reeve",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Agent-ops control plane: durable state, leases, verdicts, and a straight answer about what is true",
  "license": "Apache-2.0",
  "bin": {
    "reeve": "./bin/reeve"
  },
  "engines": {
    "node": ">=24.10.0"
  },
  "scripts": {
    "test": "for f in test/*.test.mjs; do case \"$f\" in */escape.test.mjs) continue;; esac; node \"$f\" || exit 1; done",
    "lint": "eslint .",
    "test:escape": "node test/escape.test.mjs"
  },
  "devDependencies": {
    "@anthropic-ai/sandbox-runtime": "0.0.73",
    "eslint": "^10.9.1"
  }
}
