# Handoff — 2026-08-28

**Written because the session was about to compact.** Its job is to carry every fact, decision and
obligation forward so the next session starts where this one stopped.

**Read this first, then `RESUME-PROMPT-2026-08-28.md`, which is the prompt to paste.**

**Every value here was read, not remembered.** Re-read them before trusting them — this session
spent most of its day correcting inherited facts that had gone stale, including several of its own.

---

## 0. The one-paragraph version

S3's eleven planning documents were written, then **two PRs were opened and worked through six
codex review rounds between them**. `reeve#57` (the test suite's dead network) **merged**. `reeve#58` (the planning
foundation) and `reeve#60` (a merge verifier, split out of #58) are **open, green, and clean of
every finding reported so far** — **36 and 7 review threads**, all resolved. Along the
way the review found **one defect in the design document itself**, and #60's core comparison was
found to be **wrong in the common case** and was redesigned. The next code task is **issue #50**,
whose design is written and reviewed. **No plan document has landed yet, and no S3 task has
started.**

---

## 1. Verified repository state, 2026-08-28

| fact | value | how |
|---|---|---|
| `origin/main` | **`281655a`** — `fix(doctor): R-04 must tell a failing base from one that never ran (#56)` | `git log origin/main` |
| open PRs | **#58** `docs/s3-foundation-core`, **#60** `fix/merge-verifier` — **both mine** | `gh pr list` |
| open issues | **#50, #46, #43**. **#51 has closed** since the last handoff. | `gh issue list` |
| merged this session | **#57** (mine, `89c4af6`), plus peer's #55, #59, #56 | verified by content |
| test baseline | **93 files / 5,131 PASS on `36b4107`**, plus each branch's own additions. #58 at `ffa8edf`: **94 / 5,189**. #60 at `cbff096`: **95 / 5,215**. | run per branch |
| `revnix/reeve` | **PUBLIC** since 2026-08-27 | `gh repo view --json visibility` |
| live guardian | runs from `~/Work/Products/reeve` — **never `git pull` there** | standing rule |

### Worktrees

| worktree | branch | holds |
|---|---|---|
| `~/Work/Products/reeve-wt/core` | `docs/s3-foundation-core` | **PR #58** — the foundation, docs only. Head `ffa8edf`, clean. |
| `~/Work/Products/reeve-wt/verifier` | `fix/merge-verifier` | **PR #60** — the merge verifier. Head `cbff096`, clean. |
| `~/Work/Products/reeve-wt/c4` | `docs/s3-foundation` | **the six S3 stage plans, unsubmitted**, plus this handoff |
| `~/Work/Products/reeve-wt/ghseam` | `fix/test-gh-seam` | #57, merged. Disposable. |

**`c4` is the important one to know about.** It holds the six stage plans (14,167 lines), the
design brief, the audit evidence, the issue-50 design, and the operator-surface research. **None
of it is on `main` and none is in a PR.** It is not lost; it is simply not submitted.

---

## 2. What happened this session

1. **Wrote S3's eleven planning documents** — a master plan (roadmap S3→S12 + authoring spec),
   an implementation prompt, five trackers, and six stage plans, from a fourteen-agent design brief.
2. **Ran a six-lens adversarial review over S3-A and S3-B before any PR**: 55 findings raised,
   **38 survived refutation, 23 of them P1**. Almost all confirmed by *executing* the plans' code.
3. **Built and merged #57** — the daemon tests were waiting on 404s. **550s → 303s**, PASS output
   byte-identical, plus `test/offline-tests.test.mjs` as the relapse guard.
4. **Opened #58**, worked **4 codex review rounds, 36 threads**, all resolved.
5. **Split #60 out of #58** when six findings in one round landed on 60 lines of script inside a
   documents PR. Worked **2 codex review rounds, 7 threads**, all resolved.
5b. **Redesigned #60's core comparison** after review proved it wrong in the common case.
6. **Ran a six-angle operator-surface research pass** for S3-E: 54 recommendations, 41 adopt,
   7 already-right, 6 refuted.
7. **Designed issue #50** through a measure→research→design pass.

---

## 3. The decisions taken, and where they live

All in `trackers/s3.md` §4 (on `c4`; §4 of the copy in #58 for those that landed there).

| # | decision |
|---|---|
| 1 | S3 splits into **six plan documents** |
| 5 | `specRepo`/`gateDefinitionPaths` **provisioned now** (brief Q1 option A) |
| 10 | Spec repos are **`<project>-specs`**, one private repo per project. **They do not exist yet — F5.** |
| 11 | **`--execute` stays OFF** on the live guardian until after T8 merges |
| 12 | The 15-minute watcher **is running**; job id changes per session |
| 13 | The plan cap's unit is **not lines** |
| 14 | **Plans carry properties, not inline test code.** Ten of the 38 defects were stub loops that could not run, because code in a Markdown fence is never executed. Test-first and the four-check loop are unchanged; only where the test is written moved. |
| 15 | **`admitTask`'s INSERT widens** to carry `depth` and `priority` |
| 16 | **`NEVER_TOOLS` becomes a per-action subtraction** for the three BUILD actions only |
| 17 | **The foundation lands before any stage plan**, and plans land **one at a time** |
| 18 | **`specRepoId` is resolved from GitHub by nwo**, never from the hub cache |
| 19 | **A stub is proven applied by a HASH CHANGE, not a grep** |
| 20 | The operator surface is designed from research **checked against the source** |

**Corrected twice, and worth knowing:** the plan cap. First "~1,200 lines per document", then
"3–4 plan tasks per document" — which is **arithmetically impossible** (78 tasks / 6 documents
allows at most 24). It is now **3–4 PRs per document**. A PR holds several tasks; S3's 78 tasks
are 16 PRs.

---

## 4. The findings that matter beyond their fix

**The design document contradicts itself.** `docs/2026-08-21-builder-design.md:818` says *"New ctx
keys default off, following the `ctx.reviewIngest !== false` opt-out pattern."* That expression is
**`true` when the key is absent** — default *on*. A new side effect written to the named shape runs
in every legacy call site. Recorded as contradiction **C9**; the rule is `ctx.newKey === true`.

**#60's core comparison was wrong in the common case.** It compared main against the **branch
head**. If main touches a file after the branch diverges, the squash folds both edits in, so the
merge's tree differs from the head's **even though the change landed**. Proved on a real git
fixture: `MISSING / NOT ON MAIN` over content demonstrably present. **Redesigned to compare against
the squash commit**, which already incorporates the moved base. It now deliberately no longer
claims whether a post-merge push was lost — tree entries cannot answer that.

**V6 would have measured the wrong thing.** Its two arms were guardian-idle and guardian-live, and
**both are headless**. §14's obligation is *headless versus **interactive***. Corrected to require
a real interactive arm.

**`reeve doctor` advises a command the CLI refuses.** It emits `reeve build measure-provider`;
`bin/reeve` accepts only `run` and `status`. T16 closes it.

**The recurring shape, stated plainly.** Nine of #58's findings were **a correction that existed
and was not propagated** — §B.6 amended while the prompt still ordered the old thing; the cap fixed
in one place and reinstated in the appendix; "reeve is private" fixed in five plans and left in the
prompt; the resume prompt retired and its companion handoff left live. That is §B.8 rule 3, which
this programme wrote and broke four times. **And five references pointed at things that do not
exist** — `scripts/verify-merge.mjs` (prescribed, absent — now written), a design file not in the
commit that cited it, `intake.mjs`, `measure-provider`, and two test files in S2's Verify rows.

---

## 5. What remains

### Immediately

1. **#58 and #60 need a merge grant each.** Both are green with zero unresolved threads. **Grants
   never carry over; re-verify CI and threads at the moment of merge; verify by CONTENT.**
2. **Both PRs were left clean.** As of the last read: **#58 at `ffa8edf`, #60 at `cbff096`, zero
   unresolved threads on each**, suites green (94/5,189 and 95/5,215), review re-requested on both.
   **A further round may have arrived since — check both endpoints before assuming.**
3. **F5: create the three spec repos** — `revnix/reeve-specs`, `nextlyhq/nextly-specs`,
   `revnix/rext-specs`, **private**. Blocks S3 T2.

### Then, in order

4. **P2 — issue #50.** Design in `c4:tasks/reeve-tasks/ISSUE-50-SESSION-DESIGN.md`. Measured on
   `a529096`: `tick()` is **2,282 lines**, grown 14% since the issue was filed. **`src/daemon.mjs`
   is peer territory — tell the peer lane first.**
5. **Land S3-A's plan, execute T1–T2, then S3-B**, one document at a time (decision 17).
6. **Rework S3-A and S3-B into the thinned format** and fold in the surviving interface findings
   from `S3-PLAN-REVIEW-FINDINGS.md`. About a third of the 38 dissolve under the new format; the
   interface, coverage and consistency ones do not.

### Not started

S3's sixteen tasks T1–T16. S4–S12. The six V1–V6 measurements, priced at **$29 floor / $51 central
/ $79 ceiling**.

---

## 6. Standing rules

- **Never merge without an explicit per-PR grant.** Grants never carry over. Verify by **CONTENT**
  — `node scripts/verify-merge.mjs <pr>` once #60 lands, or compare tree entries by hand.
- **Node is `~/.nvm/versions/node/v24.17.0/bin/node`.** PATH node is v22; `package.json` needs ≥24.10.
- **`REEVE_HOME` must point at a directory literally named `.reeve`.**
- **The four-check stub loop**, with check 2 proven by a **hash change**. Restore **by file copy**.
- **Never truncate a search you reason about as a set.** Every absence needs a **positive control
  that actually matches** — an inert control was reached for twice this session.
- **Do not** restart the daemon, run `launchctl`, run `reeve canary`, or `git pull` in
  `~/Work/Products/reeve`. **Never `git stash`** — the stack is shared across ~19 worktrees.
- **Tell the peer lane** (`nextly-integrations-*`, via `ListAgents`) before touching
  `src/daemon.mjs`, `src/db/**`, `src/outbox/**`, `src/github/**`, `src/pr.mjs`, `src/verdict.mjs`,
  `src/watcher.mjs`, `src/review/**`, `src/prompts.mjs`.
- **Branch from `origin/main`**, never local `main` — the workflow forbids pulling, so local `main`
  can lack the dependency you just verified.
- **A push to a branch whose PR already merged goes nowhere, and succeeds.** Check the PR is still
  `OPEN` before believing a push mattered.
- **Read both verdict endpoints.** A clean pass is an *issue* comment; findings are *review*
  objects. Check `commit_id` against the current head — a verdict at an older head is stale.
- **CI's four states**: `dead` / `ran` / `pending` / `all-skipped`. Never `jobs_with_steps == 0`.

---

## 6b. A number in this document was wrong, and how

The first draft said *"#58: six review rounds, 42 threads"*. **Both figures were wrong**: GitHub
reports **36 threads across 4 codex review objects**. The 42 came from adding the last round's six
findings to a 36 that already contained them — arithmetic on a remembered total instead of a read
one, in the document whose opening line says every value was read.

Corrected against `reviewThreads.totalCount` and the `/reviews` endpoint. **Re-derive them; do not
inherit them.** They move with every round:

```
gh api graphql -f query='{repository(owner:"revnix",name:"reeve"){pullRequest(number:58){reviewThreads(first:100){totalCount}}}}'
gh api repos/revnix/reeve/pulls/58/reviews --jq '[.[]|select(.user.login|test("codex"))]|length'
```

## 7. What this handoff does NOT establish

- **The suite figures are per branch and per moment.** Re-measure against `origin/main` before
  using any of them as a base.
- **The 38 plan findings were measured against S3-A and S3-B only.** S3-C through S3-F were not
  reviewed, and S3-C will be rewritten after #50 moves the provider calls.
- **The six V1–V6 cost figures are an estimate** from a $2.66-for-three comparator, and the
  fable/high multiplier in them is the least supported number in this programme.
- **Whether #58 or #60 attracted a further review round after this was written.** Check both
  endpoints. Do not read silence as a pass.
