> **SUPERSEDED IN PART, 2026-09-01T16:40Z.** The four steps below have been done.
> `#110` merged as `d206f6b` and `#129`, which carried the four findings deferred
> from it, merged as `b3514b8`; both lanes were told, and `#110` has zero
> unresolved threads. **Do not try to merge `#110`** — it is closed, and the
> conditional wording below is the only reason following this blind was safe.
> `main` is `eb0b181` and no longer carries the redness described under WHERE YOU
> ARE. The manifest is 183 entries and 96 grandfathered, not the numbers below.
> **The next task is T6**, and the rules from HOW TO WORK HERE downward are
> unchanged and still the reason to read this file.

Resume the reeve builder lane. Do not trust any value in this prompt — read it.
Everything here was true at 2026-09-01T15:40Z and this repository merged fifteen
pull requests today.

READ FIRST, IN THIS ORDER
1. `tasks/reeve-tasks/HANDOFF-2026-09-01B.md` — the full context. Read it
   completely before doing anything. It supersedes HANDOFF-2026-09-01.md.
   If it is not on main yet it is on the branch `fix/artifact-followups`.
2. `tasks/reeve-tasks/plans/README.md` — ~40 measured findings against the six S3
   plans, each with the command that established it. The plans are wrong in
   specific, recorded ways. This is the most valuable file in the repository.
3. `tasks/reeve-tasks/trackers/s3.md` §4 (binding decisions) and §5 (durable findings).

WHERE YOU ARE
- `origin/main` was `72c5d0f` and RED. It has been red since 11:53Z because #115
  landed a stricter sweep verdict and three pre-existing entries became visible.
  **#110 carries the fixes.** Nothing regressed.
- **#110 `fix/artifact-followups`, head `a521cf6`, is MINE and the founder has
  granted the merge explicitly.** At handoff: `mergeState=CLEAN`, Test SUCCESS,
  Stub sweep still running.
- Worktree: `~/Work/Products/reeve-wt/followup`. Node is
  `~/.nvm/versions/node/v24.17.0/bin/node` — PATH node is v22 and suites crash there.
- S3-B is COMPLETE (T3, T4, T5 merged). T6 is next and unblocked.

DO THIS FIRST
    gh pr list --repo revnix/reeve --state open --json number,headRefName
    gh pr view 110 --repo revnix/reeve --json state,mergeStateStatus,statusCheckRollup
Author CANNOT identify a lane — the sessions share the account. Identify by BRANCH.
An empty list is a real answer only if the same query with --state merged returns rows.

Then, in order:
1. **If #110 is still OPEN and its checks are green, MERGE IT.** The founder said
   "resolve conflict and merge yourself" naming that PR. Re-read CI immediately
   before merging — a PR is a moving object. Afterwards verify by CONTENT:
   `node scripts/verify-merge.mjs 110`, and check `merged-head` equals `branch-now`.
   If it is DIRTY, merge `origin/main` in first (see the manifest rule below);
   GitHub disables the merge button on a DIRTY PR, which is why the founder's own
   click did nothing.
2. **Tell the other lanes it landed.** `ListAgents`; they are `nextly-integrations-*`.
   `nextly-integrations-c0` (operator surface, holds #126) and `nextly-integrations-b4`
   (guardian) are both blocked behind it and asked to be told rather than poll.
3. **Open the follow-up PR** for the four findings deferred from #110. Their threads
   are OPEN on #110 deliberately — a resolved thread is one nobody can find again.
   Reply on each with the commit when fixed. THE GRANT FOR #110 DOES NOT EXTEND TO IT.
4. **Then T6** — `phase_run`: its lease, heartbeat, revocation and the contract
   snapshot. S3-C, branch `feat/s3-phase-run`. Read
   `plans/2026-08-27-s3c-dispatch.md` AND `plans/README.md` first: S3-C has NEVER
   been executed, so every "produces" clause in it is unverified.

THE FOUR DEFERRED FINDINGS (all verified real, all on #110's open threads)
- `bin/reeve` — a dry run's validation and its reads are not one snapshot: a newer
  reeve can migrate the hub between `missingMigrations` closing its probe and
  `fileTask` reading, and a migration that only ADDS a table is invisible to the
  old-version shape check.
- `src/build/artifact.mjs` — tab stops computed from the wrong column: `-\tparent`
  expands from column 1, not from after the marker, so a nested child is classified
  top-level and refused.
- `bin/reeve` — replacing `openHub` also removed its `quick_check(1)`, so a hub with
  a correct catalogue but a corrupt page passes every check and leaks a raw
  `database disk image is malformed` later.
- `src/build/artifact.mjs` — the done-condition scan does not stop at the next field,
  so `Done when: someone approves` followed by `Tests:` with a fenced command accepts
  the TEST field's block.
Two of these are gaps a previous fix opened. That is the shape to watch.

THE RULE THAT MATTERS MOST
**A stub does not make an assertion fail — it often makes a call THROW.** A throw
kills the file, so every assertion after it never runs, and one that never ran reads
in the log exactly like one that passed. Three entries were UNRUNNABLE this way
(22/182, 124/182, 104/122). Helpers return `ok: null` on a throw: neither true nor
false, so whichever direction the consuming assertion tests, it goes red.
**But never apply that shelter where the exception IS the subject** — the control
asserting a partial write really failed calls the real function directly, and
wrapping it would report the write as succeeding.

HOW TO WORK HERE
- Four-check stub loop on every fix: control green → stub proven by a HASH CHANGE
  not a grep → the RIGHT assertion red → restore byte-identical by file copy, never
  `git checkout`.
- The sweep now compares assertion counts itself (#115). Trust its verdict; the
  scratch checker is retired.
- `test/stub-manifest.mjs` conflicts on nearly every PR. Take `origin/main`'s copy
  WHOLE, re-append yours by NAME, and **re-apply your own MODIFIED entries** —
  taking the copy whole silently reverts those. Verify BOTH directions: no entry of
  main's missing OR altered, none of yours missing, none of your modifications
  reverted, no duplicates, `GRANDFATHERED` **byte-identical** to main's. A count
  sees none of those failures. That list may only SHRINK (98 → 96 today).
- Run `node test/anchors-resolve.test.mjs` after EVERY manifest edit.
- Never run the suite while the sweep runs — the sweep mutates the tree.
- Full suite: every `test/*.test.mjs` except `escape`, with a `fail=0` accumulator.
  Baseline 120 files, 0 red. `$?` after a pipe or `$(...)` is not your status.
- Generate string literals with `JSON.stringify` of the real source line.

HARD RULES
- NEVER MERGE without an explicit per-PR grant. Grants never carry over.
- Never `git stash` — the stack is shared across ~19 worktrees.
- Do not restart the daemon, run `launchctl`, run `reeve canary`, or `git pull` in
  `~/Work/Products/reeve`. `git fetch` is fine.
- No `as any`, no `@ts-expect-error`, no lint suppression.
- Conventional Commits, lowercase, ≤72 chars. No attribution trailer.

READING A PULL REQUEST'S STATE
CI four-state, in this order: pending FIRST, then dead, then ran — the counters are
NOT mutually exclusive. **`mergeStateStatus: DIRTY` produces NO check runs at all**,
which looks exactly like Actions being down; the positive control is a run on another
branch in the same window. UNKNOWN is transient for about a minute after a push.
A Codex clean pass is an ISSUE comment carrying **Reviewed commit:** `<sha>` — match
with `/Reviewed commit:\W{0,4}([0-9a-f]{7,40})/`. It reacts eyes while reviewing and
+1 when a review finds nothing, so a pass can be a reaction with no comment.
"Something went wrong" and "usage limits" are REFUSALS, not passes. Check every
verdict's sha against the current head. BLIND IS NOT QUIET: a read that fails is
reported as unread, never as "nothing new".
Verify every finding against the source before acting — Codex has been right on
essentially every finding across eleven rounds. Fix the CLASS, not the instance.
Reply to the thread AND resolve it via GraphQL (replying alone does not clear it),
push, then comment "@codex review". Keep a 15-minute watcher on your open PRs.
