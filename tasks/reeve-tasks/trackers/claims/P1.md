# Claim: P1 — fix the test suite's dead network

stage:      S3 (pre-stage; not an S3 task)
tracker:    ../s3.md
lane:       reeve-wt/c4 (session nextly-integrations-7b)
host:       192
branch:     fix/test-gh-seam
claimed:    2026-08-27T20:51:21Z
refreshed:  2026-08-27T20:51:21Z
state:      HELD
pr:         —

## Files this claim covers

Provisional until the measurement below lands. The handoff's figure of "four modules"
was measured at `c500cfe` and is being **re-measured at `16cd880`** rather than inherited,
because `git grep` finds **15** modules under `src/`+`bin/` that shell to `gh`.

- `src/review/ingest.mjs` (`gh api` at `:50`)
- `src/pr.mjs` (`gh api` at `:22`)
- `src/daemon.mjs` (`gh pr view` `:710`, `gh pr list` `:768`) — **guardian-shared**
- `test/*.test.mjs` — the daemon-level files that never pass a stub
- a new guard test that fails if any test process execs `gh`

## Notes

**Guardian-shared and peer-notified.** `src/review/**`, `src/pr.mjs` and `src/daemon.mjs` are
peer-lane territory; notice sent 2026-08-28 before any edit. The change is **additive and
behaviour-preserving** — each `gh` call site gains an injectable seam whose default is today's
behaviour. No existing caller's signature changes. Not touching the SHAPE of `computeVerdict`'s
clause set or the ORDER of `nextAction`'s branches.

**Why this lands before T1.** It is the instrument S3 is measured with sixteen times, and S3 adds
about ten files to it. Standalone rather than folded into T1, because it touches guardian files
and the corpus says mixed PRs converge worse.
