# Claim: P2 — reeve#50, the session that owns the guardian's hub rules

stage:      S3 (pre-stage; not an S3 task)
tracker:    ../s3.md
lane:       reeve-wt/p2 (session nextly-integrations-7b)
host:       192
branch:     refactor/provider-session
claimed:    2026-08-28T08:05:00Z
refreshed:  2026-08-28T08:05:00Z
state:      HELD
pr:         —

## Files this claim covers

- `src/daemon.mjs`
- `src/build/hubsession.mjs` (new, arrives at PR-4 of the sequence)
- `test/dispatch-e2e.test.mjs`
- `test/guardian-provider-lease.test.mjs`
- `test/hubsession.test.mjs` (new)
- `test/source-is-text.test.mjs` (PR-0 converts two source-text guards)

## Notes

**Territory cleared with the guardian lane before claiming**, not after. `nextly-integrations-f4`
derived and confirmed 2026-08-28: `src/daemon.mjs` is free, their `feat/stub-sweep` touches five
files and none is the daemon, and their stub manifest has **zero anchors** in it.

**They are holding a daemon edit for this.** Their durable-effect Stage 3 part three replaces the
`decision.action === "SPILL"` branch at roughly `daemon.mjs:2345` with an outbox enqueue. They
will not touch the file until this lands, on the reasoning that rebasing a few lines inside one
`else if` onto a restructure is cheaper than the reverse. **Two obligations follow:** tell them
the moment this lands or is abandoned, and leave that branch structurally recognisable — this
moves provider/hub *mechanics* out, not the decision branches.

**Their test names, to stay out of:** `test/stub-manifest.mjs`, `test/stubsweep.test.mjs`, and
all of `tools/**`. Their manifest names `test/outbox-dependency-edge.test.mjs` as the expected-red
for all nine of its stubs, so if that file moves under this work it is a signal the change has
gone wider than intended.

**Design of record:** the body of `reeve#50` plus `ISSUE-50-SESSION-DESIGN.md` (500 lines, on
branch `docs/s3-foundation`, not yet on `main`). Six PRs: guards, characterisation harness, one
declared behaviour change, combine in place, move, then the acceptance test. `src/daemon.mjs` is
**byte-identical to `a529096`** (`38b28b71`), so every line number in that design still holds.
