# S3 tracker — founder-filed read/report phases

**Stage definition:** `docs/2026-08-21-builder-design.md:826`. Its *Verify:* clause is the
definition of done and is reproduced in §3 below, **verbatim, never paraphrased**.

**Contract:** every claim in this file is either **measured** (say when, and name the file
under `docs/measured/`) or marked **intent**. Absence from this file means "not planned", not
"done".

**Live state is not recorded here.** Read the capability switches from the machine. What
belongs here is what was DECIDED and what was FOUND.

**How to re-derive STATE**, so no row is trusted from memory:

```bash
git log --format='%s' origin/main | grep '(#'              # every squash-merged PR
git log --format='%h %s' origin/main | grep -E '\(#NN\)$'  # one PR's squash SHA
node scripts/verify-merge.mjs <pr>   # content verification, not ancestry: 0 merged and intact,
                                     # 32 merged then changed on main, 22 not merged, 23 could
                                     # not tell, 20 usage. There is NO 31 -- the squash-based
                                     # comparison has no "did not arrive" state, because a path
                                     # absent from both squash and main is a merged DELETION.
                                     # ON MAIN since aaa558a (reeve#60). An unreadable path is
                                     # unverified, never matching.
                                     #
                                     # The rule 11 fallback is a REDUCED capability, not an
                                     # equivalent: it enumerates the squash's own diff and stops
                                     # there, so it neither cross-checks the pull request's file
                                     # list nor prints the scope statement.
                                     #
                                     # BE PRECISE ABOUT THE DIFFERENCE. The script does NOT refuse
                                     # every incomplete rebase verification -- mergecheck.mjs:161
                                     # refuses only when `branchOnly` is non-empty or the
                                     # cross-check failed, so a rebase merge whose file list has
                                     # been rewritten to expose only the last commit still exits 0.
                                     # What the script does that the fallback cannot: it refuses
                                     # when the cross-check REVEALS omitted paths, and it states
                                     # its scope on every verdict when it cannot tell. The fallback
                                     # does neither, and is silent about both.
                                     # Use it only where the script is genuinely absent, and
                                     # prefer updating the checkout to using it.
```

A row whose STATE says BUILT and whose PR appears in that first list is **stale**: fix the row,
do not argue with git. `Merge` is the **squash SHA on `main`** and is the only SHA in §1,
because per-round fix SHAs do not survive a squash merge (measured: 13 of 18 SHAs cited on
`docs/TRACKER.md` are unreachable from HEAD).

**Issue references are written `reeve#43` / `nextly#1134`, never bare `#43`** — `#N` already
overloads three namespaces at 8 of 25 sites in the old tracker.

**The one exception is a PR body's CLOSING line, which uses bare `#43`.** Not a carve-out
in the rule above: the two govern different audiences. This rule serves a READER who might
carry a reference out of context, where `reeve#43` disambiguates. A closing keyword is not
read by a person at all — GitHub parses it, and GitHub is already in the repository's
context when it does. `Closes reeve#43` is silently not a closing reference, so reeve#43
merged with its issue still open and nobody got a signal. Measured, not predicted.

**Plans:** `../plans/2026-08-27-s3{a..f}-*.md` — **written, and not in this commit.** The six stage plans land one at a time, each ahead of the tasks it describes (decision 17), so a plan is revised by what executing its predecessor taught. Until S3-A lands, that directory does not exist on `main` and every path above pointing into it is forward-looking. **Authoring rules:** `../MASTER-PLAN.md` Part B.

**`../S3-DESIGN-BRIEF.md` IS RESEARCH, NOT A SPECIFICATION — corrected 2026-08-28.** This line
previously said *"§2.2 specifies every task"*, which promoted a dated research artifact to a
normative one. It is **not in `../IMPLEMENTATION-PROMPT.md`'s authority table at all**, and the
promotion had a measured cost: **nine review findings in one round**, every one of them the brief
contradicting a correction *this tracker had already recorded* — D4, D7, D15, D31, D34, D37 and
decision 15. The brief was written first and was never back-patched, which is what happens to any
document that records research and is then read as a contract.

**Authority for a task is: the design, then `../MASTER-PLAN.md`, then the stage plan, then this
tracker.** Read the brief for *why* a choice was made and for the measurements behind it. Never
derive a task's files, flags, dependencies or acceptance checks from it — §4 and §5 here outrank
it everywhere the two disagree, and §5's brief-drift entry lists the nine that are known.

---

## 1. PRs

STATE is one of exactly four words: **BUILT · IN REVIEW · MERGED · ABANDONED.** A task with no
PR yet has STATE `—`.

### The foundation — merged, and referred to by PR number only

**These carry no `F`/`P` identifier deliberately.** `F1`–`F5` already name the founder questions
in §2 (`F1` is *"name the spec repos"*), so a second `F` series here would make "blocked on F1"
ambiguous between a merged PR and an open decision. Nothing depends on these rows by id; they are
a record. Refer to them as `reeve#58` and `reeve#60`.

| What | Branch | PR | STATE | Rounds | Findings | Merge |
|---|---|---|---|---|---|---|
| The planning foundation: roadmap, authoring spec, trackers, implementation prompt | `docs/s3-foundation-core` | reeve#58 | **MERGED** | 10 | 33 | `a136836` |
| `scripts/verify-merge.mjs` — verify a merge by tree entry | `fix/merge-verifier` | reeve#60 | **MERGED** | 7 | 13 | `aaa558a` |

### Two PRs before T1 — not S3 tasks, but S3 does not start without them

| # | What | Branch | Depends on | Base | PR | STATE | Rounds | Findings | Merge |
|---|---|---|---|---|---|---|---|---|---|
| P1 | Fix the test suite's dead network (stub `ctx.observe`; a guard that fails if a test process execs `gh`) | `fix/test-gh-seam` | — | `main` | reeve#57 | **MERGED** | 1 | 1 (1×P2) | `89c4af6` |
| P2 | **reeve#50** — extract the provider/hub session from `tick()` | `refactor/provider-session` | P1 | `main` | reeve#69, reeve#70, reeve#72, reeve#74, reeve#76 | **MERGED** | 9 | 50 | `a12c281`, `110d889`, `29a83a5`, `a939cb1`, `c02a738` |

### S3's sixteen tasks

| # | Task | Plan | Branch | Depends on | Base | PR | STATE | Rounds | Findings | Merge |
|---|---|---|---|---|---|---|---|---|---|---|
| T1 | `builder.*` FIELDS + the one reader of the capability switches | S3-A | `feat/s3-budgets`, `feat/s3-capabilities`, `feat/s3-profile-reference` | P2 | `main` | reeve#80, reeve#81, reeve#82 | **MERGED** | 6 | 17 | `0695cc7`, `25f8617`, `06035b8` |
| T2 | Registry grows `repoPath`/`profilePath`; `resolveSnapshot` gets a real `io` | S3-A | `feat/s3-registry-io` | T1 | `main` | reeve#92 | **IN REVIEW** | | | |
| T3 | `reeve task file`, with the territory grammar | S3-B | `feat/s3-task-file` | T2 | `main` | — | — | | | |
| T4 | Artifacts: the durable store, and `reviewArtifact` | S3-B | `feat/s3-artifacts` | T3 | `main` | — | — | | | |
| T5 | Phase report schemas and the report contract | S3-B | `feat/s3-report-schema` | T4 | `main` | — | — | | | |
| T6 | `phase_run`: its lease, heartbeat, **revocation**, and the contract snapshot | S3-C | `feat/s3-phase-run` | T5 | `main` | — | — | | | |
| T7 | `BUILD_SIZE`/`BUILD_RESEARCH`/`BUILD_DESIGN` reach `sandboxFor` and `promptFor` | S3-C | `feat/s3-action-cases` | T6 | `main` | — | — | | | |
| T8 | The builder tick dispatches, and claims the provider before it does | S3-C | `feat/s3-tick-dispatch` | T7 | `main` | — | — | | | |
| T9 | Resume: adopt-or-kill, `recoverEffects`, and the crash drills | S3-C | `feat/s3-resume` | T8 | `main` | — | — | | | |
| T10 | SIZING: `sizing.json` and the deterministic floors | S3-D | `feat/s3-sizing` | T9 | `main` | — | — | | | |
| T11 | RESEARCH: `--agents` fan-out and the artifact minima | S3-D | `feat/s3-research` | T10 | `main` | — | — | | | |
| T12 | DESIGN: the ordered slice list, stopping at SPEC_DRAFT | S3-D | `feat/s3-design` | T11 | `main` | — | — | | | |
| T13 | `reeve task list\|show\|why` and the derived waiting substates | S3-E | `feat/s3-task-read` | T3 | `main` | — | — | | | |
| T14 | `reeve dash` for tasks | S3-E | `feat/s3-dash` | T13 | `main` | — | — | | | |
| T15 | Escalations reach the founder from the builder process; `builder doctor` grows S3's rows | S3-E | `feat/s3-escalate-doctor` | T13 | `main` | — | — | | | |
| T16 | The six measurements, and the documents that record them | S3-F | `feat/s3-measure` | T12, T15 | `main` | — | — | | | |

**`Base` is now always `main`, and `Depends on` carries the dependency.** They were one column,
and the column held task ids — which `git worktree add -b <branch> <path> <base>` cannot take, so
**every linear task failed the mandatory workflow, not just the one row where the parallel chains
join.** The first fix corrected T16 alone; that was fixing the site rather than the invariant,
which is the thing this programme's own prompt forbids.

Branch from **`origin/main`**, never from local `main`, **after the named dependency is verified
merged** — `node scripts/verify-merge.mjs <pr>`, which is on `main` as of `aaa558a`; the by-hand
fallback in `../IMPLEMENTATION-PROMPT.md` rule 11 is only for a checkout that lacks it. **`origin/main`, because pulling in the live
checkout is forbidden**, so local `main` can lack the very dependency you just verified on the
remote. The dependency is what you check; the base is what you branch from, and conflating them
is what made a whole column unusable.

**For a JOIN task the base must contain EVERY dependency.** T16 depends on T12 and T15; either
one's squash SHA omits the other, and whichever merged second is the one silently lost. Check the
chosen base with `git merge-base --is-ancestor <dep-squash> <base>` per dependency.

**`Base` is a git ref you can branch from, never a dependency list.** They coincide for a linear
chain — T2's base *is* T1's merge — but they are different facts, and T16 is where that shows:
it joins the two parallel chains, so its dependencies are T12 **and** T15 while its base is the
`main` commit after both have merged. `git worktree add -b <branch> <path> <base>` takes a ref;
handing it "T12, T15" fails.

**Order.** T13/T14/T15 run in parallel with T6–T12 from T3 onward. Everything else is a chain.
**T7 and T8 are guardian-shared and ship alone** — the two worst-converging PRs in the corpus
(#44, #3) both changed the running guardian.

**The six plan documents are written**, 2026-08-27/28, and total **14,167 lines over 78 tasks**
— 62 non-close-out tasks each carrying `**On the broken implementation**` and a named
four-check stub loop, plus 16 close-outs.

**Fenced-block lint, RE-MEASURED 2026-08-28 and current as of this line:** **365 fenced blocks;
351 carry a checkable language tag (199 `js`, 151 `bash`, 1 `sh`) and were checked; 332 parse
standalone; 19 do not.** The remaining 14 are `markdown`, `json` or untagged and were not
checked. Both controls fire: a deliberately broken `js` block and a deliberately broken `bash`
block are each rejected, so the checker is not passing everything.

**The 19 are consistent with deliberate in-context fragments** — object-literal entries
(`Unexpected token ':'`), `case` clauses, a `return`/`continue`/`break` inside a function or
loop, and partial blocks (`Unexpected end of input`). **That is a reading of the error class, not
a confirmation of each one**, and it is deliberately weaker than the claim it replaces. Each must
be re-confirmed by wrapping it in the context its plan names before any "zero unresolved" verdict
is restated.

**Two earlier totals disagreed, and NEITHER describes the current plans.** This line previously
said *345 blocks / 326 standalone / 19 fragments*, while §5's durable finding said *272 / 257 /
15* for the same six documents on the same date. The 19 agrees with the count above; the totals
do not. They were not two snapshots of one measurement, and a readiness verdict was resting on
the larger of them. The linter and its controls are described in §5.

**Sixteen PRs at the corpus median of five rounds is roughly eighty review rounds**; at S2-C's
rate (6.5) it is ~104. That number is why the taper rule exists and why the plan family is six
documents rather than one.

---

## 2. Open questions the founder has not answered

| # | Question | Asked | Blocks | Answer | Answered |
|---|---|---|---|---|---|
| F1 | **Name the spec repos.** One private repo per project for `specRepo`, plus `gateDefinitionPaths`. | 2026-08-27 | **T2**, and every `reeve task file` after it | **Create them, one per project, `<project>-specs`**: `revnix/reeve-specs`, `nextlyhq/nextly-specs`, `revnix/rext-specs`, private. | 2026-08-27 |
| F5 | **The three repos F1 named do not exist yet.** Creating them is the founder's, not this lane's. | 2026-08-27 | **T2** | | |
| Q2 | Worker isolation for S3's read-only phases. **Default taken:** `scratch-home` unchanged, **plus** a `.git` write probe and a commit attempt added to the canary in T7. Revisit `dedicated-user` after V6 measures whether a second user can authenticate at all. | 2026-08-27 | T7 | *defaulted* | |
| Q3 | Repos carry instruction files addressed to the worker (`CLAUDE.md`/`AGENTS.md` at nextly's root). **Default taken:** neutralize before dispatch — remove them from the run checkout, record the digest — **plus** one real probe in T16 with a planted instruction file. | 2026-08-27 | T7, T16 | *defaulted* | |
| Q7 | The V6 subscription-pool measurement. **Default CORRECTED 2026-08-28 — the first one measured the wrong thing.** It said "guardian idle and guardian live", and **both of those arms are HEADLESS**: the idle case runs the builder alone, the live case adds the guardian's own print-mode worker. Neither runs an **interactive** session, so the experiment could not answer §14's actual obligation — *headless versus interactive* — and would have written a `concurrency_limit` and `guardian_reserved` into `provider_state` derived from guardian contention instead. **V6 now requires a real interactive session as one arm**, with the headless-only run as its control, and the jitter ramp retained so an acceleration limit is not misread as a pool limit. | 2026-08-27 | T16 | *defaulted, corrected* | |
| Q8 | Does S3 flip `builder.capabilities.observe` on in the live profile? **Default taken: no.** S3 *wires* it as the dispatch gate; the live flip is a separate founder action after the last S3 PR merges. | 2026-08-27 | T8, stage close-out | *defaulted* | |
| Q9 | 23 escalation identities, one operator. **Default taken:** split escalation from notification. All 23 stay durable and visible; a **closed page list** of three for S3 — `builder:sandbox:canary-failed`, `builder:backup:failed`, `bt:<id>:phase:blocked:<phase>` — everything else to `dash` and a digest. | 2026-08-27 | T15 | *defaulted* | |

**A defaulted answer is a decision that was taken without you and can be reversed cheaply.**
Each is recorded with the reasoning in `../S3-DESIGN-BRIEF.md` §6 at the line given there.

---

## 3. The §14 Verify list

> *Verify:* one real scout task through to artifacts; **measure** real phase budgets, alias-to-model resolution, sandbox behaviour under fan-out, `--json-schema` reliability across 20 runs, and the headless-versus-interactive subscription pool (§10.4), each recorded in the profile or the tracker with dates.

A row not yet satisfied names the task that will satisfy it. **A row is never marked satisfied
by a test name alone; it names a file that exists and is green.**

| # | Obligation | Where it is proven | Measured on | Document |
|---|---|---|---|---|
| V1 | one real scout task through to artifacts — FILED → SIZING → RESEARCH → DESIGN, stopping at SPEC_DRAFT, with three artifacts on disk, three shas in `phase_event`, three `phase_run` rows carrying contract snapshots, and **zero** GitHub effects | T16 | | `docs/measured/2026-XX-XX-scout-task-end-to-end.md` |
| V2 | **measure** real phase budgets — wall-clock, turns and USD per phase, against the §4.1 guesses (8 / 20-60 / 20-60 min), written into `builder.budgets.*` or here, with dates | T16 | | `docs/measured/2026-XX-XX-phase-budgets.md` |
| V3 | **measure** alias-to-model resolution — the resolved model id for `fable` and for `sonnet`, read from a real `phase_run.model_id`, with the CLI version beside it | T6 (unit half) + T16 (measured half) | | `docs/measured/2026-XX-XX-alias-to-model-resolution.md` |
| V4 | **measure** sandbox behaviour under fan-out — a RESEARCH worker at maximum subagent width, with the canary's write and network probes, run **from inside a subagent** | T11 (unit half) + T16 (measured half) | | `docs/measured/2026-XX-XX-sandbox-under-fanout.md` |
| V5 | **measure** `--json-schema` reliability across **20 runs**, on the **real** phase schemas, reporting the count of malformed or missing structured outputs and what each looked like | T16 | | `docs/measured/2026-XX-XX-json-schema-reliability.md` |
| V6 | **measure** headless-versus-interactive subscription pool (§10.4) — **one arm is a REAL INTERACTIVE session, with a headless-only run as its control** (Q7, corrected 2026-08-28; "guardian live and guardian idle" are BOTH headless and measure guardian contention, not the pool §14 asks about). Jitter ramp retained so an acceleration limit is not misread as a pool limit. Written to `provider_state` columns with `measured_at` — not to profile keys, which `FIELDS` would reject (D22). | T16 | | `docs/measured/2026-XX-XX-subscription-pool.md` |

**Every one of the six documents must carry its own `## What this does NOT establish` section.**
V5 in particular: "20 runs" is a sample, and the document must say what population 20 runs of
`BUILD_SIZE` does not cover. A measurement whose fixture cannot exhibit the thing measured
**reads as success** — that class has 7 measured instances in this repository already.

---

## 4. Decisions taken during this stage — do not re-litigate

Each with its date and its reason. **Reversals stay visible**, recorded so the reversal is
legible rather than silent.

1. **2026-08-27 — S3 splits into six plan documents, ~1,200 lines each.** The three S2 *plan*
   PRs (14 files, all Markdown) produced **561 of 1,282 findings — 43.8% of every finding this
   repository's review has ever produced.** PR#12 was one file, 213 findings, 15 rounds. A
   single S3 plan would be 6,000–8,000 lines, which is the artifact measured to be unreviewable.
2. **2026-08-27 — S3 is §14 verbatim, including all six measurements.** No obligation is dropped
   because it is expensive; V5 alone is 20 runs and the comparator for real dispatch cost is
   **$2.66 for three** (`docs/measured/2026-08-23-three-real-dispatches.md`).
3. **2026-08-27 — issue reeve#50 lands before S3's dispatcher.** #50's own acceptance test is
   *"adding a new call site must not be able to skip a rule"*, and T8 **is** that call site.
   This is the only moment that test can be written honestly rather than asserted. Measured at
   `16cd880`: `tick()` = `src/daemon.mjs:975-3225`, **2,251 lines, 50 provider/hub touch points**
   — #50 was filed against 32 touch points on a 1,996-line tick, so **the shape grew after the
   issue was filed.**
4. **2026-08-27 — the test suite's dead network is fixed in a standalone PR before T1.**
   Measured with a control: **550.1s → 159.8s**, PASS output byte-identical on the four largest
   files. Standalone rather than folded into T1, because it touches guardian files and the
   corpus says mixed PRs converge worse.
5. **2026-08-27 — `specRepo` and `gateDefinitionPaths` are provisioned now** (brief Q1, Option
   A). Option B splits `SNAPSHOT_FIELDS` into three lists, and the comment at `phases.mjs:130-155`
   records that the list was consolidated *because* it lived inside one branch and admission
   could not consult it. Splitting it re-opens the shape the list exists to close. **Blocked on
   F1.**
6. **2026-08-27 — the builder always shares the guardian's hub.** So an absent hub on a builder
   PR is the merge authority being gone, not an ordinary machine.
7. **2026-08-27 — S3 stays headless, and `--json` becomes a contract rather than a courtesy.**
   Every read surface is `compute → data → render`, and a test enumerates the read commands and
   asserts each emits parseable JSON. Reason, recorded because it is a scar and not an
   oversight: `src/dash.mjs` documents that a previous telemetry stack *"spent weeks serving
   unauthenticated admin to the LAN, and the cheapest way not to repeat that is to have no
   server at all."* A future GUI is a second renderer over the same read model and **must argue
   against that decision explicitly, not forget it.**
8. **2026-08-27 — task numbering restarts at 1 in each plan document** (`../MASTER-PLAN.md`
   §B.8). S2's continuity across a family was a residue of the retired single document.
9. **2026-08-27 — S3 adds no migration if it adds no column.** `phase_run` already exists in
   `hub.sql`; if that holds, W2's three hand-maintained inventories are untouched and issue
   reeve#43 is not a blocker. **If any S3 task finds it needs a column, #43 lands first** or S3
   owes three new inventory entries.

10. **2026-08-27 — the spec repos are `<project>-specs`, one private repo per project:**
    `revnix/reeve-specs`, `nextlyhq/nextly-specs`, `revnix/rext-specs`. Two alternatives were
    rejected on reasons worth keeping: **one shared repo** fails because `specRepoId` is a
    numeric GitHub id **per project**, so three projects sharing one id makes the snapshot stop
    distinguishing them — against §11.1's *"immutable numeric GitHub ids… with human-readable
    snapshots beside them"*; **reusing the code repos** fails because spec PRs would land beside
    code PRs, which is exactly the separation S4's gate depends on. **The repos do not exist
    yet — F5.**
11. **2026-08-27 — `--execute` stays OFF on the live guardian.** The issue reeve#52 gate is
    lifted, and this is a separate, deliberate decision not to arm. Reason: T7 and T8 modify
    files the running daemon executes, so arming before those merge means a live daemon acting
    on code that is mid-change. Revisit after T8 merges.
12. **2026-08-27 — the 15-minute watcher loop is STOPPED** (job `0011c181` cancelled). Nothing
    of this lane's is open. A watcher with no subject reports "quiet" every fifteen minutes, and
    quiet-with-no-subject is the reading that let a 22-hour CI outage pass unremarked. **Restart
    it when S3's first PR opens.**

    **RESTARTED 2026-08-28**, job `75eb5153`, subjects `reeve#58` and `reeve#60` — the condition
    this decision named has occurred. Recorded as a reversal rather than an edit, per this
    section's own rule that a decision which moved must stay visible as having moved.
13. **2026-08-27, CORRECTING decision 1's line budget — the unit of the plan cap is PRs (see the
    2026-08-28 correction at the end of this entry); it is not
    lines.** The "~1,200 lines per document" figure was computed against S3's **16 PRs**, when a
    PR decomposes into three to five *plan tasks* and the house style runs **~500 lines per plan
    task** (MEASURED: S2-A 6,328 lines / 13 tasks = 487; S2-C 2,724 / 5 = 545). Written against
    the line number, the S3 family came out at **1,677–1,957 lines per document**, and the
    overage is house-style content, not padding. **CORRECTED AGAIN 2026-08-28 — the unit is PRs,
    not plan tasks.** "Three or four plan *tasks* per document" is arithmetically impossible
    against the decomposition this very tracker records: **78 plan tasks across 6 documents**,
    which 3–4 tasks each caps at 24. A PR holds several tasks, and S3's 78 tasks are 16 PRs.
    **The rule is: at most three or four PRs per document, and never thin a task to fit a count**
    — the lever is fewer PRs per document, never shorter tasks. `../MASTER-PLAN.md` §B.1 is
    canonical and carries the reasoning; this row must not restate it differently.
    **The six-document decision is unchanged.**

14. **2026-08-28 — plan documents THIN: they carry properties, not inline test code.** A task
    still states its behavioural claim, its files, its consumed interfaces with real signatures,
    its `**On the broken implementation**` block and its **named stub**. It no longer carries the
    full failing test written out in a Markdown fence. **The measured reason:** plan documents
    draw roughly **five times** the defect density of code in this corpus, and **10 of the 38
    confirmed defects in S3-A and S3-B are stub loops that cannot be run as written** — because
    code inside a fence is never executed, so nothing catches it before an executor does. The
    executor writes the test in the editor, where it runs and fails honestly. **What is NOT
    dropped:** the failing-test-first discipline itself, the stub loop, and the requirement that
    every task name which assertion must go red and which stay green as controls. The plan says
    *what must be true and how to know it broke*; the editor is where the code is written.
15. **2026-08-28 — `admitTask` widens to store `depth` and `priority`** (closes D1). Design §5
    gives the founder a depth override, so the flag must bind. Two columns in one INSERT, inside
    S3-B's territory. **A flag that reads as working and does nothing is worse than a flag that
    is absent or one that refuses**, which is why the two cheaper options were rejected.
16. **2026-08-28 — `NEVER_TOOLS` becomes a per-action subtraction, for the three BUILD actions
    only** (closes D26). Design §4.1 grants RESEARCH `Agent(*)`, `WebSearch` and `WebFetch`, and
    the flat list withholds exactly those four; both could not hold. Every other action keeps the
    full withholding, **asserted by test with a control**, so the subtraction cannot silently
    widen. Recorded plainly: **this weakens a boundary that was added deliberately after a
    measurement** (`docs/measured/2026-08-24-the-sandbox-had-no-opinion-about-tools.md`), and it
    is a founder decision rather than a writer's because of that.

17. **2026-08-28 — the foundation lands as its own PR, ahead of any stage plan.** The master
    plan, the five trackers, the implementation prompt and the evidence go first; the six stage
    plans land one at a time after. **The reason is live friction, not tidiness:** the close-out
    rule makes the tracker line the last commit before a PR, and **P1 (#57) could not follow it
    because the tracker does not exist on `main`**. Every PR opened before the foundation lands
    silently skips its close-out step — the exact defect the per-stage tracker was designed to fix.
18. **2026-08-28 — `specRepoId` is resolved from GitHub by nwo, and does NOT use the hub cache**
    (closes the review's second P1). MEASURED: `repoIdFromHub` runs
    `SELECT repo_id FROM task WHERE project = ?` (`src/build/repoid.mjs:52-57`), keyed on the
    project **name**, so it answers with the **code** repository's id and the injected
    `fetchRepoId` is never reached. **This is a category error rather than a bug to patch:** the
    hub cache answers *"what is this project's repo id"* from admitted task rows, which is a
    different question from *"what is the id of this spec repository"*, and the spec repo will
    never appear in a `task` row so the cache would miss every time regardless. Admission is
    already network-first, so this adds no round trip in a new place. **Issue reeve#46** — an
    identity table keyed by nwo — remains the eventual convergence for both, and is deliberately
    kept **off** S3's critical path because it is a schema change and would drag in W2's three
    hand-maintained inventories and possibly reeve#43.
19. **2026-08-28 — a stub is proven applied by a HASH CHANGE, not a grep** (`../MASTER-PLAN.md`
    §B.7.5). MEASURED: three of S3-A's "stub verified applied" checks matched identically either
    way, making check 2 assumed rather than measured. A hash cannot be inert, and it is identical
    in every task, so it stops being invented per stub. The specific grep stays as a second check,
    stated as a count with its transition.

---

## 5. The durable findings

### How to read `verify-merge.mjs` on `reeve#58` after the close-out lands

`verify-merge.mjs 58` reported **13 of 13 INTACT at `main` = `4730b44`**. The close-out
(`reeve#62`) edits `trackers/MASTER.md` and `trackers/s3.md`, which are **2 of those same 13
paths** — verified by intersecting `diff-tree` on `a136836` with that branch's own diff — so once
it merges the answer becomes **`MERGED, THEN CHANGED ON MAIN`, exit 32**.

**That is the correct answer, not a regression.** The content arrived and something changed it
afterwards; the something is this file. A drifted verdict exists to ask a human whether the change
was a legitimate follow-up or an overwrite. Here it is a follow-up, and this paragraph is the
record of it.

**Do not "fix" a future exit 32 on `reeve#58` by editing a tracker row.** Re-read it as the tool
intends. `reeve#60`'s three paths are untouched by tracker work, so it stays exit 0 — which is
also the control: if both drifted, the cause would be something other than this close-out.

### A snippet in a fence ships unrun, and its failures look like successes

**MEASURED across three rounds on `reeve#58`: four false greens, all in bash the implementation
prompt added, three of them the exact shape `../IMPLEMENTATION-PROMPT.md` **rule 6** (lines 70-83,
the `fail=0` accumulator) forbids in the same file.** There is no rule 6 in this tracker; §4's
decision 6 is about sharing the guardian hub.

| the shape | why it reads as success |
|---|---|
| `git fetch … ; next-line` unchained | the failure falls through to a **cached** ref and the comparison prints no drift |
| `cmd \|\| echo "problem"` as a guard | `echo` succeeds, so the loop exits **0** and a wrapper proceeds |
| `for p in $(git … -z)` | word-splitting yields fragments **absent from both** revisions, so two empty reads compare EQUAL |
| `a=$(ls-tree …)` unchecked | a failed read is `""`, and `""` equals `""` |

Every one produces a PASS out of a FAILED read. One of them printed
`MERGED, AND INTACT ON MAIN` and exited 0 against an **unmerged** pull request, measured against
`reeve#58` itself while it was open.

**The cause is not carelessness, it is the medium.** Code inside a Markdown fence is never
executed, so nothing ever contradicts it — the same durable finding this stage recorded about
plan documents, arriving in the prompt instead. Writing the rule down a fourth time would not
have helped; three of these four were added in the round that fixed the previous one.

**The step that closes it:** extract every fenced command block **out of the document and run
it** before the commit that adds it, against cases with known different answers. The current
fallback is verified at `0` / `32` / `23` against a merged-and-intact, a drifted and an unmerged
pull request, and the extraction is re-run whenever the block changes.

### A research document read as a contract drifts, and the drift is invisible

**MEASURED 2026-08-28, in one review round: nine findings, all one shape.** `../S3-DESIGN-BRIEF.md`
§2.2 was described in §0 of this tracker as *"specifies every task"*. It is a dated research
artifact that is never back-patched, so every correction recorded here left it further behind —
and because each of its task specs reads perfectly well on its own, nothing in the document
signals that it is stale. The brief is now explicitly non-normative (§0), and carries a banner.

**The nine, kept because the PLANS were derived from the brief and may carry the same errors.
Check each when S3-A through S3-F are reworked:**

| # | Task | The brief says | What actually holds |
|---|---|---|---|
| 1 | T1 | `Depends on. Nothing` | **P2 (reeve#50)** — this tracker's T1 row and `MASTER.md` both say T1 does not start until #50 merges |
| 2 | T1 | Verify list closes on keys added and defaulted | none of it exercises the `Builds` obligation that profile docs/examples are **generated from `FIELDS`**; no such generator exists, so T1 could report success with the anti-drift requirement unimplemented |
| 3 | T2 | move the `readFileSync` loader into `src/build/registry.mjs` | **`registryio.mjs`** (D7) — `test/hub-registry.test.mjs:89-104` forbids that module's graph reaching the filesystem |
| 4 | T3 | file list omits `src/build/registry.mjs` | decision 15 requires `admitTask`'s INSERT to widen for `depth`/`priority`; without it the flags parse and are **silently discarded** |
| 5 | T3 | `--pin-territory` as a bare switch | design §2.2 specifies `--pin-territory 48h`; `grantLease` consumes `pinnedUntil` (D15). A valueless flag loses the founder-selected expiry |
| 6 | T4 | `reviewDiff` takes no optional parameters | D4 preserves the live `reviewDiff({ files, profile, lane = null, action = null })` for guardian callers; the requirement belongs to the **new `reviewArtifact` sibling** only |
| 7 | T7 | file list omits `src/canary.mjs` and `src/checkout.mjs` | Q2's `.git` write/commit probes and Q3's instruction-file neutralization are **assigned to T7** and cannot be built without those files |
| 8 | T12 | outbox control inserts a `notify` row | the predicate is `kind LIKE 'gh.%' OR kind='git.push.branch'`, so a `notify` row leaves the count **zero in both arms** — a control that cannot fail |
| 9 | T15 | adds a provider-scheduler doctor row | `builder doctor` already emits it as **H-5** (D34): six rows, not seven, and assert H-5 appears at most once |

**Finding 8 is the one to internalise.** A positive control that cannot match is the defect class
this programme has now measured four separate times, in three different documents.



The lessons, not the changes. These are about plans and designs being wrong, so they cannot
live inside a plan.

**The durable finding is that a plan can survive sixteen adversarial review rounds and still
contain a test that cannot fail.** It was found by *executing* Task 1, not by reviewing it. A
review reads for what is wrong; only a run reads for what is absent. Every S3 task therefore
names its stub as a **step**, not as a promise in the preamble — S2-B promised *"every task
names the stub explicitly"* and then contained the word `stub` exactly once, in that very
bullet.

**The durable finding is that a Markdown fence hides code from every linter that would
otherwise read it.** MEASURED 2026-08-28 across the six S3 plan documents: **272 fenced blocks,
257 parse standalone, 15 do not.** **⚠ SUPERSEDED — this run's totals do not describe the current
plans.** The re-measurement in §1 counts **365 blocks, 351 checked, 332 standalone, 19 not**, with
both controls firing. Keep this paragraph for the durable finding it records, which is unchanged
and is not about the count; take the numbers from §1. Fourteen of the fifteen are deliberate in-context fragments —
object-literal entries, a `case`, an array element, statements that live inside a function — and
each was confirmed by wrapping it in the context the plan says it lands in. **The fifteenth was
a real parse error**, and beside it sat an assertion that could not fail. Neither had been caught
by the writing, and neither would have been caught by reading: the corpus's largest single
finding shape is *"the snippet is not runnable as written"* at **176 findings, 137 of them inside
`.md` files**, precisely because no tool looks there.

The instrument is thirty lines: extract every fence, run `bash -n` or `node --check`, and print a
COUNT before any listing. It was validated against a deliberately broken block of each language
**before its output was believed**, and a second control confirmed a genuinely malformed block
parses in *no* wrapper — without that, "wrapping fixed it" would have been unfalsifiable in the
same way the defect it found was. **Run it on any plan document before opening its PR.** It is
not in the repository yet; that is a decision for the founder, not a thing to slip in beside a
docs commit.

**The durable finding is that a check shaped to the instance is not a check on the property, and
I committed it while fixing an instance of exactly that.** Back-patching the `writeArtifact`
drift (D24) into S3-D, I verified the fix with `grep -c 'writeArtifact(path'` across the family
and got 1 — S3-F's prose describing the wrong form — and reported the family clean. **S3-C
carried the same defect in a different spelling**, `writeArtifact(dir, phase, bytes)`, declared
in its consumed table and called positionally at two live sites. The grep could not see it
because I had shaped the pattern to the wrong form I had already found rather than deriving it
from the right one. The check that would have worked runs the other way: take **S3-B's shipped
signature**, which owns the file, and assert every call site in the family matches **it**. A
verification derived from the defect finds that defect; a verification derived from the contract
finds the class. Cf. [the enumeration that reports itself complete] below — same shape, and this
one was committed inside the very commit that fixed its sibling.

**The durable finding is that an enumeration reports itself complete.** A hand-built list of
side effects, call sites or cases returns the items the author thought of, and nothing in the
output says the list is partial. On reeve#44, a four-item list of what a halted guardian would
newly execute was published as three; the peer lane read the same span and found the fourth.
Say what you searched and how, so the boundary travels with the claim.

**The durable finding is that absent and unreadable are different facts, and treating them as
one is this repository's dominant defect class.** A skipped CI job and a CI job that never ran
both report zero steps. An empty branch list and an unconfigured watcher both report nothing.
The instrument that cannot tell them apart answers a smaller question and reports success.

**The durable finding is that a mechanism's limit does not travel with the mechanism.** Carry
contracts between stages, never tuned numbers: an inherited workaround constant hides a real
defect behind a green run.

---

## 6. Defect log (append-only, newest first)

One row per finding **class**, not per finding. Rows are capped at 400 characters; anything
longer belongs in §5 or in a document under `docs/measured/`. (Measured: nine rows of the old
log exceed 1,200 characters, the longest 2,435.)

| date | PR | defect | cause | fix |
|---|---|---|---|---|
| 2026-08-28 | reeve#58 | **A research document read as a specification** — nine findings in one round, each the brief contradicting a correction this tracker already held. §5. | It records *why*; it was cited as stating *what*, and is never back-patched. | Demoted and bannered; the nine kept in §5, because the plans came from it. |
| 2026-08-28 | reeve#58 | **Four false greens in bash the implementation prompt added**, three of them the shape its own rule 6 forbids. One reported a clean merge for an **unmerged** PR. §5. | Code in a Markdown fence is never executed, so a snippet ships unrun. | Each block is now extracted from the file and RUN before it ships. |
| 2026-08-28 | reeve#60 | **Whole-PR coverage cannot be proven after the fact**; attempted three ways — repo merge settings, PR commit count, PR file list. | All three infer a PAST merge from PRESENT mutable state. | The claim was removed rather than re-bound; the verdict now states its own scope on every answer. |
| 2026-08-28 | reeve#60 | **A decoder collapsed distinct paths**: every invalid UTF-8 byte becomes U+FFFD, so two byte-distinct filenames became one entry and a drifted file verified clean. | A byte-string treated as text. | Read as `latin1`; paths never returned to git as argv; decoded once at the output boundary. |
| 2026-08-28 | reeve#58 | **A correction recorded in one file and left standing in another, fifteen times**: plan cap 4, merge comparison 3, claim enum 2, watcher status 2, plus V6 arms, `hubAccess`, renderer, CI classifier. | §B.8 rule 3 requires the back-patch; no step made it happen. | One canonical statement, referenced elsewhere; grep-with-control before each commit. |
| 2026-08-28 | S3-A/B plans | **Thirty-eight defects in the two documents that land first, 23 of them P1** — full list in `../S3-PLAN-REVIEW-FINDINGS.md`. Ten are stub loops that cannot be run as written; ten are the family disagreeing with itself about names it shares. | Written to a spec, reviewed by reading. Almost every one was found by *executing* the plan's code against real source. | Neither document lands until they are worked. |
| 2026-08-28 | S3-C plan | **The `writeArtifact` back-patch was incomplete**: S3-C carried the same drift as S3-D in a different spelling, and my verification grep was shaped to S3-D's wrong form. | A check derived from the defect instead of from the contract. | Re-verify against **S3-B's shipped signature**, which owns the file, not against the known-bad string. |
| 2026-08-28 | S3-A plan | **An assertion that could not fail**, standing where the task's own preamble had promised a positive control: `check(JSON.stringify(census.call(null)) !== undefined, "control: the census helper is callable")`. `JSON.stringify` returns `undefined` for almost nothing, so the assertion is unfalsifiable — and `census` closes over a db closed two lines earlier, so it would have **thrown** rather than passed. | A control written to satisfy the *shape* of the rule rather than the property. The plan's own prose called it a placeholder and told the executor to delete it, which leaves the defect in the artifact. | Replaced with the control the preamble promised: the snapshot resolved **and** carries the repo id the hub recorded. "No row changed" is also true of a lookup that never opened the hub. |
| 2026-08-28 | S3-A plan | **A code block that cannot parse**: `await` inside a non-async arrow (`const guarded = (path) => { … await import("node:sqlite") … }`). | The dynamic import was inlined to avoid adding a top-level import. | Hoisted to a module-scope import. **A test file that does not parse reports no failures, which reads exactly like a file that passed** — the failure is silent in the direction that looks like success. |

---

## 7. Measured anchors, re-derived at `16cd880`

`../S3-DESIGN-BRIEF.md` measured its `file:line` references at **`c500cfe`**. PR reeve#49
merged at 2026-08-27T17:51:13Z and moved four of the files the brief cites. **These are the
values at `16cd880`; the brief's are one merge out of date.** Each was found by its anchor
string, not by its number, and the anchor is given so the next reader can re-find it after the
next merge.

| file | anchor string | brief said (`c500cfe`) | now (`16cd880`) |
|---|---|---|---|
| `src/daemon.mjs` | file length | 3,336 lines | **3,355** |
| | `export async function tick(ctx) {` | `:956` | **`:975`** |
| | tick's closing brace | `:3206` | **`:3225`** (still **2,251 lines**) |
| | `export function announceable(` | `:3217` | **`:3236`** |
| | `export async function run(ctx) {` | `:3290` | **`:3309`** |
| | `(ctx.providerClaim ?? claimProvider)(` ×3 | `:2053, :2115, :2302` | **`:2072, :2134, :2321`** |
| | `const retireUnconfigured = () =>` | `:1094` | **`:1061`** |
| | `committing leaves a clean tree` (W10's stale comment) | `:475` | **`:476`** |
| `src/profile/schema.mjs` | `export const FIELDS = {` | — | **`:171`** |
| | `"ci.appSlug"` | `:174` | **`:199`** |
| | `builder.capabilities.*` block | `:205-238` | **`:230-234`**, with `builder.founder.*` at `:238-239`, `builder.cancel.drainMinutes` at `:244`, `builder.network.research.allowedDomains` at `:263` |
| | `"worker.isolation"` | `:228` | **`:253`** |
| | `"worker.dependencyPaths"` | `:233` | **`:258`** |
| `src/prompts.mjs` | `export const WORKER_ACTIONS` | `:492` | **`:527`** |
| | `export const UNBUILT_ACTIONS` | `:501` | **`:536`** |
| | `export function promptFor(` | `:506` | **`:541`** |
| `src/doctor.mjs` | `export function hubFindings(` | `:1021` | **`:1075`** |

**What reeve#49 did NOT change, checked rather than assumed:** it added **no `builder.*` key**
to `FIELDS` (its `profile/schema.mjs` diff is entirely the `bodyFindings` validator), so T1's
territory is unchanged. Verified with `git diff c500cfe 16cd880 -- src/profile/schema.mjs`.
Every other file the brief cites — `src/build/**`, `src/provider.mjs`, `src/supervisor.mjs`,
`src/sandbox.mjs`, `src/workerenv.mjs`, `src/checkout.mjs`, `src/paths.mjs`, `bin/reeve` — is
**untouched** by #49; verified by `git diff --name-only c500cfe 16cd880` filtered against that
list, which returned exactly the four files above.

### The test baseline, re-measured

**93 test files, 0 failures, 5,131 PASS assertions**, excluding `test/escape.test.mjs`, on the
content of `16cd880`, under `REEVE_HOME` pointing at a directory literally named `.reeve`,
with `~/.nvm/versions/node/v24.17.0/bin/node`.

| TZ | files run | failures | PASS assertions |
|---|---|---|---|
| `UTC` | 93 | **0** | **5,131** |
| `Asia/Karachi` | 93 | **0** | **5,131** |

Both passes ran against the **same code** — the two commits they were taken on (`1537e78`,
`a15243a`) are docs-only above `16cd880`, so nothing under `src/`, `bin/` or `test/` differs
between them. The two timezones agree exactly, which is the point of running both: this suite
has a measured history of a fixed-path collision between concurrent UTC and `Asia/Karachi`
passes (`test/lifecycle.test.mjs`), so **every new S3 test uses `mkdtempSync`.**

**This is the base every S3 task is measured against — never a chained comparison against the
previous task.** The prior figure of *91 files, 5,006 PASS* was measured on `c500cfe`; reeve#49
added `test/doctor-body-detector.test.mjs` and `test/review-body-findings.test.mjs`.

**The instrument was controlled before its output was believed.** Control 1: one file
(`test/verdict.test.mjs`) reports 72 PASS lines on its own, so the counter is not counting
nothing. Control 2: a deliberately red probe exits 1 and the loop's accumulator sees it — so a
red file could not have been silently absorbed. Both were necessary: a previous session's
watchdog reported *"91 of 91 files FAILED"* with `rc=0` on the same line, because `timeout` does
not exist on macOS and every invocation was `command not found`.

### P1's scope, re-measured at `16cd880` rather than inherited

The handoff describes P1 as *"four modules need one injected `gh` seam — three already have the
parameter"*, measured at `c500cfe`. **That is not the shape of the problem.** `git grep` finds
**15** modules under `src/`+`bin/` that shell to `gh`, so the figure was re-derived from the
running suite instead of carried forward.

**Method.** A `gh` shim first on `PATH` that appends its caller and arguments to a log and exits
1 — the way an unauthenticated `gh` fails in CI. The whole suite run through it, per file, with
the log truncated between files so each file's count is its own. Controls: the shim was confirmed
to shadow the real binary (`command -v gh` under the modified `PATH`), and the real `gh` was
confirmed still reachable without it.

**Measured, 2026-08-28, `TZ=UTC`, 93 files:**

| | |
|---|---|
| suite wall-clock, stubbed `gh` | **162 s** (against ~550 s on the real network) |
| test files that exec `gh` | **11** |
| total `gh` invocations | **2,315** |
| failures under the stub | **1** — `test/outbox-drain.test.mjs`, which **deliberately** runs a real `gh api --version`. It is the expected control, not a regression, and P1 must leave it alone. |

**The distribution is the finding, and it changes the plan:**

| calls | file |
|---|---|
| 1,045 | `test/dispatch-e2e.test.mjs` |
| 911 | `test/guardian-provider-lease.test.mjs` |
| 171 | `test/checkout-root.test.mjs` |
| 79 | `test/worker-contract.test.mjs` |
| 57 | `test/flake-dispatch.test.mjs` |
| 20 | `test/cli-flags.test.mjs` |
| 19 | `test/provider-queue-order.test.mjs` |
| 6 | `test/shadow-same-moment.test.mjs` |
| 4 | `test/doctor-containment.test.mjs` |
| 2 | `test/fold-before-evaluate.test.mjs` |
| 1 | `test/outbox-drain.test.mjs` (deliberate) |

**Two files are 1,956 of 2,315 calls — 84.5%.** So P1 is not "seams for fifteen modules"; it is
the seam those two files need, then the tail. The full listing is above rather than a `head` of
it, because the claim is about a set.

---

### The base moved again, and the repository went public

**`main` is `36b4107`.** It moved twice during the session that wrote these plans: `c500cfe` →
`16cd880` (#49) → `36b4107` (#54). **Every anchor in §7 still holds**: #54 changed only
`.github/workflows/ci.yml` (+39 lines, no source file), verified with `git diff --stat` and a
control confirming the plans do cite files that *did* move under #49. Only the base label changes.

**`revnix/reeve` is PUBLIC**, since 2026-08-27. This is a **founder decision, taken deliberately
with the exposure audited beforehand, and reaffirmed. It is closed — do not reopen it.**

*Why*, recorded so nobody re-derives it: the `revnix` org has **68 private repositories sharing
one free 2,000-minute Actions allowance**, and reeve alone was measured at roughly **3,400
minutes a month**. The quota was exhausted at the ORG level, which is why three unrelated repos
showed the identical zero-step signature. **The outage was never transient and it will not
recur.** Moving orgs would have deferred it about three weeks and then broken nextly's own
repositories instead. **The earlier note in the handoff that PR #54 was "the plausible cause" of
the recovery is WRONG and is superseded by this row.**

*What was audited before the flip*, by the guardian lane, and accepted: a 298-line
`docs/2026-08-20-portfolio-readiness.md` naming client organisations beside judgements about
their engineering; the ntfy host in seven documents; and `deploy/baselines/nextlyhq/nextly.json`,
a complete map of nextly's branch-protection weaknesses. It was also stated plainly that **all
153 commits are exposed, so deleting a file afterwards does not remove it.**

**What this stage owes as a consequence**, which is *not* a reopening of the decision:

1. **The false sentence is gone from the five S3 plans.** They each carried *"This plan touches
   only `revnix/reeve`, which is private"* — a **false present-tense claim in a plan document**,
   which is the class the docs guard exists to stop and does not police plan files. Replaced with
   the rule that actually survives: **Rule 15 governs effects against OTHER repositories, and it
   is unchanged.** `docs/superpowers/plans/2026-08-23-s2a-hub-store.md` carries the same sentence
   and was **deliberately left alone** — it is a merged record, and editing merged history into
   agreement with the present is what this programme's own correction discipline forbids.
2. **The spec repos F1 named must be PRIVATE.** Design `:77` refuses to run against a spec repo
   whose visibility is anything but exactly `private`, re-queried per effect with no TTL cache.
   Reeve's own repository going public does not relax that by one inch.
3. **No credential is exposed.** Checked independently by two lanes reaching the same conclusion
   from different directions: the `ghp_`/`github_pat_`/`BEGIN RSA` hits are the redaction fixture
   at `test/notify.test.mjs:50-58` exercising `src/notify.mjs`'s `SECRETS`, beside an obvious
   `ghs_ABCDEFGHIJ…` placeholder; the two highest-entropy strings carry **36 distinct characters
   in 36 positions**, which a real token does not. Reported as locations and shapes, never values.

---

---

## 8. What the plan writers found that the brief had wrong

The six plan writers were told to verify every cited anchor **in the source** rather than copy
it, and to return every disagreement rather than smooth it over. They found the following.
Each row was re-measured at **`16cd880`**; the ones marked **VERIFIED HERE** were additionally
re-checked by this lane with a positive control, because they change what a task must build.

**Read this before writing any S3 task's code.** The brief is 145KB of measured work and it is
still one merge and several assumptions out of date; this section is the delta.

| # | The brief said | The source says, at `16cd880` | Consequence |
|---|---|---|---|
| D1 *(CLOSED, §4.15)* | T3 adds `--depth` and `--priority` flags to `reeve task file` | **`admitTask`'s INSERT (`src/build/registry.mjs:305-318`) names 22 columns and neither `depth` nor `priority` is among them.** The only `depth` in the whole file is a **comment** at `:205`. **VERIFIED HERE** with a positive control (`idempotency_key` → 3 hits; `depth` → 1, the comment). | **Both flags would be inert — accepted, validated, and silently discarded.** ~~Either `admitTask`'s INSERT widens or the flags do not ship. This needs a decision before T3 is executed.~~ **DECIDED 2026-08-28, §4 decision 15: `admitTask`'s INSERT widens** to carry `depth` and `priority`. Design §5 gives the founder a depth override, so the flag must bind. |
| D2 | `src/build/tables.mjs` declares a writer for every table (the prose-versus-DDL cross-check) | **`tables.mjs:19,20,64` name `intake.mjs` as the writer of `task`, `task_territory` and `territory_lease`, and `src/build/intake.mjs` DOES NOT EXIST.** **VERIFIED HERE**: 4 references repo-wide, all inside `tables.mjs` itself; positive control, `registry.mjs` is found in 3 other files. | **The same shape as contradiction C7 (`worktree.mjs`), and worse, because `test/hub-crosscheck.test.mjs` exists to assert every table has a declared writer — so the cross-check is green on a writer that is not there.** The real writer is `registry.mjs`'s `admitTask`, and S3-B's T3 is its first caller. |
| D3 | The raw-SQL rule is violated by **12 paths with 98 `.prepare()` calls** | **12 paths with 102 calls.** `src/review/derive.mjs` grew 16 → 19 and `src/review/ingest.mjs` 4 → 5 when reeve#49 merged. **VERIFIED HERE** with the full per-file listing and a positive control. | The rule and its consequence are unchanged; the number in `../MASTER-PLAN.md` §B.11 is corrected. **The violation is growing, which is the argument for the guard, not against it.** |
| D4 | `reviewArtifact` and `reviewDiff` are mandatory siblings with **no optional parameter in either signature** | `reviewDiff({ files, profile, lane = null, action = null })` **never throws** — it returns `{ok, why, files}` on every path — and it **already has two optional parameters**, with a live guardian caller in `src/daemon.mjs`. | Removing them is a **guardian-touching** change, which §B.1 rule 3 says must travel alone. S3-B's ruling: `reviewArtifact` ships with no optional parameter, and `reviewDiff` gains **one additive throw** for an artifact phase name and nothing else. |
| D5 | `hub.sql:27` says task ids are `bt:<ulid>`, *"minted by the hub"* | **No ULID minter exists anywhere in `src/`.** | S3-B's T3 adds `mintTaskId` in `taskfile.mjs`. A comment describing a mechanism is not the mechanism. |
| D6 | `task.text_hash` and `task.filed_via` are columns of the admission row | **Zero writers and zero readers.** One file matches, `src/build/hub.sql`, 2 hits — the two declarations. Positive control: `idempotency_key` matches 8 files. | Two more columns in the same class as `builder.capabilities.observe` was before S3: declared, and nothing reads them. |
| D7 | `registryProjects` moves out of `bin/reeve` into `src/build/registry.mjs` (T2) | **It cannot.** `test/hub-registry.test.mjs:100-103` asserts that module's import graph reaches **no filesystem module**, and the loader is built on `readFileSync`. | T2 puts the loader in a new module instead. **An assertion nobody re-read would have failed the whole PR.** |
| D8 | `doctor`'s H-7 consumes the project list (`src/doctor.mjs`) | **H-7 lives at `bin/reeve:1105`**, not in `src/doctor.mjs`. | S3-A does not modify `src/doctor.mjs` for T2. |
| D9 | The `builder.*` seed lives in `src/init.mjs` | It lives in `UNIVERSAL_DEFAULTS` at `src/profile/schema.mjs:444`. `init.mjs`'s real gap is `ORDER` at `:40`, which names neither `builder` nor `worker`. | T1 edits a different file than the brief says. |
| D10 | `nextPhase` returns `{ok:false, reason, …}` | **The field is `refusal`** (`src/build/phases.mjs:81`). | Every assertion in S3-D reads `refusal`. A test reading `reason` would compare `undefined` to `undefined` and pass. |
| D11 | `resolveSnapshot` consumes what `registryProjects` returns | `resolveSnapshot` takes an **object map** `{version, projects: {<name>: {...}}}` and reads `registry.version`; `registryProjects` returns an **array**. | The conversion happens once, in the `task` route, and S3-B says so rather than leaving an executor to discover it. |
| D12 | `withWriterLease` holds a transaction across its callback | It does **not** (`src/build/locks.mjs:88-101`), so `admitTask`'s own `hubTx` nests nothing. | Stated explicitly, because the opposite is the natural assumption and would send an executor hunting a nested-`BEGIN` throw that cannot occur. |
| D13 | `phases.mjs` refusals at `:648-654`, `:637-641`, `:127-131` | Depth refusal at **`:654`**; attribution refusals at **`:639`** and **`:641`**; `blocked_other` at **`:132`**. `DEPTHS` is at `:139` and **is not exported**. | S3-B asserts the depth set **behaviourally** rather than restating a constant no caller can see. |
| D14 | The sibling-function ruling is design "§15.2" | **There is no §15.2.** It is item 2 of the §15 list at `docs/2026-08-21-builder-design.md:875` (§15 starts at `:872`). | Cite by line. |
| D15 | `--pin-territory` is a switch | Design `:127` writes `[--pin-territory 48h]`, and `grantLease` takes `pinnedUntil`. **It is valued.** | The design wins over the brief's paraphrase. |
| D16 | `--territory` repeatability is new machinery | The argv walk at `bin/reeve:411` **already** pushes every occurrence into an array, and `all(n)` at `:432` returns the list. | Only a `FLAGS` entry is needed. Do not build a parser that exists. |
| D17 | `buildTick` has never refreshed a real gate-state row | It already does for a registry-shaped project — `test/hub-gatestate.test.mjs:313` is green today. | S3-A's task proves the narrower and still-true thing: that it does so **from the loader's own rows**. |
| D18 | `src/paths.mjs:69` is `hubPathFor` | It is at **`:68`**; the file is 70 lines. | Trivial, and recorded because the plan family cites it four times. |

| D19 | `reeve build measure-provider` is the writer of `provider_state`'s measured limits | **The subcommand does not exist.** `bin/reeve` `die()`s on any `build` subcommand that is not `run` or `status`. `measure-provider` appears in exactly three files: a comment in `hub.sql:696`, a *declared* writer in `tables.mjs:66`, and **two `doctor.mjs` action strings (`:1243`, `:1247`) that tell the operator to run it.** **VERIFIED HERE.** | **`reeve doctor` advises a command that cannot be run** — the operator following its advice gets a usage error. T16 is the first writer, and it closes a live operator-facing defect as a side effect. |
| D20 | `provider_state.measured_at` is written by the measurement | **It has no writer at all.** Exactly **one** statement writes `provider_state` in `src/`+`bin/` — `recordRateLimit` at `src/build/providerdb.mjs:282` — and it names `provider, concurrency_limit, guardian_reserved, cooldown_until, last_429_at, last_signature`. **Not `measured_at`.** **VERIFIED HERE** with a positive control (`cooldown_until` **is** named, on the next line). | Nothing above `doctor` can distinguish a **measured** limit from a **defaulted** one — which is precisely what design `:572`'s *"Limits are measured before they are chosen"* exists to prevent. T16's Task 1 is that distinction. |
| D21 | `providerState()` returns the row | It **SELECTs `measured_at` and drops it** from the returned object (`providerdb.mjs:114` vs `:117-123`). `src/doctor.mjs:1232` gets around this with its own `SELECT *`. | One field added, four call sites, none in `daemon.mjs`. |
| D22 | V6 is recorded into `builder.provider.concurrencyLimit` / `guardianReserved` | Those **profile keys are not in `FIELDS`**; the **columns** exist (`hub.sql:691-696`). The design and `MASTER-PLAN` name columns; the brief named profile keys. | The columns win. Recording a measurement into a key the validator would reject is not a recording. |
| D23 | S3 owes a migration for the measurement columns | **It does not.** `SCHEDULER_COLUMNS` already declares `provider_state.measured_at` (`providerdb.mjs:83`), and `phase_run`'s whole contract-snapshot column family already exists in `hub.sql`. | **W2's three hand-maintained inventories are untouched**, so issue reeve#43 is not an S3 blocker. This is the condition decision 9 was conditional on, now checked. |
| D24 | *(family drift, found by S3-F reconciling against its siblings rather than against the brief)* | **S3-D's consumed table spelled S3-B's schema files with hyphens** where S3-B ships `build_size.json` with underscores, **and derived `writeArtifact(path, bytes)` where S3-B ships `writeArtifact({dir, phase, bytes}) -> {path, sha256, bytes}`.** | **S3-B owns those files and wins.** Had it gone unchecked, S3-F's Task 9 schema guard would have refused every real schema and reported V5 as *unrunnable* rather than *broken*. **Back-patched into S3-D on 2026-08-27**, per §B.8 rule 3. Verified afterwards: zero hyphenated names survive in the family; 19 underscore forms and 11 object-signature forms do. |


| D25 | T7 adds *"three new cases in `sandboxFor`'s per-action switch"* — design `:280` calls it *"the intended extension seam"* | **`sandboxFor` has no `switch` statement.** `grep -c '\bswitch\b' src/sandbox.mjs` → **0**. **VERIFIED HERE** with a positive control (`switch` is found in `src/baseline.mjs`, `hubaccess.mjs`, `hubguest.mjs` and others). The seam is **two ternaries** (`:534`, `:539`) plus a module-private `NETWORK_DOMAINS` map (`:347`). | T7 builds the per-action table the design *describes* rather than extending a switch that is not there. **A design that names a mechanism is not a design that has one** — the same shape as D2, D5 and D19. |
| **D26** *(CLOSED, §4.16)* | RESEARCH gets `Agent(*)` + `WebSearch` + `WebFetch`, and DESIGN gets `Agent(*)` (design §4.1) | **`NEVER_TOOLS` (`src/sandbox.mjs:129-135`) contains `"WebFetch", "WebSearch", "Task", "Agent"`**, and it reaches **both** `disallowedTools` (`:614`) **and** `permissions.deny` (`:617`). **VERIFIED HERE.** | **The design and the code cannot both hold, and neither document says which gives way.** S3-C resolved it as a **per-action subtraction** (`neverToolsFor`), asserted to subtract nothing for every action other than the three BUILD ones. **This is a founder decision, taken by a writer — see below.** |
| D27 | `isRevoked` is a seam T6 wires, and the test asserts `readStart(pid) === null` | **The seam is real and the assertion is reachable** — `probeRevoked()` at `src/supervisor.mjs:406` is polled by `setInterval(…, 2000)`, sets `killedByUs`, `SIGTERM`s the group and `SIGKILL`s after `graceMs`. But **four details change the test**, none of them in the brief: (a) the probe is **polled every 2,000 ms, not on demand**, so an assertion reading `readStart(pid)` immediately after `applyTransition` fails against *correct* code; (b) **`/^cancelled\b/.test(lateWhy)` at `:429` decides the outcome** — a reason string that does not begin `cancelled` records a founder cancel as `LEASE_LOST`; (c) **a throwing probe is itself a revocation** (`probeRevoked` catches and returns the error as the reason), so an unreadable hub *kills* the worker, which is fail-closed and must not be softened; (d) **absent ≠ live** — a deleted row leaves a worker with no durable claim. **VERIFIED HERE** at `:406-416`, `:429`, and `transition.mjs:605`. | T6's drill uses a **bounded poll** and waits for the *binding*, never a fixed sleep. (b) is a live silent-defect risk: **a founder cancel durably recorded as a lost lease**. (d) was a **decision the writer took** — treat a missing row as revoked — and it is flagged in PR-C1's review-focus rather than buried. |
| D28 | `CLAUDE_CODE_PRINT_BG_WAIT_CEILING_MS` is T7 work | **It already exists**, set from `bgWaitMs` in `workerEnv`'s base env. | The only genuinely new environment variable in T7 is `CLAUDE_CODE_SUBPROCESS_ENV_SCRUB`. |
| D29 | Design §3.3 step 1 calls the recovery function `recoverOutbox` | **The function is `recoverEffects`** (`src/build/outbox.mjs:523`). Here **the brief is right and the design is wrong** — the only row in this table where that is true. | Cite the code. |
| D30 | The brief's own §2.0 rule 3 names the two guardian-shared tasks as *"T7 … and T9 provider claim"* | **§2.2 and this tracker both say T7 and T8.** The brief contradicts itself. | The plan follows **T7 and T8**. T8 is the provider claim; T9 is resume. |


| D31 | T13 consumes *"the `hubAccess`/`openHub` read path … `phase_event`, `phase_run`, `hub_event`, `provider_lease`, `escalation`"* | **`hubAccess` cannot read `escalation`.** `openHubAsGuest`'s authorizer allows exactly four tables (`provider_lease`, `provider_state`, `pr_hold` read, `maintenance_lock` read/delete) and denies everything else. **VERIFIED HERE empirically, with a positive control**: the same guest handle returns `{"c":0}` for `provider_lease` and `not authorized` for `escalation`. | The read routes open a **read-only `DatabaseSync`**, not the guest handle. **And the same measurement makes half of T15's two-store property already true**, so that task asserts the guardian half rather than building it. |
| **D32** | `src/build/tables.mjs` declares a **reader** for every table, and `test/hub-crosscheck.test.mjs` is the executable prose-versus-DDL check that guards it | **`tables.mjs:22,25,46` name `why`, `dash` and `notify.mjs` as the readers of `phase_event`, `phase_run` and `escalation`. `src/build/why.mjs` and `src/build/dash.mjs` do not exist.** And the guard, at `test/hub-crosscheck.test.mjs:64-66`, asserts only `typeof o.reader === "string" && o.reader.trim().length > 0`. **VERIFIED HERE.** | **This is the root cause of D2, D19 and D32 together, and it explains why the shape recurs.** The cross-check cannot distinguish a real writer from a typo from a file that was never written — it proves *somebody typed something into the cell*. `hub-crosscheck` is the one test in the repo whose whole purpose is to stop a declaration drifting from an implementation, and it checks the declaration against **nothing**. |
| D33 | `singleton_lease` carries a heartbeat the dash can read (§11.6 asks for *"heartbeat + last-seen"*) | **There is no `heartbeat_at` column.** The columns are `(name, pid, lstart, command, acquired_at, expires_at)`, and `heartbeatSingleton` slides `expires_at` by `LEASE_SECONDS`. The lease is named **`"builder"`** (`bin/reeve:1421`), not `"build"`. | Last-seen is **derived** from `expires_at - LEASE_SECONDS`, and T14 says so rather than reading a column that is not there. |
| D34 | `builder doctor` gains seven new S3 rows, including *"provider scheduler state and stale leases"* | **That row already exists as `H-5`.** Also `H-7` and `H-0` are emitted by `bin/reeve`, not `src/doctor.mjs`, and `H-4` only ever appears as `H-4:<nwo>`. | T15 adds **six** rows, not seven, and asserts `H-5` appears **at most once** — a duplicate row is how a checklist grows a second copy of a check nobody notices is doubled. |
| D35 | `task_pr` is a table in `hub.sql` | **It is not there at all** (0 grep hits); migration 2 creates it at `hubdb.mjs:100`. Its CHECK **rejects `generation`/`slice` on a `spec` row**, so the obvious fixture fails with `CHECK constraint failed`. Both forms were executed against a real hub. | A plan whose fixture is written from `hub.sql` alone cannot insert into this table. |
| D36 | `bin/reeve:1136-1145`'s comment says unknown flags are ignored | **They are refused.** And `bin/reeve:1116` cites `:364` for the exit-code convention; `:364` is the argv parser's unknown-flag branch — the convention's only statement is the usage text at `:1723`. | Two stale comments in the file S3-E edits most. This is why its Task 2 exists. |
| D37 | T14: *"the HTML and the JSON derive from one value"* | **There is no HTML.** Founder decision 7 (stay headless) and research R11 (a CLI digest, not a dashboard) both contradict it. | The property is kept and the nouns changed: **text and JSON** derive from one value. |
| D38 | W9's control: `schema.mjs` is referenced in **3** test files | It is now **4**. The `dash.mjs` zero still holds. | Recorded because a control that has drifted is not a control. |


**The pattern worth keeping.** Thirty-eight disagreements across six documents, and **not one was
found by reading the brief** — every one came from opening the file the brief cited. The brief
was itself the product of a fourteen-agent audit that measured carefully; it went stale in two
ways at once, by a merge (D3, D13) and by an assumption that a declaration implies an
implementation (**D1, D2, D5, D6** — four instances of the same shape in one stage). That shape
already has a name here: *permission to read is not a reader*, and *a comment describing a
mechanism is not the mechanism*. **D1, D2, D5, D6, D19, D20, D25 and D32 are eight instances of it in one
stage** — a declared writer with no writer, twice; a minter named in a comment and never
written; a column in an INSERT's table but not in its column list; and a subcommand that
`doctor` advises and `bin/reeve` refuses. **After the third instance of one shape the design is
wrong, not the instances** (`../MASTER-PLAN.md` §B.11's W2 is the structural version): the
cross-check at `test/hub-crosscheck.test.mjs` can prove a writer *claim* was made and can never
prove the writer exists, and that is the gap all eight fall through. **D32 is that gap stated
exactly**: the guard asserts `typeof o.reader === "string" && o.reader.trim().length > 0`. It
checks the declaration against **nothing**. The fix is one line of intent and some work — resolve
each declared writer and reader to a module or symbol that exists — and it belongs to whoever
next opens `tables.mjs`, which on the current plan is S3-C's T6 (`phase_run`'s first writer).

### Two that needed a founder decision — BOTH CLOSED 2026-08-28

> **These are history, not a gate.** Both were answered and the answers are §4 decisions **15**
> and **16**, which are binding and are not to be re-litigated. They are kept here because the
> *reasoning* is the useful part — the measurement that made each a question at all — and because
> a decision whose grounds are lost gets reopened.
>
> **An executor reaching T3 or T7 does not stop here.** §4 governs. Leaving this section reading
> as open would have deadlocked both tasks: the workflow halts on an unanswered blocking decision
> **and** forbids re-litigating §4, so the two sections together said "stop" and "do not ask" at
> once.

**D1 — the depth flag. CLOSED: `admitTask`'s INSERT widens** (decision 15).

**D1.** `reeve task file --depth deep` would be accepted, validated against the closed depth
set, and then **thrown away**, because `admitTask` has no column for it in its INSERT. The task
would file at whatever depth SIZING later computes, and the founder's stated depth would have
had no effect — **a flag that reads as working**. Design §5 does give the founder a depth
override, so dropping the flag is not obviously right either. The choice is to widen
`admitTask`'s INSERT (two columns, inside S3-B's territory, with a migration only if the
columns are absent from `hub.sql` as well as from the INSERT) or to ship T3 without the two
flags and add them when the override path lands. ~~This is recorded here and not decided.~~ **Decided 2026-08-28 — §4 decision 15: widen the INSERT.** A flag that reads as working and does nothing is worse than one that is absent or one that refuses.

**D26 — the tool grant. CLOSED: a per-action subtraction for the three BUILD actions only** (decision 16). `NEVER_TOOLS` is a **flat, unconditional** list that reaches both the
`--disallowedTools` argument and `permissions.deny`, and it contains the four tools design §4.1
grants the RESEARCH and DESIGN phases: `Agent`, `Task`, `WebSearch`, `WebFetch`. **The design
and the code contradict each other, and neither says which gives way.** S3-C resolved it as a
per-action subtraction — `neverToolsFor(action)`, asserted to subtract **nothing** for every
action other than the three BUILD ones — which is the smallest change that lets §4.1 hold. But
`NEVER_TOOLS` is *withheld* rather than merely denied, and the measured reason it exists is
`docs/measured/2026-08-24-the-sandbox-had-no-opinion-about-tools.md`: a tool no Bash rule could
reach. **Turning any part of it into a per-action list is a real weakening of a boundary that
was added deliberately**, and it is the one place in S3 where a writer's resolution changes the
worker's authority rather than its plumbing. ~~Confirm the subtraction, or rule that RESEARCH runs without subagents and web access and §4.1 is amended instead.~~ **Confirmed 2026-08-28 — §4 decision 16: the per-action subtraction, with every other action asserted to subtract nothing.** Recorded there as a deliberate weakening of a boundary added after a measurement.
