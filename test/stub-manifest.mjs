/**
 * Defects to reintroduce on purpose, and the assertion that must notice each one.
 *
 * A stub here is a claim: "if someone deletes this line, THIS assertion fails."
 * `node scripts/stub-sweep.mjs` checks every claim. A stub that leaves the suite
 * green means the assertion it names cannot fail for that reason — a test
 * reporting success regardless of the code.
 *
 * ADD ONE WHENEVER YOU ADD A GUARD. That is the whole point: writing the guard and
 * proving the guard is reached become one action instead of two, the second of
 * which is otherwise optional and gets skipped exactly when it matters.
 *
 * `expectRed` is text from the ASSERTION, not a file. A file-level expectation is
 * satisfied by any failure in that file, so a stub that breaks something adjacent
 * would read as success while the property it names stayed unmeasured.
 *
 * The seed set is the sweep that was run by hand over the dependency edge. Three
 * of those hand-run stubs came back green on first writing, and all three were
 * repaired by moving the assertion rather than by changing the code — which is the
 * evidence that this file should exist.
 */
// NOT HERE, deliberately: the byte budget on retained assertion lines.
//
// It prevents an out-of-memory, not a wrong reading. With the budget and without
// it the verdict is identical — the only difference is how much is held, and the
// failure it guards is the process dying, which a test would have to actually
// provoke to observe. A stub of it therefore comes back NOT_CAUGHT, correctly, and
// the honest response is to leave it unmanifested rather than to bend a test until
// it appears covered.
//
// The same reasoning applies to reaping a worker from a normally-exited test; see
// test/stubsweep.test.mjs, where the limit is written down instead of asserted.

/**
 * FROZEN DEBT: test files that predate the rule that every test file carries a stub.
 *
 * NOT AN EXEMPTION LIST, and the difference is enforced. A file here loses its place
 * the moment anyone MODIFIES it: the gate intersects a change with this list and
 * refuses, so the demand to prove a test can fail arrives in front of the person who
 * already has the file open and knows which assertion is load-bearing.
 *
 * That shape was chosen over a deadline and over a plain ratchet for one reason:
 * paying down a list is a separate act of virtue, it competes with real work, and it
 * loses. A calendar picks files at random and picks them when nobody is looking at
 * them; an edit picks exactly the file someone is already holding. A file nobody
 * touches costs nothing, and that is correct rather than a compromise -- an untouched
 * test is not accruing risk. The risk arrives with the edit, and so does the demand.
 *
 * NOTHING MAY BE ADDED HERE. A new test file has no history to be grandfathered by,
 * so it arrives with an entry in STUBS or it does not arrive. Growth is visible in
 * the diff, which is the point: re-grandfathering cannot be done quietly.
 *
 * The list is checked for ROT as well as for growth. A name that has since gained a
 * STUBS entry, or that no longer exists, must be removed -- a list nobody is required
 * to correct becomes a blanket exemption without anyone deciding to grant one.
 *
 * ONE OPERATIONAL NOTE, learned the hard way. This list freezes the tree as it stood
 * when it was written, and CI judges a pull request MERGED INTO the default branch --
 * so any test file that lands on main between generating this and merging it arrives
 * as an orphan and turns the gate red. That is the rule working rather than failing:
 * a file that appeared after the freeze genuinely has no claim to be grandfathered.
 * It does mean a long-lived branch carrying this list must re-check it against main
 * before merging, and the failure names the file, so the cost is one line.
 */
export const GRANDFATHERED = [
  "test/backup.test.mjs",
  "test/base-health-steps.test.mjs",
  "test/baseline.test.mjs",
  "test/build-capabilities.test.mjs",
  "test/canary.test.mjs",
  "test/cause-identity.test.mjs",
  "test/characterise-tick.test.mjs",
  "test/check-accounting.test.mjs",
  "test/checkout-root.test.mjs",
  "test/checkout.test.mjs",
  "test/checkpoint-lease.test.mjs",
  "test/ci-rootcause.test.mjs",
  "test/clean-merge.test.mjs",
  "test/containment.test.mjs",
  "test/denial-policy.test.mjs",
  "test/deploy.test.mjs",
  "test/dispatch-e2e.test.mjs",
  "test/doctor-body-detector.test.mjs",
  "test/doctor-containment.test.mjs",
  "test/doctor-signatures.test.mjs",
  "test/doctor-state.test.mjs",
  "test/durable-run.test.mjs",
  "test/effects-capability.test.mjs",
  "test/escalation-dedup.test.mjs",
  "test/escape.test.mjs",
  "test/flake-dispatch.test.mjs",
  "test/fold-before-evaluate.test.mjs",
  "test/freshness.test.mjs",
  "test/gitguard.test.mjs",
  "test/guardian-hub-access.test.mjs",
  "test/guardian-hub-allowlist.test.mjs",
  "test/guardian-provider-lease.test.mjs",
  "test/hub-derived-schema.test.mjs",
  "test/hub-doctor.test.mjs",
  "test/hub-locks.test.mjs",
  "test/hub-outbox.test.mjs",
  "test/hub-phases.test.mjs",
  "test/hub-registry.test.mjs",
  "test/hub-schema.test.mjs",
  "test/hub-transition.test.mjs",
  "test/hubsession-acceptance.test.mjs",
  "test/hubsession.test.mjs",
  "test/inherited.test.mjs",
  "test/init.test.mjs",
  "test/lease-expiry.test.mjs",
  "test/lifecycle.test.mjs",
  "test/log-dedup.test.mjs",
  "test/mergecheck.test.mjs",
  "test/missing-required.test.mjs",
  "test/node-floor-is-one-fact.test.mjs",
  "test/notify.test.mjs",
  "test/offline-tests.test.mjs",
  "test/outbox-drain.test.mjs",
  "test/outbox-fencing.test.mjs",
  "test/policy-self-exclusion.test.mjs",
  "test/profile-detect.test.mjs",
  "test/profile-validate.test.mjs",
  "test/prompt-sandbox-agreement.test.mjs",
  "test/prompt-study.test.mjs",
  "test/provider-queue-order.test.mjs",
  "test/provider-scheduler.test.mjs",
  "test/reconciler.test.mjs",
  "test/repair-message.test.mjs",
  "test/required-evidence.test.mjs",
  "test/retry-brake.test.mjs",
  "test/review-body-findings.test.mjs",
  "test/review-derive.test.mjs",
  "test/review-facts-wire.test.mjs",
  "test/review-gate.test.mjs",
  "test/review-ingest.test.mjs",
  "test/review-request-effect.test.mjs",
  "test/review-shadow.test.mjs",
  "test/reviewer-refusal-shapes.test.mjs",
  "test/reviewer-status.test.mjs",
  "test/sandbox.test.mjs",
  "test/schema-is-one-file.test.mjs",
  "test/schema-migration.test.mjs",
  "test/selfaudit.test.mjs",
  "test/settlement-persistence.test.mjs",
  "test/shadow-same-moment.test.mjs",
  "test/spill-handlers.test.mjs",
  "test/status-vocabulary.test.mjs",
  "test/status.test.mjs",
  "test/supervisor-contract.test.mjs",
  "test/supervisor.test.mjs",
  "test/uncommitted-baseline.test.mjs",
  "test/verdict.test.mjs",
  "test/watcher.test.mjs",
  "test/worker-args.test.mjs",
  "test/worker-contract.test.mjs",
  "test/worker-report.test.mjs",
  "test/worker-tool-boundary.test.mjs",
  "test/workerenv.test.mjs",
];

export const STUBS = [
  {
    name: "lease-gate",
    why: "let a dependent effect be leased before the one it waits for has delivered",
    test: "test/outbox-dependency-edge.test.mjs",
    expectRed: "the child is NOT leased while its parent is inflight",
    edits: [{
      file: "src/db/ops.mjs",
      find: `                  AND (o.depends_on IS NULL
                       OR EXISTS (SELECT 1 FROM outbox p
                                  WHERE p.id = o.depends_on AND p.status='done'))
`,
      replace: "",
    }],
  },
  {
    name: "missing-value-throws",
    why: "substitute a missing value silently instead of refusing, so a body reading \"see #\" ships",
    test: "test/outbox-dependency-edge.test.mjs",
    expectRed: "and the error NAMES the path that was missing",
    edits: [{
      file: "src/outbox/depends.mjs",
      find: "    if (v === undefined || v === null) { missing.add(path); return whole; }",
      replace: '    if (v === undefined || v === null) { return ""; }',
    }],
  },
  {
    name: "scalar-guard",
    why: "let an object stringify to [object Object] and be delivered as a comment",
    test: "test/outbox-dependency-edge.test.mjs",
    expectRed: "an object is refused, not stringified",
    edits: [{
      file: "src/outbox/depends.mjs",
      find: '    if (typeof v === "object") { nonScalar.add(path); return whole; }\n',
      replace: "",
    }],
  },
  {
    name: "inert-gate",
    why: "resolve every row rather than only those with an edge, dead-lettering ordinary comments that quote the token syntax",
    test: "test/outbox-dependency-edge.test.mjs",
    expectRed: "and its literal token text reaches the handler untouched",
    edits: [{
      file: "src/outbox/drain.mjs",
      find: "        if (job.depends_on != null) {",
      replace: "        if (true) {",
    }],
  },
  {
    name: "attempt-refund",
    why: "charge a delivery attempt for a handler that was never invoked",
    test: "test/outbox-dependency-edge.test.mjs",
    expectRed: "and no delivery attempt is charged",
    edits: [{
      file: "src/outbox/drain.mjs",
      find: "                             retryable: false, unattempted: true, error: err.message });",
      replace: "                             retryable: false, error: err.message });",
    }],
  },
  {
    name: "index-before-column",
    why: "create an added column's index before the column exists, which throws at open() on every store holding history",
    test: "test/outbox-dependency-edge.test.mjs",
    expectRed: "opening a store whose outbox predates the column does not throw",
    edits: [{
      file: "src/db/ops.mjs",
      find: "  addMissingColumns(db);\n  // AFTER the columns, never before. See ADDED_INDEXES.\n  addMissingIndexes(db);",
      replace: "  addMissingIndexes(db);\n  addMissingColumns(db);",
    }],
  },
  {
    name: "supersede-dependents",
    why: "retire a parent without its dependents, so the foreign key throws and rolls back every reconciliation",
    test: "test/outbox-dependency-edge.test.mjs",
    expectRed: "and the dependent is retired with it",
    edits: [{
      file: "src/db/ops.mjs",
      find: "    deletable.push(...kin.reverse(), r);",
      replace: "    deletable.push(r);",
    }],
  },
  {
    name: "cascade-budget",
    why: "let the cascade run past the pass budget, delaying evaluation, heartbeats and alerts",
    test: "test/outbox-dependency-edge.test.mjs",
    expectRed: "a spent budget cascades nothing",
    edits: [{
      file: "src/db/ops.mjs",
      find: "    if (now() >= deadlineAt) break;",
      replace: "",
    }],
  },
  {
    name: "cascade-deadline-wiring",
    why: "stop the drainer handing its deadline down, so the bound is correct and nothing is plugged into it",
    test: "test/outbox-dependency-edge.test.mjs",
    expectRed: "the drainer's own deadline reaches the cascade",
    edits: [{
      file: "src/outbox/drain.mjs",
      find: "cascadeDeadLetter(db, { deadlineAt: passDeadlineAt, now })",
      replace: "cascadeDeadLetter(db)",
    }],
  },
  {
    name: "sweep-restores-on-signal",
    why: "stop the sweep restoring the tree when it is killed, leaving a deliberately broken file behind",
    test: "test/stubsweep.test.mjs",
    expectRed: "a sweep killed mid-stub restores the source before exiting",
    edits: [{
      file: "scripts/stub-sweep.mjs",
      find: `for (const sig of ["SIGINT", "SIGTERM", "SIGHUP"]) {`,
      replace: "for (const sig of []) {",
    }],
  },
  {
    name: "overlap-count",
    why: "skip overlapping occurrences, so a repeated anchor reads as unique and is applied to the first",
    test: "test/stubsweep.test.mjs",
    expectRed: "an anchor occurring at overlapping offsets is refused",
    edits: [{ file: "src/stubsweep.mjs", find: "    i = at + 1;", replace: "    i = at + needle.length;" }],
  },
  {
    name: "trailing-terminator",
    why: "count the empty segment after a trailing newline as another anchor line",
    test: "test/stubsweep.test.mjs",
    expectRed: "reports two lines, not three",
    edits: [{ file: "src/stubsweep.mjs",
              find: `  const anchorLines = find.replace(/\\n$/, "").split("\\n").length;`,
              replace: `  const anchorLines = find.split("\\n").length;` }],
  },
  {
    name: "move-hint-exact",
    why: "call a divergence INSIDE the first line a probable move, collapsing the two cases the hint separates",
    test: "test/stubsweep.test.mjs",
    expectRed: "is not called a move",
    edits: [{ file: "src/stubsweep.mjs",
              find: "  const shape = anchorLines > 2 && completeLinesMatched === 1",
              replace: "  const shape = anchorLines > 2 && completeLinesMatched <= 1" }],
  },
  {
    name: "timeout-is-not-red",
    why: "read a killed run as a failing assertion when its partial output happens to match",
    test: "test/stubsweep.test.mjs",
    expectRed: "is UNRUNNABLE even when the output matches",
    edits: [{ file: "src/stubsweep.mjs",
              find: `  if (stubExit === TIMED_OUT_EXIT)\n    return { verdict: UNRUNNABLE,\n             why: "the test was killed for exceeding its time limit, so the run never completed — " +\n                  "whatever it printed first is not a verdict" };\n`,
              replace: "" }],
  },
  {
    name: "assertion-delimiter",
    why: "treat any line starting with FAIL as an assertion, so a crash printing FAILURE: <text> reads as CAUGHT",
    test: "test/stubsweep.test.mjs",
    expectRed: "lines beginning with FAIL but lacking the two-space delimiter are not assertions",
    edits: [{ file: "src/stubsweep.mjs",
              find: "const ASSERTION = /^(PASS|FAIL) {2}(.+)$/;",
              replace: "const ASSERTION = /^(PASS|FAIL)\\s*(.*)$/;" }],
  },
  {
    name: "expectred-type",
    why: "accept a truthy non-string expectRed, so `[]` coerces to \"\" and matches every failing assertion",
    test: "test/stubsweep.test.mjs",
    expectRed: "expectRed as an array is refused",
    edits: [{ file: "src/stubsweep.mjs",
              find: `    if (typeof e.expectRed !== "string" || !e.expectRed.trim())`,
              replace: "    if (!e.expectRed)" }],
  },
  {
    name: "path-containment",
    why: "let a manifest edit resolve outside the repository, where the cleanliness guard cannot see it",
    test: "test/stubsweep.test.mjs",
    expectRed: "resolving outside the repository is refused outright",
    edits: [{ file: "scripts/stub-sweep.mjs",
              find: "for (const e of manifest)\n  for (const ed of e.edits) {\n    const real = contained(ed.file);",
              replace: "for (const e of [])\n  for (const ed of e.edits) {\n    const real = contained(ed.file);" }],
  },
  {
    name: "side-effect-check",
    why: "stop rechecking the tree, so a stubbed test's litter passes unnoticed and later entries run against it",
    test: "test/stubsweep.test.mjs",
    expectRed: "a stub whose test litters the repository does not pass the sweep",
    // Reports a CLEAN reading in the shape the code now expects, rather than a
    // bare string. A stub whose replacement no longer type-checks against the
    // surrounding code breaks a different assertion and reads as WRONG_RED — which
    // is the sweep telling you the manifest has rotted, not that the guard failed.
    edits: [{ file: "scripts/stub-sweep.mjs",
              find: "  const after = treeState();",
              replace: "  const after = { tracked: \"\", ignored: beforeStub.ignored };" }],
  },
  {
    // COMPOUND, and it BECAME compound, like `inert-raw-body`. Prepending the kept
    // assertions used to be the only thing keeping a buried failure reachable; the
    // verdict now comes from counters that never consult the tail, so prepending is
    // the HUMAN's evidence rather than the verdict's and removing it alone correctly
    // changes nothing. Both halves, or the entry reports NOT_CAUGHT honestly.
    name: "keep-verdict-lines",
    why: "discard assertion lines with the output tail AND read the verdict back out of that tail, so a noisy failure reads as CRASHED",
    test: "test/stubsweep.test.mjs",
    expectRed: "a named assertion still counts when the failure buries it",
    edits: [{ file: "scripts/stub-sweep.mjs",
              find: '              output: kept.length ? `${kept.join("\\n")}\\n${body}` : body });',
              replace: "              output: body });" },
            { file: "scripts/stub-sweep.mjs",
              find: "              observed,\n",
              replace: "" }],
  },
  {
    // BOTH defences at once, and deliberately so.
    //
    // The process group and the descendant sweep are redundant BY DESIGN: either
    // alone kills a helper the test spawned. Removing one therefore changes
    // nothing observable, and an entry stubbing only `detached` came back
    // NOT_CAUGHT — correctly, because the property still held.
    //
    // The honest stub for a redundant pair is a compound one. Removing both must
    // break it, or neither is load-bearing and the entry proves nothing about
    // either.
    name: "process-tree-kill",
    why: "remove BOTH the process group and the descendant sweep, so a helper the test spawned outlives it and acts on a restored tree",
    test: "test/stubsweep.test.mjs",
    expectRed: "the helper the STUBBED test spawned was killed with it",
    edits: [
      { file: "scripts/stub-sweep.mjs",
        find: "  const child = spawn(process.execPath, [`--test-reporter=${TEST_REPORTER}`, \"--test-reporter-destination=stdout\", join(ROOT, file)],\n    { cwd: ROOT, detached: true });",
        replace: "  const child = spawn(process.execPath, [`--test-reporter=${TEST_REPORTER}`, \"--test-reporter-destination=stdout\", join(ROOT, file)],\n    { cwd: ROOT });" },
      { file: "scripts/stub-sweep.mjs",
        find: "  for (const pid of stragglers) {\n    try { process.kill(pid, \"SIGKILL\"); } catch { /* already gone, or not ours */ }\n  }",
        replace: "" },
    ],
  },
  {
    name: "manifest-url-safe",
    why: "import the manifest by raw path, which breaks on a win32 drive letter and on any path holding a URL fragment character",
    test: "test/stubsweep.test.mjs",
    expectRed: "still loads and runs",
    edits: [{ file: "scripts/stub-sweep.mjs",
              find: "await import(pathToFileURL(manifestPath).href)",
              replace: "await import(manifestPath)" }],
  },
  {
    name: "git-dir-excluded",
    why: "allow an edit inside .git, where a failed restore is invisible to the cleanliness guard",
    test: "test/stubsweep.test.mjs",
    expectRed: "inside the git directory is refused outright",
    // BOTH halves. The resolved directory and the literal `.git` pointer are
    // redundant for an ordinary repository — either alone refuses `.git/config` —
    // so removing one changes nothing observable and the entry reads NOT_CAUGHT
    // correctly. The honest stub removes the whole condition.
    edits: [{ file: "scripts/stub-sweep.mjs",
              find: "    if (real === GIT_DIR || real.startsWith(GIT_DIR + sep) ||\n        real === GIT_POINTER || real.startsWith(GIT_POINTER + sep))",
              replace: "    if (false)" }],
  },
  {
    name: "ignored-artifacts",
    why: "compare only tracked files, so an ignored cache the control run creates is invisible",
    test: "test/stubsweep.test.mjs",
    expectRed: "an ignored artifact left by the control run voids the reading",
    edits: [{ file: "scripts/stub-sweep.mjs",
              find: "  if (beforeStub === null || beforeStub.tracked !== \"\" || beforeStub.ignored !== atEntry.ignored) {",
              replace: "  if (beforeStub === null || beforeStub.tracked !== \"\") {" }],
  },
  {
    name: "real-git-dir",
    why: "assume `<root>/.git` is the metadata directory, which is only a pointer file in a worktree or separate-git-dir repository",
    test: "test/stubsweep.test.mjs",
    expectRed: "an edit inside a SEPARATE git directory is refused",
    edits: [{ file: "scripts/stub-sweep.mjs",
              find: '  GIT_DIR = realpathSync(\n    execFileSync("git", ["rev-parse", "--absolute-git-dir"], { cwd: ROOT, encoding: "utf8" }).trim());',
              replace: '  GIT_DIR = join(REAL_ROOT, ".git");' }],
  },
  {
    // COMPOUND, and it BECAME compound. Making the raw body inert used to be the
    // only thing stopping a forged line from being classified; the verdict now
    // comes from counters taken per stream, which a forged line never enters, so
    // the two defences are redundant and removing either alone correctly changes
    // nothing. The entry read NOT_CAUGHT honestly until it removed both.
    name: "inert-raw-body",
    why: "classify the raw interleaved capture AND read the verdict back out of it, where a line neither stream emitted can name the expected assertion",
    test: "test/stubsweep.test.mjs",
    expectRed: "an assertion forged by interleaving does not pass the sweep",
    edits: [{ file: "scripts/stub-sweep.mjs",
              find: '    const raw = (dropped ? `[${dropped} earlier byte(s) dropped]\\n${out}` : out)\n      .split("\\n").map(l => `  ${l}`).join("\\n");\n    const body = raw;',
              replace: '    const body = dropped ? `[${dropped} earlier byte(s) dropped]\\n${out}` : out;' },
            { file: "scripts/stub-sweep.mjs",
              find: "              observed,\n",
              replace: "" }],
  },
  {
    name: "ignored-fingerprint",
    why: "compare ignored artifacts by PATH only, so a control run overwriting one in place is invisible",
    test: "test/stubsweep.test.mjs",
    expectRed: "an ignored artifact the control OVERWROTE voids the reading",
    edits: [{ file: "src/stubsweep.mjs",
              find: '  return h.digest("hex").slice(0, 16);',
              replace: '  return "same";' }],
  },
  {
    name: "named-line-reserved",
    why: "let unrelated failures fill the retention budget so the named assertion is crowded out and the entry reads WRONG_RED",
    test: "test/stubsweep.test.mjs",
    expectRed: "the named assertion is counted even when 21,000 other failures fill the budget",
    edits: [{ file: "scripts/stub-sweep.mjs",
              find: "    if (isNamed) observed.namedFailSeen = true;",
              replace: "    if (false) observed.namedFailSeen = true;" }],
  },
  {
    // The only entry here that stubs a FIXTURE rather than the runner, and it is
    // the entry this whole sweep exists for. MEASURED: with the pauses removed the
    // two stdout writes coalesce into one data event, the forged line never reaches
    // the parent, and the runner returns the SAME WRONG_RED and the SAME exit 1 it
    // returns when the forging does happen. Both of the forged-line assertions
    // therefore stay green over a tree that cannot exhibit the defect. Only the
    // control separates them, so the control has to be shown to fail.
    name: "forging-control",
    why: "remove the pauses, so the fixture stops forging the line and its assertions pass for a second reason",
    test: "test/stubsweep.test.mjs",
    expectRed: "control: the fixture really does forge a line neither stream emitted",
    edits: [{ file: "test/stubsweep.test.mjs",
              find: "    `  const pause = () => Atomics.wait(new Int32Array(new SharedArrayBuffer(4)), 0, 0, 250);\\n` +",
              replace: "    `  const pause = () => {};\\n` +" }],
  },
  {
    // BOTH halves, because they are one mechanism: asking git for `-z` and reading
    // NUL-separated fields. Reverting only the flag leaves the parser splitting a
    // newline-delimited blob on NUL and reading one enormous field, which fails for
    // a reason that has nothing to do with quoting.
    name: "porcelain-z",
    why: "read the human-readable porcelain form, where a path holding a space is C-QUOTED and the quoted string is not the path",
    test: "test/stubsweep.test.mjs",
    expectRed: "an ignored artifact with a QUOTED name still voids the reading",
    edits: [{ file: "scripts/stub-sweep.mjs",
              find: '["status", "--porcelain", "-z", "--ignored"]',
              replace: '["status", "--porcelain", "--ignored"]' },
            { file: "src/stubsweep.mjs",
              find: "    if (i === buf.length || buf[i] === 0) { if (i > start) fields.push(buf.subarray(start, i)); start = i + 1; }",
              replace: "    if (i === buf.length || buf[i] === 0x0a) { if (i > start) fields.push(buf.subarray(start, i)); start = i + 1; }" }],
  },
  {
    name: "porcelain-bytes",
    why: "decode the porcelain output as UTF-8, which replaces every undecodable byte and hands the filesystem a path that does not exist",
    test: "test/stubsweep.test.mjs",
    expectRed: "an undecodable ignored path reaches the filesystem call as its original bytes",
    edits: [{ file: "src/stubsweep.mjs",
              find: '  const buf = Buffer.isBuffer(raw) ? raw : Buffer.from(String(raw), "binary");',
              replace: '  const buf = Buffer.from(String(raw), "utf8");' }],
  },
  {
    name: "symlink-by-target-text",
    why: "stop hashing an ignored symlink's target text, so every symlink fingerprints as one constant and retargeting it is invisible",
    test: "test/stubsweep.test.mjs",
    expectRed: "an ignored symlink RETARGETED by the control run voids the reading",
    edits: [{ file: "src/stubsweep.mjs",
              find: "  if (st.isSymbolicLink()) {",
              replace: "  if (false) {" }],
  },
  {
    // SINGLE-PURPOSE, and that is the whole point of it. This exact regression
    // shipped: `observed` was computed correctly and never put on the resolved run,
    // so `classify` took its documented fallback and the counting path was inert.
    // The suite stayed green -- both paths return the same verdict when the new one
    // is unused -- and two COMPOUND entries reported CAUGHT for the other half of
    // their stub, which is what hid it. A compound stub cannot say which defence
    // carried the entry, so a seam whose wiring is unproven needs one of these.
    name: "observed-reaches-the-verdict",
    why: "compute the counters and never hand them to the classifier, which is the inert-seam regression this file already shipped once",
    test: "test/stubsweep.test.mjs",
    expectRed: "the runner's own counters reach the verdict: only-passes is WRONG_RED, not CRASHED",
    edits: [{ file: "scripts/stub-sweep.mjs",
              find: "              observed,\n",
              replace: "" }],
  },
  {
    name: "verdict-from-observed",
    why: "derive the verdict by re-reading the retained buffer, so what a run REPORTED and what SURVIVED retention become the same question",
    test: "test/stubsweep.test.mjs",
    expectRed: "a stubbed run reporting only PASSes is WRONG_RED rather than CRASHED",
    edits: [{ file: "src/stubsweep.mjs",
              find: "  const seen = observed ? observed.assertionsSeen : (reportedAnyAssertion(stubOutput) ? 1 : 0);",
              replace: "  const seen = reportedAnyAssertion(stubOutput) ? 1 : 0;" }],
  },
  {
    // COMPOUND, and it has to be. Two independent defences stop a PASS from
    // crowding out the named FAIL: ingestion keys the reservation on FAIL, and the
    // verdict does not read the retention buffer at all. Removing either alone
    // changes nothing observable, and the entry would read NOT_CAUGHT correctly.
    name: "pass-cannot-spend-the-reservation",
    why: "let a PASS naming the expected text spend the reserved slot, AND let the verdict be read back out of the buffer that then evicts the real failure",
    test: "test/stubsweep.test.mjs",
    expectRed: "a PASS naming the expected text does not crowd out the named FAIL",
    edits: [{ file: "scripts/stub-sweep.mjs",
              find: '    if (m[1] !== "FAIL") return;',
              replace: '    if (m[1] !== "FAIL") { if (expectRed && m[2].trim().includes(expectRed)) namedKept = true; return; }' },
            { file: "src/stubsweep.mjs",
              find: "  const failures = observed ? observed.failures : failedAssertions(stubOutput);",
              replace: "  const failures = failedAssertions(stubOutput);" },
            { file: "src/stubsweep.mjs",
              find: "  const namedFailed = observed\n    ? observed.namedFailSeen\n    : failures.some(f => f.includes(expectRed));",
              replace: "  const namedFailed = failures.some(f => f.includes(expectRed));" }],
  },
  {
    // COMPOUND for the same reason: the tails going through the one ingestion site,
    // and the verdict not being read back out of the buffer, are both sufficient.
    name: "tails-go-through-ingest",
    why: "give the close-time tails their own copy of the rules again — the budget without the named reservation — AND read the verdict back out of the buffer",
    test: "test/stubsweep.test.mjs",
    expectRed: "an unterminated named FAIL after the budget fills is still counted",
    edits: [{ file: "scripts/stub-sweep.mjs",
              find: "    for (const tail of [partial.out, partial.err]) if (tail) ingest(tail);",
              replace: "    for (const tail of [partial.out, partial.err]) {\n" +
                       "      const m = tail && ASSERTION_LINE.exec(tail);\n" +
                       "      if (m && m[1] === \"FAIL\" && kept.length < MAX_ASSERTION_LINES && keptBytes < MAX_ASSERTION_BYTES) {\n" +
                       "        kept.push(tail); keptBytes += tail.length;\n" +
                       "      }\n" +
                       "    }" },
            { file: "src/stubsweep.mjs",
              find: "  const failures = observed ? observed.failures : failedAssertions(stubOutput);",
              replace: "  const failures = failedAssertions(stubOutput);" },
            { file: "src/stubsweep.mjs",
              find: "  const namedFailed = observed\n    ? observed.namedFailSeen\n    : failures.some(f => f.includes(expectRed));",
              replace: "  const namedFailed = failures.some(f => f.includes(expectRed));" }],
  },
  {
    name: "premerge-unfinished-check-is-unknown",
    why: "treat a check that has not finished as though it had passed, which is the window a merge lands in",
    test: "test/premerge.test.mjs",
    expectRed: "an unfinished check is UNKNOWN, not clear",
    edits: [{ file: "src/premerge.mjs",
              find: "  if (unfinished.length)",
              replace: "  if (false)" }],
  },
  {
    name: "premerge-no-checks-is-not-a-pass",
    why: "read no checks at all as nothing-failed, so a repository with no CI reads as passing",
    test: "test/premerge.test.mjs",
    expectRed: "no checks at all is UNKNOWN, not clear",
    edits: [{ file: "src/premerge.mjs",
              find: "  if (runs.length === 0)",
              replace: "  if (false)" }],
  },
  {
    name: "premerge-truncated-listing-is-unknown",
    why: "drop the completeness check, so a page cap makes a partial read look settled",
    test: "test/premerge.test.mjs",
    expectRed: "a truncated listing is UNKNOWN, because 'none unresolved' would be about a page",
    edits: [{ file: "src/premerge.mjs",
              find: "  if (nodes.length !== totalCount)",
              replace: "  if (false)" }],
  },
  {
    name: "premerge-verdict-never-rounds-up",
    why: "invert the ranking so the best news wins and a summary line reads as a pass while carrying a refusal",
    test: "test/premerge.test.mjs",
    expectRed: "a pull request nobody reviewed is not clear even with everything else green",
    edits: [{ file: "src/premerge.mjs",
              find: "  const rank = { [REFUSE]: 3, [UNKNOWN]: 2, [UNREVIEWED]: 1, [CLEAR]: 0 };",
              replace: "  const rank = { [REFUSE]: 0, [UNKNOWN]: 0, [UNREVIEWED]: 0, [CLEAR]: 1 };" }],
  },
  {
    name: "premerge-unreadable-refs-are-unknown",
    why: "treat refs that could not be read as though the branch matched, which is absence read as agreement",
    test: "test/premerge.test.mjs",
    expectRed: "refs that could not be read are UNKNOWN, not clear",
    edits: [{ file: "src/premerge.mjs",
              find: "  if (branchRead !== \"read\")",
              replace: "  if (false)" }],
  },
  {
    name: "premerge-pending-status-is-not-clear",
    why: "read a legacy status context's PENDING state as finished, which is how a pending pull request reported CLEAR",
    test: "test/premerge.test.mjs",
    expectRed: "a legacy status in PENDING is UNKNOWN, not clear",
    edits: [{ file: "src/premerge.mjs",
              find: "  const unfinished = runs.filter(r => !FAILED.has(norm(r)) && !PASSED.has(norm(r)));",
              replace: "  const unfinished = runs.filter(r => !norm(r));" }],
  },
  {
    name: "premerge-rollup-completeness",
    why: "drop the completeness check on the check rollup, so a first page of passes reads as all of them",
    test: "test/premerge.test.mjs",
    expectRed: "a truncated check rollup is UNKNOWN, not clear",
    edits: [{ file: "src/premerge.mjs",
              find: "  if (totalCount !== null && nodes.length !== totalCount)",
              replace: "  if (false)" }],
  },
  {
    name: "premerge-stale-is-terminal",
    why: "put STALE back among the unfinished, so a run that will never complete reads as one still running and a caller retries for ever",
    test: "test/premerge.test.mjs",
    expectRed: "a STALE check run is refused, not reported as unfinished",
    edits: [{ file: "src/premerge.mjs",
              find: "                          \"ERROR\", \"STARTUP_FAILURE\", \"STALE\"]);",
              replace: "                          \"ERROR\", \"STARTUP_FAILURE\"]);" }],
  },
  {
    name: "premerge-binds-the-verified-head",
    why: "drop the verified head, so a caller cannot bind the merge to the commit the gate actually checked",
    test: "test/premerge.test.mjs",
    expectRed: "a clear verdict names the FULL head it verified",
    edits: [{ file: "src/premerge.mjs",
              find: "           verifiedHead: head?.prHead ?? null,",
              replace: "           verifiedHead: null," }],
  },
  {
    name: "premerge-tip-difference-does-not-overclaim",
    why: "restore the claim that a tip difference loses commits, which is false when a branch was force-reset backward to an ancestor",
    test: "test/premerge.test.mjs",
    expectRed: "the tip difference does not claim anything is lost",
    edits: [{ file: "src/premerge.mjs",
              find: "                \"whether that difference loses anything is NOT established here \u2014 \" +",
              replace: "                \"commits on the branch would not be carried \u2014 \" +" }],
  },
  {
    name: "premerge-delegates-mergeability",
    why: "widen the permitted merge states so BLOCKED and DIRTY read as clear, which is the gate printing a merge command GitHub would refuse",
    test: "test/premerge.test.mjs",
    expectRed: "a merge state of BLOCKED is refused rather than reported clear",
    edits: [{ file: "src/premerge.mjs",
              find: "  const ok = new Set([\"CLEAN\", \"HAS_HOOKS\"]);",
              replace: "  const ok = new Set([\"CLEAN\", \"HAS_HOOKS\", \"BLOCKED\", \"DIRTY\", \"DRAFT\", \"BEHIND\", \"UNSTABLE\"]);" }],
  },
  {
    name: "premerge-transient-merge-state",
    why: "let an unresolved merge state fall through to REFUSE, reporting an asynchronous transient as an actionable blocker",
    test: "test/premerge.test.mjs",
    expectRed: "a resolved mergeable with an unresolved merge state is a transient, not a refusal",
    edits: [{ file: "src/premerge.mjs",
              find: "  if (status === \"UNKNOWN\")",
              replace: "  if (false)" }],
  },
  {
    name: "premerge-null-review-is-not-approval",
    why: "let a null review decision fall through to clear, so 'no review is required' reads as 'somebody reviewed'",
    test: "test/premerge.test.mjs",
    expectRed: "no review decision is UNREVIEWED, not clear",
    edits: [{ file: "src/premerge.mjs",
              find: "  return { state: UNREVIEWED,\n           why: \"GitHub reports no review decision, which means none is REQUIRED rather than that one was given\" };",
              replace: "  return { state: CLEAR,\n           why: \"GitHub reports no review decision\" };" }],
  },
  {
    name: "outbox-edge-survives-a-rerun",
    why: "write the dependants with no dependency at all, which is the orphan: they drain at once and their token has no parent to read",
    test: "test/outbox-edge-rerun.test.mjs",
    expectRed: "a child is written with the parent's id, not with no dependency at all",
    edits: [{ file: "src/db/ops.mjs",
              find: "  for (const child of dependants) ids.push(enqueue(db, { ...child, dependsOn: parentId }));",
              replace: "  for (const child of dependants) ids.push(enqueue(db, { ...child, dependsOn: null }));" }],
  },
  {
    name: "migration-counter-absence",
    why: "key the legacy counters by a plain object, so a `__proto__` kind or status hits the inherited setter and its count vanishes from the report entirely",
    test: "test/migration-counters.test.mjs",
    expectRed: "an unknown kind named '__proto__' is PRESENT in unknownKind",
    edits: [{ file: "src/db/migrate.mjs",
              find: "                 statusChanges:0, coercedStatus:counters(), unknownKind:counters(),",
              replace: "                 statusChanges:0, coercedStatus:{}, unknownKind:{}," }],
  },
  {
    name: "migration-counter-garbage",
    why: "the same defect's OTHER half, which the presence assertion cannot see: `constructor` DOES become an own property, so only its VALUE reveals that the count is a stringified inherited function",
    test: "test/migration-counters.test.mjs",
    expectRed: "'constructor' counts to the NUMBER 1 in unknownKind, not a stringified inherited function",
    edits: [{ file: "src/db/migrate.mjs",
              find: "                 statusChanges:0, coercedStatus:counters(), unknownKind:counters(),",
              replace: "                 statusChanges:0, coercedStatus:{}, unknownKind:{}," }],
  },
  {
    name: "premerge-approval-bound-to-head",
    why: "stop filtering the approvals by commit, so an approval given on an EARLIER commit reads as an approval of the head being merged -- the original defect, an APPROVED aggregate mapping straight to CLEAR",
    test: "test/premerge.test.mjs",
    expectRed: "an approval on an EARLIER commit does not clear the head being merged",
    edits: [{ file: "src/premerge.mjs",
              find: "    const atHead = approvals.filter(a => a.commit.oid === head);",
              replace: "    const atHead = approvals;" }],
  },
  {
    name: "premerge-unreadable-approval-is-not-clear",
    why: "treat an approving review whose commit did not resolve as an approval on some earlier commit, so an incomplete READ is reported as a finding and automation asks for another review instead of saying the read failed",
    test: "test/premerge.test.mjs",
    expectRed: "an approval whose commit is null is UNKNOWN, not an approval on an earlier commit",
    edits: [{ file: "src/premerge.mjs",
              find: "      return { state: UNKNOWN,\n               why: `GitHub reports APPROVED, but ${unreadable.length} of ${approvals.length} approving review(s) carry no resolvable commit",
              replace: "      return { state: UNREVIEWED,\n               why: `GitHub reports APPROVED, but ${unreadable.length} of ${approvals.length} approving review(s) carry no resolvable commit" }],
  },
  {
    name: "premerge-truncated-reviews-are-not-a-verdict",
    why: "read a truncated page of opinionated reviews as the whole set, so an approval of this head sitting on a later page is reported as `every approval is on an earlier commit` -- a page presented as a finding",
    test: "test/premerge.test.mjs",
    expectRed: "a TRUNCATED approvals listing is UNKNOWN, not a verdict about the page that was read",
    edits: [{ file: "src/premerge.mjs",
              find: "  if (nodes.length !== totalCount)\n    return { ok: false, state: UNKNOWN,",
              replace: "  if (false)\n    return { ok: false, state: UNKNOWN," }],
  },
  {
    name: "premerge-completeness-precedes-the-filter",
    why: "check completeness AFTER narrowing to approvals, so totalCount (which counts every opinionated review) can never match and any pull request carrying a change-request is refused as truncated -- the completeness rule producing a false refusal of its own making",
    test: "test/premerge.test.mjs",
    expectRed: "a CHANGES_REQUESTED review sitting in the listing is not truncation",
    edits: [{ file: "src/premerge.mjs",
              find: "    const complete = completeness({ nodes: reviews, totalCount: reviewsTotal,",
              replace: "    const complete = completeness({ nodes: reviews.filter(r => String(r?.state).toUpperCase() === \"APPROVED\"), totalCount: reviewsTotal," }],
  },
  {
    name: "sweep-covers-every-test-file",
    why: "let a test file be neither named by an entry nor listed as frozen debt, which is how 103 of 106 files came to have no stub while the required job claimed the tests could fail. NAMED AGAINST THE SYNTHETIC ASSERTION, not the repository-wide one: that one asserts the current tree has zero orphans, and a value already at zero cannot be moved by breaking the detector, so a stub of it comes back NOT_CAUGHT however true the assertion is",
    test: "test/stubsweep.test.mjs",
    expectRed: "coverage() NAMES a test file that has neither an entry nor a place on the list",
    edits: [{ file: "src/stubsweep.mjs",
              find: "  const orphans = files.filter(f => !named.has(f) && !spared.has(f));",
              replace: "  const orphans = [];" }],
  },
  {
    name: "sweep-grandfather-list-cannot-rot",
    why: "stop noticing that a grandfathered file has gained an entry or stopped existing, so the list quietly becomes a blanket exemption nobody granted",
    test: "test/stubsweep.test.mjs",
    expectRed: "coverage() reports a grandfathered file that has SINCE gained an entry",
    edits: [{ file: "src/stubsweep.mjs",
              find: "  const stale = [...spared].filter(f => named.has(f) || !files.includes(f)).sort();",
              replace: "  const stale = [];" }],
  },
  {
    name: "sweep-unresolvable-base-is-not-an-empty-diff",
    why: "return an empty file list when the base cannot be resolved instead of refusing, which is how a depth-1 checkout makes the gate pass while measuring nothing",
    test: "test/stubsweep.test.mjs",
    expectRed: "an unresolvable base REFUSES rather than reporting an empty diff, which would pass",
    edits: [{ file: "src/stubsweep.mjs",
              find: "  if (!base) return { ok: false, files: [], why:",
              replace: "  if (!base) return { ok: true, files: [], why:" }],
  },
  {
    name: "sweep-grandfathering-ends-at-the-first-edit",
    why: "let a grandfathered file be edited without demanding a stub, which removes the only mechanism that ever pays the debt down",
    test: "test/stubsweep.test.mjs",
    expectRed: "editing a grandfathered test file REFUSES, and names the file",
    edits: [{ file: "src/stubsweep.mjs",
              find: "  const touched = (changed ?? []).filter(f => spared.has(f));",
              replace: "  const touched = [];" }],
  },
  {
    name: "sweep-frozen-list-may-only-shrink",
    why: "accept additions to GRANDFATHERED, so a change can drop a test's entry and add the test to the list instead -- the file is never edited, the edit rule sees only the manifest in the diff, and coverage falls while the gate reports success",
    test: "test/stubsweep.test.mjs",
    expectRed: "a name ADDED to the frozen list is refused, and named",
    edits: [{ file: "src/stubsweep.mjs",
              find: "  const added = (after ?? []).filter(f => !was.has(f));",
              replace: "  const added = [];" }],
  },
  {
    name: "registryio-nonzero-git-exit-is-not-no-entry",
    why: "accept a nonzero git exit as an answer, so a checkout git refuses to read -- dubious ownership, a corrupt or locked index, exit 128 -- comes back as empty stdout, parses to `null`, and the caller admits the claim without ever establishing whether the index records a symlink or a submodule. `ls-files` exits 0 when nothing matches, so a nonzero status never means \"no entry\"; it always means the question could not be asked, and answering it as absence is fail-OPEN on the one probe that guards the admission path",
    test: "test/registry-io.test.mjs",
    expectRed: "git failing with exit 128 refuses, rather than reading as \"nothing is tracked\"",
    edits: [{ file: "src/build/registryio.mjs",
              find: "      if (res.status !== 0 || res.signal)",
              replace: "      if (false)" }],
  },
  {
    name: "sweep-ratio-counts-proven-not-present",
    why: "report every manifest entry as coverage regardless of whether it could run, so an entry whose anchor a refactor moved keeps counting as a guard while guarding nothing -- the ratio overstating by exactly the thing the sweep exists to prevent",
    test: "test/stubsweep.test.mjs",
    expectRed: "the ratio says how many entries were PROVEN when that is fewer than exist",
    edits: [{ file: "src/stubsweep.mjs",
              find: "  const head = proven === null || proven === c.entries",
              replace: "  const head = true" }],
  },
  {
    name: "sweep-file-count-follows-proven-too",
    why: "count a test file as covered when its only entry could not run, so the FILE-level number keeps overstating after the entry-level number was fixed -- half a repair reading as a whole one",
    test: "test/stubsweep.test.mjs",
    expectRed: "a file whose only entry could not run is NAMED but not PROVEN",
    edits: [{ file: "src/stubsweep.mjs",
              find: "           provenCovered: provenNamed === null ? null : files.filter(f => provenNamed.has(f)) };",
              replace: "           provenCovered: provenNamed === null ? null : files.filter(f => named.has(f)) };" }],
  },
  {
    name: "source-is-text-nul-scan",
    why: "stop finding the NUL byte, so a file git treats as BINARY passes the scan -- a file whose diff every reviewer sees as empty, which is how a NUL delimiter written as a literal byte survived in the module that decides whether a check run counts",
    test: "test/source-is-text.test.mjs",
    expectRed: "the NUL detector finds the byte in a buffer that contains one",
    edits: [{ file: "test/source-is-text.test.mjs",
              find: "const nulAt = buf => buf.indexOf(0);",
              replace: "const nulAt = () => -1;" }],
  },
  {
    name: "lint-rule-reports-a-pathname-read",
    why: "stop matching the property name, so the rule reports nothing and every invalid case in its tests passes vacuously -- a lint rule that fires on nothing is the same shape as a test that cannot fail",
    test: "test/lint-no-url-pathname.test.mjs",
    expectRed: "a direct read is reported",
    edits: [{ file: "tools/eslint-rules/no-url-pathname.js",
              find: 'if (node.computed || node.property?.name !== "pathname") return;',
              replace: 'if (node.computed || node.property?.name !== "never-matches") return;' }],
  },
  {
    name: "lint-config-reaches-the-extensionless-cli",
    why: "narrow the lint config's file patterns so the extensionless CLI matches none of them, which makes eslint SKIP it while `eslint .` prints nothing -- a skipped file and a clean file are the same output, and that is how widening a glob removed all coverage of the production entry point while every command still reported success",
    test: "test/lint-config-covers-bin.test.mjs",
    expectRed: "resolves a config that enables the rule",
    edits: [{ file: "eslint.config.js",
              find: '"bin/!(*.*)", "bin/*.mjs"',
              replace: '"bin/*"' }],
  },
  {
    name: "taskfile-empty-claim-is-the-root",
    why: "filter blank claims out of the territory list before counting it -- the shape an author reaches for first -- so a whitespace-only --territory reads as \"no territory declared\" and is refused by the grammar. The refusal looks correct, and the grammar assertions stay green, but the claim that conflicts with EVERYTHING in its project has quietly become a filing that conflicts with nothing. `normalizeClaim` returns the repository root for a blank claim deliberately; the absence of a territory claim must never read as the absence of conflict",
    test: "test/task-file.test.mjs",
    expectRed: "an empty claim is admitted as a claim, not dropped",
    edits: [{ file: "src/build/taskfile.mjs",
              find: "  if (!Array.isArray(territory) || territory.length === 0)",
              replace: "  if (!Array.isArray(territory) || territory.filter(t => String(t).trim()).length === 0)" }],
  },
  {
    name: "taskfile-refusal-leaves-nothing",
    why: "catch admitTask's refusal OUTSIDE its transaction and re-insert a bare task row so the operator \"has something to look at\" -- the shape an author reaches for when a refusal loses the title the founder typed. The refusal is still returned and still names the blocking task, so every message assertion stays green, while the hub gains a task holding no territory, blocking nothing, and reading as FILED in every later view. A refusal that is RETURNED and a refusal that CHANGED NOTHING are two different facts",
    test: "test/task-file.test.mjs",
    expectRed: "and the task-row COUNT is unchanged",
    edits: [{ file: "src/build/taskfile.mjs",
              find: "  if (!r.ok) return { ok: false, refusal: r.refusal };",
              replace: "  if (!r.ok) { db.prepare(\"INSERT INTO task(id,project,repo_id,nwo_snapshot,title,phase,generation,source_kind,source_key,repo_path,profile_path,profile_hash,default_branch,visibility,registry_version,created_at,updated_at) VALUES(?,?,?,?,?,'FILED',1,'founder',?,?,?,?,?,?,?,unixepoch(),unixepoch())\").run(id, project, snapshot.repoId, snapshot.nwo, title, id, snapshot.repoPath, snapshot.profilePath, snapshot.profileHash, snapshot.defaultBranch, snapshot.visibility, snapshot.registryVersion); return { ok: false, refusal: r.refusal }; }" }],
  },
  {
    name: "taskfile-liveness-is-never-defaulted",
    why: "give the liveness predicate a default instead of refusing without one. assertWritable throws exactly when the predicate says the restore's holder is ALIVE, so `() => true` never reaps a lock whose holder is long dead and wedges the hub read-only for good, while `() => false` reaps a live restore and admits a filing into a file being replaced underneath it. One costs availability and the other costs the write; a default picks one silently on behalf of the only caller that could tell them apart",
    test: "test/task-file.test.mjs",
    expectRed: "fileTask throws rather than defaulting isAlive",
    edits: [{ file: "src/build/taskfile.mjs",
              find: "  if (typeof isAlive !== \"function\")",
              replace: "  if (false)" }],
  },
  {
    name: "taskfile-honours-the-predicate-it-is-given",
    why: "accept the caller's liveness predicate and then not use it, passing `() => true` to the writer lease instead -- which reads EVERY recorded restore holder as alive, including one whose process died mid-restore. The filing is then refused with a correct-looking `a restore is in progress` message naming a pid that no longer exists, and it is refused for ever: nothing else reaps that row, so one crashed restore makes the hub permanently unwritable while every message about it reads as normal operation",
    test: "test/task-file.test.mjs",
    expectRed: "a filing proceeds once the dead holder's lock is reaped",
    edits: [{ file: "src/build/taskfile.mjs",
              find: "    { command: \"reeve task file\", pid, lstart, isAlive },",
              replace: "    { command: \"reeve task file\", pid, lstart, isAlive: () => true }," }],
  },
  {
    name: "taskfile-dry-run-writes-nothing",
    why: "let --dry-run fall through into the real admission, so the command that exists to answer \"what would happen\" performs it instead. The founder's habit of checking before committing becomes the thing that files the task, and on free territory it succeeds -- so the operator sees a plan-shaped failure and a real task row. Both counters are asserted, and on DISJOINT territory: a dry run whose claims are already held would be refused by the conflict and write nothing either way, which makes the counter agree with a correct implementation and a broken one alike",
    test: "test/task-file.test.mjs",
    expectRed: "and it still inserts no task row",
    edits: [{ file: "src/build/taskfile.mjs",
              find: "  if (dryRun) return { ok: true, dryRun: true,",
              replace: "  if (false) return { ok: true, dryRun: true," }],
  },
  {
    name: "taskfile-next-sub-shape-is-frozen",
    why: "collapse `next` from {phase, generation} to a bare phase string. Every mutating command in this system returns the same envelope and the commands written against it do not exist yet, so this is the change that breaks every future reader while breaking no current test: the KEY SET is untouched, so a freeze that checks only the envelope's keys stays green, and only an assertion over next's OWN keys can see it. The half a freeze already covers is not the half that needs the freeze",
    test: "test/task-file.test.mjs",
    expectRed: "and so is next's, which is the half a consumer reads a phase out of",
    edits: [{ file: "src/build/taskfile.mjs",
              find: "             next: { phase: \"FILED\", generation: 1 }, evidence_id: ev,",
              replace: "             next: \"FILED\", evidence_id: ev," }],
  },
  {
    name: "cli-valued-flag-given-no-value-is-refused",
    why: "accept a valued flag that was given no value, so `--title` with nothing after it becomes the empty string instead of a refusal. Every route then reads `opt(\"title\")` as \"\" and proceeds: a task is filed with no title, which is what names it in every later view, and the operator sees a success. The parser is the only place that can tell an omitted value from an intended empty one, because by the time a route reads it the two are the same string",
    test: "test/cli-flags.test.mjs",
    expectRed: "an empty valued flag is refused",
    edits: [{ file: "bin/reeve",
              find: "    if (v === undefined || (v === \"\" && !EMPTY_IS_A_VALUE.has(name))) {",
              replace: "    if (false) {" }],
  },
  {
    name: "read-model-matches-the-run-ref-the-dispatcher-writes",
    why: "match a builder provider lease on the bare task id. The dispatcher claims a slot under `<task>:<phase>`, because two phases of one task would otherwise collide in the live-request unique index -- so an equality on the task id finds nothing, WAITING_FOR_QUOTA becomes a substate the code can compute and can never show, and an operator watching a task sit behind capacity is told it is waiting for nothing at all. This is the shape the plan for this work carried, and its own fixture inserted the bare form, so its test would have passed against a lease shaped the way the test wrote it and failed against one shaped the way the writer writes it",
    test: "test/task-show.test.mjs",
    expectRed: "a queued builder lease written the way the dispatcher writes it is WAITING_FOR_QUOTA",
    edits: [{
      file: "src/build/show.mjs",
      find: "          AND (run_ref = ? OR substr(run_ref, 1, ?) = ?)",
      replace: "          AND (run_ref = ? OR substr(run_ref, 1, ?) = run_ref || ?)",
    }],
  },
  {
    name: "read-model-reads-the-capability-switch",
    why: "stop the capability derivation from reading the switch at all. The six waiting substates are DERIVED on every read precisely so that turning a switch off changes the answer with no write; a branch that ignores the switch keeps reporting `a worker is coming` for a task nothing can dispatch, and no write happens at read time, so a no-write assertion alone cannot see it",
    test: "test/task-show.test.mjs",
    expectRed: "turning observe off makes the same row read WAITING_FOR_CAPABILITY",
    edits: [{
      file: "src/build/show.mjs",
      find: "      if (ev.switches[need] !== true) { all.push(\"WAITING_FOR_CAPABILITY\"); capability = need; }",
      replace: "      if (false) { all.push(\"WAITING_FOR_CAPABILITY\"); capability = need; }",
    }],
  },
  {
    name: "read-model-reports-a-section-with-no-rows-as-absent",
    why: "let a section with no rows render as an empty success. `why` on a task that never dispatched has no runs, no lease and no PR, and the failure that matters is not a throw -- a throw is loud -- but an empty render that reads as `nothing went wrong`, which is byte-for-byte what a healthy task with nothing to report produces",
    test: "test/task-show.test.mjs",
    expectRed: "runs is reported ABSENT, not as an empty success",
    edits: [{
      file: "src/build/why.mjs",
      find: "  for (const section of SECTIONS) if (!rows[section].length) model.absent.push(section);",
      replace: "  for (const section of SECTIONS) if (!rows[section].length && section !== \"runs\") model.absent.push(section);",
    }],
  },
  {
    name: "dash-waiting-on-you-is-only-what-a-human-can-clear",
    why: "let `waiting_on_you` mean every waiting task. A quota frees itself, a guardian finishes, a reviewer answers -- so listing those beside a founder hold turns the one line an operator scans into a list of everything, which is the same as not having the line. The degradation is silent, because a surface nobody reads still renders perfectly, and the measured mechanism is that acceptance of a repeated alert drops about a third each time it repeats",
    test: "test/build-dash.test.mjs",
    expectRed: "control: a task waiting on QUOTA is not waiting on you",
    edits: [{
      file: "src/build/dash.mjs",
      find: "      .filter(t => t.waiting.first && HUMAN_WAITS.has(t.waiting.first))",
      replace: "      .filter(t => t.waiting.first !== null)",
    }],
  },
  {
    name: "dash-age-comes-from-the-event-log",
    why: "read age-in-state from `task.updated_at`, which every transition compensation moves whether or not it changed a phase -- so a task stuck for ten minutes reports zero seconds the moment anything unrelated touches its row. The measured instance of the class is that a pull request's updated_at does not move when a review thread is resolved; the class is general, and a column touched by writes unrelated to the change being measured is not a change signal",
    test: "test/build-dash.test.mjs",
    expectRed: "age-in-state is 600s, from the phase_event that entered this phase",
    edits: [{
      file: "src/build/show.mjs",
      find: "  const entered = db.prepare(\n    `SELECT at FROM phase_event WHERE task = ? AND to_phase = ?\n      ORDER BY seq DESC LIMIT 1`).get(row.id, row.phase)?.at;",
      replace: "  const entered = row.updated_at;",
    }],
  },
  {
    name: "dash-aliveness-reads-the-clock-not-the-row",
    why: "make `running` require the lease to be UNEXPIRED as well as its holder alive. `build status` reads liveness from isSameProcess alone, and acquireSingleton refuses a takeover from a live holder even after expiry -- because a busy process can miss a heartbeat and killing its authority mid-effect is the race, not the fix. Requiring both reports a live builder as stopped whenever a tick blocks past the 120-second lease, and disagrees with the authority rule about who holds the hub",
    test: "test/build-dash.test.mjs",
    expectRed: "an EXPIRED lease whose holder is alive is still running",
    edits: [{
      file: "src/build/dash.mjs",
      find: "  const holderAlive = !!lease && isAlive(lease.pid, lease.lstart);",
      replace: "  const holderAlive = unexpired && isAlive(lease.pid, lease.lstart);",
    }],
  },
  {
    name: "dash-renders-the-pull-requests-it-carries",
    why: "finish a task row without reading `t.prs`. The model carries every open pull request and --json hands them over, so the machine shape stays right while the DEFAULT rendering -- the one an operator actually reads -- shows a task waiting on a review that already exists as a task waiting on nothing. This is the ninth instance of one shape on this surface, a value carried in the model and dropped by the renderer, and it survived the structural guard built to close that shape: the walk descended objects and treated every ARRAY as an opaque leaf, so each list a row carried was exempt without ever being named as an exception",
    test: "test/build-dash.test.mjs",
    expectRed: "every value the digest carries into the model reaches the human render",
    edits: [{
      file: "src/build/dash.mjs",
      find: "    for (const p of t.prs)\n      out.push(`      pull request ${p.kind} #${p.pr}  ${p.head_sha}`);\n",
      replace: "",
    }],
  },
  {
    name: "dash-names-the-project-on-every-row",
    why: "drop `project` from the rows that name a task. The digest spans every project on the machine and the switches block lists project names without associating any task with one, so an operator reading a row cannot tell which repository it belongs to -- in the list that tells them what to act on. It passed a structural render guard for a full round because the guard matched values against the WHOLE document, and the project name appears in the switches block: text belonging to one row satisfied a claim about another",
    test: "test/build-dash.test.mjs",
    expectRed: "every value the digest carries into the model reaches the human render",
    edits: [{
      file: "src/build/dash.mjs",
      find: "    out.push(`  ${t.id}  ${oneLine(t.project)}  ${t.phase}  ${oneLine(t.title)}` +",
      replace: "    out.push(`  ${t.id}  ${t.phase}  ${oneLine(t.title)}` +",
    }],
  },
  {
    name: "dash-names-every-wait-not-just-the-headline",
    why: "print only `waiting.first` on a task row. The read model keeps the whole set because clearing the headline can leave the task blocked on something else, so a row naming one condition tells an operator that acting on it is enough -- and they act, and find the task exactly where they left it. Every task in the fixture waited on exactly one thing before this, so `all` and `first` were the same single value and a render that printed the headline was indistinguishable from one that printed the set",
    test: "test/build-dash.test.mjs",
    expectRed: "the task row names WAITING_FOR_QUOTA, so acting on the headline is not presented as enough",
    edits: [{
      file: "src/build/dash.mjs",
      find: "             (t.waiting.all.length > 1\n               ? `  also ${t.waiting.all.filter(w => w !== t.waiting.first).join(\" \")}` : \"\") +",
      replace: "             \"\" +",
    }],
  },
  {
    name: "dash-says-when-a-live-holder-has-outrun-its-lease",
    why: "say only RUNNING when the holder process is alive past `expires_at`. The two facts are kept apart in the model precisely because collapsing them loses this one: a wedged or very slow tick presents exactly like a healthy builder, and an operator would have to know the lease interval and infer the condition from the last-seen figure. The JSON exposed it as `lease_unexpired: false` while the default rendering -- the one an operator reads -- did not",
    test: "test/build-dash.test.mjs",
    expectRed: "the first line says RUNNING and says the lease is stale, because both are true",
    edits: [{
      file: "src/build/dash.mjs",
      find: "      (m.alive.lease_unexpired ? \"\" : \"  LEASE STALE: this tick has outrun its lease\")",
      replace: "      \"\"",
    }],
  },
  {
    name: "task-readers-refuse-an-argument-they-do-not-read",
    why: "let a bare word typed at a task reader be discarded in silence. The argv walk refuses an unknown flag and `inapplicable` refuses a known flag on a command that cannot act on it; a POSITIONAL was governed by neither, so `reeve task dash <cursor>` dropped the cursor, answered from the beginning of the log, printed an empty movement list and exited 0 -- which is byte-identical to a genuinely quiet period and is the one combination nothing downstream can detect. `task show a b c` answered about `a` on the same terms",
    test: "test/build-dash.test.mjs",
    expectRed: "a cursor typed as a positional is refused, not discarded",
    edits: [{
      file: "bin/reeve.flags.mjs",
      find: "  return rest.length > spec.takes ? rest.slice(spec.takes) : null;",
      replace: "  return null;",
    }],
  },
  {
    name: "cli-task-reaches-its-own-body",
    why: "stop the `task` label from matching, so the command falls past its own body. This CLI's case labels share fall-through blocks and `task` sits directly above `build` -- the position that captured status, statusline and dash for a full day when `shadow` landed there. A route that does not reach its body does not error in an obvious way; it runs the NEXT command's body against the operator's arguments, which is how a read command became a write one",
    test: "test/cli-routing.test.mjs",
    expectRed: "task reaches its own body",
    edits: [{ file: "bin/reeve",
              find: "  case \"task\": {",
              replace: "  case \"task-disabled\": {" }],
  },
  {
    name: "anchors-resolve-detects-a-rotted-one",
    why: "stop reporting an anchor that resolves nowhere, so a stub the sweep cannot place reads as a manifest in good order -- the state the default branch was in for two entries while the suite and the linter both passed, because neither reads an anchor",
    test: "test/anchors-resolve.test.mjs",
    expectRed: "an anchor that appears NOWHERE is reported",
    edits: [{ file: "src/stubsweep.mjs",
              find: "          bad.push({ name: entry.name, file, count: n, why: String(e.message) });",
              replace: "          void n;" }],
  },
  {
    name: "artifact-write-is-atomic-against-a-crash",
    why: "write the artifact straight to its final path, because the rename looks like an optimisation. A process killed mid-write then leaves a SHORT research.md rather than nothing -- present, correctly named, and incomplete -- and the next reader finds an artifact where there should be none. The drill kills a real child between the write and the rename, so the assertion is about what survives a crash rather than about what the function returns",
    test: "test/artifact.test.mjs",
    expectRed: "no artifact exists after a write interrupted before its rename",
    edits: [{ file: "src/build/artifact.mjs",
              find: "  const tmp = `${path}.tmp-${process.pid}-${randomBytes(4).toString(\"hex\")}`;",
              replace: "  const tmp = path;" }],
  },
  {
    name: "artifact-read-back-checks-the-bytes",
    why: "return the file's contents without comparing its hash to the one recorded, so a read certifies that something is there rather than that it is the thing the transition was justified by. An artifact edited after its write then reads back clean, and the sha in phase_event describes bytes nobody has since seen",
    test: "test/artifact.test.mjs",
    expectRed: "and a file mutated after the write is refused on read-back",
    edits: [{ file: "src/build/artifact.mjs",
              find: "  if (got !== expectSha)",
              replace: "  if (false)" }],
  },
  {
    name: "path-ids-cannot-escape-their-directory",
    why: "stop sanitising the identifier used as a directory name. Task ids and repository names arrive from a command line, and a separator that survives turns join(home, \"tasks\", id) into a path that leaves the home entirely -- so a filing can read or write anywhere the process can. The assertion resolves the path and requires containment rather than searching for two dots, because `../../etc` sanitises to `--..-etc`, which still contains them and still cannot traverse: a segment is not a path",
    test: "test/state-paths.test.mjs",
    expectRed: "a task id cannot escape the tasks directory",
    edits: [{ file: "src/paths.mjs",
              find: "const safe = s => String(s).replace(/[^A-Za-z0-9._-]/g, \"-\").replace(/^\\.+/, \"-\");",
              replace: "const safe = s => String(s);" }],
  },
  {
    name: "artifact-citation-is-checked-per-claim",
    why: "check the artifact for a citation anywhere in the FILE rather than on each claim. Nine cited claims and one bare assertion then read as clean -- and the bare one is the claim that needed checking. The whole-file form passes as soon as any line carries a citation, so it grows weaker the longer the artifact gets, which is the opposite of what a gate should do",
    test: "test/artifact.test.mjs",
    expectRed: "and exactly the one uncited claim is named, not the whole file",
    edits: [{ file: "src/build/artifact.mjs",
              find: "      if (nested) continue;",
              replace: "      if (nested) continue;\n      if (CITATION.test(withoutEndpoints(text))) { claims++; continue; }" }],
  },
  {
    name: "reviewdiff-refuses-an-artifact-phase",
    why: "let a report phase fall through into the diff gate, on the argument that nothing calls reviewDiff with a phase name. It then arrives with an empty file list and is refused as \"the worker produced an empty diff\" -- a refusal that reads as the worker's fault and is the gate's, so an operator debugs a worker that did exactly what it was asked. The guard is derived from the artifact-phase map rather than listing the three names, so a fourth report phase is covered without anyone remembering",
    test: "test/artifact.test.mjs",
    expectRed: "reviewDiff refuses the dispatch name BUILD_RESEARCH",
    edits: [{ file: "src/sandbox.mjs",
              find: "  if (action !== null && (BUILD_ACTIONS.includes(action) || Object.hasOwn(ARTIFACT_FILE, action)))",
              replace: "  if (false)" }],
  },
  {
    name: "reviewdiff-guard-matches-the-dispatch-vocabulary",
    why: "key the guard on PHASE names only, which is the form that shipped and could never fire: reviewDiff receives `decision.action`, and a report phase is dispatched under a BUILD_ name. The guard then looks correct, is covered by a green test that passes it phase names, and is unreachable from every real caller -- a fixture written to match the implementation rather than production. Both vocabularies are refused now, and phases.mjs records why neither can be derived from the other: SIZING dispatches as BUILD_SIZE, not BUILD_SIZING",
    test: "test/artifact.test.mjs",
    expectRed: "reviewDiff refuses the dispatch name BUILD_SIZE",
    edits: [{ file: "src/sandbox.mjs",
              find: "  if (action !== null && (BUILD_ACTIONS.includes(action) || Object.hasOwn(ARTIFACT_FILE, action)))",
              replace: "  if (action !== null && Object.hasOwn(ARTIFACT_FILE, action))" }],
  },
  {
    name: "artifact-absence-does-not-satisfy-the-citation-rule",
    why: "let an artifact with no claims pass. The per-claim loop runs zero times, every claim is trivially cited, and an artifact that is all headings and prose satisfies a gate whose entire subject is the claims it does not contain. RESEARCH then advances having produced nothing, and the phase that consumes its findings finds none -- with no refusal anywhere, because nothing was checked and nothing was wrong",
    test: "test/artifact.test.mjs",
    expectRed: "a research artifact with NO claims is refused",
    edits: [{ file: "src/build/artifact.mjs",
              find: "    if (claims < minClaims)",
              replace: "    if (false)" }],
  },
  {
    name: "artifact-url-port-is-not-a-citation",
    why: "count a URL's port as a file:line citation. `[\\w./-]+:\\d+` matches localhost:3000 exactly as it matches src/x.mjs:170, and research is full of links -- so a claim supported by nothing but a URL reads as sourced, and the gate accepts precisely the unsupported claims it exists to reject. The controls matter as much: a claim carrying BOTH a link and a real citation must still pass, or the fix has become a ban on URLs",
    test: "test/artifact.test.mjs",
    expectRed: "a claim supported only by http://localhost:3000 is refused",
    edits: [{ file: "src/build/artifact.mjs",
              find: "      const cites = (withoutEndpoints(line).match(new RegExp(CITATION.source, \"g\")) ?? []).length;",
              replace: "      const cites = (line.match(new RegExp(CITATION.source, \"g\")) ?? []).length;" }],
  },
  {
    name: "artifact-design-minima-are-per-slice",
    why: "check the design's required lines across the whole DOCUMENT instead of within each slice. It then passes as soon as one slice carries each label, so a complete first slice makes an empty second slice invisible and the phase advances with a slice naming no files, no tests and no done condition. This is the same per-unit distinction the citation check makes, and it was missing here in the same file -- one site fixed, its sibling left",
    test: "test/artifact.test.mjs",
    expectRed: "a second slice missing everything is refused",
    edits: [{ file: "src/build/artifact.mjs",
              find: "        const rows = body.split(\"\\n\");",
              replace: "        const rows = text.split(\"\\n\");" }],
  },
  {
    name: "artifact-sizing-must-be-a-sizing",
    why: "accept any syntactically valid JSON as a sizing. `null`, `[]`, `7` and `{}` all parse, and none carries a decision -- so the task advances out of SIZING with nothing to size by, and the depth the phase machine reads is simply absent. Parsing proves the bytes are JSON, which is not the property the gate is for",
    test: "test/artifact.test.mjs",
    expectRed: "sizing.json that is null is refused",
    edits: [{ file: "src/build/artifact.mjs",
              find: "      if (!isSizingObject)",
              replace: "      if (false)" }],
  },
  {
    name: "artifact-failed-rename-takes-its-temporary",
    why: "leave the rename outside the cleanup, so a rename that fails after the bytes were safely written leaves a COMPLETE temporary and every retry leaves another. Worse than the partial-write leak it sits three lines below, because each orphan is full size -- and it is that leak's sibling in the same function, which is how it survived the commit that fixed the first one",
    test: "test/artifact.test.mjs",
    expectRed: "a failed rename leaves no temporary behind",
    edits: [{ file: "src/build/artifact.mjs",
              find: "    try { rmSync(tmp, { force: true }); } catch { /* the rename failure is the real one */ }",
              replace: "    /* leak it */" }],
  },
  {
    name: "artifact-stale-temporaries-are-reaped-by-age",
    why: "reap every temporary rather than only those older than the threshold, which deletes a CONCURRENT writer's file out from under it mid-write -- a corruption far worse than the leak the reaper exists to fix. Age is the discriminator precisely because pid is not: pids are reused, so a live writer's temporary can carry a pid this process believes is dead",
    test: "test/artifact.test.mjs",
    expectRed: "and a RECENT one is left alone, because it may be a live writer's",
    edits: [{ file: "src/build/artifact.mjs",
              find: "        if (now - statSync(full).mtimeMs > STALE_TMP_MS) rmSync(full, { force: true });",
              replace: "        rmSync(full, { force: true });" }],
  },
  {
    name: "artifact-slice-ends-at-the-next-section",
    why: "bound the last slice at end-of-file instead of at the next section, so a trailing section's labels are read as the slice's own. An empty final slice followed by a Notes block carrying the four labels then passes -- and the labels it was credited with belong to prose nobody will implement. This is the per-slice fix's own blind spot: bounding slices by the next SLICE leaves the last one unbounded",
    test: "test/artifact.test.mjs",
    expectRed: "an empty final slice does not inherit a trailing section's labels",
    edits: [{ file: "src/build/artifact.mjs",
              find: "      const next = sections.find(i => i > starts[k]) ?? lines.length;",
              replace: "      const next = starts[k + 1] ?? lines.length;" }],
  },
  {
    name: "artifact-citation-must-look-like-a-path",
    why: "accept any token of the form word:number as a file citation. A claim mentioning a time (12:30), a ticket (issue:42) or a pull request then reads as sourced, and research prose is full of all three -- so the gate passes precisely the unsupported claims it exists to catch. Removing URLs closed one leak in that pattern and left the rest, which is the same shape one round later",
    test: "test/artifact.test.mjs",
    expectRed: "12:30 is not a file citation",
    edits: [{ file: "src/build/artifact.mjs",
              find: "const CITATION = /(?:[\\w.-]*\\/[\\w./-]*|[\\w-]+\\.[A-Za-z][\\w-]*):\\d+/;",
              replace: "const CITATION = /[\\w./-]+:\\d+/;" }],
  },
  {
    name: "artifact-design-labels-need-values",
    why: "check that a slice CONTAINS each label rather than that each label has something after it. The bare scaffold -- Files:, Packages:, Tests:, Done when: and nothing else -- then advances as though the slice named its files, its tests and its done condition. The label is the question and the gate was reading it as the answer",
    test: "test/artifact.test.mjs",
    expectRed: "a slice carrying the bare scaffold is refused",
    edits: [{ file: "src/build/artifact.mjs",
              find: "        if (!has) { findings.push(`${heading} has a ${label} line with nothing after it`); continue; }",
              replace: "        if (false) { findings.push(`${heading} has a ${label} line with nothing after it`); continue; }" }],
  },
  {
    name: "artifact-sizing-carries-its-whole-contract",
    why: "require only a depth of the sizing artifact. The design states the shape it emits -- depth, est_files, est_weighted_files, est_packages, est_slices, risk_paths_touched, rationale -- and a sizing carrying only a depth advances the task with no estimate, no slice count, no risk paths and no reasoning, which is every input the floors and the spec section read. I declined this once on the argument that requiring fields would be guessing at an unwritten task; the shape is written down, so it was not a guess to enforce",
    test: "test/artifact.test.mjs",
    expectRed: "an omitted field is reported as omitted, not as a value of the wrong type",
    edits: [{ file: "src/build/artifact.mjs",
              find: "          if (!(field in parsed)) { findings.push(`sizing.json omits ${field}`); continue; }",
              replace: "          if (false) { findings.push(`sizing.json omits ${field}`); continue; }" }],
  },
  {
    name: "run-path-components-are-single-segments",
    why: "sanitise only the task id and check the other components for presence alone. A phase or stream carrying a separator then escapes the task tree entirely -- `phase: \"x/../../../../escape\"` resolves to a file directly under the reeve home, outside any task. It is the defect `safe` exists to prevent, in the same function, on the arguments beside the one that was protected. These throw rather than being sanitised: a task id is typed by a person, but a phase and a stream are chosen by this codebase, so a separator in one is a wiring error and rewriting it quietly would hide the bug and produce a file nobody can find by name",
    test: "test/state-paths.test.mjs",
    expectRed: "a run path refuses {\"phase\":\"x/../../../../escape\"}",
    edits: [{ file: "src/paths.mjs",
              find: "    if (String(value) !== safe(value))",
              replace: "    if (false)" }],
  },
  {
    name: "artifact-claim-sees-every-list-marker",
    why: "recognise only some of Markdown's list markers, so a claim written with + or 1) is not a claim at all and the citation rule never sees it. The artifact then passes with an uncited claim in it, and the gate reports success on precisely the input it skipped -- a check that narrows its own input answers a smaller question than the one it was asked",
    test: "test/artifact.test.mjs",
    expectRed: "an uncited claim written with \"+\" is seen and refused",
    edits: [{ file: "src/build/artifact.mjs",
              find: "const CLAIM = /^([ \\t]*)([-*+]|\\d+[.)])([ \\t]+)\\S/;",
              replace: "const CLAIM = /^([ \\t]*)([-*]|\\d+\\.)([ \\t]+)\\S/;" }],
  },
  {
    name: "artifact-accepts-the-producers-slice-headings",
    why: "match only this plan's level-two `## Slice` heading. The phase plan that will actually emit design.md writes `## Slices` holding level-three `### Slice 1: ...`, so the gate finds ZERO slices in the artifact its producer is specified to write and refuses it with \"carries no ordered slice list\" -- correct work rejected, and every hand-written fixture in the suite passing because they were all written to match the checker rather than the producer",
    test: "test/artifact.test.mjs",
    expectRed: "and accepts the documented design artifact with a depth-carrying expect",
    edits: [{ file: "src/build/artifact.mjs",
              find: "    const SLICE = /^#{2,3}\\s+Slice\\b/;",
              replace: "    const SLICE = /^##\\s+Slice\\b/;" }],
  },
  {
    name: "artifact-expect-needs-no-depth",
    why: "demand a depth inside the expect object. The phase helpers that call this return the requirement set -- minCitationsPerClaim and minClaims -- and carry no depth, because the depth is an INPUT to them and not an output. Demanding one throws before the artifact is read, so the gate refuses its own documented callers while every test that hand-built an expect object passes",
    test: "test/artifact.test.mjs",
    expectRed: "reviewArtifact does not throw on the helper's requirement set",
    edits: [{ file: "src/build/artifact.mjs",
              find: "  if (!expect || typeof expect !== \"object\")",
              replace: "  if (!expect || typeof expect.depth !== \"string\")" }],
  },
  {
    name: "artifact-claims-are-scoped-to-findings",
    why: "treat every bullet in the document as a claim rather than only those under the Findings heading the contract names. A valid research report is then REFUSED for a Limitations note saying the network was unavailable -- the gate rejecting exactly the honest disclosure research is supposed to carry, and refusing correct work rather than admitting wrong work, which is the failure direction nobody looks for",
    test: "test/artifact.test.mjs",
    expectRed: "a bullet outside Findings is not a claim and does not need a citation",
    edits: [{ file: "src/build/artifact.mjs",
              find: "      if (at === -1) return rows;",
              replace: "      return rows;" }],
  },
  {
    name: "artifact-honours-the-requirements-it-is-given",
    why: "ignore the requirement flags the caller passes and key the measured-context check on depth alone. The phase helper returns {requireSliceList, requireDoneCondition, requireMeasuredContext, minSlices} and carries NO depth, so every one of those checks silently does not run -- a quieter failure than refusing, because the gate reports ok and the minima it was asked to apply were never applied",
    test: "test/artifact.test.mjs",
    expectRed: "requireMeasuredContext true is honoured, with no depth in sight",
    edits: [{ file: "src/build/artifact.mjs",
              find: "    const wantsMeasured = \"requireMeasuredContext\" in expect",
              replace: "    const wantsMeasured = false && \"requireMeasuredContext\" in expect" }],
  },
  {
    name: "measurement-kind-lookup-ignores-the-prototype",
    why: "look the kind up with plain property access instead of an own-property test, so `toString` and `constructor` answer as declared kinds. The allowed-results list then comes back as a FUNCTION, and the vocabulary check downstream fails for a reason that has nothing to do with the caller's mistake. An undeclared name has to be refused BY NAME, because a lookup returning undefined for a typo is indistinguishable from one returning undefined for a kind nobody has run yet -- a misspelling would store and then read back for ever as `not measured`",
    test: "test/provider-measurement.test.mjs",
    expectRed: "an inherited property name is not a kind: toString",
    edits: [{ file: "src/build/measurementdb.mjs",
              find: "const known = (kind) => Object.hasOwn(MEASUREMENT_KINDS, kind);",
              replace: "const known = (kind) => MEASUREMENT_KINDS[kind] !== undefined;" }],
  },
  {
    name: "measurement-refuses-a-future-timestamp",
    why: "accept a measurement dated in the future. `reviewer_supply.since` holds 1800000000 -- January 2027 -- written by a test clock that reached a real store, and nothing downstream has ever called it stale because it is permanently recent. A measurement is read by the arming gate to decide whether an answer is still current, so a future date does not merely mislead: it removes the only question that would have caught it",
    test: "test/provider-measurement.test.mjs",
    expectRed: "a future timestamp is refused BY THE WRITER: it would be permanently recent",
    edits: [{ file: "src/build/measurementdb.mjs",
              find: "  if (measuredAt > now)",
              replace: "  if (false)" }],
  },
  {
    name: "measurement-requires-its-evidence",
    why: "drop the evidence requirement, so a result can be recorded with nothing behind it. A stored result with no provenance is a claim, and once the person who made it is gone nothing distinguishes it from a measurement -- which is precisely what the arming gate reads it as. The table carries the same rule as a CHECK, so this stub must be caught by the writer's own refusal rather than by the store catching it later",
    test: "test/provider-measurement.test.mjs",
    expectRed: "a result with empty evidence is a claim, not a measurement",
    edits: [{ file: "src/build/measurementdb.mjs",
              find: "  if (typeof evidence !== \"string\" || evidence.trim() === \"\")",
              replace: "  if (false)" }],
  },
  {
    name: "hub-tables-covers-every-live-table",
    why: "drop a table from the derived HUB_TABLES, so the set a snapshot is validated against is smaller than the set the store actually has. `backup.mjs` validates a snapshot by checking the tables in HUB_TABLES, so a table missing from it is a table nobody checks -- the snapshot passes validation while carrying a table that could be absent or malformed, and the failure surfaces at restore, which is the one moment there is no fallback. This is the cross-check the whole file exists for: three inventories of the schema, and the guard is that they agree",
    test: "test/hub-crosscheck.test.mjs",
    expectRed: "HUB_TABLES equals the live table set exactly, so snapshot validation checks every table",
    edits: [{ file: "src/build/hubdb.mjs",
              find: "  return Object.freeze(Object.keys(shapeAt(version).tables).sort());",
              replace: "  return Object.freeze(Object.keys(shapeAt(version).tables).sort().slice(1));" }],
  },
  {
    name: "measurement-appends-its-event",
    why: "record the row and skip its event. restoreHub copies the snapshot and rebuilds everything after it from the hub_event tail ALONE, so a measurement taken since the last snapshot exists only in that tail -- and with no event a NORMAL restore of a healthy hub drops it while reporting success. What disappears is the answer the arming gate reads, together with the evidence that makes it a measurement rather than a claim, and nothing in the restore's output says anything was lost",
    test: "test/provider-measurement.test.mjs",
    expectRed: "recording a measurement appends its event",
    edits: [{ file: "src/build/measurementdb.mjs",
              find: "    hubEvent(db, { kind: \"provider_measurement.recorded\",",
              replace: "    if (false) hubEvent(db, { kind: \"provider_measurement.recorded\"," }],
  },
  {
    name: "measurement-event-carries-the-whole-row",
    why: "put the primary key in the event payload instead of the row image. The event still appears, the handler still fires and the restore still reports success -- but replay rebuilds a row whose result and evidence are absent. Downstream that does not read as data loss, it reads as a measurement that concluded nothing, which is the more dangerous of the two because it looks like an answer",
    test: "test/provider-measurement.test.mjs",
    expectRed: "and the event carries the whole row image: result",
    edits: [{ file: "src/build/measurementdb.mjs",
              find: "        `SELECT provider, kind, result, evidence, measured_at FROM provider_measurement",
              replace: "        `SELECT provider, kind, measured_at FROM provider_measurement" }],
  },
  {
    name: "restore-replays-the-post-snapshot-tail",
    why: "drop ONE kind on the replay path, so a measurement written after the snapshot is skipped while every other table restores. The snapshot restores, the tail is replayed, the command exits 0, and the only thing missing is the row the arming gate reads -- a restore reporting success while silently returning that one answer to whenever the last snapshot was taken. Scoped to a single kind deliberately: making the lookup fail for EVERY event aborts the file long before the drill, and a stub that stops the run proves nothing about whether the drill would have noticed",
    test: "test/hub-backup-restore.test.mjs",
    expectRed: "provider_measurement matches the pre-drop export row for row",
    edits: [{ file: "src/build/replay.mjs",
              find: "      const h = HANDLERS[e.kind];",
              replace: "      const h = e.kind === \"provider_measurement.recorded\" ? undefined : HANDLERS[e.kind];" }],
  },
  {
    name: "measurement-checks-the-restore-lock-inside-the-lock",
    why: "move the maintenance check out of the transaction, back to where it ran before review found it. A top-level SAVEPOINT is DEFERRED and takes no write lock until its first write, so a restore can take `maintenance_lock` and capture its tail in the window after the check passes -- the row and its event then land in the database the restore is about to replace, and the swap drops both while the call reports success. The stub is the weaker ordering, not a deleted check, because the check being ABSENT is not the defect: the defect is the check being true at a moment that no longer holds when the write happens",
    test: "test/provider-measurement.test.mjs",
    expectRed: "a write is refused while a restore holds the maintenance lock",
    edits: [{ file: "src/build/measurementdb.mjs",
              find: "  const body = () => {\n    assertWritable(db, { isAlive, inTx: true });",
              replace: "  const body = () => {" }],
  },
  {
    name: "sweep-refuses-a-run-shorter-than-its-control",
    why: "drop the comparison between the stubbed run's assertion count and the control's, which is the state this file was in before. A stub does not merely make an assertion fail -- it changes what the code does, so the file's later steps can stop being reachable. When one aborts after printing the NAMED failure, the text says the right assertion went red and the exit code is non-zero, so every other input to the verdict says CAUGHT while the assertions after the abort never ran. In a log an assertion that never ran is indistinguishable from one that passed, which is how three entries in one morning reported a property measured while proving a fraction of it. The TIMED_OUT branch already refuses a run that did not finish on exactly this reasoning; an abort is the same fact through a different exit",
    test: "test/stubsweep.test.mjs",
    expectRed: "a file that aborts after printing the named failure is UNRUNNABLE end to end, not CAUGHT",
    edits: [{ file: "src/stubsweep.mjs",
              find: "  if (controlObserved && seen < controlObserved.assertionsSeen)",
              replace: "  if (false)" }],
  },
  {
    name: "report-a-non-object-is-not-a-report",
    why: "read the error list from `walk`'s RETURN VALUE, which is `errors.push(...)` -- a number -- on every early return. `errors.length` is then undefined, which is falsy, so a report that is a string, a number, null or an array validates as OK. It is not an exotic input: it is what a worker answering in prose instead of JSON produces, and it would reach `evidenceFor` as a validated report. TWO EDITS, because two things independently prevent it: the caller owning the array, and every early return handing the array back. Reverting either alone leaves the other, and an entry that stubs one of two sufficient guards cannot go red -- which is a stub reporting that a property is measured when it is not",
    test: "test/phase-report.test.mjs",
    expectRed: "a report that is a string is refused",
    edits: [{ file: "src/build/report.mjs",
              find: "  const errors = [];\n  walk(schemaFor(action), value, \"\", errors);",
              replace: "  const errors = walk(schemaFor(action), value, \"\", []);\n  void 0;" },
            { file: "src/build/report.mjs",
              find: "  if (schema.type === \"object\" && !isObject) {",
              replace: "  if (false) {" }],
  },
  {
    name: "report-an-undeclared-outcome-does-not-advance",
    why: "map anything that is not infeasible or blocked to phase.succeeded, so a report that skipped validation, or one validated against a different action, ADVANCES the task on a word nobody declared. TWO EDITS, because two things independently prevent it now: validating the report against its action refuses the unknown word, and the evidence table has no entry for it. Reverting either alone leaves the other, and an entry that stubs one of two sufficient guards cannot go red",
    test: "test/phase-report.test.mjs",
    expectRed: "an outcome of \"nonsense\" is refused rather than advanced",
    edits: [{ file: "src/build/report.mjs",
              find: "  if (!v.ok)",
              replace: "  if (false)" },
            { file: "src/build/report.mjs",
              find: "  const make = EVIDENCE[report.outcome];",
              replace: "  const make = EVIDENCE[report.outcome] ?? EVIDENCE.ok;" }],
  },
  {
    name: "report-actions-are-the-machines-own-list",
    why: "write the three action names out again instead of taking the list phases.mjs already exports. `BUILD_ACTION_FOR` carries a comment saying in as many words that a second inventory of these three names is the defect this codebase keeps finding; a literal is correct on the day it is written, which is the only day a duplicate ever is",
    test: "test/phase-report.test.mjs",
    expectRed: "ACTIONS is the machine's own action list, not a copy of it",
    edits: [{ file: "src/build/report.mjs",
              find: "export const ACTIONS = BUILD_ACTIONS;",
              replace: "export const ACTIONS = Object.freeze([\"BUILD_SIZE\", \"BUILD_RESEARCH\", \"BUILD_DESIGN\"]);" }],
  },
  {
    name: "report-refusal-carries-the-kind-its-consumer-reads",
    why: "return a refusal with no `kind`, when S3-D's consumed table names `{ok:false, kind:\"BAD_REPORT\", errors}` and dispatches on it. The consumer then branches on a field that is never set, so a malformed report takes whichever path an absent kind falls into",
    test: "test/phase-report.test.mjs",
    expectRed: "a refusal is labelled BAD_REPORT for its consumer",
    edits: [{ file: "src/build/report.mjs",
              find: "{ ok: false, kind: \"BAD_REPORT\", errors }",
              replace: "{ ok: false, errors }" }],
  },
  {
    name: "report-evidence-names-the-phase-it-came-from",
    why: "drop the phase from a SIZING success. `nextPhase` refuses an unattributed report -- an unattributed success advances nothing -- so the evidence map omitting it means every well-formed sizing report is refused at the transition with a message about attribution, and the worker that produced a perfectly good report is told its report came from nowhere. A pure-function test of the map cannot see this; only driving it through applyTransition can",
    test: "test/phase-report.test.mjs",
    expectRed: "a well-formed report advances the task",
    edits: [{ file: "src/build/report.mjs",
              find: "    ? { kind: \"phase.succeeded\", phase, depth: report.depth }",
              replace: "    ? { kind: \"phase.succeeded\", depth: report.depth }" }],
  },
  {
    name: "report-hold-never-manufactures-an-escalation",
    why: "take the escalation identity from the report instead of minting it. `nextPhase` checks only that the string is non-blank and `applyTransition` persists it as the hub's escalation KEY, so a worker could file its hold under another task's cause -- by accident or deliberately -- and misroute both the notification and the retirement. The identity is derived from the phase this map knows and the task id applyTransition substitutes; the worker never had a say",
    test: "test/phase-report.test.mjs",
    expectRed: "under an identity minted from the phase, with the id left for applyTransition",
    edits: [{ file: "src/build/report.mjs",
              find: "                                     escalation: `bt:<id>:phase:blocked:${phase}` }),",
              replace: "                                     escalation: report.escalation })," }],
  },
  {
    name: "report-schema-freeze-notices-a-declared-property",
    why: "add a property to build_size.json without anyone noticing. The schema files are what every dispatched worker is ASKED for -- they go to the CLI as --json-schema -- so a change here changes the contract for work already in flight. This stub is chosen because no other assertion in the file catches it: `refuses a property the schema does not declare` still passes, since the fixture's value is fractional and the new property is an integer. The freeze is the only thing that sees it, which is the whole reason a freeze exists",
    test: "test/phase-report.test.mjs",
    expectRed: "BUILD_SIZE's schema is frozen",
    edits: [{ file: "src/build/schemas/build_size.json",
              find: "    \"rationale\": {",
              replace: "    \"confidence\": { \"type\": \"integer\" },\n    \"rationale\": {" }],
  },
  {
    name: "report-evidence-map-freeze-notices-a-new-field",
    why: "add a field to the evidence a SIZING success produces. The outcome-to-evidence map is the half a JSON freeze cannot see, and this is a change no named assertion covers: the evidence still carries the right kind, phase and depth, so every behavioural assertion stays green while what reaches applyTransition has quietly changed shape. A freeze over the schema files alone would report the contract intact",
    test: "test/phase-report.test.mjs",
    expectRed: "and so is the outcome-to-evidence map, which the JSON freeze cannot see",
    edits: [{ file: "src/build/report.mjs",
              find: "    ? { kind: \"phase.succeeded\", phase, depth: report.depth }",
              replace: "    ? { kind: \"phase.succeeded\", phase, depth: report.depth, source: \"worker\" }" }],
  },
  {
    name: "report-schema-reaches-the-worker-as-text",
    why: "hand the PARSED schema to a caller that needs text. `workerArgs` pushes its jsonSchema straight into the argv array and its tested contract is serialized JSON, so an object becomes the literal string `[object Object]` at spawn -- an argument that is syntactically fine and semantically nothing, on all three phase dispatches at once. Nothing fails: the worker simply has no structured-output contract",
    test: "test/phase-report.test.mjs",
    expectRed: "BUILD_SIZE's schema is available as text",
    edits: [{ file: "src/build/report.mjs",
              find: "export const schemaTextFor = (action) => load(action).text;",
              replace: "export const schemaTextFor = (action) => load(action).schema;" }],
  },
  {
    name: "report-evidence-validates-against-its-action",
    why: "check only that the outcome WORD is declared, instead of validating the report against the action it is being attributed to. A valid RESEARCH report handed in as BUILD_DESIGN then becomes a successful DESIGN evidence, which nextPhase accepts while the task is in DESIGN -- the miswired or adopted-report case the phase-attribution guard exists for, defeated one layer above it",
    test: "test/phase-report.test.mjs",
    expectRed: "a valid RESEARCH report is refused as DESIGN evidence",
    edits: [{ file: "src/build/report.mjs",
              find: "  if (!v.ok)",
              replace: "  if (false)" }],
  },
  {
    name: "report-every-declared-outcome-is-mapped",
    why: "let the set of mapped outcomes drift from the set the schemas declare. The map used to fall through to phase.succeeded, so an outcome added later -- cancelled, partial -- would ADVANCE the task the day it was declared. A freeze cannot catch that, because a freeze exercises the outcomes that exist; only comparing the map against the enum fails at the moment the enum grows",
    test: "test/phase-report.test.mjs",
    expectRed: "BUILD_SIZE's declared outcome blocked has evidence to become",
    edits: [{ file: "src/build/report.mjs",
              find: "export const MAPPED_OUTCOMES = Object.freeze(Object.keys(EVIDENCE));",
              replace: "export const MAPPED_OUTCOMES = Object.freeze(Object.keys(EVIDENCE).filter((o) => o !== \"blocked\"));" }],
  },
  {
    name: "report-schema-cache-cannot-be-mutated",
    why: "hand every consumer a mutable reference to the one cached schema. A consumer that adds a property to what it was given changes what every later validation enforces, and the freeze goes on reporting the contract intact because it hashes the same mutated object -- a gate whose own evidence moves with the thing it is guarding",
    test: "test/phase-report.test.mjs",
    expectRed: "a caller cannot add a property to the shared schema",
    edits: [{ file: "src/build/report.mjs",
              find: "  if (v && typeof v === \"object\") { for (const k of Object.keys(v)) deepFreeze(v[k]); Object.freeze(v); }",
              replace: "  if (false) { for (const k of Object.keys(v)) deepFreeze(v[k]); Object.freeze(v); }" }],
  },
  {
    name: "report-evidence-map-freeze-covers-the-hold-branch",
    why: "add a field to the evidence a BLOCKED report produces. Every behavioural assertion stays green -- the hold still carries its kind, reason, detail and escalation -- and a freeze that exercised only `ok` would report the map intact. This is the half of the freeze that had to be widened: the two outcomes the machine REFUSES are the ones whose evidence a failure would surface as a refusal nobody could trace back to this file",
    test: "test/phase-report.test.mjs",
    expectRed: "and so is the outcome-to-evidence map, which the JSON freeze cannot see",
    edits: [{ file: "src/build/report.mjs",
              find: "  blocked: ({ phase, report }) => ({ kind: \"hold\", reason: \"blocked_other\",",
              replace: "  blocked: ({ phase, report }) => ({ kind: \"hold\", source: \"worker\", reason: \"blocked_other\"," }],
  },
  {
    name: "report-an-ok-report-carries-its-phase-output",
    why: "drop the conditional requirement, so `required` names only outcome and reason. A SIZING success carrying just a depth then advances the task, and the deterministic floors read est_packages and est_weighted_files as ABSENT -- comparing against nothing and preserving a depth the estimates never supported. Requiring them unconditionally is the other error, because a blocked worker would have to invent them, which is how a stop becomes a fabricated success",
    test: "test/phase-report.test.mjs",
    expectRed: "an ok SIZING report with no estimates is refused",
    edits: [{ file: "src/build/report.mjs",
              find: "  if (schema.if !== undefined) {",
              replace: "  if (false) {" }],
  },
  {
    name: "report-a-reason-with-no-words-is-not-a-reason",
    why: "count CHARACTERS instead of requiring a word. `minLength: 1` accepted a reason of \" \", which records no explanation for the founder -- and `nextPhase` refuses an infeasible on exactly that ground, so validation passed, the transition refused, and the BAD_REPORT retry path that exists for a malformed report was never reached. Validation and the machine now agree",
    test: "test/phase-report.test.mjs",
    expectRed: "a whitespace-only reason is refused for BUILD_SIZE",
    edits: [{ file: "src/build/report.mjs",
              find: "    if (schema.pattern !== undefined && !new RegExp(schema.pattern).test(value))",
              replace: "  if (false)" }],
  },
  {
    name: "report-the-keyword-scan-visits-every-property",
    why: "stop the keyword scan at the top level instead of descending into every declared property. That is the OLD behaviour with the call site moved: checking during validation followed only the properties a report happened to carry, so an unimplemented keyword beneath an optional property was never visited by a report that omitted it and the schema silently enforced less than it declares. Moving the scan to load time is worth nothing unless the scan is COMPLETE, and the completeness is the half a stub can measure -- the call site is exercised by every other assertion in the file, all of which load a schema",
    test: "test/phase-report.test.mjs",
    expectRed: "an unimplemented keyword under an optional property is refused when the schema loads",
    edits: [{ file: "src/build/report.mjs",
              find: "    if (k === \"properties\") for (const [p, sub] of Object.entries(v)) assertKnownKeywords(sub, `${where}${p}.`);",
              replace: "    if (k === \"properties\") continue;" }],
  },
  {
    name: "report-shape-checks-are-not-gated-on-a-declared-type",
    why: "nest `required` and `properties` inside the `type === \"object\"` branch, where every subschema that does not declare a type cannot see them -- which is every `if` and every `then`, since a conditional constrains the object its parent already typed. The conditional requirement then applies NOTHING: `if` matches everything and `then` requires nothing, silently, while reading exactly as though it worked",
    test: "test/phase-report.test.mjs",
    expectRed: "an ok DESIGN report with no slices is refused",
    edits: [{ file: "src/build/report.mjs",
              find: "  if (isObject) {",
              replace: "  if (isObject && schema.type === \"object\") {" }],
  },
  {
    name: "identity-is-readable-by-the-guest",
    why: "take the identity off the guardian's allowlist, which is the state before this table existed. Every other assertion about the table still passes -- it is created, it holds the right id, the resolver reads it through a full connection -- and the ONE thing the table exists for is gone: the guardian's restricted connection cannot see it, so the id can only reach the tick through a privileged handle again. A grant is the whole feature here, and a test that read the table through a privileged connection would prove nothing about it",
    test: "test/project-identity.test.mjs",
    expectRed: "THE GUARDIAN'S RESTRICTED CONNECTION CAN READ THE IDENTITY",
    edits: [{ file: "src/build/hubguest.mjs",
              find: "  project_identity: [\"read\"],",
              replace: "" }],
  },
  {
    name: "identity-read-is-not-a-write-grant",
    why: "widen the identity grant from read to read-and-write. The guardian would then be able to change the numeric id it scopes every provider_lease on -- the one value that decides which repository a lease belongs to -- and nothing downstream re-derives it. The allowlist is default-deny precisely so a capability arrives refused rather than by omission, and a grant that is wider than the need is the same defect as a missing one, pointing the other way",
    test: "test/project-identity.test.mjs",
    expectRed: "and the grant is READ ONLY -- the guardian never writes an identity",
    edits: [{ file: "src/build/hubguest.mjs",
              find: "  project_identity: [\"read\"],",
              replace: "  project_identity: [\"read\", \"insert\", \"update\"]," }],
  },
  {
    name: "identity-refuses-an-id-that-cannot-be-one",
    why: "drop the positivity constraint on repo_id, so zero and negatives store. A repository id is a positive integer GitHub issued; zero is what a failed parse produces, and a zero id scopes every provider lease onto a repository that does not exist -- admission succeeds, the lease is taken, and the guardian is restraining nothing. The writer is not the only way into this table, and the value it holds is the one thing the whole scheduler is keyed on",
    test: "test/project-identity.test.mjs",
    expectRed: "the table refuses a zero repository id -- zero is not an id",
    edits: [{ file: "src/build/hubdb.mjs",
              find: "          CHECK (repo_id > 0),",
              replace: "" }],
  },
  {
    name: "identity-survives-a-restore",
    why: "drop the replay handler for the identity. restoreHub rebuilds everything after the snapshot from the hub_event tail alone, so a project admitted since the last snapshot exists only there -- and with no handler a NORMAL restore of a healthy hub loses its identity while reporting success. The guardian then falls through to GitHub for a value the hub had, turning a local read into a network dependency at exactly the moment the operator restored, and a GitHub outage in that window refuses every dispatch",
    test: "test/project-identity.test.mjs",
    expectRed: "a replay handler exists, so an identity learned after the last snapshot survives a restore",
    edits: [{ file: "src/build/replay.mjs",
              find: "  \"project_identity.learned\": { table: \"project_identity\", key: [\"project\"] },",
              replace: "" }],
  },
  {
    name: "repo-id-lookup-propagates-a-failed-query",
    why: "swallow the lookup's query failure and answer null, which is the catch this file was written to remove. A structurally damaged hub -- one that records a completed migration but has lost the table, and still passes quick_check because the pages it does have are intact -- would then report `no repository id` on every tick. That is indistinguishable from a registered project with no work yet, which is an ORDINARY state, so the guardian skips the project, gate state ages into staleness, and the tick reports a clean pass over a repository it never looked at. The one failure a diagnostic must never render as a benign absence",
    test: "test/repo-id-lookup.test.mjs",
    expectRed: "a store that recorded a migration and lost `project_identity` PROPAGATES",
    edits: [{ file: "src/build/repoid.mjs",
              find: "  return hub.prepare(\n    `SELECT repo_id FROM project_identity WHERE project = ?`)\n    .get(project.name)?.repo_id ?? null;",
              replace: "  try {\n    return hub.prepare(`SELECT repo_id FROM project_identity WHERE project = ?`)\n      .get(project.name)?.repo_id ?? null;\n  } catch { return null; }" }],
  },
  {
    name: "gatestate-records-the-app-a-check-is-bound-to",
    why: "record the EXPECTED app as the bound one, so a required check satisfied by a different App reads as satisfied by reeve's. The gate state is what the guardian renders a merge decision from: `bound_app_id` is how it tells `the ruleset requires this check and reeve's App produces it` from `the ruleset requires a check of that name and something else produces it`. A check name is not an identity -- anything installed on the repository can publish one -- so recording the expectation in place of the observation makes an unrelated integration's green look like reeve's, and drift that a human must decide on disappears into a pass",
    test: "test/hub-gatestate.test.mjs",
    expectRed: "a check bound to another app is recorded as drift, not as a pass",
    edits: [{ file: "src/build/gatestate.mjs",
              find: "    bound_app_id: required ? (required.integration_id ?? null) : null,",
              replace: "    bound_app_id: required ? (expectedAppId ?? null) : null," }],
  },
  {
    name: "sweep-refuses-an-orphan-test-file",
    why: "stop reading the orphan count, which is the state this file was in. `coverage` has always classified a test file that is neither covered by an entry nor grandfathered as an orphan, and its own comment calls that the failure -- a test nobody has shown can fail, and that nobody declared. Nothing read it: the exit was the entry verdicts alone, so a new test file arrived with no entry, the ratio moved by one, and no gate said anything. A rule stated in a comment and not wired is the exact shape this instrument exists to find, which is a poor shape for the instrument",
    test: "test/stubsweep.test.mjs",
    expectRed: "a test file with no entry and no grandfathering FAILS the sweep",
    edits: [{ file: "scripts/stub-sweep.mjs",
              find: "  if (whole && cov.orphans.length) {",
              replace: "  if (false) {" }],
  },
  {
    name: "migration-history-is-not-a-maximum",
    why: "ask whether the HIGHEST recorded migration is the current one instead of whether every migration is present. A hub recording 1 and 3 with 2 absent answers 3, satisfies the equality, and then fails on a table migration 2 was supposed to create -- the uncaught trace the guard exists to replace, arriving through the guard itself. The control is what makes this measurable: completedVersion answers the CURRENT version for that same holed file, so the old check provably would have passed it",
    test: "test/migration-history.test.mjs",
    expectRed: "and the gap is reported, naming which migration is absent",
    edits: [{ file: "src/build/hubdb.mjs",
              find: "    for (let v = 1; v <= HUB_SCHEMA_VERSION; v++) if (!have.has(v)) missing.push(v);",
              replace: "    for (let v = HUB_SCHEMA_VERSION; v <= HUB_SCHEMA_VERSION; v++) if (!have.has(v)) missing.push(v);" }],
  },
  {
    name: "artifact-sizing-fields-have-kinds",
    why: "check that a sizing field is PRESENT without checking what it is, so est_files: \"lots\" satisfies the contract. The floors that read those numbers compare them, so a string passes the gate and then produces a comparison nobody can reason about -- a value that is present and unusable, which every later reader treats as a number",
    test: "test/artifact.test.mjs",
    expectRed: "sizing.json's est_files as \"lots\" is refused, and the finding names it",
    edits: [{ file: "src/build/artifact.mjs",
              find: "          if (!ok(parsed[field]))",
              replace: "          if (false)" }],
  },
  {
    name: "artifact-citation-extension-is-not-length-capped",
    why: "cap a citation's file extension at six characters, so src/x.markdown:12 is not a citation and a correctly cited claim is REFUSED for having a long filename. What distinguishes a file reference from a URL port is the dot, not how many letters follow it, and a bound nobody chose deliberately becomes a rule nobody can find",
    test: "test/artifact.test.mjs",
    expectRed: "the bare filename x.markdown:12 is a citation",
    edits: [{ file: "src/build/artifact.mjs",
              find: "[\\w-]+\\.[A-Za-z][\\w-]*):\\d+/;",
              replace: "[\\w-]+\\.[A-Za-z][\\w]{0,5}):\\d+/;" }],
  },
  {
    name: "artifact-claims-are-top-level-bullets",
    why: "count a NESTED bullet as a claim of its own. A sub-item elaborating a cited claim then needs its own citation, so the more carefully a finding is broken down the more likely the artifact is refused -- the gate penalising exactly the structure it wants, and refusing correct work rather than admitting wrong work",
    test: "test/artifact.test.mjs",
    expectRed: "a nested bullet is not a claim of its own",
    edits: [{ file: "src/build/artifact.mjs",
              find: "      if (nested) continue;",
              replace: "      if (false) continue;" }],
  },
  {
    name: "artifact-applies-the-citation-count-asked-for",
    why: "accept minCitationsPerClaim and ignore it, so a claim carrying one citation satisfies a caller that asked for two. An argument read and not applied is worse than one that is absent: the caller believes the stricter minimum took effect and gets the default, with nothing anywhere reporting the difference",
    test: "test/artifact.test.mjs",
    expectRed: "a caller asking for two citations is not satisfied by one",
    edits: [{ file: "src/build/artifact.mjs",
              find: "      if (cites < minCites)",
              replace: "      if (cites < 1)" }],
  },
  {
    name: "artifact-done-condition-fence-must-close",
    why: "treat an UNTERMINATED block as a done condition. Half a block is not a block: the rest of the document is inside it, so what the slice actually asks for is whatever happens to follow, which no reader would guess from a gate that reported ok",
    test: "test/artifact.test.mjs",
    expectRed: "an unterminated done-condition fence is refused",
    edits: [{ file: "src/build/artifact.mjs",
              find: "            return null;",
              replace: "            return first;" }],
  },
  {
    name: "artifact-skip-reads-the-callers-flag",
    why: "key the skipped-phase refusal on expect.depth, which the documented helper does not supply -- so the one path that check exists for is unreachable from the caller that would take it. The branch looks present, is covered by a test that hand-builds a depth, and never runs in production",
    test: "test/artifact.test.mjs",
    expectRed: "a caller declaring the phase skipped is refused without carrying a depth",
    edits: [{ file: "src/build/artifact.mjs",
              find: "    const researchSkipped = \"skipped\" in expect ? expect.skipped : expect.depth === \"trivial\";",
              replace: "    const researchSkipped = expect.depth === \"trivial\";" }],
  },
  {
    name: "sizing-counts-are-whole-numbers",
    why: "check the sizing counts with Number.isFinite, so est_files: 0.5 satisfies this gate while build_size.json declares those fields as integers. One artifact then carries two verdicts -- durably approved here, refused there -- and the approving one is what lets the work proceed",
    test: "test/artifact.test.mjs",
    expectRed: "a fractional est_files is refused",
    edits: [{ file: "src/build/artifact.mjs",
              find: "        const count = (v) => Number.isInteger(v) && v >= 0;",
              replace: "        const count = (v) => Number.isFinite(v) && v >= 0;" }],
  },
  {
    name: "sizing-risk-paths-are-paths",
    why: "check that risk_paths_touched is an array without checking what is IN it, so [3] and [\"\"] pass. The sizing floor intersects that list against the profile's risk paths and a non-string matches nothing, so the artifact reads as touching no risk path -- and the floor that exists for exactly that case does not fire, silently, on the sizing that most needed it",
    test: "test/artifact.test.mjs",
    expectRed: "a risk path that is not a string is refused",
    edits: [{ file: "src/build/artifact.mjs",
              find: "            (v) => Array.isArray(v) && v.every((p) => typeof p === \"string\" && p.trim() !== \"\")],",
              replace: "            (v) => Array.isArray(v)]," }],
  },
  {
    name: "hub-hole-is-not-a-short-history",
    why: "report a holed history and a merely short one as the same fault. Both arrive as a non-empty `missing` and only one of them migrates: openHub refuses a hole outright, so a caller that cannot tell them apart sends an operator with a holed hub to a command that will refuse it, and the second refusal is the one naming the real repair",
    test: "test/migration-history.test.mjs",
    expectRed: "a hub missing a version BENEATH one it carries is reported as holed",
    edits: [{ file: "src/build/hubdb.mjs",
              find: "    return { readable: true, missing, have: present, holed: hasHistoryHole(present),",
              replace: "    return { readable: true, missing, have: present, holed: false," }],
  },
  {
    name: "task-file-dry-run-prescribes-the-remedy-that-works",
    why: "swap the two migration remedies, so each fault is sent to the repair that belongs to the other. The decision moved out of the --dry-run guard into `hubfault.mjs` when both callers were routed through it, and this entry followed it: the rule distinguishing a hole from a tail can be entirely correct while the route still reads past it. The rule distinguishing them can be entirely correct while the route still reads past it; this entry is the difference between the discrimination existing and the operator receiving it",
    test: "test/migration-history.test.mjs",
    expectRed: "the route tells an operator with a HOLED hub to restore a snapshot",
    edits: [{ file: "src/build/hubfault.mjs",
              find: "  return hist.holed",
              replace: "  return !hist.holed" }],
  },
  {
    name: "artifact-done-condition-fence-closer-matches-its-opener",
    why: "accept a closing delimiter shorter than the one that opened the block. Four backticks closed by three is NOT closed -- CommonMark requires the closer to be at least as long -- so the whole remainder of design.md is inside the fence while this gate reports a machine-checkable done condition",
    test: "test/artifact.test.mjs",
    expectRed: "a done condition opened with four backticks and closed with three is refused",
    edits: [{ file: "src/build/artifact.mjs",
              find: "              if (m && m[1].length >= open.length && m[2].trim() === \"\") {",
              replace: "              if (m && m[1].length >= 3 && m[2].trim() === \"\") {" }],
  },
  {
    name: "hub-recorded-version-is-not-a-loop-bound",
    why: "derive the hole by ENUMERATING from 1 to the recorded maximum. That maximum comes out of `schema_version`, the one table an operator is most likely to have edited by hand, so the loop's cost is chosen by the file rather than by this binary. Measured on node v24.17.0: a hub whose only row is version 1000000000 killed the process with a V8 heap OOM, so `reeve task file --dry-run` crashed on exactly the corrupt marker it exists to report. Counting answers the same question in one pass",
    test: "test/migration-history.test.mjs",
    expectRed: "missingMigrations returns on a hub recording a very large version, rather than exhausting memory",
    edits: [{ file: "src/build/hubdb.mjs",
              find: "  return have.size !== highest(have);",
              replace: "  { const top = highest(have); const gaps = []; for (let v = 1; v <= top; v++) if (!have.has(v)) gaps.push(v); return gaps.length > 0; }" }],
  },
  {
    name: "hub-gap-enumeration-refuses-an-untrusted-bound",
    why: "let `historyGaps` enumerate up to whatever bound it is handed. The bound has to be one this binary knows, because a store recorded above it is refused for being NEWER before its history is enumerated -- and that ordering in openHub and validateSnapshot is the only thing standing between an edited schema_version and an unbounded allocation. Without the refusal the ordering is a convention, and a convention is what a later edit reorders",
    test: "test/migration-history.test.mjs",
    expectRed: "historyGaps refuses to enumerate up to a version this binary does not know",
    edits: [{ file: "src/build/hubdb.mjs",
              find: "  if (!Number.isSafeInteger(upTo) || upTo < 0 || upTo > HUB_SCHEMA_VERSION)",
              replace: "  if (false)" }],
  },
  {
    name: "task-file-dry-run-refuses-a-newer-hub-unopened",
    why: "let a hub whose contiguous history is NEWER than this binary through the shared decision, which both the dry-run guard and the read routes now consult. It is missing none of what this binary expects and has no hole, so `missing` and `holed` both say nothing is wrong. The write that used to follow is now prevented by the read-only handle, so what this guard uniquely does is REFUSE: without it the command proceeds against a store written by a binary whose migrations it cannot interpret, and the operator is told nothing about why the answer is wrong",
    test: "test/migration-history.test.mjs",
    expectRed: "the route refuses a hub NEWER than this binary and names the remedy",
    edits: [{ file: "src/build/hubfault.mjs",
              find: "  if (hist.version > expect)",
              replace: "  if (false && hist.version > expect)" }],
  },
  {
    name: "artifact-nested-levels-close",
    why: "never close an open list level. A sibling appearing to the LEFT of an open level ends it; without that, every bullet after the first nested one is treated as nested and the citation rule stops running for the rest of the document -- an artifact whose second finding is uncited passes because its first finding had a sub-bullet",
    test: "test/artifact.test.mjs",
    expectRed: "a top-level bullet AFTER a nested one is top-level again",
    edits: [{ file: "src/build/artifact.mjs",
              find: "      while (open.length && indent < open[open.length - 1]) open.pop();",
              replace: "      while (false) open.pop();" }],
  },
  {
    name: "artifact-endpoint-with-a-path-is-not-a-citation",
    why: "leave a scheme-less `host:port/path` in the line. The URL strip only removes `scheme://...`, so `api.internal:3000/health` survived it and its PORT read as a line number -- the gate accepting precisely the unsupported claim it exists to reject. This one is separable from a file reference because a path follows the digits, which no file:line citation carries",
    test: "test/artifact.test.mjs",
    expectRed: "a host:port with a path is not a citation",
    edits: [{ file: "src/build/artifact.mjs",
              find: "  /\\b[\\w-]+(?:\\.[\\w-]+)+:\\d+\\/\\S*/g,          // host:port followed by a path",
              replace: "  // the host-with-path strip removed" }],
  },
  {
    name: "artifact-nesting-boundary-is-the-content-column",
    why: "close a level only at column zero, which is right for a list that starts at the left margin and wrong for every other case. The boundary is the enclosing item's CONTENT column: under `- ` it is 2, so one space is a sibling and two is nested; under `1.  ` it is 4, so two spaces is still a sibling. A rule written as a fixed number of spaces is correct for one marker width and silently wrong for the others",
    test: "test/artifact.test.mjs",
    expectRed: "a bullet indented one space under a `- ` parent is a SIBLING, and is still required to cite",
    edits: [{ file: "src/build/artifact.mjs",
              find: "      while (open.length && indent < open[open.length - 1]) open.pop();",
              replace: "      while (open.length && indent === 0) open.pop();" }],
  },
  {
    name: "artifact-empty-fenced-block-is-not-a-command",
    why: "return the first line of an EMPTY block as its command. An opener and an immediate closer contain nothing, and the delimiters after them are a NEW block -- so a document whose done condition is an empty block followed by prose and one more delimiter was reported as carrying a machine-checkable command, over a block with no command in it and a final delimiter left opening a block that never closes",
    test: "test/artifact.test.mjs",
    expectRed: "an empty fenced block is not a done condition, whatever follows it",
    edits: [{ file: "src/build/artifact.mjs",
              find: "                if (first !== null) return first;",
              replace: "                return first ?? \"\";" }],
  },
  {
    name: "artifact-sync-chain-stops-at-its-anchor",
    why: "ignore the anchor and walk the sync chain to the filesystem root. Every ancestor of the reeve home belongs to the operator or the OS and this write changes none of their entries -- but the parent-sync loop opens and fsyncs each one, so a single execute-only directory anywhere above the home makes it RETHROW and a successfully written and renamed artifact is reported as failed. The previous bound was a count of eight, which is the opposite mistake: it stopped syncing a deeper tree that really did need it",
    test: "test/artifact.test.mjs",
    expectRed: "a write anchored at the home succeeds with an unopenable directory above it",
    edits: [{ file: "src/build/artifact.mjs",
              find: "    for (let d = start; ; d = dirname(d)) { chain.push(d); if (d === top || d === dirname(d)) break; }",
              replace: "    for (let d = start; ; d = dirname(d)) { chain.push(d); if (d === dirname(d)) break; }" }],
  },
  {
    name: "artifact-a-list-ends-at-a-new-block",
    why: "never close the open list levels. `open` described the list being read and nothing ended it, so a cited list, a blank line, a paragraph and then a NEW list had its first item measured against the OLD list's content column and skipped as nested. The second list's uncited claim disappeared from the count and the artifact passed -- the further apart the two lists, the more certainly the rule stopped applying",
    test: "test/artifact.test.mjs",
    expectRed: "a new list after a paragraph is top-level again, however the previous one was indented",
    edits: [{ file: "src/build/artifact.mjs",
              find: "        if (blank && (open.length === 0 || indent < open[open.length - 1])) open.length = 0;",
              replace: "        if (false) open.length = 0;" }],
  },
  {
    name: "task-file-dry-run-opens-the-hub-read-only",
    why: "open a healthy, current hub through the WRITABLE opener during a dry run. The version checks stop it MIGRATING, which was the irreversible write, and not from writing at all: openHub's first act is PRAGMA journal_mode = WAL, which rewrites the header of a database in DELETE mode -- and a hub restored from a snapshot is in delete mode, because VACUUM INTO writes it that way. So the ordinary case, a fully migrated hub with nothing wrong with it, was the one still being modified by the command whose whole promise is that it modifies nothing",
    test: "test/migration-history.test.mjs",
    expectRed: "and does not write to it -- the version checks stopped the migration, not the pragma",
    edits: [{ file: "bin/reeve",
              find: "        db = new DatabaseSync(hubPath, { readOnly: true, timeout: HUB_BUSY_TIMEOUT_MS });",
              replace: "        db = openHub(hubPath);" }],
  },
  {
    name: "artifact-a-fence-is-indented-at-most-three-spaces",
    why: "accept a fence at any indent. Four or more leading spaces makes the line an indented CODE BLOCK, so a `Done when:` written that way renders as literal text and contains no fenced block at all -- and the gate approved it as a machine-checkable command. The limit is CommonMark's rather than one chosen to stop a loop, and a tab is worth four, so neither is admitted",
    test: "test/artifact.test.mjs",
    expectRed: "a done condition indented four spaces is an indented code block, not a fence",
    edits: [{ file: "src/build/artifact.mjs",
              find: "              const m = /^ {0,3}(`{3,})([^`\\n]*)$/.exec(line);",
              replace: "              const m = /^[ \\t]*(`{3,})([^`\\n]*)$/.exec(line);" }],
  },
  {
    name: "artifact-an-ambiguous-research-expectation-is-refused",
    why: "assume an expectation that says nothing means NOT skipped. This branch has been unreachable twice -- first keyed on expect.depth, which the documented helper does not carry, then on expect.skipped, which S3-D's researchExpectations does not carry either. Both times it fell through to false and the gate would review a research artifact for a phase that should not have run. The two errors are not symmetric: assuming not-skipped gates a phase silently, and refusing to guess names the field the helper must carry",
    test: "test/artifact.test.mjs",
    expectRed: "an expectation carrying neither skipped nor depth is refused, naming what it must carry",
    edits: [{ file: "src/build/artifact.mjs",
              find: "    if (!(\"skipped\" in expect) && !(\"depth\" in expect))",
              replace: "    if (false)" }],
  },
  {
    name: "hub-a-refused-marker-does-not-leak-the-handle",
    why: "leave the SQLite handle open when the gap rule refuses the marker itself. schema_version is an INTEGER PRIMARY KEY, so a hand-edited marker may hold a negative number, which reaches historyGaps' refusal rather than the hole branch where the close lives. A long-running process that catches and retries then leaks one descriptor per attempt until unrelated file operations start failing -- a failure nobody would trace back to a corrupt version marker",
    test: "test/migration-history.test.mjs",
    expectRed: "a refused marker does not leak a descriptor per attempt",
    edits: [{ file: "src/build/hubdb.mjs",
              find: "    catch (e) { closeHub(); throw e; }",
              replace: "    catch (e) { throw e; }" }],
  },
  {
    name: "artifact-anchor-is-resolved-on-the-filesystem",
    why: "compare the anchor lexically. `resolve` only normalises text, so a SYMLINK beneath the anchor satisfies startsWith while the write follows the link -- mkdir, the temporary and the rename all landing outside the task tree, where the rename replaces whatever artifact it finds. TWO EDITS, because BOTH sides have to be resolved the same way: stubbing one alone makes the comparison disagree with itself and every write is refused, which is a different defect and kills the file rather than exhibiting this one",
    test: "test/artifact.test.mjs",
    expectRed: "a symlinked component beneath the anchor is refused, not followed",
    edits: [{ file: "src/build/artifact.mjs",
              find: "    const top = realOf(anchor);",
              replace: "    const top = resolve(anchor);" },
            { file: "src/build/artifact.mjs",
              find: "    const start = realOf(dir);",
              replace: "    const start = resolve(dir);" }],
  },
  {
    name: "hub-a-marker-that-is-not-a-version-is-reported",
    why: "filter an invalid recorded version away instead of reporting it. schema_version is an INTEGER PRIMARY KEY, so -1 is valid SQLite: every expected version then reads as missing and nothing reads as a hole, so the caller says `migrate` -- and openHub refuses that same store, because historyGaps will not take a negative bound. The advertised remedy could not work, and the fault was invisible to every other field in the answer",
    test: "test/migration-history.test.mjs",
    expectRed: "a marker that is not a version is named as the fault",
    edits: [{ file: "src/build/hubdb.mjs",
              find: "    const invalid = all.filter((v) => !Number.isSafeInteger(v) || v < 1).sort((a, b) => a - b);",
              replace: "    const invalid = [];" }],
  },
  {
    name: "task-file-dry-run-checks-the-schema-shape",
    why: "hand the raw handle to fileTask on the strength of the version history alone. The history says which migrations RAN; it does not say the tables they created are still there. The same hand-repaired store these checks exist for can carry a complete history over a dropped territory_lease, and liveLeases then throws a bare `no such table` from inside a command that had already reported the hub healthy",
    test: "test/migration-history.test.mjs",
    expectRed: "a hub whose tables do not match its recorded version is refused before any application query",
    edits: [{ file: "bin/reeve",
              find: "        const defects = schemaDefectsAt(db, hist.version);",
              replace: "        const defects = [];" }],
  },
  {
    name: "artifact-tab-stops-are-counted-from-the-marker",
    why: "count every tab as four columns wherever it appears. Markdown advances a tab to the next multiple-of-four column, so the tab in `-\\tparent` -- sitting at column 1, after the marker -- reaches column 4 and not 1 + 4. Counting the marker and then adding four recorded a content column of 5, and a child indented four spaces, an ordinary nested elaboration, fell to the LEFT of its own parent: read as a top-level claim and refused for carrying no citation of its own",
    test: "test/artifact.test.mjs",
    expectRed: "a tab after the marker reaches column 4, so a four-space child is nested under it",
    edits: [{ file: "src/build/artifact.mjs",
              find: "      for (const ch of s) col = ch === \"\\t\" ? col + 4 - (col % 4) : col + 1;",
              replace: "      for (const ch of s) col = ch === \"\\t\" ? col + 4 : col + 1;" }],
  },
  {
    name: "artifact-done-scan-stops-at-the-next-field",
    why: "bound the done-condition scan at the next HEADING while the presence check beside it stops at the next heading OR the next label. Two answers to where a field ends, and they disagree exactly where it matters: a slice whose `Done when:` holds only prose, followed by a `Tests:` carrying a fenced command, has the TEST field's block read as its done condition -- `Done when: someone approves` reported as machine-checkable over a command belonging to another field",
    test: "test/artifact.test.mjs",
    expectRed: "a fenced command under a LATER field is not this field's done condition",
    edits: [{ file: "src/build/artifact.mjs",
              find: "            if (j > from && isBoundary(rs[j])) return chars;",
              replace: "            if (j > from && /^#{2,3}\\s+/.test(rs[j])) return chars;" }],
  },
  {
    name: "task-file-dry-run-checks-the-file-not-just-the-catalogue",
    why: "emit a plan over a hub damaged in a data or index page. The history says the migrations ran and `schemaDefectsAt` says the tables are there; neither reads a PAGE. `openHub` runs `quick_check(1)` for that reason, and the dry run -- which cannot go through `openHub`, whose first act is a write -- took the read-only route around the check as well as around the write. The damage then surfaced later out of `liveLeases`, as a raw `database disk image is malformed` from a command that had already called the hub healthy",
    test: "test/migration-history.test.mjs",
    expectRed: "a hub damaged in a page is refused by the dry run, on the integrity check openHub would have run",
    edits: [{ file: "bin/reeve",
              find: "        const damaged = hubIntegrityError(db, hubPath);",
              replace: "        const damaged = hubIntegrityError(db, hubPath) && null;" }],
  },
  {
    name: "migration-state-reads-the-connection-it-is-handed",
    why: "read the history from a store other than the caller's connection. The route has to read the history and then read the tables as ONE view of ONE store: read through a connection of its own, closed before the next one opens, the version check answers about a hub that a newer reeve may have migrated in between -- and a migration that only ADDS a table is invisible to the shape check that follows, so the binary emits a plan from exactly the schema it promised to refuse",
    test: "test/migration-history.test.mjs",
    expectRed: "and it still reads it once the path is gone, because it reads the CONNECTION",
    edits: [{ file: "src/build/hubdb.mjs",
              find: "    const have = new Set(db.prepare(\"SELECT version FROM schema_version\").all().map((r) => r.version));",
              replace: "    const have = new Set(new DatabaseSync(\":memory:\").prepare(\"SELECT version FROM schema_version\").all().map((r) => r.version));" }],
  },
  {
    name: "canary-page-is-the-guardians-identity",
    why: "raise the BUILDER's identity from the guardian daemon. §4.4 splits this escalation by raising process and §11.7 forbids either daemon from writing the other's, because `announceable` runs in the process that owns the store it reads -- so a builder row in the guardian's per-repo store is one that no process which owns it can see, announce or retire. Every other assertion in the suite imports the constant and therefore FOLLOWS a rename, which is why one of them spells the name out",
    test: "test/canary-page.test.mjs",
    expectRed: "the identity is the GUARDIAN's",
    edits: [{ file: "src/daemon.mjs",
              find: "export const CANARY_PAGE = \"guardian:sandbox:canary-failed\";",
              replace: "export const CANARY_PAGE = \"builder:sandbox:canary-failed\";" }],
  },
  {
    name: "canary-page-is-raised-at-all",
    why: "read no canary out of the containment verdict, which is the state this change found. The founder's page list names three identities that may interrupt a human; no site minted this one, so the one condition under which NOTHING may dispatch reached the daily digest and never a phone",
    test: "test/canary-page.test.mjs",
    expectRed: "a canary that ran and FAILED raises the page identity",
    edits: [{ file: "src/daemon.mjs",
              find: "  const canary = containment?.canary ?? null;",
              replace: "  const canary = null;" }],
  },
  {
    name: "canary-page-key-carries-no-subject",
    why: "append the canary id to the key. §11.7 says the key is the bare identity and the report rides in the alert body; there is no body field yet, so the detail lives in the log. A key carrying the id MOVES whenever the CLI, the binary or the policy changes, and `announceable` retires a key that moves and raises the new one -- two pushes for one unchanged fault, which is how a channel earns being muted",
    test: "test/canary-page.test.mjs",
    expectRed: "the KEY is the bare identity",
    edits: [{ file: "src/daemon.mjs",
              find: "raise(CANARY_PAGE);\n    log(logPath, `containment: canary",
              replace: "raise(`${CANARY_PAGE}: ${canary.id}`);\n    log(logPath, `containment: canary" }],
  },
  {
    name: "canary-page-stands-across-a-quiet-tick",
    why: "stop re-raising the page the store already carries. Containment is measured only when a worker task wants dispatching, so a quiet tick runs no canary -- and `announceable` reads an escalation absent from a complete tick as resolved, so the next quiet tick announces CLEARED for a sandbox nobody re-measured",
    test: "test/canary-page.test.mjs",
    expectRed: "the page still stands on a second tick",
    edits: [{ file: "src/daemon.mjs",
              find: "  } else if (canaryPageStands(db)) {",
              replace: "  } else if (false) {" }],
  },
  {
    name: "canary-page-survives-a-restart",
    why: "answer the standing-page query with nothing, which is what a latch kept in this process's memory amounts to across a restart. `bin/reeve` builds a fresh context per process, so a daemon restart or a one-shot `reeve tick` arrives with an empty latch while the escalation ROW is still in the store -- and the next complete tick finds that row standing and absent and retires it. A restart would announce that a sandbox nobody re-measured is fine",
    test: "test/canary-page.test.mjs",
    expectRed: "the page survives a RESTART",
    edits: [{ file: "src/daemon.mjs",
              find: "SELECT 1 FROM escalation WHERE why = ?",
              replace: "SELECT 1 FROM escalation WHERE why = ? AND 0" }],
  },
  {
    name: "canary-page-retires-on-a-positive-measurement",
    why: "leave retirement to `announceable`, which infers repair from ABSENCE and refuses to infer it on a tick that did not finish looking -- `complete` is false whenever one pull request could not be evaluated. The canary is a repo-wide measurement that depends on no pull request, so one unreadable PR on the tick that PROVED the sandbox healthy leaves the row standing, the next quiet tick re-raises it from the store, and the page never clears again",
    test: "test/canary-page.test.mjs",
    expectRed: "retires the page even on a tick that did not finish looking",
    edits: [{ file: "src/daemon.mjs",
              find: "    if (retireCanaryPage(db)) log(logPath,",
              replace: "    if (false) log(logPath," }],
  },
  {
    name: "canary-page-does-not-re-raise-after-a-pass",
    why: "fall through to the standing-page branch on a tick whose canary RAN and passed, so the page is re-raised immediately after the measurement that disproved it. The page then becomes a latch that only a restart can silence, and an operator who is never told a fault is over cannot tell `resolved` from `reeve stopped looking`",
    test: "test/canary-page.test.mjs",
    expectRed: "a canary that passes afterwards clears the page",
    edits: [{ file: "src/daemon.mjs",
              find: "  } else if (canaryRan) {",
              replace: "  } else if (false) {" }],
  },
  {
    name: "canary-page-ignores-a-skipped-canary",
    why: "count a SKIPPED canary as one that ran. `skipped` means containment was already open for a cheaper reason so the canary never ran at all, and paging the sandbox identity for it sends the founder to the policy when the fault is a credential in the login keychain",
    test: "test/canary-page.test.mjs",
    expectRed: "a SKIPPED canary raises no page",
    edits: [{ file: "src/daemon.mjs",
              find: "  const canaryRan = Boolean(canary && !canary.skipped && canary.id != null);",
              replace: "  const canaryRan = Boolean(canary && canary.id != null);" }],
  },
  {
    name: "canary-page-needs-a-canary-that-ran",
    why: "count a canary with no id as one that ran. A null id means no CLI version or sandbox block existed to run one under -- a configuration fault, reported by the generic containment identity -- and calling it a failed canary claims the sandbox stopped containing a worker when nothing measured whether it does",
    test: "test/canary-page.test.mjs",
    expectRed: "a canary with no id raises no page",
    edits: [{ file: "src/daemon.mjs",
              find: "  const canaryRan = Boolean(canary && !canary.skipped && canary.id != null);",
              replace: "  const canaryRan = Boolean(canary && !canary.skipped);" }],
  },
  {
    name: "canary-failure-reason-reaches-the-log",
    why: "drop the canary's own reason from the log line. The reason is deliberately NOT in the key -- §11.7 wants the bare identity there -- so until an alert body exists the log is the only place it survives. Without it the page says a canary failed and nothing anywhere says why",
    test: "test/canary-page.test.mjs",
    expectRed: "the REASON is in the log rather than in the key",
    edits: [{ file: "src/daemon.mjs",
              find: "${canary.why ?? \"no reason recorded\"}; nothing may dispatch until one passes`);",
              replace: "no reason recorded; nothing may dispatch until one passes`);" }],
  },
  {
    name: "escalation-keys-are-declared-identities",
    why: "let `escalationKey` mint any well-formed key. A key matching no declared shape is a situation nobody wrote down: the page list cannot decide about it, no announcer can name it and nothing can clear it, so the escalation is raised into a namespace nothing reads. Not hypothetical -- `transition.mjs`'s `blocked_other` branch takes an escalation key straight from its caller and writes it to the table with no check at all, which is the hole this closed set exists to close",
    test: "test/build-escalations.test.mjs",
    expectRed: "and a well-formed key nothing declares, which would be raised where nothing reads",
    edits: [{
      file: "src/build/announce.mjs",
      find: "  if (shapeOf(key) === null)",
      replace: "  if (key === \"\")",
    }],
  },

  {
    name: "an-escalation-absent-from-a-blind-pass-is-not-retired",
    why: "retire a standing cause the moment a pass does not produce it. A pass that could not examine a task produces no escalation for it, so retiring on that silence announces RESOLVED for something nobody looked at -- and re-announces it on the next pass that does look, with the reason string identical each time. The guardian paid for this in production rather than reasoning its way to it: on nextly#834 a cause was retired and re-announced twice, four and twenty-five minutes apart, because seven waiting ticks each produced no escalation. Two pushes for one unchanged condition is how a channel earns being muted, and a muted channel is worse than none",
    test: "test/build-escalations.test.mjs",
    expectRed: "a cause absent from a pass that did NOT examine its task is not retired",
    edits: [{
      file: "src/build/announce.mjs",
      find: "      if (subject === null || examined === null || !examined.has(subject)) continue;",
      replace: "      if (false) continue;",
    }],
  },
  {
    name: "the-builder-announcer-refuses-the-guardians-store",
    why: "announce into whatever store the caller passes. The two processes share exactly three tables -- escalation, inbox and outbox -- so the guardian's store carries an escalation table of the SAME shape: handed one, the builder's announcer writes builder identities into the guardian's escalations, the write succeeds, and nothing anywhere reports that it happened. The guardian half of this symmetry is already structural, because the guest allowlist admits only provider_lease, provider_state, pr_hold and maintenance_lock; the builder half is refused by nothing until this check exists",
    test: "test/build-escalations.test.mjs",
    expectRed: "the builder's announcer refuses the guardian's store, which has an escalation table of the same shape and would have taken the write",
    edits: [{
      file: "src/build/announce.mjs",
      find: "  assertHub(db);\n  // EXPLICIT, never defaulted.",
      replace: "  // EXPLICIT, never defaulted.",
    }],
  },

  {
    name: "a-page-matches-by-shape-not-by-literal-key",
    why: "compare a concrete escalation key against the page list directly. The list is written in SHAPES, so a literal comparison pages for the one identity that carries no task id and matches no blocked task at all -- every blocked task then goes to the digest in silence, which is the fail-quiet outcome the closed page list exists to prevent. A startsWith is the same defect from the other side: it pages for a key missing its phase. Both are second statements of `which identity is this`, and shapeOf is the first",
    test: "test/build-escalations.test.mjs",
    expectRed: "the templated entry pages for any task and any phase",
    edits: [{
      file: "src/build/announce.mjs",
      find: "export const pages = (key) => PAGES.includes(shapeOf(key));",
      replace: "export const pages = (key) => PAGES.includes(key);",
    }],
  },
  {
    name: "a-page-that-was-not-delivered-is-not-reported-as-paged",
    why: "treat every attempted send as a page. Whether a human was actually reached is the one thing this cannot infer, and reporting a phone call that never happened is worse than reporting none: the escalation still stands in the store, so the operator's own record says somebody was told. A sender that returns ok:false and one that throws are the same fact to a reader who needs to know nobody was reached, and both must land in `declined` rather than in `paged` or in nothing",
    test: "test/build-escalations.test.mjs",
    expectRed: "a refused send is not reported as paged",
    edits: [{
      file: "src/build/announce.mjs",
      find: "    if (result?.ok || accepted.length)",
      replace: "    if (true)",
    }],
  },

  {
    name: "a-channel-reports-a-reference-or-a-reason-there-is-none",
    why: "let an absent delivery reference be `undefined`. notify.mjs has promised since it was written that nothing declines silently, and a caller could not tell `delivered, this channel issues no id` from `the field was never populated` -- both read as absent, and the promise had never been checked because every path needed a real escalation and a real server. Folding the reason into `why` is the same defect wearing a value: `why` already means THIS CHANNEL FAILED, so one field answering both questions makes `ok` the only way to know which it answered, and a caller logging `why` reports a delivery failure that never happened",
    test: "test/build-escalations.test.mjs",
    expectRed: "and the absent reference is null WITH a reason, never undefined",
    edits: [{
      file: "src/notify.mjs",
      find: "  const referenced = channels.map(c => (typeof c.ref === \"string\" && c.ref !== \"\")",
      replace: "  const referenced = channels; const _unused = ((c) => (typeof c.ref === \"string\" && c.ref !== \"\")",
    }],
  },

  {
    name: "a-page-is-marked-announced-only-after-it-is-delivered",
    why: "write announced_count at the moment the cause is raised. The row then says it has been announced before anything was sent, so a page the sender refused is never offered again: the next pass compares count against announced_count, finds them equal, produces no fresh item, and the operator is never told. The refusal is visible in `declined`, and a returned array scrolls past — the durable row is the only thing that survives the pass, and it was lying",
    test: "test/build-escalations.test.mjs",
    expectRed: "the row is NOT marked announced, because nothing was announced",
    edits: [{
      file: "src/build/announce.mjs",
      find: "                    VALUES(?,?,?,?,0,?)`).run(why, count, at, at, serialiseBody(offeredBody(bodies, why)));",
      replace: "                    VALUES(?,?,?,?,?,?)`).run(why, count, at, at, count, serialiseBody(offeredBody(bodies, why)));",
    }],
  },
  {
    name: "the-announcers-mutations-are-replayable",
    why: "clear an escalation without appending an event. `escalation` is in the replayed set and `escalation.raised` was its only handler, so a restore whose tail spans a clear replays the raise and resurrects the row — and pages the founder again about something that was resolved before the restore. The durable store is what a restore reconstructs from, so a mutation with no event is a mutation the system forgets while believing it remembers",
    test: "test/build-escalations.test.mjs",
    expectRed: "and clearing one appends escalation.cleared",
    edits: [{
      file: "src/build/announce.mjs",
      find: "  hubEvent(db, { kind: \"escalation.cleared\",\n                 task: /^bt:/.test(why) ? subjectOf(why) : null, payload: { why } });",
      replace: "",
    }],
  },
  {
    name: "only-a-real-phase-reduces-to-a-phase",
    why: "reduce any uppercase trailing component to <phase>. A detail component then masquerades as a phase: a key ending in a shouted word matches the declared blocked shape and therefore PAGES, and that is reachable today through `blocked_other`, which writes a caller-supplied escalation key with no validation at all. Membership of PHASES is the whole difference between a phase and a word that is shouting, and the closed page list is only closed if the reduction into it is",
    test: "test/build-escalations.test.mjs",
    expectRed: "bt:7:phase:blocked:DETAIL reduces to nothing: DETAIL is not a phase",
    edits: [{
      file: "src/build/announce.mjs",
      find: "  const shape = tail && PHASES.includes(tail[1])",
      replace: "  const shape = tail",
    }],
  },
  {
    name: "notify-test-refuses-another-repositorys-profile",
    why: "send the test through whatever profile `loadProfile` returns. It prefers ./.ops/profile.json over the sidecar and never checks whose it is, so `notify --test owner/B` run inside repository A sends through A's channels while titling the alert B — reporting B's notification setup healthy without having touched it, and putting an unexpected real alert on somebody's phone. A command whose entire purpose is to tell an operator the truth about a channel is the last one that may guess which channel it is talking about",
    test: "test/build-escalations.test.mjs",
    expectRed: "asking about another repository from inside this one is refused, not answered",
    edits: [{
      file: "bin/reeve",
      find: "    const declared = profile?.identity?.key;\n    if (declared && declared !== nwo)",
      replace: "    const declared = null;\n    if (declared && declared !== nwo)",
    }],
  },

  {
    name: "a-clearing-waits-for-its-notification",
    why: "delete a paged cause's row before its recovery has been delivered. The raise path was already taught to hold `announced_count` until a send landed; the clear path destroyed the only durable record that a clearing was owed, so a channel down for a single pass lost the recovery permanently — the next pass had no standing cause left to classify as cleared. An operator who is only ever told about problems cannot tell resolved from stopped-looking, which is the whole reason clearing is announced at all",
    test: "test/build-escalations.test.mjs",
    expectRed: "control: and the row still stands, because nothing has been told yet",
    edits: [{
      file: "src/build/announce.mjs",
      find: "      if (pages(why) && standing.get(why).announced_count > 0) {",
      replace: "      if (false) {",
    }],
  },
  {
    name: "an-alert-is-sanitised-before-it-leaves-the-machine",
    why: "assemble the outbound message without `redact`. That is the only place the whole alert is cut to what a phone shows, so a 2,000-character CI error goes out whole. It is also a second layer against secret shapes, but the body's credentials are scrubbed before it is stored (a-credential-never-becomes-durable), which is why this entry names the cap and not the credential check. `buildAlert` applies it and `notify` itself does not, so a second producer of alerts is a second place it has to be applied",
    test: "test/build-escalations.test.mjs",
    expectRed: "a message is capped before it leaves, however long its body",
    edits: [{
      file: "src/build/announce.mjs",
      find: "    const message = redact(raw);",
      replace: "    const message = raw;",
    }],
  },
  {
    name: "every-alert-names-the-action-that-changes-it",
    why: "send a page carrying only the identity. The design requires every alert to name the single founder action needed, and a page read at night without the one command that changes it is a notification the reader can only file away — a channel whose alerts cannot be acted on is one that gets muted, which is the outcome the closed page list exists to avoid",
    test: "test/build-escalations.test.mjs",
    expectRed: "and the rendered alert carries it, which is the only place a phone shows it",
    edits: [{
      file: "src/build/announce.mjs",
      find: "        `${action ? `\\n-> ${clean(action)}` : \"\"}${detail}`;",
      replace: "        `${detail}`;",
    }],
  },
  {
    name: "notify-test-refuses-a-malformed-repository",
    why: "scan the positionals for the first token that looks like a repository and discard the rest. A typo then matches nothing, falls through to detectNwo() and sends the test through whatever checkout the operator is standing in — and a second repository argument is dropped in silence. For the one route here that SENDS, a mistyped repository quietly becoming 'the current one' is the same defect as the profile mismatch, arriving through the argument instead of through the lookup",
    test: "test/build-escalations.test.mjs",
    expectRed: "notify --test owner/repo/ is refused rather than falling back to this checkout",
    edits: [{
      file: "bin/reeve",
      find: "    if (stray.length || positionals.length > 1)",
      replace: "    if (false)",
    }],
  },

  {
    name: "the-triage-command-names-the-actual-task",
    why: "render the identity shape's action with its `<id>` placeholder intact. The alert then hands the founder a command they cannot paste — under a shell `<id>` is input redirection — and makes them reconstruct by hand the identifier the alert is already holding. An action that has to be edited before it can be run is not the single action the design requires; it is a hint",
    test: "test/build-escalations.test.mjs",
    expectRed: "and no placeholder survives into something a founder is asked to run",
    edits: [{
      file: "src/build/announce.mjs",
      find: "  return subject === null ? action : action.replaceAll(\"<id>\", subject);",
      replace: "  return action;",
    }],
  },
  {
    name: "a-page-is-sent-as-an-interruption",
    why: "send a page with no priority. `notify` falls back to \"default\" for an alert that names none, so the closed page list — the whole point of which is that these few identities interrupt a human — would deliver ordinary notifications indistinguishable from anything else. `buildAlert`, the existing escalation producer, sets \"high\" explicitly for exactly this reason",
    test: "test/build-escalations.test.mjs",
    expectRed: "a raised page names high priority, or `notify` falls back to default and the page list promises an interruption it does not deliver",
    edits: [{
      file: "src/build/announce.mjs",
      find: "        priority: isCleared ? \"default\" : \"high\",",
      replace: "        priority: undefined,",
    }],
  },
  {
    name: "one-dead-channel-does-not-repage-the-live-one",
    why: "treat `notify`'s aggregate ok:false as undelivered. It reports failure when ANY configured channel fails, while the healthy channel has already delivered — so the cause is never marked announced and the working channel is paged again on every pass until the broken one recovers. That is precisely the alert fatigue the closed page list exists to prevent, produced by the deduplication that exists to prevent it. Nothing accepted is still a refusal; the failed channel is not hidden, it rides on `channels`",
    test: "test/build-escalations.test.mjs",
    expectRed: "a page one channel accepted is paged, not declined",
    edits: [{
      file: "src/build/announce.mjs",
      find: "    const accepted = (result?.channels ?? []).filter(c => c.ok);",
      replace: "    const accepted = [];",
    }],
  },

  {
    name: "an-alerts-own-line-breaks-are-real",
    why: "sanitise the FINISHED message rather than its parts. `printable` escapes every control character and a line feed is one, so this surface's own separators become a literal backslash-n and the identity, the action and the detail arrive as a single run-on line with visible escape sequences. The boundary added to protect the alert destroys the layout the action line depends on, and an assertion that the action is PRESENT cannot see the difference — only one about the STRUCTURE can. Sanitising the parts keeps the property that mattered: a newline inside a body is still escaped and cannot forge a line",
    test: "test/build-escalations.test.mjs",
    expectRed: "the message is rendered as separate lines, not one run-on string",
    edits: [{
      file: "src/build/announce.mjs",
      find: "    const message = redact(raw);",
      replace: "    const message = redact(printable(raw));",
    }],
  },
  {
    name: "no-recovery-notice-for-an-alarm-nobody-heard",
    why: "queue a clearing for a cause whose page every channel declined. Its `announced_count` is still zero, so the CLEARED notice tells the reader that a situation they were never informed of has ended — which is worse than silence, because it sends them looking for an alert that does not exist. The cause must still be RETIRED, or an undelivered page stands for ever precisely because it was never delivered",
    test: "test/build-escalations.test.mjs",
    expectRed: "the cause is retired without a recovery notice, because none was owed",
    edits: [{
      file: "src/build/announce.mjs",
      find: "      if (pages(why) && standing.get(why).announced_count > 0) {",
      replace: "      if (pages(why)) {",
    }],
  },

  {
    name: "an-untrusted-part-cannot-forge-a-line",
    why: "assemble the message from raw parts. `clean` is `printable` applied to each untrusted value BEFORE the trusted separators are added, and it is what stops a body from forging a line: a detail containing a line feed would otherwise render as its own line, indistinguishable from the identity or the action this surface writes. It is the half of the boundary that survived splitting the sanitisation, and it is asserted separately from the redaction half because a stub that removes one leaves the other standing",
    test: "test/build-escalations.test.mjs",
    expectRed: "a newline inside the body is escaped, so a detail cannot forge a line",
    edits: [{
      file: "src/build/announce.mjs",
      find: "    const clean = (v) => printable(String(v ?? \"\"));",
      replace: "    const clean = (v) => String(v ?? \"\");",
    }],
  },
  {
    name: "incarnation-is-minted-on-every-hub",
    why: "create the table and leave it empty. A NULL incarnation has to mean `cannot tell`, and a reader that treats that as a match accepts every stale cursor while one that treats it as a difference rejects every good one -- neither is better than the ambiguity this table replaces. The migration mints, so no hub is ever without one",
    test: "test/hub-incarnation.test.mjs",
    expectRed: "a freshly created hub carries an incarnation",
    edits: [{ file: "src/build/hubdb.mjs",
              find: "      mintIncarnation(db);\n",
              replace: "" }],
  },
  {
    name: "incarnation-row-is-unique",
    why: "drop the single-row constraint, so a second incarnation is storable. A table that merely HAPPENS to hold one row is one INSERT away from two answers to a question that must have exactly one, and the reader takes whichever the query returns first",
    test: "test/hub-incarnation.test.mjs",
    expectRed: "a second incarnation row is REFUSED",
    edits: [{ file: "src/build/hubdb.mjs",
              find: "          only       INTEGER PRIMARY KEY CHECK (only = 1),",
              replace: "          only       INTEGER PRIMARY KEY," }],
  },
  {
    name: "incarnation-refuses-a-malformed-id",
    why: "drop the length CHECK. The id is the whole comparison, and a truncated or empty one compares equal to another truncated or empty one -- so two different incarnations would prove themselves the same log",
    test: "test/hub-incarnation.test.mjs",
    expectRed: "an id that is not 128 bits of hex is refused",
    edits: [{ file: "src/build/hubdb.mjs",
              find: "          id         TEXT    NOT NULL CHECK (length(id) = 32 AND id NOT GLOB '*[^0-9a-f]*'),",
              replace: "          id         TEXT    NOT NULL," }],
  },
  {
    name: "incarnation-mint-replaces-rather-than-appends",
    why: "mint with a plain INSERT. It throws on the existing row, so a restore fails outright -- or, written as an UPDATE that matches nothing, changes nothing and reports success, which means a restored hub keeps the identity of the incarnation it replaced. `INSERT OR REPLACE` is the only form that is correct in both directions",
    test: "test/hub-incarnation.test.mjs",
    expectRed: "and the store now answers with the new one, not the old",
    edits: [{ file: "src/build/hubdb.mjs",
              find: "  db.prepare(\"INSERT OR REPLACE INTO hub_incarnation(only, id, started_at) VALUES(1, ?, ?)\")",
              replace: "  db.prepare(\"INSERT OR IGNORE INTO hub_incarnation(only, id, started_at) VALUES(1, ?, ?)\")" }],
  },
  {
    name: "a-restore-begins-a-new-incarnation",
    why: "stop re-minting after a restore, which is the state before this change and the whole defect. The staging database is a COPY OF THE SNAPSHOT, so it carries the incarnation the hub had when that snapshot was taken -- the same id a reader's cursor carries, because nothing between those two moments changes it. The restored store then hands back an id that MATCHES, a matching id is exactly the proof the cursor is asked for, and every event of the new incarnation through that sequence is skipped for ever and reported as a quiet period",
    test: "test/hub-incarnation.test.mjs",
    expectRed: "a restore begins a NEW incarnation",
    edits: [{ file: "src/backup.mjs",
              find: "        mintIncarnation(back);",
              replace: "        void mintIncarnation;" }],
  },
  {
    name: "incarnation-id-must-be-hex",
    why: "check the LENGTH and not the alphabet, which accepts \"z\" repeated 32 times. The writer in this module is not the only way into the table -- an import, a hand repair or a direct statement reaches it too -- and the id is the whole proof a cursor carries, so a value no hexadecimal parser accepts leaves the hub unable to issue one",
    test: "test/hub-incarnation.test.mjs",
    expectRed: "32 characters that are not hex are refused",
    edits: [{ file: "src/build/hubdb.mjs",
              find: "          id         TEXT    NOT NULL CHECK (length(id) = 32 AND id NOT GLOB '*[^0-9a-f]*'),",
              replace: "          id         TEXT    NOT NULL CHECK (length(id) = 32)," }],
  },
  {
    name: "incarnation-read-answers-null-before-the-table-exists",
    why: "let the missing table throw instead of answering null. This function promises null for a hub that predates migration 6 and a caller is told to read that as `cannot prove` -- but on such a hub the prepare throws, so the compatibility path the documentation describes was unreachable. A promise a caller cannot rely on is worse than no promise, because the caller writes no handling for the case that actually occurs",
    test: "test/hub-incarnation.test.mjs",
    expectRed: "reading a hub that predates the table does not throw",
    edits: [{ file: "src/build/hubdb.mjs",
              find: "    if (!/no such table/i.test(e?.message ?? \"\")) throw e;",
              replace: "    throw e;" }],
  },
  {
    name: "both-damage-refusals-name-the-snapshot",
    why: "let the pragma-damage refusal give generic advice again. There are TWO damage refusals in `openHub` -- one when a pragma WRITE hits a damaged page, one when `quick_check` READS one -- and which an operator gets depends on where the damage happens to lie. `installs the newest usable snapshot` without naming it is the generic advice this drill exists to rule out, and it was reachable by luck: it took a new table changing the file layout for the drill to land on it at all",
    test: "test/hub-drills.test.mjs",
    expectRed: "the refusal names the newest usable snapshot by path",
    edits: [{ file: "src/build/hubdb.mjs",
              find: "          (newestHubSnapshot(path)",
              replace: "          (false" }],
  },
  {
    name: "a-damaged-v6-hub-is-not-an-old-one",
    why: "answer null for ANY missing table, which renders a v6 hub that has LOST `hub_incarnation` as an ordinary older store and suppresses the only evidence a caller has that it is gone. The null this function promises is for a hub that PREDATES the table, and telling the two apart needs a positive reading of `schema_version` rather than the absence of the table. Review found this in the FIXTURE: the test asserting the null dropped the table from a v6 hub, so it built the damaged case and certified it as the old one",
    test: "test/hub-incarnation.test.mjs",
    expectRed: "a v6 hub that has LOST the table propagates",
    edits: [{ file: "src/build/hubdb.mjs",
              find: "    if (predatesIncarnation(db)) return null;",
              replace: "    if (true) return null;" }],
  },
  {
    name: "an-empty-incarnation-table-is-damage-too",
    why: "answer null when the table is present and EMPTY on a migrated hub. It is the same misclassification as answering null for a missing table, one row further down: `DELETE FROM hub_incarnation` from a hand repair, or a partially applied restore, leaves a v6 store with the table and no row -- and a cursor reader downstream files that under `cannot prove` and carries on. Migration 6 mints and nothing removes, so on a hub recording the migration there is no legitimate way to have no row",
    test: "test/hub-incarnation.test.mjs",
    expectRed: "a v6 hub whose incarnation ROW is gone propagates too",
    edits: [{ file: "src/build/hubdb.mjs",
              find: "  if (predatesIncarnation(db)) return null;\n  throw Object.assign(new Error(",
              replace: "  if (true) return null;\n  throw Object.assign(new Error(" }],
  },
  {
    name: "a-hole-is-not-sent-to-a-writing-command",
    why: "give a HOLE the missing-tail remedy, which is the state issue #121 reported. Both faults arrive as a non-empty `missing` and only one migrates: `openHub` re-runs a missing tail and refuses a hole outright, because a migration beneath an applied one cannot be re-run over a store that has already moved past it. The reader then sends the operator to a second refusal, and the second refusal is the one naming the actual repair",
    test: "test/task-show.test.mjs",
    expectRed: "a HOLE is sent to a snapshot restore",
    edits: [{ file: "src/build/hubfault.mjs",
              find: "  return hist.holed",
              replace: "  return false" }],
  },
  {
    name: "the-read-route-consults-the-history-fault",
    why: "answer no fault at all, which is what the read routes did for every shape they did not model. They computed the history by hand and had no branch for an invalid marker or a hole, so the guard passed and the command reached an application query against a store missing the tables it was about to read -- surfacing as a bare `no such table` from a command that had already reported the hub healthy",
    test: "test/task-show.test.mjs",
    expectRed: "a hub older than this binary is a typed refusal",
    edits: [{ file: "bin/reeve",
              find: "        const fault = historyFault(hist);",
              replace: "        const fault = null; void historyFault;" }],
  },
  {
    name: "an-unreadable-history-keeps-its-cause",
    why: "drop the exception on the floor, which is what `unreadableHistory()` did before it carried one. A caller refusing on an unreadable history has to tell an operator whether to retry, and that is `faultKind`'s question about the ORIGINAL exception -- a store held by another process past the busy timeout answers SQLITE_BUSY and is worth retrying, while a damaged one never is. Without the cause the read route hard-coded `retryable: false` and reported a transient lock as permanent",
    test: "test/task-show.test.mjs",
    expectRed: "an unreadable schema_version is refused",
    edits: [{ file: "src/build/hubdb.mjs",
              find: "  } catch (e) { return unreadableHistory(e); }",
              replace: "  } catch { return unreadableHistory(); }" }],
  },
  {
    name: "an-invalid-marker-beats-a-high-version",
    why: "compare versions before checking for an invalid marker, which declares a store recording `[-1, 6]` a healthy newer hub and tells the operator NOT to restore it. `schema_version` is an INTEGER PRIMARY KEY, so a hand-edited `-1` is valid SQLite -- and such a store is not newer, it is damaged and also happens to carry a high number",
    test: "test/hub-fault.test.mjs",
    expectRed: "an invalid marker beats a high version",
    edits: [{ file: "src/build/hubfault.mjs",
              find: "  if (hist.invalid.length)",
              replace: "  if (false && hist.invalid.length)" }],
  },
  {
    name: "ahead-and-missing-is-damage",
    why: "treat a store that is ahead AND missing a version below it as an ordinary newer hub. A forward-only history that reached version N carries every version beneath it, so both at once cannot have been produced by migrating -- and the healthy-ahead remedy tells the operator not to restore, which is the one instruction that store must not receive",
    test: "test/hub-fault.test.mjs",
    expectRed: "ahead AND missing something below it is damage",
    edits: [{ file: "src/build/hubfault.mjs",
              find: "  if (hist.version > expect && (hist.missing.length || hist.holed))",
              replace: "  if (false && hist.version > expect && (hist.missing.length || hist.holed))" }],
  },
  {
    name: "an-unreadable-history-is-not-yet-damage",
    why: "prescribe a restore for any unreadable history. `openHub` creates the file and then runs the schema DDL, so a command opening it in that window holds a read transaction whose view has the file and not the table -- a perfectly healthy hub, one moment from ready. Telling that operator to force-restore replaces a working store to fix nothing",
    test: "test/hub-fault.test.mjs",
    expectRed: "told to look again before concluding damage",
    edits: [{ file: "src/build/hubfault.mjs",
              find: "               return { snapshotRestore: \"if-it-persists\",\n                        remedy: \"re-run: a hub being created for the first time reads this way for an \" +",
              replace: "               return { snapshotRestore: \"if-it-persists\",\n                        remedy: \"restore a snapshot (`reeve restore --hub --force`), then retry\" || \"\" +" }],
  },
  {
    name: "a-gap-above-the-known-range-is-still-a-gap",
    why: "look only at `missing`, which is about the range 1..expect. A history like [1,2,3,4,5,7] against expect 5 is missing none of what this binary knows while skipping 6, so `missing` is empty and only `holed` sees it -- and the store falls through to the healthy-ahead remedy and is told NOT to restore, which is the one instruction a store with a gap must not receive",
    test: "test/hub-fault.test.mjs",
    expectRed: "a gap ABOVE this binary's range is still not a newer hub",
    edits: [{ file: "src/build/hubfault.mjs",
              find: "hist.version > expect && (hist.missing.length || hist.holed)",
              replace: "hist.version > expect && (hist.missing.length || false)" }],
  },
  {
    name: "the-ahead-and-holed-remedy-can-be-run",
    why: "send an ahead-and-holed hub to `reeve restore --hub --force`, which `restoreHub` refuses BEFORE taking the lock when the live store records a newer version -- and `--force` does not reach that check. Following the advice produces the second refusal this module exists to prevent, written into the module itself. Upgrading does not help either: a newer binary reads the same non-contiguous history and refuses it for the same reason, so the store has to stop being the live hub before anything will touch it",
    test: "test/hub-fault.test.mjs",
    expectRed: "told to move the store aside",
    edits: [{ file: "src/build/hubfault.mjs",
              find: "             // rather than deleting it.\n             remedy: restoreAdvice(\"aside-first\"),\n             snapshotRestore: \"aside-first\" };",
              replace: "             // rather than deleting it.\n             remedy: \"restore a snapshot (`reeve restore --hub --force`), then retry\",\n             snapshotRestore: \"aside-first\" };" }],
  },
  {
    name: "every-damage-remedy-asks-the-store-not-the-diagnosis",
    why: "give the plain `reeve restore --hub --force` to a store recording a version above this binary's. `restoreHub` refuses exactly that, before the lock and where `--force` cannot reach, so the advice produces the second refusal this module exists to prevent. Three findings were this same defect in three different branches -- the condition is a property of the STORE rather than of which fault was diagnosed, so it is asked once and every damage branch consults it",
    test: "test/hub-fault.test.mjs",
    expectRed: "is told to move the store aside, because a plain restore refuses it",
    edits: [{ file: "src/build/hubfault.mjs",
              find: "const restoreAdvice = (stance) => stance === \"aside-first\"",
              replace: "const restoreAdvice = (stance) => false" }],
  },
  {
    name: "the-restore-stance-is-a-field",
    why: "tell the forward-version fault that a restore is the remedy. The stance exists BECAUSE prose could not hold it: two assertions guarding this same case passed a remedy recommending the downgrade, one matching the word and one matching the phrase. If the field can be wrong without anything noticing, the meaning has simply moved rather than been secured, and every consumer that branches on it inherits the old defect in a new place",
    test: "test/hub-fault.test.mjs",
    expectRed: "stance: a healthy newer hub forbids it",
    edits: [{ file: "src/build/hubfault.mjs",
              find: "             snapshotRestore: \"forbidden\" };",
              replace: "             snapshotRestore: \"in-place\" };" }],
  },
  {
    name: "the-remedy-must-agree-with-the-stance",
    why: "turn the forward-version remedy affirmative while leaving the field saying forbidden. A field is only worth having if the sentence beside it cannot drift away from it -- otherwise the operator reads prose that recommends destroying a healthy store while every machine-readable signal says the opposite, which is worse than the ambiguity the field replaced",
    test: "test/hub-fault.test.mjs",
    expectRed: "the remedy agrees with the field: a healthy newer hub forbids it",
    edits: [{ file: "src/build/hubfault.mjs",
              find: "             remedy: \"run the newer binary. Do NOT restore a snapshot over it: this store is \"",
              replace: "             remedy: \"run the newer binary. Restore a snapshot over it: this store is \"" }],
  },
  {
    name: "moving-aside-names-every-file-of-the-database",
    why: "name two of the three files. `openHub` forces `journal_mode = WAL`, so a live hub IS `hub.db` and `hub.db-wal` and `hub.db-shm` -- measured: three files while open, one after a clean close. An operator who moves only what the sentence names leaves a -wal holding committed pages beside a restored store, which is the state this remedy is reached in after a crash",
    test: "test/hub-fault.test.mjs",
    expectRed: "moving aside names all three files",
    edits: [{ file: "src/build/hubfault.mjs",
              find: "`hub.db` AND `hub.db-wal` AND `hub.db-shm`, together, since a WAL database is all three and ",
              replace: "`hub.db` AND `hub.db-wal`, together, since a WAL database is all three and " }],
  },
  {
    name: "a-conditional-restore-is-not-confirmed-damage",
    why: "call a history that may be transient confirmed damage. `openHub` creates the file and THEN runs the DDL, so a reader in that window sees `no such table: schema_version` on a hub one moment from being ready -- which is why this branch is `retryable: true` and says to re-run. A field reading `in-place` beside that contradicts it, and a consumer acting on the machine-readable value rather than the sentence would overwrite a healthy hub during its creation window. Review found this in the field itself: the stance was introduced to stop prose misstating the meaning and then misstated it in a new place",
    test: "test/hub-fault.test.mjs",
    expectRed: "stance: an unreadable history takes the restore only if it PERSISTS",
    edits: [{ file: "src/build/hubfault.mjs",
              find: "snapshotRestore: \"if-it-persists\",",
              replace: "snapshotRestore: \"in-place\"," }],
  },
  {
    name: "an-affirmative-need-is-an-instruction",
    why: "exempt `needs` from the recommendation rule again. It was there for one EXPLANATORY clause -- \"a restore needs more room rather than less\" -- which `rather than` already excuses, so the exemption bought nothing and silently swallowed \"this store needs a snapshot restore\", which is an instruction rather than a hypothesis. A blanket mood exemption cannot tell those apart, and the whole point of this rule is that it can",
    test: "test/hub-fault.test.mjs",
    expectRed: "an affirmative NEED is read as a recommendation",
    edits: [{ file: "test/restore-stance.mjs",
              find: "const SUBJUNCTIVE = /\\b(?:would|could)\\b/i;",
              replace: "const SUBJUNCTIVE = /\\b(?:would|could|needs)\\b/i;" }],
  },
  {
    name: "the-forward-version-remedy-forbids-a-restore",
    why: "turn the ahead remedy's own forbidding clause affirmative: `Do NOT restore a snapshot over it` becomes `Restore a snapshot over it`. That store is HEALTHY and this binary is old, so an older snapshot discards everything the newer binary wrote and repairs nothing -- it is the one fault in this module whose correct advice is the ABSENCE of a restore. Nothing proved the guard could fail: two versions of the assertion passed a remedy recommending the downgrade, the first without the word and the second with the phrase, and the sweep caught neither because the assertion had no entry at all",
    test: "test/task-show.test.mjs",
    expectRed: "a hub newer than this binary is refused too",
    edits: [{ file: "src/build/hubfault.mjs",
              find: "             remedy: \"run the newer binary. Do NOT restore a snapshot over it: this store is \"",
              replace: "             remedy: \"run the newer binary. Restore a snapshot over it: this store is \"" }],
  },
  {
    name: "an-out-of-range-marker-is-not-transient",
    why: "treat a `schema_version` value beyond JavaScript's safe integer range as an ordinary unreadable history, which is told to re-run and then to restore. Neither works: the value IS the fault, so re-running reproduces it, and `restoreHub` reads versions the same way and refuses with the same error -- measured, along with the fact that moving all three files aside and restoring then succeeds",
    test: "test/hub-fault.test.mjs",
    expectRed: "an out-of-range marker is told to move the store aside",
    edits: [{ file: "src/build/hubfault.mjs",
              find: "  if (hist?.readable !== true && outOfRange(hist?.cause))",
              replace: "  if (false && outOfRange(hist?.cause))" }],
  },
  {
    name: "an-unreadable-history-reaches-the-shared-decision",
    why: "refuse an unreadable history in the read route before `historyFault` runs, which is where this block started. The branch that tells a marker no reader can represent from a hub being created for an instant is then unreachable from `task list|show|why|dash`, the refusal carries no remedy, and -- because ERR_OUT_OF_RANGE has no SQLite errcode and `faultKind` calls it operational -- the envelope says `retryable: true` for a failure every retry reproduces",
    test: "test/task-show.test.mjs",
    expectRed: "a schema_version value no reader can represent is a typed refusal",
    edits: [{ file: "bin/reeve",
              find: "          if (fault.kind === \"unreadable-marker\" || fault.kind === \"unreadable\")",
              replace: "          if (false)" }],
  },
  {
    name: "an-out-of-range-marker-is-never-retryable",
    why: "derive retryability for a marker no reader can represent, the way the transient case does. `ERR_OUT_OF_RANGE` carries no SQLite errcode, so `faultKind` calls it operational and the envelope tells a client to try again -- for a failure where the VALUE is the fault and every read reproduces it. The two causes of an unreadable history differ in exactly this, which is why they are two branches",
    test: "test/task-show.test.mjs",
    expectRed: "it is NOT retryable, because the value is the fault",
    edits: [{ file: "src/build/hubfault.mjs",
              find: "             retryable: false };",
              replace: "             retryable: true };" }],
  },
  {
    name: "a-lock-is-not-damage",
    why: "send every unreadable history to the restore, whatever made it unreadable. A SQLITE_BUSY from another process holding the file persists for exactly as long as the holder holds it, so `if it persists, the store is damaged` is false for it -- and force-restoring then replaces a HEALTHY hub to fix a lock. The refusal that renders this already derives `retryable` from `faultKind`, so a remedy ignoring the cause contradicted the bit printed beside it",
    test: "test/hub-fault.test.mjs",
    expectRed: "unreadable because something HOLDS the file is never sent to a restore",
    edits: [{ file: "src/build/hubfault.mjs",
              find: "               if (kind === \"operational\")",
              replace: "               if (false)" }],
  },
  {
    name: "a-full-store-is-not-damage",
    why: "ask a yes/no question of `faultKind`, which returns THREE values. `SQLITE_FULL` then falls to the fallback and is told the store is damaged and to restore -- when the classifier reports FULL separately precisely because it is resource exhaustion, and `openHub`'s own full branch says there is nothing wrong with the file and that a restore needs MORE room rather than less",
    test: "test/hub-fault.test.mjs",
    expectRed: "unreadable because the store is FULL is never sent to a restore",
    edits: [{ file: "src/build/hubfault.mjs",
              find: "               if (kind === \"full\")",
              replace: "               if (false)" }],
  },
  {
    name: "retryability-travels-with-the-sentence",
    why: "let the caller derive retryability from the cause while this decides the remedy. A hub read during its own creation answers errcode 1, which `faultKind` calls damage -- so the envelope said `retryable: false` beside a remedy telling the operator to look again. The decision that writes the sentence is the one that knows whether trying again can change it",
    test: "test/hub-fault.test.mjs",
    expectRed: "a history that may be a hub mid-creation is RETRYABLE",
    edits: [{ file: "src/build/hubfault.mjs",
              find: "             retryable: true };",
              replace: "             retryable: false };" }],
  },
  {
    name: "an-escalations-report-is-durable",
    why: "send the report and store nothing. The identity is the bare cause by design, so the report is what makes a bare key affordable rather than an addition to it — and until it is on the row, `task show` and `task why` cannot recover afterwards what an alert said, and a process-scoped cause cannot name the repository it is about, which is what decides whose channel pages for it. The detail reaching a phone and nothing else is the half that scrolls past",
    test: "test/build-escalations.test.mjs",
    expectRed: "the report is stored on the row, not only sent",
    edits: [{
      file: "src/build/announce.mjs",
      find: "VALUES(?,?,?,?,0,?)`).run(why, count, at, at, serialiseBody(offeredBody(bodies, why)));",
      replace: "VALUES(?,?,?,?,0,?)`).run(why, count, at, at, null);",
    }],
  },
  {
    name: "the-report-rides-in-the-replayed-row-image",
    why: "leave `body` out of the `escalation.raised` payload. `escalation` is replayed from that event, so a column absent from the image is a column a restore silently empties — and what it empties is the only durable record of what the alert said. The row would come back looking intact, with its counters and timestamps restored and its report gone, which is worse than a row that failed to restore at all",
    test: "test/build-escalations.test.mjs",
    expectRed: "and rides in the escalation.raised image, so a replay restores what the alert said",
    edits: [{
      file: "src/build/announce.mjs",
      find: "    \"SELECT why, count, first_seen_at, last_seen_at, announced_count, body FROM escalation WHERE why = ?\")",
      replace: "    \"SELECT why, count, first_seen_at, last_seen_at, announced_count FROM escalation WHERE why = ?\")",
    }],
  },
  {
    name: "a-stored-report-is-recalled-when-the-pass-offers-none",
    why: "read the report only from the caller's map. Every cause `applyTransition` raises arrives through no map at all — which is most of them — so a pass that did not itself measure the fault would page it bare while the durable row held the report the whole time. The map is this pass's knowledge; the row is what the system knows",
    test: "test/build-escalations.test.mjs",
    expectRed: "a later pass with no bodies map still renders the stored report",
    edits: [{
      file: "src/build/announce.mjs",
      find: "    const stored = db.prepare(\"SELECT body FROM escalation WHERE why = ?\").get(why)?.body ?? null;",
      replace: "    const stored = null;",
    }],
  },
  {
    name: "an-unstorable-report-is-refused-by-name",
    why: "hand an unserialisable body to the write. The column carries a json_valid CHECK, so it fails there with a message naming a constraint rather than the caller's mistake — and the report, which exists precisely because the key refuses detail, is lost at the moment it mattered. A named refusal at the boundary tells the caller what to fix; a constraint error tells them where it broke",
    test: "test/build-escalations.test.mjs",
    expectRed: "a body that cannot serialise is refused with its own kind, not a constraint error",
    edits: [{
      file: "src/build/announce.mjs",
      find: "  catch (e) { if (e?.kind) throw e; text = undefined; }",
      replace: "  catch (e) { if (e?.kind) throw e; text = \"{}\"; }",
    }],
  },
  {
    name: "an-offered-empty-body-is-not-an-offer",
    why: "read the offer with `has` instead of through the shared emptiness rule, which is what the two sides of this seam did before they were made to agree. A key mapped to `undefined` reads as an offer of nothing, so the alert pages bare while the row still holds the report",
    test: "test/build-escalations.test.mjs",
    expectRed: "a key mapped to nothing is not an offer, so the alert still renders the stored report",
    edits: [{
      file: "src/build/announce.mjs",
      find: "        if (body === null)\n          db.prepare(\"UPDATE escalation SET count=?, last_seen_at=? WHERE why=?\").run(count, at, why);",
      replace: "        if (false)\n          db.prepare(\"UPDATE escalation SET count=?, last_seen_at=? WHERE why=?\").run(count, at, why);",
    }],
  },
  {
    name: "a-body-that-is-not-an-object-is-refused",
    why: "accept any serialisable value as a body. The alert renders one by walking its entries, so a string pages one line per letter and a number pages nothing at all -- and both serialise and both satisfy the column's json_valid CHECK, so this refusal is the only thing that catches them",
    test: "test/build-escalations.test.mjs",
    expectRed: "a string is refused as a body, not rendered as one",
    edits: [{
      file: "src/build/announce.mjs",
      find: "  if (shape === null || typeof shape !== \"object\" || Array.isArray(shape))\n",
      replace: "  if (false)\n",
    }],
  },
  {
    name: "a-body-is-checked-after-serialisation",
    why: "check the shape of the value HANDED IN rather than the shape that is STORED. `toJSON` decides what `JSON.stringify` produces, so a Date passes as an object and stores as a bare JSON string: the alert renders no detail because Object.entries of it is empty, and the next pass parses the scalar back, finds it unrenderable and drops it -- the report lost while every other signal reports success",
    test: "test/build-escalations.test.mjs",
    expectRed: "a body whose toJSON returns a scalar is refused, though the body itself is an object",
    edits: [{
      file: "src/build/announce.mjs",
      find: "  if (shape === null || typeof shape !== \"object\" || Array.isArray(shape))",
      replace: "  if (body === null || typeof body !== \"object\" || Array.isArray(body))",
    }],
  },
  {
    name: "a-holed-current-store-is-not-an-old-one",
    why: "prove the store predates the incarnation by the ABSENCE OF EXACTLY migration 6, which stopped being proof the moment a 7 existed. A damaged history recording 1-5 and 7 has no 6 either, so a holed CURRENT store is classified as an old one and answers null -- which callers read as `cannot prove`, suppressing the only evidence of damage they had",
    test: "test/hub-incarnation.test.mjs",
    expectRed: "a holed CURRENT store propagates damage rather than answering null like an old one",
    edits: [{
      file: "src/build/hubdb.mjs",
      find: "    return db.prepare(\"SELECT COUNT(*) n FROM schema_version WHERE version >= ?\")",
      replace: "    return db.prepare(\"SELECT COUNT(*) n FROM schema_version WHERE version = ?\")",
    }],
  },
  {
    name: "a-credential-never-becomes-durable",
    why: "store the report without scrubbing it, leaving `redact` to catch credentials on the way to the phone. The body goes somewhere the alert does not: `escalation.body` and the `escalation.raised` payload, which outlive the alert, ride into every snapshot, appear in exported event tails and survive the row being cleared -- so an echoed token is absent from the page and present verbatim, for ever, in the authority store",
    test: "test/build-escalations.test.mjs",
    expectRed: "a credential is scrubbed from the STORED report",
    edits: [{
      file: "src/build/announce.mjs",
      find: "  try { text = JSON.stringify(body, scrubbing); }",
      replace: "  try { text = JSON.stringify(body); }",
    }],
  },
  {
    name: "a-report-is-bounded",
    why: "accept a report of any size. Every pass over a standing cause re-appends the row image to the append-only authority table, so an unbounded body is an unbounded WRITE RATE rather than merely a large row -- a megabyte of CI excerpt re-measured once a minute is gigabytes a day, and the file it exhausts is the one everything else depends on",
    test: "test/build-escalations.test.mjs",
    expectRed: "an oversized report is refused by its own name, not silently cut down",
    edits: [{
      file: "src/build/announce.mjs",
      find: "  if (bytes > BODY_LIMIT)",
      replace: "  if (false)",
    }],
  },
  {
    name: "the-alert-says-what-the-row-says",
    why: "render the body the caller handed in rather than the one that was stored. `toJSON` decides what is persisted, so an object whose toJSON replaces its shape stores the useful report and pages the ORIGINAL -- whose own enumerable entries are the toJSON method itself. The first alert then carries neither the failure type nor the detail, and disagrees with every later alert for the same cause, which reads from the row",
    test: "test/build-escalations.test.mjs",
    expectRed: "the FIRST alert renders what was stored, not the object it was handed",
    edits: [{
      file: "src/build/announce.mjs",
      find: "    const stored = db.prepare(\"SELECT body FROM escalation WHERE why = ?\").get(why)?.body ?? null;\n    if (stored === null) return null;",
      replace: "    const offered = offeredBody(bodies, why);\n    if (offered !== null) return offered;\n    const stored = db.prepare(\"SELECT body FROM escalation WHERE why = ?\").get(why)?.body ?? null;\n    if (stored === null) return null;",
    }],
  },
  {
    name: "the-failure-vocabulary-is-enforced-at-the-boundary",
    why: "enforce the failure type only in `body()`, leaving `announce` to take the bodies map as given. The exported signature invites a caller to build that map directly, and an empty object, an Error that serialises to {}, or a type outside the vocabulary is then persisted and marked DELIVERED while producing an alert with no type and no detail -- the type being what tells `it stopped` from `it may have stopped`",
    test: "test/build-escalations.test.mjs",
    expectRed: "an empty object is refused at the announce boundary, not only by body()",
    edits: [{
      file: "src/build/announce.mjs",
      find: "      !FAILURE_TYPES.includes(shape.type))",
      replace: "      false)",
    }],
  },
  {
    name: "a-detail-json-would-discard-is-refused",
    why: "treat serialising without error as proof the whole report survived. JSON.stringify DROPS an undefined, a function and a symbol from an object without a word and turns a non-finite number into null, so a body loses a fact the caller supplied, passes every shape check, is stored, and is marked DELIVERED",
    test: "test/build-escalations.test.mjs",
    expectRed: "a detail of undefined is refused rather than silently dropped from the report",
    edits: [{
      file: "src/build/announce.mjs",
      find: "  throw refuse(\"escalation_body_value\",\n    `an escalation body cannot carry ${typeof v}:",
      replace: "  return undefined; refuse(\"escalation_body_value\",\n    `an escalation body cannot carry ${typeof v}:",
    }],
  },
  {
    name: "the-report-cap-is-measured-in-bytes",
    why: "measure the ceiling in UTF-16 code units while the cap and its message both promise bytes. About 4000 CJK characters measure as 4000 and occupy roughly 12KB, so a body three times over the ceiling passes the check whose whole purpose is bounding what gets appended on every pass",
    test: "test/build-escalations.test.mjs",
    expectRed: "a body measured in bytes is refused though its code-unit count is inside the cap",
    edits: [{
      file: "src/build/announce.mjs",
      find: "  const bytes = typeof text === \"string\" ? Buffer.byteLength(text, \"utf8\") : 0;",
      replace: "  const bytes = typeof text === \"string\" ? text.length : 0;",
    }],
  },
  {
    name: "a-scrubbed-key-never-displaces-another",
    why: "build the scrubbed object in one pass, so two credential-shaped keys reducing to the same replacement keep only the last. A field vanishes from a report that was otherwise entirely valid -- a fact lost to the guard that was protecting it",
    test: "test/build-escalations.test.mjs",
    expectRed: "both values survive a key collision, under distinct names",
    edits: [{
      file: "src/build/announce.mjs",
      find: "      for (let n = 2; Object.hasOwn(out, name); n++) name = `${safe} (${n})`;",
      replace: "",
    }],
  },
  {
    name: "a-boxed-primitive-is-refused",
    why: "walk a boxed primitive as an ordinary object, which is what a hand-written traversal does. `new Number(5)` and `new Boolean(true)` then store as {} and `new String(\"abc\")` as a map of character indices -- the report accepted, and stored carrying different facts from the ones supplied",
    test: "test/build-escalations.test.mjs",
    expectRed: "a boxed Number is refused rather than stored as something else",
    edits: [{
      file: "src/build/announce.mjs",
      find: "    if (BOXED.has(tag))",
      replace: "    if (false)",
    }],
  },
  {
    name: "an-own-proto-key-survives",
    why: "rebuild the scrubbed object with an ordinary prototype. An own `__proto__` key -- which JSON.parse produces -- then hits the legacy setter instead of becoming a property, so the call succeeds and the stored report is silently missing that field while plain JSON.stringify would have kept it",
    test: "test/build-escalations.test.mjs",
    expectRed: "an own __proto__ key survives, exactly as plain JSON.stringify keeps it",
    edits: [{
      file: "src/build/announce.mjs",
      find: "    const out = Object.create(null);",
      replace: "    const out = {};",
    }],
  },
  {
    name: "the-serialisation-hook-is-not-a-detail",
    why: "refuse every function value, including a `toJSON` property. The serialiser CALLS that one and then omits it, so a body defining a self-returning hook stringifies perfectly and loses nothing -- and refusing it rejects a valid report over a value that was never going to be stored",
    test: "test/build-escalations.test.mjs",
    expectRed: "a self-returning toJSON is stored exactly as plain JSON.stringify stores it",
    edits: [{
      file: "src/build/announce.mjs",
      find: "  if (_key === \"toJSON\" && typeof v === \"function\") return undefined;\n",
      replace: "",
    }],
  },
  {
    name: "a-restored-log-is-caught-by-identity",
    why: "decide the rewind by timestamp alone, which is the rule this replaced. `phase_event.at` is integer seconds with no uniqueness constraint, so a log restored and regrown to the same sequence within ONE SECOND presents an identical (seq, at) and the stale cursor is accepted -- every event through that sequence in the new incarnation is then omitted permanently and the digest reports it as a quiet period",
    test: "test/build-dash.test.mjs",
    expectRed: "a cursor from a PREVIOUS incarnation is rewound, though its (seq, at) still matches",
    edits: [{
      file: "src/build/dash.mjs",
      find: "    if (provable && since.incarnation !== incarnation)\n      return { verdict: \"different-log\", proof: \"incarnation\" };",
      replace: "    if (false)\n      return { verdict: \"different-log\", proof: \"incarnation\" };",
    }],
  },
  {
    name: "the-cursor-carries-the-hub-identity",
    why: "format a cursor without its incarnation, so the digest hands back the two fields it always did. A cursor that cannot name the log it came from cannot be checked against one, and the rewind check silently falls back to the weaker rule for every cursor the digest itself issued",
    test: "test/build-dash.test.mjs",
    expectRed: "and the identity it round-trips is this hub's own, not a shape that merely parses",
    edits: [{
      file: "src/build/dash.mjs",
      find: "  incarnation ? `${seq}.${at}.${incarnation}` : `${seq}.${at}`;",
      replace: "  `${seq}.${at}`;",
    }],
  },
  {
    name: "a-weaker-cursor-answer-says-it-is-weaker",
    why: "report every answer as proof. `cursor_rewound: false` reached by comparing identities is proof; the same false from the timestamp fallback is only an absence of evidence, and a client deciding whether to trust the movement list is entitled to know which one it got",
    test: "test/build-dash.test.mjs",
    expectRed: "but the digest reports that the weaker check answered it",
    edits: [{
      file: "src/build/dash.mjs",
      find: "    return { verdict: \"ok\", proof: provable ? \"incarnation\" : \"timestamp\" };",
      replace: "    return { verdict: \"ok\", proof: \"incarnation\" };",
    }],
  },
  {
    name: "a-mistyped-cursor-identity-is-refused",
    why: "accept anything in the cursor's third field. A fumbled paste then differs from the hub's identity and is reported as a RESTORE THAT NEVER HAPPENED, sending an operator to look for damage that is not there -- an unparseable cursor is a misuse the CLI can name, and a plausible one is not",
    test: "test/build-dash.test.mjs",
    expectRed: "a cursor whose identity is too short is refused, not guessed",
    edits: [{
      file: "src/build/dash.mjs",
      find: "(?:\\.([0-9a-f]{32}))?$/",
      replace: "(?:\\.(.+))?$/",
    }],
  },
  {
    name: "identity-replaces-the-timestamp-rule",
    why: "OR the two rules together, which is the plausible way to write this and refuses the same cursors. It reports a RESTORE for a cursor whose identity MATCHES -- so the log IS the one that issued it and only the cursor is wrong -- sending an operator to hunt damage that is not there instead of to re-copy a mistyped bookmark",
    test: "test/build-dash.test.mjs",
    expectRed: "and is NEVER reported as a different log, which would send an operator hunting damage that is not there",
    edits: [{
      file: "src/build/dash.mjs",
      find: "    if (provable && since.incarnation !== incarnation)\n",
      replace: "    if (provable && (since.incarnation !== incarnation || !namesAKnownEvent()))\n",
    }],
  },
  {
    name: "a-damaged-incarnation-row-is-a-visible-state",
    why: "let the damage verdict out of the digest raw. `hubIncarnation` answers three things -- a row, null for a store older than the table, and a throw for one that records the migration with no row -- and a stack trace is not an interface for a state an operator has to act on. The digest exists to say what is going on rather than to fail at it",
    test: "test/build-dash.test.mjs",
    expectRed: "the digest still answers on a hub whose incarnation row is gone",
    edits: [{
      file: "src/build/dash.mjs",
      find: "    if (!e?.hubDamaged) throw e;\n    incarnationDamaged = e.message;",
      replace: "    throw e;",
    }],
  },
  {
    name: "a-cursor-that-names-no-known-event-is-refused",
    why: "trust the sequence once the incarnation matches, checking the pair no further. A paste that alters one digit of the sequence while leaving the identity intact is then accepted, and `movedSince` silently skips every transition between the real mark and the altered one -- reported as a quiet period, which is this surface's worst failure and the one it exists to prevent",
    test: "test/build-dash.test.mjs",
    expectRed: "a cursor carrying THIS hub's identity but naming an event it does not have is a corrupt cursor",
    edits: [{
      file: "src/build/dash.mjs",
      find: "    if (beyond || !namesAKnownEvent())",
      replace: "    if (beyond)",
    }],
  },
  {
    name: "an-unusable-cursor-is-not-always-called-ahead",
    why: "render one sentence for every unusable cursor, the way this surface did before: `is ahead of this hub's log (it was restored)`. Three of the four verdicts are not ahead at all, and the restore this whole change exists to catch is precisely one whose sequence is INSIDE the log -- so the operator is handed a false diagnosis in the case that matters most",
    test: "test/build-dash.test.mjs",
    expectRed: "and the text says the cursor is wrong rather than that the hub was restored",
    edits: [{
      file: "src/build/dash.mjs",
      find: "    \"unknown-event\": \"does not name an event this hub has, though it carries this hub's identity — \" +\n                     \"the cursor itself is wrong rather than the log\",",
      replace: "    \"unknown-event\": \"is ahead of this hub's log (it was restored)\",",
    }],
  },
  {
    name: "identity-is-asked-before-the-sequence",
    why: "classify an out-of-range sequence before consulting the incarnation. A restore from a SHORTER snapshot -- the common one -- leaves the log below the saved cursor, so both arms match and the sequence wins: the digest reports `ahead`, which is the symptom, and discards the identity sitting beside it, which is the cause. The proof this whole change adds is thrown away in precisely the case it was added for",
    test: "test/build-dash.test.mjs",
    expectRed: "a restore to a SHORTER log is judged by identity, not reported as merely `ahead`",
    edits: [{
      file: "src/build/dash.mjs",
      find: "    if (since.seq === 0) return { verdict: \"ok\", proof: \"sequence\" };\n    if (provable",
      replace: "    if (since.seq === 0) return { verdict: \"ok\", proof: \"sequence\" };\n    if (beyond) return { verdict: \"ahead\", proof: \"sequence\" };\n    if (provable",
    }],
  },
  {
    name: "an-unproven-answer-names-what-could-not-prove-it",
    why: "print one note for both reasons an answer is unproven. A legacy cursor and a hub that cannot supply an identity are opposite faults with opposite advice, and the shared note tells the second operator that the cursor above carries an id and the next call will be provable -- both false, since next_cursor was formatted with no identity at all",
    test: "test/build-dash.test.mjs",
    expectRed: "and when its timestamp proves the cursor, the note still blames the HUB, not the cursor",
    edits: [{
      file: "src/build/dash.mjs",
      find: "  if (m.cursor_proof === \"timestamp\" && m.incarnation !== null)",
      replace: "  if (m.cursor_proof === \"timestamp\")",
    }],
  },
  {
    name: "a-matching-identity-past-the-log-is-the-cursor",
    why: "call any out-of-range sequence `ahead`, whose sentence explains that the cursor carries no incarnation. With a MATCHING identity that is false twice over: the cursor carries one, and cursor_proof says `incarnation` on the same screen -- while the real finding, that this log never reached that sequence, goes unsaid",
    test: "test/build-dash.test.mjs",
    expectRed: "a cursor with THIS hub's identity whose sequence is past the log is the cursor being wrong",
    edits: [{
      file: "src/build/dash.mjs",
      find: "      return provable ? { verdict: \"unknown-event\", proof: \"incarnation\" }",
      replace: "      return beyond ? { verdict: \"ahead\", proof: \"sequence\" } : provable ? { verdict: \"unknown-event\", proof: \"incarnation\" }",
    }],
  },
  {
    name: "a-damaged-hub-exits-non-zero",
    why: "decide the exit status from the cursor alone. A plain `task dash` with no --since on a hub altered outside reeve then prints HUB DAMAGED and exits 0, so anything watching the status -- which is what monitoring watches -- reads a damaged store as healthy while the payload says otherwise",
    test: "test/build-dash.test.mjs",
    expectRed: "and `task dash` exits NON-ZERO on a damaged hub even with no --since given",
    edits: [{
      file: "bin/reeve",
      find: "          process.exit(model.cursor_rewound || model.incarnation_damaged\n            ? EXITS.degraded : EXITS.ok);",
      replace: "          process.exit(model.cursor_rewound ? EXITS.degraded : EXITS.ok);",
    }],
  },
  {
    name: "sequence-zero-survives-a-restore",
    why: "judge a cursor at sequence 0 by its incarnation like any other. A first digest taken before any phase event exists issues one, and if the hub is restored before events arrive the identity differs -- so every transition since is withheld, permanently, because the operator resyncs to the new high-water mark. Sequence 0 names the beginning of any log rather than an event in a particular one",
    test: "test/build-dash.test.mjs",
    expectRed: "a cursor at sequence 0 is accepted even though its incarnation differs",
    edits: [{
      file: "src/build/dash.mjs",
      find: "    if (since.seq === 0) return { verdict: \"ok\", proof: \"sequence\" };\n",
      replace: "",
    }],
  },
  {
    name: "the-proof-names-what-decided-it",
    why: "report `timestamp` for a cursor settled by the sequence alone. A legacy cursor beyond the high-water mark is decided without looking up any event, so the renderer then prints a note about a restore inferred from timestamps directly beneath a line saying the cause cannot be told apart -- naming evidence that was never consulted",
    test: "test/build-dash.test.mjs",
    expectRed: "and the proof says SEQUENCE, because that is what settled it",
    edits: [{
      file: "src/build/dash.mjs",
      find: "        : beyond ? { verdict: \"ahead\", proof: \"sequence\" }",
      replace: "        : beyond ? { verdict: \"ahead\", proof: \"timestamp\" }",
    }],
  },
  {
    name: "a-missing-incarnation-table-is-damage",
    why: "rethrow SQLite's bare `no such table` for a migrated hub that has lost the whole table, while the missing-ROW case is marked. Both mean the store was altered outside reeve, and a caller branching on the mark renders one as an actionable damage report and lets the other out as a generic unreadable-hub refusal with no remedy attached",
    test: "test/hub-incarnation.test.mjs",
    expectRed: "and it is MARKED as damage, so a caller can tell it from an unreadable store",
    edits: [{
      file: "src/build/hubdb.mjs",
      find: "      { hubDamaged: true, cause: e });",
      replace: "      { cause: e });",
    }],
  },
  {
    name: "the-zero-cursor-proof-names-the-sequence",
    why: "ground the sequence-zero answer in the incarnation because both ids happen to exist. The identity would have REJECTED that cursor -- it is accepted only because zero names the origin of any log -- so reporting `incarnation` tells a client that identity proved an answer identity disagreed with. Third instance on this surface of a proof naming evidence that decided nothing, and the third arrived inside the fix for the second",
    test: "test/build-dash.test.mjs",
    expectRed: "and the proof names the SEQUENCE, not an identity that would have rejected it",
    edits: [{
      file: "src/build/dash.mjs",
      find: "    if (since.seq === 0) return { verdict: \"ok\", proof: \"sequence\" };",
      replace: "    if (since.seq === 0) return { verdict: \"ok\", proof: provable ? \"incarnation\" : \"timestamp\" };",
    }],
  },
  {
    name: "a-hub-with-no-identity-says-so-however-the-answer-was-reached",
    why: "key the hub-identity note on this answer's proof. A hub that cannot supply an identity issues cursors carrying none, so the NEXT call is unprovable however this one was settled -- and keying it on `cursor_proof` silences the note for exactly the operator whose hub is damaged and whose answer happened to be reached some other way",
    test: "test/build-dash.test.mjs",
    expectRed: "the note blames the HUB, which is what cannot supply the identity",
    edits: [{
      file: "src/build/dash.mjs",
      find: "  else if (m.incarnation === null && m.since !== null)",
      replace: "  else if (m.cursor_proof === \"timestamp\")",
    }],
  },
  {
    name: "run-refusals-are-told-apart-by-errcode",
    why: "read the refusal out of SQLite's MESSAGE, as the plan does, looking for `index 'one_live_run'`. SQLite never names an index -- it names the constrained columns -- so the branch cannot fire and a routine refusal reaches the tick as a raw hub error instead of {ok:false, reason:'live-run-exists'}",
    test: "test/phase-run.test.mjs",
    expectRed: "a second live run for the SAME task is refused",
    edits: [{ file: "src/build/run.mjs",
              find: "      const reason = REFUSAL_FOR.get(e.errcode);",
              replace: "      const reason = /UNIQUE constraint failed: index 'one_live_run'/.test(e.message) ? \"live-run-exists\" : undefined;" }],
  },
  {
    name: "run-drift-keeps-its-nested-values",
    why: "serialise the contract drift with `JSON.stringify(v, Object.keys(v).sort())`, as the plan does. A replacer ARRAY is a key WHITELIST applied at every level rather than an ordering, so every nested object empties: contract_drift records WHICH fields drifted and destroys what they drifted to, which is the only thing the column is for",
    test: "test/phase-run.test.mjs",
    expectRed: "the drift's nested values SURVIVE serialisation",
    edits: [{ file: "src/build/run.mjs",
              find: "             snapshot.snapshotHash, drift == null ? null : canonicalHub(drift));",
              replace: "             snapshot.snapshotHash, drift == null ? null : JSON.stringify(drift, Object.keys(drift).sort()));" }],
  },
  {
    name: "run-heartbeat-extends-the-lease-from-now",
    why: "extend the lease from the run's started_at rather than from the moment of the beat. The lease then shrinks with every heartbeat and expires under a worker that is still answering, which hands its run to the reaper",
    test: "test/phase-run.test.mjs",
    expectRed: "a heartbeat extends the lease from NOW, not from the run's start",
    edits: [{ file: "src/build/run.mjs",
              find: "    const expiresAt = at + leaseSeconds;",
              replace: "    const expiresAt = db.prepare(\"SELECT started_at FROM phase_run WHERE \" + KEY_SQL).get(...keyArgs(k)).started_at + leaseSeconds;" }],
  },
  {
    name: "run-a-vanished-row-is-revoked",
    why: "read a missing run row as `nothing has revoked this`. Absent and live are not the same fact, and only one of them entitles a process to keep running: a hub restored from a snapshot taken before the run existed leaves the worker going, against an authority that no longer records it",
    test: "test/phase-run.test.mjs",
    expectRed: "a run row that has VANISHED is revoked too: absent and live are not the same fact, and only one of them entitles a process to keep running",
    edits: [{ file: "src/build/run.mjs",
              find: "  if (!r) return \"the run row is gone: a restore or a purge removed the record this process runs under\";",
              replace: "  if (!r) return null;" }],
  },
  {
    name: "run-binding-fails-closed",
    why: "return a refusal from bindRun instead of throwing. S1 turns a throw out of onSpawn into a killed process group; a returned {ok:false} is a value the caller may ignore, and the worker keeps running with no row able to name it",
    test: "test/phase-run.test.mjs",
    expectRed: "binding a process to a run that does not exist THROWS, so the spawn is killed rather than orphaned",
    edits: [{ file: "src/build/run.mjs",
              find: "      throw new Error(`cannot bind a process to ${JSON.stringify(k)}: no live run row to bind it to`);",
              replace: "      return { ok: false, reason: \"no-such-run\" };" }],
  },
  {
    name: "dispatch-a-phase-name-cannot-reach-a-prototype",
    why: "look a phase spec up the obvious way -- a plain object literal, read with `?? null`. Phase names come out of the task row, so this lookup takes untrusted input, and both halves have to go for the hole to open: a null-prototype table has no `toString` to return, and `Object.hasOwn` refuses an inherited name even on a plain one. Written without either, `specFor(\"toString\")` answers with Object's method and a phase nobody registered resolves to a spec nobody wrote",
    test: "test/build-dispatch.test.mjs",
    expectRed: "a phase named toString resolves to null, not to Object's prototype",
    edits: [{ file: "src/build/dispatch.mjs",
              find: "export const PHASE_SPECS = Object.freeze(Object.assign(Object.create(null), {\n}));",
              replace: "export const PHASE_SPECS = Object.freeze({\n});" },
            { file: "src/build/dispatch.mjs",
              find: "  return Object.hasOwn(PHASE_SPECS, phase) ? PHASE_SPECS[phase] : null;",
              replace: "  return PHASE_SPECS[phase] ?? null;" }],
  },
  {
    name: "dispatch-spec-draft-dispatches-nothing",
    why: "register a fourth phase. `ADVANCE.DESIGN` is `SPEC_DRAFT`, so a finished DESIGN really does move the task there; what must not happen is the next tick dispatching it. The absence of that key IS the boundary between S3 and S4, and a key added here for any reason crosses it",
    test: "test/build-dispatch.test.mjs",
    expectRed: "SPEC_DRAFT dispatches nothing: that absence IS the boundary between S3 and S4",
    edits: [{ file: "src/build/dispatch.mjs",
              find: "export const PHASE_SPECS = Object.freeze(Object.assign(Object.create(null), {\n}));",
              replace: "export const PHASE_SPECS = Object.freeze(Object.assign(Object.create(null), {\n  SPEC_DRAFT: { phase: \"SPEC_DRAFT\" },\n}));" }],
  },
  {
    name: "dispatch-a-throw-settles-the-run-row",
    why: "clear the heartbeat on a throw and leave the row live, which is what a bare `finally` does. A runWorker that THREW rather than returning then leaves phase_run saying `live` for a process that is gone, and `one_live_run` refuses the task's next dispatch forever on behalf of nothing -- invisible until the task simply stops progressing",
    test: "test/build-dispatch.test.mjs",
    expectRed: "and the run row is SETTLED, not left live for a process that is gone",
    edits: [{ file: "src/build/dispatch.mjs",
              find: "    settleRun(db, { ...runKey, status: \"failed\", outcome: OUTCOMES.CRASHED,\n                    evidence: { why: `the dispatch threw: ${e.message}`, denials: [], cost: null },\n                    truncated: 0, isAlive });\n",
              replace: "" }],
  },
  {
    name: "dispatch-a-refused-row-never-spawns",
    why: "spawn the worker even though the database refused its run row. The row is the PERMISSION to run -- it is what admission, the reaper and a restart all read -- so a process started without one is a worker nothing can see, account for, or stop",
    test: "test/build-dispatch.test.mjs",
    expectRed: "and NOTHING was spawned: the row is the permission to run, so a refused row must not produce a process",
    edits: [{ file: "src/build/dispatch.mjs",
              find: "  if (!inserted.ok) return { ok: false, reason: inserted.reason };",
              replace: "" }],
  },
  {
    name: "dispatch-clean-drift-is-null",
    why: "return the empty accumulator instead of null when nothing drifted. `{}` is truthy, so every caller written as `if (drift)` records drift on every clean dispatch -- and contract_drift stops meaning anything, because it is set on runs whose environment matched exactly",
    test: "test/build-dispatch.test.mjs",
    expectRed: "an unchanged environment drifts by null, not by {}: an empty object is truthy and would record drift on every clean dispatch",
    edits: [{ file: "src/build/dispatch.mjs",
              find: "  return Object.keys(out).length ? out : null;",
              replace: "  return out;" }],
  },
  {
    name: "dispatch-an-alias-model-is-refused",
    why: "accept a model alias into the contract snapshot. `fable` resolves to a different id after an upgrade, so the task's model would change with nobody deciding it -- and the snapshot, whose whole purpose is to say what this attempt ran under, would record a name rather than a version",
    test: "test/build-dispatch.test.mjs",
    expectRed: "an alias is refused rather than resolved",
    edits: [{ file: "src/build/dispatch.mjs",
              find: "  if (!model || ALIASES.has(String(model)))",
              replace: "  if (!model)" }],
  },
  {
    name: "dispatch-a-cancel-kills-the-process-group",
    why: "hand runWorker no revocation probe, which is the implementation that exists today for every other caller. `terminate-worker` marks phase_run.status='killed' and kills no OS process, so the database write is then the WHOLE of the revocation: `reeve task cancel` returns success, the task reads CANCELLING, and the worker keeps running, keeps writing its artifact and keeps drawing on the subscription. The failure is silent in the direction that reads as working, which is why the assertion is the PROCESS and never the row -- the compensation sets that status whether or not anything died",
    test: "test/build-dispatch.test.mjs",
    expectRed: "THE PROCESS GROUP IS DEAD -- not merely marked dead",
    edits: [{ file: "src/build/dispatch.mjs",
              find: "      isRevoked: () => revocationProbe(db, runKey) ?? heartbeatFailure,",
              replace: "      isRevoked: () => null," }],
  },
  {
    name: "phase-run-contract-columns-are-frozen",
    why: "add a column to phase_run without a numbered migration or an entry in TABLES_AT/COLUMNS_AT. The contract snapshot is what a retry reuses verbatim and what drift is computed against, so its shape cannot move quietly -- a store built from this schema and one built by migration would disagree, and the disagreement surfaces at a restore rather than at the change",
    test: "test/phase-run.test.mjs",
    expectRed: "phase_run's columns are unchanged since PR-C1 froze them",
    edits: [{ file: "src/build/hub.sql",
              find: "  snapshot_hash TEXT,                              -- the registry snapshot; guardian runs have none",
              replace: "  snapshot_hash TEXT,                              -- the registry snapshot; guardian runs have none\n  smuggled_in TEXT," }],
  },
  {
    name: "phase-run-writer-is-frozen",
    why: "change the writer that fills the contract columns without re-generating the fixture. The column list and the statement that populates it are two halves of one promise, and a freeze that watches only the table cannot see a snapshot stop being written",
    test: "test/phase-run.test.mjs",
    expectRed: "and so is the writer that fills them",
    edits: [{ file: "src/build/run.mjs",
              find: "// run -- every statement that touches phase_run, and nothing else.",
              replace: "// run -- every statement that touches phase_run, and nothing else. (edited)" }],
  },
  {
    name: "phase-run-every-contract-column-is-written",
    why: "leave a contract column out of the insert while the column still exists. That is worse than a missing column: it reads as recorded, drift is computed against a permanent NULL, and nothing reports it. The sha over the file cannot catch this on its own -- it reddens on a comment and says nothing about which columns the statement reaches",
    test: "test/phase-run.test.mjs",
    expectRed: "every contract column is reached by the insert, so none can exist unwritten",
    edits: [{ file: "src/build/run.mjs",
              find: "     lease_expires_at,out_path,err_path,cli_version,model_id,effort,argv_hash,prompt_hash,",
              replace: "     lease_expires_at,out_path,err_path,cli_version,model_id,effort,argv_hash," }],
  },
  {
    name: "run-is-fenced-against-the-task-epoch",
    why: "admit a run on the strength of the foreign key alone. The key says the task exists; it says nothing about the task still being at the phase and generation the run was chosen for. A cancel that observes no live run, commits, and is then followed by this insert leaves a live row for the phase the task has LEFT -- and revocationProbe reads it as entitled, so the worker runs after the cancellation",
    test: "test/phase-run.test.mjs",
    expectRed: "a run for a phase the task has left is refused",
    edits: [{ file: "src/build/run.mjs",
              find: "    if (epoch.generation !== generation || epoch.phase !== phase)",
              replace: "    if (false)" }],
  },
  {
    name: "run-settled-event-carries-the-whole-row",
    why: "emit a partial payload on the settled event. Replay is a primary-key upsert and phase_run.started is deliberately not replayed, so when a snapshot predates a completed run this event is the ONLY thing that can recreate the row -- a partial image reaches an INSERT and dies on `NOT NULL constraint failed: phase_run.started_at`, taking the whole replay with it rather than one row",
    test: "test/phase-run.test.mjs",
    expectRed: "the settled event carries every NOT NULL column, so a replay that has to INSERT the row can",
    edits: [{ file: "src/build/run.mjs",
              find: "    hubEvent(db, { kind: \"phase_run.settled\", task,\n      payload: db.prepare(`SELECT * FROM phase_run WHERE ${KEY_SQL}`).get(...keyArgs(k)) });",
              replace: "    hubEvent(db, { kind: \"phase_run.settled\", task,\n      payload: { ...k, status, outcome, truncated: truncated ? 1 : 0 } });" }],
  },
  {
    name: "run-settle-persists-the-session",
    why: "drop the session id at settle. It is learned from the worker's init event and arrives on the RESULT, long after onSpawn bound a null -- so every real row loses the session `--resume` needs for the permitted timeout, rate-limit and BAD_REPORT retries, and loses it hardest across a daemon restart, which is when resuming matters",
    test: "test/build-dispatch.test.mjs",
    expectRed: "the session the worker reported is persisted, so --resume has something to resume",
    edits: [{ file: "src/build/run.mjs",
              find: "           sessionId, ...keyArgs(k));",
              replace: "           null, ...keyArgs(k));" }],
  },
  {
    name: "dispatch-argv-must-match-the-snapshot",
    why: "spawn argv the snapshot does not name. The snapshot is the contract the attempt is judged against and a retry reuses verbatim; if the caller rebuilt or mutated argv after taking it, the worker runs one command while the row, the file and every later comparison label it with the hash of another -- undetectable downstream, because everything downstream trusts the hash",
    test: "test/build-dispatch.test.mjs",
    expectRed: "argv the snapshot does not name is refused before anything is granted",
    edits: [{ file: "src/build/dispatch.mjs",
              find: "  if (actualArgvHash !== snapshot.argvHash)",
              replace: "  if (false)" }],
  },
  {
    name: "dispatch-a-refused-dispatch-keeps-the-argv-record",
    why: "write the argv record before the row is granted. A refused dispatch then overwrites the SETTLED attempt's durable record with argv that never ran: the row keeps its original argv_hash while the file beside it holds different bytes, so the audit trail disagrees with itself and the retry evidence is gone",
    test: "test/build-dispatch.test.mjs",
    expectRed: "and the settled attempt's argv record is untouched: the row keeps its argv_hash, so a file holding other bytes would make the audit trail disagree with itself",
    edits: [{ file: "src/build/dispatch.mjs",
              find: "  writeFileSync(argvPath, JSON.stringify({ argv, argvHash: snapshot.argvHash }, null, 2) + \"\\n\");",
              replace: "" },
            { file: "src/build/dispatch.mjs",
              find: "  const inserted = insertRun(db, { ...runKey, outPath, errPath, snapshot, drift,",
              replace: "  writeFileSync(argvPath, JSON.stringify({ argv, argvHash: snapshot.argvHash }, null, 2) + \"\\n\");\n  const inserted = insertRun(db, { ...runKey, outPath, errPath, snapshot, drift," }],
  },
  {
    name: "dispatch-a-failed-heartbeat-revokes",
    why: "swallow a heartbeat that could not be written. The failure does not correct itself from the row's side: revocationProbe reads a lease_expires_at still in the future -- precisely because nobody could move it -- and answers null, so the worker acts for the rest of the lease with a claim nobody renewed. A live maintenance lock is the case that matters, because writes are refused for the whole of a restore",
    test: "test/build-dispatch.test.mjs",
    expectRed: "a heartbeat that could not be written revokes the worker rather than being swallowed",
    edits: [{ file: "src/build/dispatch.mjs",
              find: "      isRevoked: () => revocationProbe(db, runKey) ?? heartbeatFailure,",
              replace: "      isRevoked: () => revocationProbe(db, runKey)," }],
  },
  {
    name: "dispatch-drift-covers-every-contract-field",
    why: "compare a hand-kept list of contract fields instead of the snapshot's own keys. The list is a second spelling of contractSnapshot's shape and had already drifted from it: maxTurns, maxBudgetUsd, argvHash and promptHash were all absent, so a live profile change to the worker's own limits measured as a perfectly clean dispatch",
    test: "test/build-dispatch.test.mjs",
    expectRed: "every field the snapshot carries is compared, so a field added to the contract cannot be silently unwatched",
    edits: [{ file: "src/build/dispatch.mjs",
              find: "  for (const f of Object.keys(snapshot))\n    if (f !== \"ok\" && snapshot[f] !== live[f]) out[f] = { was: snapshot[f] ?? null, now: live[f] ?? null };",
              replace: "  for (const f of [\"cliVersion\", \"modelId\", \"effort\", \"settingsHash\", \"toolsHash\", \"agentsHash\", \"snapshotHash\"])\n    if (snapshot[f] !== live[f]) out[f] = { was: snapshot[f] ?? null, now: live[f] ?? null };" }],
  },
  {
    name: "run-records-the-task-resume-sequence",
    why: "let resume_seq take its column default. A plain resume leaves the generation unchanged on purpose and resets the bounded attempt budget by counting runs under the NEW resume sequence -- so every post-resume attempt labelled 0 is counted with the attempts from before it, and the budget either re-exhausts at once or never sees the new work. The column exists for exactly this",
    test: "test/phase-run.test.mjs",
    expectRed: "the run carries the task's CURRENT resume sequence, not the column default",
    edits: [{ file: "src/build/run.mjs",
              find: "        .run(task, generation, phase, slice, attempt, epoch.resume_seq,",
              replace: "        .run(task, generation, phase, slice, attempt, 0," }],
  },
  {
    name: "replay-keeps-a-tail-only-attempt-terminal",
    why: "withhold phase_run.started from replay entirely. restoreHub converts the live rows it finds IN the snapshot to killed BEFORE the tail is replayed, so an attempt created after the snapshot and not yet settled exists only in the tail -- skipping its events removes it from the projection altogether. task why loses it, the retry budget is refunded, and its attempt number and output paths are free for another run to reuse",
    test: "test/phase-run.test.mjs",
    expectRed: "a tail-only attempt is APPLIED rather than skipped",
    edits: [{ file: "src/build/replay.mjs",
              find: "  \"phase_run.started\":        { table: \"phase_run\", key: [\"task\",\"generation\",\"phase\",\"slice\",\"attempt\"],",
              replace: "  \"phase_run.started_withheld\": { table: \"phase_run\", key: [\"task\",\"generation\",\"phase\",\"slice\",\"attempt\"]," }],
  },
  {
    name: "dispatch-a-failed-argv-write-settles-the-row",
    why: "let a failed argv write escape after the row is committed. Moving that write below the insert closed one hole and opened its mirror: a full disk, a changed permission or a directory occupying the path throws AFTER the grant and BEFORE the try that would settle it, leaving phase_run live with no process -- and one_live_run then blocks every later dispatch of that task until something else intervenes",
    test: "test/build-dispatch.test.mjs",
    expectRed: "and the granted row is SETTLED, so one_live_run does not block the task for ever",
    edits: [{ file: "src/build/dispatch.mjs",
              find: "    settleRun(db, { ...runKey, status: \"failed\", outcome: OUTCOMES.CRASHED,\n                    evidence: { why: `the argv record could not be written: ${e.message}`, denials: [], cost: null },\n                    truncated: 0, isAlive });",
              replace: "    /* the row is left as granted */" }],
  },
  {
    name: "dispatch-either-stream-truncated-marks-the-row",
    why: "read only stdout's truncation flag. runWorker reports stderr's separately as stderrTruncated and classifies a stderr-only truncation as FAILED for the same reason this bit exists -- the durable record is incomplete. Storing 0 beside a run whose evidence is missing makes replay and every audit surface report a complete record",
    test: "test/build-dispatch.test.mjs",
    expectRed: "a stderr-only truncation still marks the row truncated, so no audit surface reports a complete record",
    edits: [{ file: "src/build/dispatch.mjs",
              find: "                  truncated: (result.truncated || result.stderrTruncated) ? 1 : 0,",
              replace: "                  truncated: result.truncated ? 1 : 0," }],
  },
  {
    name: "dispatch-forwards-the-callers-spawn-hook",
    why: "bind only the run row and drop the caller's onSpawn. The tick claims a provider slot BEFORE the worker exists, so the lease records the DAEMON's pid and lstart and its hook re-binds it to the worker. Without that, liveness is asked about a long-lived daemon that is always alive -- so a worker that dies takes its slot with it until expiry, and the reaper, whose whole basis is pid-and-lstart death, can never fire for it",
    test: "test/build-dispatch.test.mjs",
    expectRed: "the caller's onSpawn is invoked with the worker's pid and start",
    edits: [{ file: "src/build/dispatch.mjs",
              find: "        if (onSpawn) onSpawn({ pid, lstart });",
              replace: "" }],
  },
  {
    name: "dispatch-forwards-the-halt-probe",
    why: "leave runWorker on its default isHalted of () => false. A HALT marker raised AFTER a worker starts then leaves it running -- modifying its worktree and spending the subscription -- and the pre-dispatch halt check cannot reach that case by construction, because it already ran",
    test: "test/build-dispatch.test.mjs",
    expectRed: "the builder's halt probe reaches runWorker and is asked",
    edits: [{ file: "src/build/dispatch.mjs",
              find: "      isHalted,\n",
              replace: "" }],
  },
  {
    name: "run-an-overdue-beat-does-not-revive-a-lease",
    why: "renew a lease whose expiry has already passed. A blocked or suspended event loop delivers a beat later than the lease it renews, and honouring it turns a lost lease back into an apparently valid one before runWorker's poll observes the expiry -- while another actor may already have adopted the run",
    test: "test/phase-run.test.mjs",
    expectRed: "a beat arriving after the lease expired is REFUSED, not honoured",
    edits: [{ file: "src/build/run.mjs",
              find: " AND lease_expires_at > ?`)",
              replace: "`)" }],
  },
  {
    name: "resume-args-refuse-unknown",
    why: "skip a flag nobody declared instead of refusing it. `claim.mjs --issue 150 --relase` then takes the claim path, assigning the user and posting a claim, the opposite of the release they typed",
    test: "test/resume-work.test.mjs",
    expectRed: "a misspelled flag is refused, not ignored",
    edits: [{ file: ".agents/skills/resume-work/scripts/lib.mjs",
              find: "    if (!kind) return { error: `unknown argument: ${flag}` };",
              replace: "    if (!kind) continue;" }],
  },
  {
    name: "resume-post-reads-back-before-retrying",
    why: "retry a failed comment POST without first asking whether it landed. A request whose response was lost has often been delivered, so the retry posts a second claim or checkpoint",
    test: "test/resume-work.test.mjs",
    expectRed: "a comment whose response was lost is not posted again",
    edits: [{ file: ".agents/skills/resume-work/scripts/lib.mjs",
              find: "      if (delivered()) return;\n",
              replace: "" }],
  },
  {
    name: "resume-post-is-single-shot-in-gh",
    why: "let gh's own retry loop resend the comment POST. It retries up to six times before postOnce's read-back runs, so the check that prevents a duplicate comes too late",
    test: "test/resume-work.test.mjs",
    expectRed: "the comment POST asks gh not to retry it",
    edits: [{ file: ".agents/skills/resume-work/scripts/lib.mjs",
              find: "      { once: true }),",
              replace: "      {})," }],
  },
  {
    name: "resume-checks-stale-is-failing",
    why: "take STALE out of the failures. A stale run never completes, so the pull request reads as waiting on a check that will never finish, instead of as its author's move",
    test: "test/resume-work.test.mjs",
    expectRed: "a STALE check is failing, not unfinished",
    edits: [{ file: ".agents/skills/resume-work/scripts/lib.mjs",
              find: "                        \"ERROR\", \"STARTUP_FAILURE\", \"STALE\"]);",
              replace: "                        \"ERROR\", \"STARTUP_FAILURE\"]);" }],
  },
  {
    name: "resume-checks-pass-only-when-named",
    why: "count as unfinished only PENDING and a check still running, which is the rule this replaced. EXPECTED, and any state nobody listed, then read as passing, and the snapshot tells a person the pull request is ready for them",
    test: "test/resume-work.test.mjs",
    expectRed: "an EXPECTED status is unfinished, not passing",
    edits: [{ file: ".agents/skills/resume-work/scripts/lib.mjs",
              find: "    unfinished: runs.filter((c) => !FAILED.has(state(c)) && !PASSED.has(state(c))).length,",
              replace: "    unfinished: runs.filter((c) => [\"PENDING\", \"\"].includes(state(c))).length," }],
  },
  {
    name: "resume-checks-unread-is-not-passing",
    why: "call checks passing when only the first page of them was read. The one failing check can be on the page that wasn't fetched",
    test: "test/resume-work.test.mjs",
    expectRed: "a pull request whose checks weren't all read is not called passing",
    edits: [{ file: ".agents/skills/resume-work/scripts/lib.mjs",
              find: "  const settled = !checks.failing.length && !checks.unfinished && checks.read === checks.total;",
              replace: "  const settled = !checks.failing.length && !checks.unfinished;" }],
  },
  {
    name: "resume-changes-requested-is-the-authors-move",
    why: "ignore a review that requested changes in its body alone. It leaves no thread, so the pull request reads as waiting for someone else, and the snapshot sends its author to another task",
    test: "test/resume-work.test.mjs",
    expectRed: "a pull request with changes requested is its author's move",
    edits: [{ file: ".agents/skills/resume-work/scripts/lib.mjs",
              find: "  if (pr.reviewDecision === \"CHANGES_REQUESTED\") reasons.push(\"changes requested\");\n",
              replace: "" }],
  },
  {
    name: "resume-closers-come-from-the-pull-requests",
    why: "drop the closing references the open pull requests carry. A task a pull request already covers then reads as ready, and a second session claims it and does the work twice",
    test: "test/resume-work.test.mjs",
    expectRed: "a task an open pull request will close is in review, not ready",
    edits: [{ file: ".agents/skills/resume-work/scripts/lib.mjs",
              find: "      closers.set(ref.number, [...(closers.get(ref.number) ?? []), pr.number]);",
              replace: "      void ref;" }],
  },
  {
    name: "resume-checkpoint-shows-without-next",
    why: "show a checkpoint only when it has `next`, which is optional. A checkpoint recorded exactly as AGENTS.md shows then disappears from the snapshot, with its branch and remaining work",
    test: "test/resume-work.test.mjs",
    expectRed: "a checkpoint without next still shows done and remaining",
    edits: [{ file: ".agents/skills/resume-work/scripts/lib.mjs",
              find: "  if (!cp) return [];",
              replace: "  if (!cp?.next) return [];" }],
  },
  {
    name: "resume-stash-on-the-branch-is-found",
    why: "stop looking in the stash. After `git stash` the tree is clean, so a checkpoint is posted while the task's edits exist only on this machine",
    test: "test/resume-work.test.mjs",
    expectRed: "work stashed on the task's branch is found",
    edits: [{ file: ".agents/skills/resume-work/scripts/lib.mjs",
              find: "  return subjects.filter((s) => s.startsWith(`WIP on ${branch}:`) || s.startsWith(`On ${branch}:`));",
              replace: "  return [];" }],
  },
  {
    name: "resume-claim-loser-gives-up-assignment",
    why: "leave the loser of a race against another account assigned. The snapshot then shows an abandoned session holding the task, and after the winner releases it the task still isn't ready",
    test: "test/resume-work.test.mjs",
    expectRed: "the loser of a race against another account gives up its assignment",
    edits: [{ file: ".agents/skills/resume-work/scripts/lib.mjs",
              find: "    unassign: !won && winner?.who !== me,",
              replace: "    unassign: false," }],
  },
  {
    name: "resume-claim-needs-an-assigned-author",
    why: "count a claim whose author is no longer assigned. A holder unassigned by hand, with no release posted, then beats every later claim, and nobody can take the task again",
    test: "test/resume-work.test.mjs",
    expectRed: "a claim whose author is no longer assigned does not win",
    edits: [{ file: ".agents/skills/resume-work/scripts/lib.mjs",
              find: "    c.body.startsWith(CLAIM) && assignees.includes(c.who) && !releasedAfter(comments, i, c.who)) ?? null;",
              replace: "    c.body.startsWith(CLAIM) && !releasedAfter(comments, i, c.who)) ?? null;" }],
  },
  {
    name: "resume-draft-is-the-authors-move",
    why: "drop drafts from the author's queue. A draft with nothing failing then needs nobody, its task reads as in review, and the snapshot suggests claiming new work while the draft sits unfinished",
    test: "test/resume-work.test.mjs",
    expectRed: "an authored draft stays its author's move",
    edits: [{ file: ".agents/skills/resume-work/scripts/lib.mjs",
              find: "  if (pr.isDraft) reasons.push(\"still a draft\");\n",
              replace: "" }],
  },
  {
    name: "resume-threads-unread-are-the-authors-move",
    why: "judge a pull request by its first page of review threads. An unresolved thread past that page goes unseen, and the pull request reads as waiting for a person",
    test: "test/resume-work.test.mjs",
    expectRed: "a pull request whose review threads weren't all read stays its author's move",
    edits: [{ file: ".agents/skills/resume-work/scripts/lib.mjs",
              find: "  if (threadTotal > threads.length) reasons.push(`only ${threads.length} of ${threadTotal} review threads read`);\n",
              replace: "" }],
  },
  {
    name: "resume-phase-read-in-full",
    why: "keep only a phase's first page of sub-issues. Every later task drops out of the plan, the in-progress list and the ready list, so the snapshot can say nothing is ready while work waits",
    test: "test/resume-work.test.mjs",
    expectRed: "a phase with more than 100 tasks is read in full",
    edits: [{ file: ".agents/skills/resume-work/scripts/lib.mjs",
              find: "  if ((phase.subIssues.totalCount ?? nodes.length) <= nodes.length) return phase;",
              replace: "  return phase;" }],
  },
  {
    name: "resume-next-one-step-at-a-time",
    why: "suggest the held task beside a pull request that still needs its author. That offers two next steps, and the lower one can be taken first, against the order AGENTS.md gives",
    test: "test/resume-work.test.mjs",
    expectRed: "while a pull request needs its author, no task is suggested alongside it",
    edits: [{ file: ".agents/skills/resume-work/scripts/lib.mjs",
              find: "  else if (held.length) lines.push(`Continue #${held[0].number}, which you hold.`);",
              replace: "  if (held.length) lines.push(`Continue #${held[0].number}, which you hold.`);" }],
  },
  {
    name: "resume-release-ends-only-its-authors-claims",
    why: "let a release end every earlier claim. One account's --release, even a mistaken one, then takes a task from another account that still holds it",
    test: "test/resume-work.test.mjs",
    expectRed: "a release ends only its author's claims",
    edits: [{ file: ".agents/skills/resume-work/scripts/lib.mjs",
              find: "  comments.slice(i + 1).some((r) => r.body.startsWith(RELEASE) && r.who === who);",
              replace: "  comments.slice(i + 1).some((r) => r.body.startsWith(RELEASE));" }],
  },
  {
    name: "resume-release-can-be-finished",
    why: "never find an unreleased claim. A release that removed the assignment but failed to post its comment then can't be finished, and the old claim comes back to life when the account is assigned again",
    test: "test/resume-work.test.mjs",
    expectRed: "a claim its author hasn't released is found, so a halfway release can be finished",
    edits: [{ file: ".agents/skills/resume-work/scripts/lib.mjs",
              find: "  return comments.some((c, i) => c.body.startsWith(CLAIM) && c.who === me && !releasedAfter(comments, i, me));",
              replace: "  return false;" }],
  },
  {
    name: "platform-linux-reads-real-cores",
    why: "put back the fixed guess of 10 cores on Linux, where the macOS sysctl keys don't exist. A 4-core host is then scheduled as if it had 10, and workers compete with the founder's own work",
    test: "test/platform.test.mjs",
    expectRed: "on Linux, load and cores come from the host, not a guess",
    edits: [{ file: "src/platform.mjs",
              find: "      return { load1: os.loadavg()[0], cores: os.availableParallelism() };",
              replace: "      return { load1: 0, cores: 10 };" }],
  },
  {
    name: "platform-linux-awake-ends-with-daemon",
    why: "hold the Linux keep-awake lock with a command that never ends. A crashed daemon then leaves the machine unable to sleep for good, which caffeinate -w prevents on macOS",
    test: "test/platform.test.mjs",
    expectRed: "on Linux, the keep-awake lock ends when the daemon does",
    edits: [{ file: "src/platform.mjs",
              find: '      return { pid: spawn("systemd-inhibit", [...lock, "tail", `--pid=${pid}`, "-f", "/dev/null"]), via: "systemd-inhibit" };',
              replace: '      return { pid: spawn("systemd-inhibit", [...lock, "sleep", "infinity"]), via: "systemd-inhibit" };' }],
  },
  {
    name: "platform-linux-clone-probe-is-honest",
    why: "probe cloning with --reflink=auto, which falls back to a full copy and never fails. Every filesystem then reports copy-on-write, and the space saving it reports is a guess",
    test: "test/platform.test.mjs",
    expectRed: "cloning is reported exactly when the filesystem can clone",
    edits: [{ file: "src/platform.mjs",
              find: '    cloneProbeArgs: (from, to) => ["--reflink=always", from, to],',
              replace: '    cloneProbeArgs: (from, to) => ["--reflink=auto", from, to],' }],
  },
  {
    name: "notify-linux-title-is-text",
    why: "drop the -- before the notification text. A title that starts with a dash, taken from reviewer text or CI output, is then read by notify-send as an option",
    test: "test/platform.test.mjs",
    expectRed: "a title that starts with a dash is passed as text, not read as an option",
    edits: [{ file: "src/notify.mjs",
              find: '    exec("notify-send", ["--app-name=reeve", "--", title, body], { stdio: ["ignore", "ignore", "pipe"], timeout: 8000 });',
              replace: '    exec("notify-send", ["--app-name=reeve", title, body], { stdio: ["ignore", "ignore", "pipe"], timeout: 8000 });' }],
  },
  {
    name: "service-names-absolute-node",
    why: "start the systemd service with a bare `node`. systemd sources no shell profile, so that finds the system's node, or none, instead of v24, and the daemon can't open its database",
    test: "test/platform.test.mjs",
    expectRed: "the service names an absolute node and states the repository it watches",
    edits: [{ file: "deploy/reeve.service",
              find: "ExecStart=%h/.nvm/versions/node/v24.21.0/bin/node %h/code/nextly-workspace/reeve/bin/reeve run nextlyhq/nextly",
              replace: "ExecStart=node %h/code/nextly-workspace/reeve/bin/reeve run nextlyhq/nextly" }],
  },
  {
    name: "platform-linux-refused-lock-is-reported",
    why: "spawn the keep-awake lock without taking it once first. Where logind refuses it, the daemon then logs 'staying awake' with the pid of a process that had already exited, a success that never happened",
    test: "test/platform.test.mjs",
    expectRed: "on Linux, a refused keep-awake lock is reported, not claimed",
    edits: [{ file: "src/platform.mjs",
              find: '      try { exec("systemd-inhibit", [...lock, "true"], { stdio: ["ignore", "ignore", "pipe"], timeout: 8000 }); }\n      catch (e) { return { pid: null, via: null, why: String(e.stderr || e.message).trim().split("\\n")[0] }; }\n',
              replace: "" }],
  },
  {
    name: "platform-linux-idle-lock-only",
    why: "ask for a lock that blocks sleep as well as idle. logind refuses that to a user service without interactive authentication, so on WSL2 the daemon holds no lock at all",
    test: "test/platform.test.mjs",
    expectRed: "on Linux, the lock prevents idle sleep only, as caffeinate -i does",
    edits: [{ file: "src/platform.mjs",
              find: '      const lock = ["--what=idle", "--who=reeve", "--why=the reeve daemon is running", "--mode=block"];',
              replace: '      const lock = ["--what=idle:sleep", "--who=reeve", "--why=the reeve daemon is running", "--mode=block"];' }],
  },
  {
    name: "copy-deps-retry-starts-clean",
    why: "retry a failed copy-on-write copy into whatever it left. cp -R then copies the tree inside the partial destination, one level too deep, and succeeds, so the worker has no dependencies where it looks",
    test: "test/copy-deps.test.mjs",
    expectRed: "a failed copy-on-write copy is retried from a clean destination, not nested inside what it left",
    edits: [{ file: "src/checkout.mjs",
              find: "    // nest, so it goes first.\n    rmSync(to, { recursive: true, force: true });\n",
              replace: "    // nest, so it goes first.\n" }],
  },
  {
    name: "copy-deps-refuses-an-existing-destination",
    why: "copy into a destination the repository already has. cp -R then puts the tree inside it, one level too deep, and succeeds",
    test: "test/copy-deps.test.mjs",
    expectRed: "a destination already in the checkout is refused, not copied into one level deep",
    edits: [{ file: "src/checkout.mjs",
              find: "  if (present(to)) return { ok: false, why: `${to} is already in the checkout, and copying there would put the dependencies one level too deep; leave it out with worker.dependencyPaths`, cow: false };\n",
              replace: "" }],
  },
  {
    name: "copy-deps-dangling-link-is-present",
    why: "test the destination with existsSync, which follows links. A dangling symlink then reads as absent, and the cleanup after a failed copy deletes it",
    test: "test/copy-deps.test.mjs",
    expectRed: "a dangling symlink at the destination is refused, and neither followed nor removed",
    edits: [{ file: "src/checkout.mjs",
              find: "  if (present(to)) return",
              replace: "  if (existsSync(to)) return" }],
  },
  {
    name: "copy-deps-failure-leaves-nothing",
    why: "return a failed copy without removing what it made. The daemon removes the whole checkout then, but any other caller keeps a half-copied tree it never asked for",
    test: "test/copy-deps.test.mjs",
    expectRed: "a copy that fails for good leaves no partial tree behind",
    edits: [{ file: "src/checkout.mjs",
              find: "    rmSync(to, { recursive: true, force: true });       // what the failed copy made; `to` didn't exist before\n",
              replace: "" }],
  },
  {
    name: "daemon-stop-ends-the-sleep",
    why: "sleep out the rest of the interval after a stop request. A stop between ticks then takes up to 90 more seconds, past the time systemd gives a service to exit, so the daemon is killed and the unit recorded as failed",
    test: "test/daemon-stop.test.mjs",
    expectRed: "a stop request ends the sleep between ticks, and the daemon exits cleanly at once",
    edits: [{ file: "src/daemon.mjs",
              find: "    await new Promise(r => { const t = setTimeout(r, intervalMs); wake = () => { clearTimeout(t); r(); }; });\n",
              replace: "    await new Promise(r => setTimeout(r, intervalMs));\n" }],
  },
  {
    name: "service-lets-a-tick-finish",
    why: "leave the service on systemd's default 90-second stop limit. A tick outlasts it, measured at 109 seconds over 3 pull requests, so a stop kills the daemon mid-tick and records the unit as failed",
    test: "test/platform.test.mjs",
    expectRed: "the service gives a tick in progress time to finish before systemd kills it",
    edits: [{ file: "deploy/reeve.service",
              find: "TimeoutStopSec=10min\n",
              replace: "" }],
  },
  {
    name: "process-identity-pinned-to-utc",
    why: "read a process's start time in the caller's own timezone again. The same live daemon then has a different identity for a CLI in another timezone, which calls it dead and suggests --takeover",
    test: "test/process-identity.test.mjs",
    expectRed: "a process's identity is the same in every timezone",
    edits: [{ file: "src/supervisor.mjs",
              find: "  return psStart(pid, { ...process.env, ...PINNED });",
              replace: "  return psStart(pid, process.env);" }],
  },
  {
    name: "process-identity-format-unchanged",
    why: "change the token's format, here by marking it with a suffix. A process still running an earlier version compares tokens as strings, reads every new token as a dead process, and may reap a live lock",
    test: "test/process-identity.test.mjs",
    expectRed: "a token is exactly ps's lstart in UTC and the C locale, the format earlier versions write and compare",
    edits: [{ file: "src/supervisor.mjs",
              find: "  return psStart(pid, { ...process.env, ...PINNED });",
              replace: "  const utc = psStart(pid, { ...process.env, ...PINNED });\n  return utc === null ? null : `${utc} UTC`;" }],
  },
  {
    name: "process-identity-exact-match",
    why: "also accept a token that matches when read in the caller's own timezone. A stranger that reused the pid and started at the same wall-clock second there then passes as the recorded holder, and a pre-pin token passes as current",
    test: "test/process-identity.test.mjs",
    expectRed: "a token recorded before the pin names no process, even read in the timezone it was recorded in",
    edits: [{ file: "src/supervisor.mjs",
              find: "  return now === storedStart;\n",
              replace: "  if (now === storedStart) return true;\n  return psStart(pid, process.env) === storedStart;\n" }],
  },
  {
    name: "process-identity-no-offset-match",
    why: "accept a token off by a timezone's offset. A stranger that reused the pid and started that much later, to the second, then passes as the recorded holder, and keeps a dead holder's lock alive",
    test: "test/process-identity.test.mjs",
    expectRed: "a token that differs by any amount names a different process, a timezone's offset included",
    edits: [{ file: "src/supervisor.mjs",
              find: "  return now === storedStart;\n",
              replace: "  return now === storedStart || (Date.parse(`${now} UTC`) - Date.parse(`${storedStart} UTC`)) % 1800000 === 0;\n" }],
  },
  {
    name: "shadow-publishes-its-own-name",
    why: "publish shadow results under the enforcement check's name. A neutral result there passes a rule that requires the check, today or after a rule changes, and a pull request merges unjudged",
    test: "test/shadow-required-check.test.mjs",
    expectRed: "shadow mode publishes under its own name, never the enforcement check's, so no rule requiring that check can accept it",
    edits: [{ file: "src/pr.mjs",
              find: "  const name = shadow ? shadowContextOf(context) : context;",
              replace: "  const name = context;" }],
  },
  {
    name: "shadow-supersedes-stale-neutral",
    why: "leave a neutral an earlier version published under the enforcement name. It still passes that check if a rule comes to require it",
    test: "test/shadow-required-check.test.mjs",
    expectRed: "a passing result an earlier version published under the enforcement name at this head is marked superseded",
    edits: [{ file: "src/pr.mjs",
              find: "    else if (stale && stale.app === POLICY_APP && PASSING_RUN.has(stale.conclusion)) {",
              replace: "    else if (false) {" }],
  },
  {
    name: "shadow-raises-a-blocked-gate",
    why: "say nothing when a rule requires the check shadow mode never publishes. Every pull request on that branch is then blocked, and nobody is told why",
    test: "test/shadow-required-check.test.mjs",
    expectRed: "with the enforcement check required on the base, shadow mode says every pull request there is blocked",
    edits: [{ file: "src/pr.mjs",
              find: "    else if (required === true) held = blocked;\n",
              replace: "" }],
  },
  {
    name: "required-honours-app-binding",
    why: "count a check bound to another App as reeve's. GitHub waits for that App, so BLOCKED is then read as reeve's own block and passes",
    test: "test/shadow-required-check.test.mjs",
    expectRed: "a rule requiring the check from another App doesn't require reeve's: GitHub waits for that App",
    edits: [{ file: "src/pr.mjs",
              find: "  const ours = (bound) => (bound == null || Number(bound) === -1 ? true : appId == null ? null : String(bound) === String(appId));",
              replace: "  const ours = () => true;" }],
  },
  {
    name: "required-unknown-app-is-unknown",
    why: "take a check bound to some App as reeve's when reeve's own App id isn't known. It may be another App's, and a guess then passes BLOCKED",
    test: "test/shadow-required-check.test.mjs",
    expectRed: "with reeve's own App id unknown, a check bound to some App is unknown, never a guess",
    edits: [{ file: "src/pr.mjs",
              find: "  const ours = (bound) => (bound == null || Number(bound) === -1 ? true : appId == null ? null : String(bound) === String(appId));",
              replace: "  const ours = (bound) => (bound == null || Number(bound) === -1 ? true : appId == null ? true : String(bound) === String(appId));" }],
  },
  {
    name: "required-context-matches-exactly",
    why: "count any required check as this one. Every protected branch then reads as requiring reeve's check",
    test: "test/shadow-required-check.test.mjs",
    expectRed: "control: required nowhere, or only another check required, is not required",
    edits: [{ file: "src/pr.mjs",
              find: "    if (c !== context) { others.push({ context: c, app: bound == null || Number(bound) === -1 ? null : String(bound) }); return; }\n",
              replace: "" }],
  },
  {
    name: "required-reads-classic-protection",
    why: "read only rulesets. A check required by classic branch protection is then missed",
    test: "test/shadow-required-check.test.mjs",
    expectRed: "classic branch protection requiring it from reeve's App requires it",
    edits: [{ file: "src/pr.mjs",
              find: "    byProtection = verdictOf(answers);\n",
              replace: "    byProtection = false;\n" }],
  },
  {
    name: "required-unreadable-is-unknown",
    why: "read rules or protection that couldn't be read as not requiring the check. A token without access then counts as a definite no",
    test: "test/shadow-required-check.test.mjs",
    expectRed: "rules or protection that couldn't be read are unknown, never taken for not required",
    edits: [{ file: "src/pr.mjs",
              find: "  const own = byRules === true || byProtection === true ? true : byRules === null || byProtection === null ? null : false;",
              replace: "  const own = byRules === true || byProtection === true ? true : false;" }],
  },
  {
    name: "required-reads-every-page",
    why: "read only the first page of a branch's rules. A requiring rule on a later page is then missed",
    test: "test/shadow-required-check.test.mjs",
    expectRed: "a base's rules are read with every page, once a minute however many pull requests target it",
    edits: [{ file: "src/pr.mjs",
              find: "  const value = requirementsOn({\n    rules: gh([\"--paginate\", `repos/${nwo}/rules/branches/${encodeURIComponent(base)}`, \"--jq\", \".[]\"]),",
              replace: "  const value = requirementsOn({\n    rules: gh([`repos/${nwo}/rules/branches/${encodeURIComponent(base)}`, \"--jq\", \".[]\"])," }],
  },
  {
    name: "required-cached-per-base",
    why: "read a base's rules again for every pull request. Twenty pull requests on one branch then cost forty identical calls a tick",
    test: "test/shadow-required-check.test.mjs",
    expectRed: "a base's rules are read with every page, once a minute however many pull requests target it",
    edits: [{ file: "src/pr.mjs",
              find: "  if (hit && now - hit.at < REQUIRED_TTL_MS) return hit.value;\n",
              replace: "" }],
  },
  {
    name: "daemon-passes-the-base",
    why: "publish without the pull request's base branch. The publisher then can't tell that a rule requires the enforcement check",
    test: "test/shadow-required-check.test.mjs",
    expectRed: "the daemon tells the publisher which branch the pull request targets",
    edits: [{ file: "src/daemon.mjs",
              find: "    const pub = await (ctx.publish ?? publishVerdict)({ nwo, verdict: e.verdict, shadow, base: e.baseRef });",
              replace: "    const pub = await (ctx.publish ?? publishVerdict)({ nwo, verdict: e.verdict, shadow });" }],
  },
  {
    name: "daemon-raises-held-shadow",
    why: "log a blocked gate without raising it. The person then learns only from a log that every pull request is stuck",
    test: "test/shadow-required-check.test.mjs",
    expectRed: "and a blocked gate shadow mode found is logged and raised once for the person, not once per pull request",
    edits: [{ file: "src/daemon.mjs",
              find: "      raise(`shadow mode: ${pub.held}`);\n",
              replace: "" }],
  },
  {
    name: "own-check-blocked-reaches-pass",
    why: "block whenever GitHub says BLOCKED. With reeve's check required, its absence keeps GitHub BLOCKED, so every verdict after the first is BLOCK, for ever",
    test: "test/own-check-blocked.test.mjs",
    expectRed: "with reeve's check required and absent, a head that passes everything else reaches PASS",
    edits: [{ file: "src/verdict.mjs",
              find: "    else add(\"mergeable\", PASS, \"mergeStateStatus BLOCKED by reeve's own required check, which this verdict decides; nothing else the base requires is outstanding\");",
              replace: "    else add(\"mergeable\", BLOCK, \"mergeStateStatus BLOCKED by reeve's own required check, which this verdict decides; nothing else the base requires is outstanding\");" }],
  },
  {
    name: "own-check-conflict-still-blocks",
    why: "stop reading a conflict out of BLOCKED. A branch that can't merge into its base then isn't blocked for it",
    test: "test/own-check-blocked.test.mjs",
    expectRed: "a conflict with the base still blocks",
    edits: [{ file: "src/verdict.mjs",
              find: "    if (parts.mergeable === \"CONFLICTING\") add(\"mergeable\", BLOCK, \"mergeStateStatus BLOCKED: the branch conflicts with its base\");\n",
              replace: "    if (false) {}\n" }],
  },
  {
    name: "own-check-review-still-blocks",
    why: "stop reading the review decision out of BLOCKED. A change a reviewer refused, or one still waiting for a required review, then passes",
    test: "test/own-check-blocked.test.mjs",
    expectRed: "an outstanding or refused review still blocks",
    edits: [{ file: "src/verdict.mjs",
              find: "    const review = parts.reviewDecision === \"CHANGES_REQUESTED\" || parts.reviewDecision === \"REVIEW_REQUIRED\";",
              replace: "    const review = false;" }],
  },
  {
    name: "own-check-unknown-is-not-pass",
    why: "treat an unreadable answer to whether reeve's check is required as yes. BLOCKED then passes whenever those rules can't be read",
    test: "test/own-check-blocked.test.mjs",
    expectRed: "BLOCKED when whether reeve's check is required couldn't be read is UNKNOWN, never a pass",
    edits: [{ file: "src/verdict.mjs",
              find: "    else if (parts.ownCheckRequired === null) add(\"mergeable\", UNKNOWN, \"mergeStateStatus BLOCKED, and whether reeve's own required check is among the reasons couldn't be read\");\n",
              replace: "" }],
  },
  {
    name: "own-check-not-required-blocks",
    why: "set BLOCKED aside even when reeve's check isn't required. Whatever GitHub is blocking on then passes unjudged",
    test: "test/own-check-blocked.test.mjs",
    expectRed: "control: BLOCKED while reeve's check isn't required is someone else's block, and blocks",
    edits: [{ file: "src/verdict.mjs",
              find: "    else if (parts.ownCheckRequired === false) add(\"mergeable\", BLOCK, \"mergeStateStatus BLOCKED, and not by reeve's own check\");\n",
              replace: "" }],
  },
  {
    name: "mergeable-unsettled-is-unknown",
    why: "pass BLOCKED before GitHub has settled whether the branch merges. A conflict still being computed then passes",
    test: "test/own-check-blocked.test.mjs",
    expectRed: "BLOCKED while GitHub hasn't settled whether the branch merges is UNKNOWN",
    edits: [{ file: "src/verdict.mjs",
              find: "    else if (parts.mergeable !== \"MERGEABLE\") add(\"mergeable\", UNKNOWN, `mergeStateStatus BLOCKED, and GitHub hasn't settled whether the branch merges (${parts.mergeable ?? \"unread\"})`);\n",
              replace: "" }],
  },
  {
    name: "merge-parts-unreadable-clause",
    why: "judge BLOCKED from parts GitHub reported an error reading. A null review decision then reads as no review outstanding, and the verdict passes",
    test: "test/own-check-blocked.test.mjs",
    expectRed: "a GraphQL error in the pull request's read makes its mergeability parts unknown, never a clear review",
    edits: [{ file: "src/verdict.mjs",
              find: "  else if (MS === \"BLOCKED\" && parts?.readable === false) add(\"mergeable\", UNKNOWN, \"mergeStateStatus BLOCKED, and GitHub reported an error reading its parts\");\n",
              replace: "" }],
  },
  {
    name: "merge-parts-errors-are-unknown",
    why: "ignore GraphQL errors in the pull request's read. A field that failed reads as null, and a null review decision as no review outstanding",
    test: "test/own-check-blocked.test.mjs",
    expectRed: "a GraphQL error in the pull request's read makes its mergeability parts unknown, never a clear review",
    edits: [{ file: "src/pr.mjs",
              find: "    if (parsed.errors?.length) partsReadable = false;\n",
              replace: "" }],
  },
  {
    name: "merge-parts-read-when-blocked",
    why: "never read what the base requires. BLOCKED is then always unknown, and a required check still can't reach PASS",
    test: "test/own-check-blocked.test.mjs",
    expectRed: "when BLOCKED, the parts read whether reeve's check is required on the base, from its rules and its protection",
    edits: [{ file: "src/pr.mjs",
              find: "  if (String(threads?.mergeState ?? \"\").toUpperCase() !== \"BLOCKED\" || !baseRef) return parts;",
              replace: "  return parts;" }],
  },
  {
    name: "threads-read-carries-merge-parts",
    why: "drop mergeable from the pull request's read. The verdict then can't tell a conflict from reeve's own block",
    test: "test/own-check-blocked.test.mjs",
    expectRed: "the pull request's own read carries mergeable and the review decision",
    edits: [{ file: "src/pr.mjs",
              find: "    mergeable = pr_.mergeable ?? null;\n",
              replace: "" }],
  },
  {
    name: "blocked-other-check-failing-blocks",
    why: "pass BLOCKED while another required check fails. GitHub still refuses the merge, and the verdict says only reeve's own check stands in the way",
    test: "test/own-check-blocked.test.mjs",
    expectRed: "another required check that fails still blocks, and is named",
    edits: [{ file: "src/verdict.mjs",
              find: "    else if (failing.length) add(\"mergeable\", BLOCK, `mergeStateStatus BLOCKED: required check(s) not passing: ${failing.join(\", \")}`);\n",
              replace: "" }],
  },
  {
    name: "blocked-waits-for-other-checks",
    why: "pass BLOCKED while another required check hasn't reported or is still running. GitHub is waiting for it, not for reeve",
    test: "test/own-check-blocked.test.mjs",
    expectRed: "another required check that hasn't reported, is still running, or can't be matched to its App keeps it from passing",
    edits: [{ file: "src/verdict.mjs",
              find: "    else if (waiting.length) add(\"mergeable\", UNKNOWN, `mergeStateStatus BLOCKED, waiting for required check(s): ${waiting.join(\", \")}`);\n",
              replace: "" }],
  },
  {
    name: "blocked-unevaluated-rules-are-unknown",
    why: "pass BLOCKED on a base with a rule reeve doesn't evaluate. A required deployment, a signature rule or a merge queue can be what GitHub is waiting for",
    test: "test/own-check-blocked.test.mjs",
    expectRed: "a requirement reeve doesn't evaluate keeps it from passing, and is named",
    edits: [{ file: "src/verdict.mjs",
              find: "    else if (parts.unevaluated.length) add(\"mergeable\", UNKNOWN, `mergeStateStatus BLOCKED, and the base requires what reeve doesn't evaluate: ${parts.unevaluated.join(\", \")}`);\n",
              replace: "" }],
  },
  {
    name: "blocked-conversation-resolution-blocks",
    why: "pass BLOCKED with a conversation unresolved on a base that requires them resolved. GitHub refuses the merge until it is",
    test: "test/own-check-blocked.test.mjs",
    expectRed: "an unresolved conversation the base requires resolved blocks",
    edits: [{ file: "src/verdict.mjs",
              find: "    else if (parts.unresolvedBlocks === true) add(\"mergeable\", BLOCK, \"mergeStateStatus BLOCKED: the base requires every conversation resolved\");\n",
              replace: "" }],
  },
  {
    name: "blocked-unread-requirements-are-unknown",
    why: "take what else the base requires, unread, for nothing. A base whose rules or protection couldn't be read then passes BLOCKED",
    test: "test/own-check-blocked.test.mjs",
    expectRed: "what else the base requires, unread, is UNKNOWN, never a pass",
    edits: [{ file: "src/verdict.mjs",
              find: "    else if (!Array.isArray(parts.others) || parts.unresolvedBlocks == null || !Array.isArray(parts.unevaluated)\n             || parts.strict == null || (parts.strict && !Number.isInteger(parts.behind)))\n      add(\"mergeable\", UNKNOWN, \"mergeStateStatus BLOCKED, and what else the base requires couldn't be read\");\n",
              replace: "" }],
  },
  {
    name: "requirements-name-unevaluated-rules",
    why: "skip the rules reeve doesn't evaluate. A base that requires a deployment, a merge queue or a rule GitHub adds later then reads as requiring nothing more",
    test: "test/own-check-blocked.test.mjs",
    expectRed: "rules reeve doesn't evaluate are named, a new kind included; reviews, and rules that can't stop a merge, aren't",
    edits: [{ file: "src/pr.mjs",
              find: "      else if (!HARMLESS_RULES.has(r?.type)) unevaluated.push(`rule ${r?.type ?? \"of no type\"}`);\n",
              replace: "" }],
  },
  {
    name: "requirements-read-thread-resolution",
    why: "ignore a rule that requires conversations resolved. An unresolved one then blocks at GitHub while the verdict passes BLOCKED",
    test: "test/own-check-blocked.test.mjs",
    expectRed: "an unresolved conversation blocks where the base requires them resolved, and only there",
    edits: [{ file: "src/pr.mjs",
              find: "      else if (r?.type === \"pull_request\") threadResolution ||= r.parameters?.required_review_thread_resolution === true;",
              replace: "      else if (r?.type === \"pull_request\") void 0;" }],
  },
  {
    name: "protection-signatures-unevaluated",
    why: "ignore classic protection's signed-commit requirement. An unsigned commit then blocks at GitHub while the verdict passes BLOCKED",
    test: "test/own-check-blocked.test.mjs",
    expectRed: "and signatures, a locked branch or push restrictions are named, and required conversation resolution is read",
    edits: [{ file: "src/pr.mjs",
              find: "      if (p.required_signatures?.enabled) unevaluated.push(\"protection required_signatures\");\n",
              replace: "" }],
  },
  {
    name: "protection-lock-unevaluated",
    why: "ignore a locked base. Nothing merges into it, and the verdict passes BLOCKED",
    test: "test/own-check-blocked.test.mjs",
    expectRed: "and signatures, a locked branch or push restrictions are named, and required conversation resolution is read",
    edits: [{ file: "src/pr.mjs",
              find: "      if (p.lock_branch?.enabled) unevaluated.push(\"protection lock_branch\");\n",
              replace: "" }],
  },
  {
    name: "protection-restrictions-unevaluated",
    why: "ignore classic protection's push restrictions. Who may merge is then limited in a way the verdict doesn't see",
    test: "test/own-check-blocked.test.mjs",
    expectRed: "and signatures, a locked branch or push restrictions are named, and required conversation resolution is read",
    edits: [{ file: "src/pr.mjs",
              find: "      if (p.restrictions) unevaluated.push(\"protection restrictions\");\n",
              replace: "" }],
  },
  {
    name: "protection-conversation-resolution",
    why: "ignore classic protection's required conversation resolution. An unresolved conversation then blocks at GitHub while the verdict passes BLOCKED",
    test: "test/own-check-blocked.test.mjs",
    expectRed: "and signatures, a locked branch or push restrictions are named, and required conversation resolution is read",
    edits: [{ file: "src/pr.mjs",
              find: "      if (p.required_conversation_resolution?.enabled) threadResolution = true;\n",
              replace: "" }],
  },
  {
    name: "requirements-classic-unreadable-is-unknown",
    why: "read classic protection that couldn't be read as none. A token without an administrator's read then passes BLOCKED on a base whose protection it can't see",
    test: "test/own-check-blocked.test.mjs",
    expectRed: "classic protection that couldn't be read leaves what else the base requires unknown",
    edits: [{ file: "src/pr.mjs",
              find: "    } else if (!/Branch not protected/.test(protection?.err ?? \"\")) whole = false;\n",
              replace: "    }\n" }],
  },
  {
    name: "requirements-unreadable-branch-is-unknown",
    why: "read a base whose branch couldn't be read as whole. The checks its classic protection requires are read only there, so a required check that blocks the merge drops out, and BLOCKED passes",
    test: "test/own-check-blocked.test.mjs",
    expectRed: "a branch that couldn't be read leaves the checks its protection requires unknown, so BLOCKED can't pass",
    edits: [{ file: "src/pr.mjs",
              find: "  if (!b) whole = false;\n",
              replace: "" }],
  },
  {
    name: "requirements-classic-read-only-where-enabled",
    why: "ask for classic protection on every base. Most bases only rulesets protect, and a token without an administrator's read then reads their requirements as unknown, so BLOCKED never passes",
    test: "test/own-check-blocked.test.mjs",
    expectRed: "a base only rulesets protect, requiring reeve's check alone, leaves nothing else outstanding, and classic protection isn't asked for",
    edits: [{ file: "src/pr.mjs",
              find: "    protection: classicProtection(parsed(branch)) === false ? null : gh([`${path}/protection`]),",
              replace: "    protection: gh([`${path}/protection`])," }],
  },
  {
    name: "other-checks-honour-app-binding",
    why: "match another required check by name alone. A run of that name from a different App then counts, while GitHub waits for the App the rule names",
    test: "test/own-check-blocked.test.mjs",
    expectRed: "each other required check is read at the head: passing, failing, running, never reported, or run by an App other than the one it's bound to",
    edits: [{ file: "src/pr.mjs",
              find: "  const candidates = app == null ? named : named.filter((r) => r.source === \"check_run\" && String(r.appId) === app);",
              replace: "  const candidates = named;" }],
  },
  {
    name: "other-checks-unread-head-is-unknown",
    why: "judge the other required checks from a head whose checks couldn't be read. Every one then reads as never reported, and the reason is lost",
    test: "test/own-check-blocked.test.mjs",
    expectRed: "and so do rules that couldn't be read, or a head whose checks couldn't be",
    edits: [{ file: "src/pr.mjs",
              find: "  parts.others = req.others && Array.isArray(rows) ? req.others.map((r) => ({ ...r, state: requiredCheckState(rows, r) })) : null;",
              replace: "  parts.others = req.others ? req.others.map((r) => ({ ...r, state: requiredCheckState(rows ?? [], r) })) : null;" }],
  },
  {
    name: "checks-carry-app-id",
    why: "drop the App's id from the head's check runs. A required check bound to an App then never matches its run, and BLOCKED never passes",
    test: "test/own-check-blocked.test.mjs",
    expectRed: "the head's check runs carry their App's id, so a check bound to that App is met by its run and by no other App's",
    edits: [{ file: "src/github/reconciler.mjs",
              find: "    appId: c.app?.id != null ? String(c.app.id) : null,\n",
              replace: "" }],
  },
  {
    name: "shadow-unsuperseded-fails-publication",
    why: "report a publication clean while a passing result under the enforcement name couldn't be superseded. That result passes the check if a rule requires it, and nobody is told",
    test: "test/shadow-required-check.test.mjs",
    expectRed: "a passing result under the enforcement name that can't be superseded fails the publication, and says why",
    edits: [{ file: "src/pr.mjs",
              find: "  if (unwritten || left)\n",
              replace: "  if (unwritten)\n" }],
  },
  {
    name: "shadow-unreadable-runs-fail-publication",
    why: "report a publication clean when the runs couldn't be read to look for a passing result under the enforcement name. One may stand there, passing that check",
    test: "test/shadow-required-check.test.mjs",
    expectRed: "and so does one that can't read the runs to look for it, though its own result is still published",
    edits: [{ file: "src/pr.mjs",
              find: "    if (!runs) left = `the check runs at ${verdict.head.slice(0, 8)} couldn't be read, so a passing result an earlier version may have left there under ${context} couldn't be superseded`;\n    else if",
              replace: "    if" }],
  },
  {
    name: "shadow-held-says-unjudged",
    why: "say only that pull requests are blocked when a passing result under the required name couldn't be superseded. That pull request can merge unjudged, and the person is told the opposite",
    test: "test/shadow-required-check.test.mjs",
    expectRed: "with a rule requiring the check, a passing result that can't be superseded is held as a pull request that can merge unjudged",
    edits: [{ file: "src/pr.mjs",
              find: "    if (left && required !== false)\n      held = ",
              replace: "    if (false)\n      held = " }],
  },
  {
    name: "daemon-raises-held-on-failure",
    why: "raise what a publication held only when it succeeded. One that couldn't supersede a passing result under the required name is the case where the pull request can merge unjudged",
    test: "test/shadow-required-check.test.mjs",
    expectRed: "a failed publication still raises what it held, and logs the failure",
    edits: [{ file: "src/daemon.mjs",
              find: "    if (pub.held) {\n      log(logPath, `    shadow: ${pub.held}`);",
              replace: "    else if (pub.held) {\n      log(logPath, `    shadow: ${pub.held}`);" }],
  },
  {
    name: "shadow-held-unread-runs-may-pass",
    why: "say every pull request is blocked when the runs couldn't be read to rule out a passing result under the required name. One may stand there, and the person is told the opposite",
    test: "test/shadow-required-check.test.mjs",
    expectRed: "and so is one whose runs couldn't be read to rule it out",
    edits: [{ file: "src/pr.mjs",
              find: "    if (left && required !== false)\n",
              replace: "    if (left && stale && required !== false)\n" }],
  },
  {
    name: "shadow-updates-only-its-own-runs",
    why: "take another App's run under the same name for reeve's own. Reeve then updates a run it may not, and its own passing result under the enforcement name stands",
    test: "test/shadow-required-check.test.mjs",
    expectRed: "another App's runs under the same names are never updated, and reeve's own passing result under the enforcement name is still superseded",
    edits: [{ file: "src/pr.mjs",
              find: "rows.filter((c) => c.name === n && c.app === POLICY_APP)",
              replace: "rows.filter((c) => c.name === n)" }],
  },
  {
    name: "other-checks-need-every-result",
    why: "count a required check as passing once any result under its name passes. A passing status beside a failing check run of the same name then passes, while GitHub still holds the merge",
    test: "test/own-check-blocked.test.mjs",
    expectRed: "a check reported both as a status and as a check run passes only when both do",
    edits: [{ file: "src/pr.mjs",
              find: "  if (candidates.some((r) => r.state === \"completed\" && !passes(r) && !superseded(r))) return \"failing\";",
              replace: "  if (candidates.some((r) => r.state === \"completed\" && passes(r))) return \"passing\";" }],
  },
  {
    name: "checks-read-every-page",
    why: "read one page of a head's check runs. A required check on a later page then reads as missing, and a failing result there goes unseen beside a passing one of the same name",
    test: "test/own-check-blocked.test.mjs",
    expectRed: "a head's check runs and statuses are read past their first page, 100 at a time",
    edits: [{ file: "src/github/reconciler.mjs",
              find: "\".check_runs[]\", { paginate: true }",
              replace: "\".check_runs[]\"" }],
  },
  {
    name: "statuses-read-every-page",
    why: "read one page of a head's statuses, 30 of them by default. A status past it goes unread, failing or not",
    test: "test/own-check-blocked.test.mjs",
    expectRed: "a head's check runs and statuses are read past their first page, 100 at a time",
    edits: [{ file: "src/github/reconciler.mjs",
              find: "const st = gh(`repos/${nwo}/commits/${sha}/status?per_page=100`, \".statuses[]\", { paginate: true });",
              replace: "const st = gh(`repos/${nwo}/commits/${sha}/status`, \".statuses[]\");" }],
  },
  {
    name: "checks-whole-needs-both-surfaces",
    why: "call a read of a head's checks ok when only one of check runs and statuses was read. A failing result on the unread one then hides behind a passing one of the same name",
    test: "test/own-check-blocked.test.mjs",
    expectRed: "a head whose check runs or statuses couldn't be read gives the base's other checks nothing to pass on, so BLOCKED is UNKNOWN",
    edits: [{ file: "src/github/reconciler.mjs",
              find: "  const ok = crRead && stRead;",
              replace: "  const ok = crRead || stRead;" }],
  },
  {
    name: "merge-rows-need-a-whole-read",
    why: "judge the base's other required checks from a partial read of the head's checks. A failing result on the unread surface then goes unseen, and BLOCKED can pass",
    test: "test/own-check-blocked.test.mjs",
    expectRed: "a head whose check runs or statuses couldn't be read gives the base's other checks nothing to pass on, so BLOCKED is UNKNOWN",
    edits: [{ file: "src/pr.mjs",
              find: "(read?.ok ? [",
              replace: "(read ? [" }],
  },
  {
    name: "shadow-failed-write-still-cleans-up",
    why: "stop at a failed shadow write. A passing result an earlier version left under the required name then stands, and the rule requiring it goes unsaid",
    test: "test/shadow-required-check.test.mjs",
    expectRed: "a shadow write that fails still supersedes a passing result under the required name, and still says the rule requires it",
    edits: [{ file: "src/pr.mjs",
              find: "  if (!res.ok && !shadow) return { ok: false, why: res.err.split(\"\\n\")[0] };",
              replace: "  if (!res.ok) return { ok: false, why: res.err.split(\"\\n\")[0] };" }],
  },
  {
    name: "daemon-reads-requirements-each-tick",
    why: "keep what each base requires across ticks. A rule added between two ticks then goes unseen for as long as the reading is kept, and BLOCKED can pass without it",
    test: "test/shadow-required-check.test.mjs",
    expectRed: "each tick starts from a fresh reading of what the base requires, however soon it follows the last",
    edits: [{ file: "src/daemon.mjs",
              find: "  clearRequirements();\n",
              replace: "" }],
  },
  {
    name: "other-checks-superseded-waits",
    why: "count a required check whose run was cancelled or went stale as failing. A branch waiting for the run that replaces it is then blocked, and escalated",
    test: "test/own-check-blocked.test.mjs",
    expectRed: "a required check whose run was cancelled or went stale is superseded, not failing",
    edits: [{ file: "src/pr.mjs",
              find: "&& !passes(r) && !superseded(r))) return \"failing\";",
              replace: "&& !passes(r))) return \"failing\";" }],
  },
  {
    name: "blocked-superseded-waits",
    why: "read a superseded required check as nothing outstanding. BLOCKED then passes while GitHub waits for the run that replaces it",
    test: "test/own-check-blocked.test.mjs",
    expectRed: "a required check whose run was cancelled or went stale waits for the run that replaces it: UNKNOWN, never BLOCK",
    edits: [{ file: "src/verdict.mjs",
              find: "...others(\"running\"), ...others(\"superseded\"), ",
              replace: "...others(\"running\"), " }],
  },
  {
    name: "requirements-read-strict-rule",
    why: "ignore a rule that wants branches up to date. A head behind its base then passes BLOCKED while GitHub waits for it to be updated",
    test: "test/own-check-blocked.test.mjs",
    expectRed: "a base that wants branches up to date, by a rule or by classic protection, has the head's distance behind read, and only then",
    edits: [{ file: "src/pr.mjs",
              find: "        strict ||= r.parameters?.strict_required_status_checks_policy === true;\n",
              replace: "" }],
  },
  {
    name: "requirements-read-strict-protection",
    why: "ignore classic protection that wants branches up to date. A head behind its base then passes BLOCKED while GitHub waits for it to be updated",
    test: "test/own-check-blocked.test.mjs",
    expectRed: "a base that wants branches up to date, by a rule or by classic protection, has the head's distance behind read, and only then",
    edits: [{ file: "src/pr.mjs",
              find: "      if (p.required_status_checks?.strict) strict = true;\n",
              replace: "" }],
  },
  {
    name: "merge-parts-read-how-far-behind",
    why: "never read how far the head is behind a base that wants branches up to date. Whether it is behind is then unknown, and BLOCKED can't pass even when it isn't",
    test: "test/own-check-blocked.test.mjs",
    expectRed: "a base that wants branches up to date, by a rule or by classic protection, has the head's distance behind read, and only then",
    edits: [{ file: "src/pr.mjs",
              find: "  if (req.strict && head) {",
              replace: "  if (false) {" }],
  },
  {
    name: "blocked-behind-a-strict-base-blocks",
    why: "pass BLOCKED with the head behind a base that wants branches up to date. GitHub refuses the merge until it is updated",
    test: "test/own-check-blocked.test.mjs",
    expectRed: "a base that wants branches up to date blocks one that is behind, passes one that isn't, and is UNKNOWN when that couldn't be read",
    edits: [{ file: "src/verdict.mjs",
              find: "    else if (parts.strict === true && parts.behind > 0) add(\"mergeable\", BLOCK, `mergeStateStatus BLOCKED: the base requires branches up to date, and this one is ${parts.behind} commit(s) behind`);\n",
              replace: "" }],
  },
  {
    name: "blocked-unmeasured-behind-is-unknown",
    why: "pass BLOCKED when how far behind the head is couldn't be read, on a base that wants branches up to date. It may be behind",
    test: "test/own-check-blocked.test.mjs",
    expectRed: "a base that wants branches up to date blocks one that is behind, passes one that isn't, and is UNKNOWN when that couldn't be read",
    edits: [{ file: "src/verdict.mjs",
              find: "\n             || parts.strict == null || (parts.strict && !Number.isInteger(parts.behind)))",
              replace: ")" }],
  },
  {
    name: "shadow-supersedes-before-writing",
    why: "write the shadow result before superseding a passing result under the enforcement name. A run stopped between the two leaves that result passing the check",
    test: "test/shadow-required-check.test.mjs",
    expectRed: "the passing result under the enforcement name is superseded before the shadow result is written",
    edits: [{ file: "src/pr.mjs",
              find: "  let superseded = false, left = null, stale = null;\n",
              replace: "  void api(auth.token, [\"-X\", \"POST\", `repos/${nwo}/check-runs`, \"-f\", `name=${name}`, \"-f\", `head_sha=${verdict.head}`, ...fields]);\n  let superseded = false, left = null, stale = null;\n" }],
  },
  {
    name: "shadow-held-when-requirement-unknown",
    why: "raise a passing result that can't be superseded only when a rule is known to require the check. With the rules unread it may be required, and the pull request may merge unjudged unseen",
    test: "test/shadow-required-check.test.mjs",
    expectRed: "a passing result that can't be superseded, on a base whose rules couldn't be read, is held as one that may merge unjudged",
    edits: [{ file: "src/pr.mjs",
              find: "    if (left && required !== false)\n",
              replace: "    if (left && required === true)\n" }],
  },
  {
    name: "requirements-foreign-results-beside-own",
    why: "judge only reeve's own result when its check is required with no App bound. Another's failing result under the same name then goes unseen, while GitHub holds the merge for it",
    test: "test/own-check-blocked.test.mjs",
    expectRed: "with reeve's check required and no App bound, another's failing result under its name blocks, and none at all is nothing outstanding",
    edits: [{ file: "src/pr.mjs",
              find: "    else if (bound == null || Number(bound) === -1) others.push({ context: c, app: null, besideOwn: true });\n",
              replace: "" }],
  },
  {
    name: "other-checks-beside-own-absent-is-clear",
    why: "count the absence of any other result under reeve's own name as a missing check. BLOCKED then waits for a result that is never coming, and can't pass",
    test: "test/own-check-blocked.test.mjs",
    expectRed: "with reeve's check required and no App bound, another's failing result under its name blocks, and none at all is nothing outstanding",
    edits: [{ file: "src/pr.mjs",
              find: "return besideOwn ? \"passing\" : app != null",
              replace: "return app != null" }],
  },
  {
    name: "shadow-unbound-requirement-checks-others",
    why: "treat a rule requiring reeve's check with no App bound as blocking every pull request. Another App's run or a commit status passing under the name then satisfies it, and the pull request merges unjudged while shadow mode says it is blocked",
    test: "test/shadow-required-check.test.mjs",
    expectRed: "a rule requiring reeve's check with no App bound, where another App's run or a commit status passes under its name, is held as a pull request that can merge unjudged",
    edits: [{ file: "src/pr.mjs",
              find: "    const unbound = required === true && !(",
              replace: "    const unbound = false && !(" }],
  },
  {
    name: "shadow-unbound-reads-statuses",
    why: "look only at other Apps' runs under reeve's name. A commit status passing under it then satisfies an unbound rule unseen",
    test: "test/shadow-required-check.test.mjs",
    expectRed: "a rule requiring reeve's check with no App bound, where another App's run or a commit status passes under its name, is held as a pull request that can merge unjudged",
    edits: [{ file: "src/pr.mjs",
              find: ",\n                   ...states.map((state) => state === \"success\")];",
              replace: "];" }],
  },
  {
    name: "other-checks-expire-after-a-week",
    why: "count a required check's pass however old it is. GitHub holds the merge for a rerun once a pass is seven days old, and BLOCKED then passes while it waits",
    test: "test/own-check-blocked.test.mjs",
    expectRed: "another required check's pass older than seven days, or of no readable time, no longer counts: BLOCKED waits for a rerun",
    edits: [{ file: "src/pr.mjs",
              find: "  if (candidates.some((r) => !(now - Date.parse(r.completedAt) <= REQUIRED_RESULT_MS))) return \"expired\";\n",
              replace: "" }],
  },
  {
    name: "blocked-expired-waits",
    why: "read an expired required check as nothing outstanding. BLOCKED then passes while GitHub waits for it to rerun",
    test: "test/own-check-blocked.test.mjs",
    expectRed: "another required check's pass older than seven days, or of no readable time, no longer counts: BLOCKED waits for a rerun",
    edits: [{ file: "src/verdict.mjs",
              find: "...others(\"expired\"), ",
              replace: "" }],
  },
  {
    name: "checks-carry-completion-time",
    why: "drop when each check run finished. Every required check's pass then reads as of no known time, and never counts",
    test: "test/own-check-blocked.test.mjs",
    expectRed: "the head's check runs carry their App's id, so a check bound to that App is met by its run and by no other App's",
    edits: [{ file: "src/github/reconciler.mjs",
              find: "    completedAt: c.completed_at ?? null,\n",
              replace: "" }],
  },
  {
    name: "shadow-unbound-needs-every-foreign-pass",
    why: "call an unbound rule exposed once any other result under reeve's name passes. GitHub needs every result there to pass, so one beside a failure is still blocked, and the person is paged for nothing",
    test: "test/shadow-required-check.test.mjs",
    expectRed: "and one where another's pass stands beside another's failure under the name is blocked, not exposed",
    edits: [{ file: "src/pr.mjs",
              find: "  return results.length > 0 && results.every(Boolean);",
              replace: "  return results.some(Boolean);" }],
  },
  {
    name: "requirements-keep-only-whole-readings",
    why: "keep a partial reading of a base as if it were whole. A token without the reads it needed then decides for the rest of the tick, and reeve's App, which could read the base, is never asked",
    test: "test/shadow-required-check.test.mjs",
    expectRed: "a partial reading of a base isn't kept, so the next caller reads it again",
    edits: [{ file: "src/pr.mjs",
              find: "  if (value.own !== null && value.others !== null) REQUIRED.set(key, { at: now, value });",
              replace: "  REQUIRED.set(key, { at: now, value });" }],
  },
  {
    name: "init-creates-the-state-database",
    why: "stop creating the store. A fresh machine then has no way to start the daemon at all, since `reeve run` rightly refuses a missing store",
    test: "test/init-store.test.mjs",
    expectRed: "on a fresh machine, init creates the state database",
    edits: [{ file: "src/init.mjs",
              find: "    openStore(tmp).close();\n",
              replace: "" }],
  },
  {
    name: "init-leaves-an-existing-store-alone",
    why: "reopen an existing store on every init. Opening runs the schema's reshaping, so a routine init would rewrite a live store the daemon is using",
    test: "test/init-store.test.mjs",
    expectRed: "an existing state database is left alone",
    edits: [{ file: "src/init.mjs",
              find: '  if (status.state === "exists") { clearMoveLock(status.path); return { changed: false, line: null }; }\n',
              replace: "" }],
  },
  {
    name: "init-moves-a-legacy-store",
    why: "create a fresh store beside one at the legacy path. The daemon then reads the empty store, and the real history sits unread at the old path with nothing failing",
    test: "test/init-store.test.mjs",
    expectRed: "a legacy store is moved into place, never replaced by an empty one",
    edits: [{ file: "src/init.mjs",
              find: '  if (status.state === "legacy") {',
              replace: "  if (false) {" }],
  },
  {
    name: "missing-store-names-the-fix",
    why: "say only that the store is missing. On a fresh machine that is a dead end, because nothing says which step creates one",
    test: "test/init-store.test.mjs",
    expectRed: "a missing store's message names the step that creates it",
    edits: [{ file: "src/paths.mjs",
              find: "  return `no state database at ${dbPath}\\n-> reeve init --write   creates it, run inside ${nwo ? `a checkout of ${nwo}` : \"the repository's checkout\"}`;",
              replace: "  return `no state database at ${dbPath}`;" }],
  },
  {
    name: "missing-store-hint-only-for-the-default-path",
    why: "name init whatever --db says. init creates the store at the default path, so a reader whose --db names another file is sent to a step that doesn't create it",
    test: "test/init-store.test.mjs",
    expectRed: "a --db that names a missing file isn't answered with init, which wouldn't create it",
    edits: [{ file: "src/paths.mjs",
              find: "  if (!initCreatesIt) return `no state database at ${dbPath}`;\n",
              replace: "" }],
  },
  {
    name: "missing-store-backup-names-init",
    why: "let reeve backup say only that the store is missing. On a fresh machine that is a dead end, because nothing says which step creates one",
    test: "test/init-store.test.mjs",
    expectRed: "every command that needs the state database names the step that creates it",
    edits: [{ file: "bin/reeve",
              find: "    if (!existsSync(dbPath)) { console.error(`reeve backup: ${missingStoreMessage(dbPath, { initCreatesIt: !opt(\"db\"), nwo })}`); process.exit(1); }",
              replace: "    if (!existsSync(dbPath)) { console.error(`reeve backup: no state at ${dbPath}`); process.exit(1); }" }],
  },
  {
    name: "missing-store-shadow-names-init",
    why: "let reeve shadow say only that the store is missing. On a fresh machine that is a dead end, because nothing says which step creates one",
    test: "test/init-store.test.mjs",
    expectRed: "every command that needs the state database names the step that creates it",
    edits: [{ file: "bin/reeve",
              find: "    if (!existsSync(dbPath)) { console.error(`reeve shadow: ${missingStoreMessage(dbPath, { initCreatesIt: !opt(\"db\"), nwo })}`); process.exit(1); }",
              replace: "    if (!existsSync(dbPath)) { console.error(`reeve shadow: no state at ${dbPath}`); process.exit(1); }" }],
  },
  {
    name: "missing-store-status-names-init",
    why: "let reeve status, dash and why say only that the store is missing. On a fresh machine that is a dead end, because nothing says which step creates one",
    test: "test/init-store.test.mjs",
    expectRed: "every command that needs the state database names the step that creates it",
    edits: [{ file: "bin/reeve",
              find: "      fail(\"store_absent\", dbPath ? `reeve ${cmd}: ${missingStoreMessage(dbPath, { initCreatesIt: !opt(\"db\"), nwo })}` : `reeve ${cmd}: no state database`);",
              replace: "      fail(\"store_absent\", `reeve ${cmd}: no state database${dbPath ? ` at ${dbPath}` : \"\"}`);" }],
  },
  {
    name: "init-legacy-move-failure-says-why",
    why: "report a failed move without its reason. The person running init then learns the history stayed at the old path but not what to fix",
    test: "test/init-store.test.mjs",
    expectRed: "a legacy store that can't be moved says why, and stays where it was",
    edits: [{ file: "src/init.mjs",
              find: "      : { changed: false, failed: true, line: said ?? `could not move the legacy store; using ${used}` };",
              replace: "      : { changed: false, failed: true, line: `could not move the legacy store; using ${used}` };" }],
  },
  {
    name: "legacy-move-main-file-last",
    why: "move the main file before its -wal. A failure between them leaves the canonical store without the committed writes still in the WAL, and every later run takes it for the whole store",
    test: "test/init-store.test.mjs",
    expectRed: "a move killed after any file is finished by the next run, with every committed row",
    edits: [{ file: "src/paths.mjs",
              find: "      for (const suffix of [\"-wal\", \"-shm\", \"\"]) {",
              replace: "      for (const suffix of [\"\", \"-wal\", \"-shm\"]) {" }],
  },
  {
    name: "legacy-move-puts-back-on-failure",
    why: "leave the moved sidecars where they are when the main file fails to move. The legacy store then opens without its WAL, and the WAL waits at the new path for a main file that isn't its own",
    test: "test/init-store.test.mjs",
    expectRed: "a move that fails on the main file leaves the store whole at one path, never split",
    edits: [{ file: "src/paths.mjs",
              find: "      for (const suffix of moved.reverse()) try { rename(next + suffix, legacy + suffix); } catch { /* found by whole() */ }\n",
              replace: "" }],
  },
  {
    name: "init-create-is-atomic",
    why: "create the store at its final path. A create that stops partway then leaves a half-made store the next init reports as done",
    test: "test/init-store.test.mjs",
    expectRed: "an interrupted create leaves no store at the canonical path, so the next init makes it again",
    edits: [{ file: "src/init.mjs",
              find: "    openStore(tmp).close();\n",
              replace: "    openStore(status.path).close();\n" }],
  },
  {
    name: "init-clears-killed-creates",
    why: "leave what a killed init made. Its half-made temporary files then sit beside the store for ever",
    test: "test/init-store.test.mjs",
    expectRed: "the next init creates the store and clears what a killed init left",
    edits: [{ file: "src/init.mjs",
              find: "  clearInterruptedCreates(dir, stem);\n",
              replace: "" }],
  },
  {
    name: "init-failed-store-exits-1",
    why: "exit 2 whether or not the store was made. Automation then reads the init as applied while the database is missing",
    test: "test/init-store.test.mjs",
    expectRed: "init exits 1 when the store couldn't be made or moved, and 2 only when everything was applied",
    edits: [{ file: "src/init.mjs",
              find: "  return { code: made.failed ? 1 : 2, output: out };",
              replace: "  return { code: 2, output: out };" }],
  },
  {
    name: "missing-store-names-the-checkout",
    why: "name the step without the repository. A service runs reeve run owner/repo from anywhere, and init run from there detects whatever checkout it happens to be in",
    test: "test/init-store.test.mjs",
    expectRed: "the step names the repository whose checkout to run it in",
    edits: [{ file: "src/paths.mjs",
              find: "  return `no state database at ${dbPath}\\n-> reeve init --write   creates it, run inside ${nwo ? `a checkout of ${nwo}` : \"the repository's checkout\"}`;",
              replace: "  return `no state database at ${dbPath}\\n-> reeve init --write   creates it`;" }],
  },
  {
    name: "legacy-move-takes-a-lock",
    why: "move without the lock. Two commands started together then move each other's sidecars, and a rollback can strand the WAL at the old path",
    test: "test/init-store.test.mjs",
    expectRed: "while another process moves the store, this one waits and uses what it made, moving nothing itself",
    edits: [{ file: "src/paths.mjs",
              find: "    lock.exec(\"BEGIN EXCLUSIVE\");\n",
              replace: "" }],
  },
  {
    name: "legacy-move-releases-its-lock",
    why: "keep the lock after a move that failed. The next run in the same process, the daemon's next tick say, then waits on a lock nobody is using and never finishes the move",
    test: "test/init-store.test.mjs",
    expectRed: "a move killed after any file is finished by the next run, with every committed row",
    edits: [{ file: "src/paths.mjs",
              find: "    try { lock.exec(\"ROLLBACK\"); } catch { /* nothing to undo */ }\n    lock.close();\n",
              replace: "" }],
  },
  {
    name: "legacy-move-fails-loudly-when-held",
    why: "use the old path while another live process is still moving the store. The command then opens a store split between two paths",
    test: "test/init-store.test.mjs",
    expectRed: "a holder part way through a move is never taken for dead, whatever its lock file says, and one that doesn't finish in time is refused, touching nothing",
    edits: [{ file: "src/paths.mjs",
              find: "      throw storeError(\"STORE_BUSY\", `the state store at ${legacy} is being moved to ${next} by another process; try again once it finishes`);",
              replace: "      return legacy;" }],
  },
  {
    name: "legacy-move-refuses-a-split-store",
    why: "answer with the legacy path while part of the store sits at the new one. The legacy main file, opened without its WAL, hides every committed write still in that WAL",
    test: "test/init-store.test.mjs",
    expectRed: "a move whose rollback also fails refuses both paths, and says which files are where",
    edits: [{ file: "src/paths.mjs",
              find: "  if (split.length)\n    throw storeError(\"STORE_SPLIT\",",
              replace: "  if (false)\n    throw storeError(\"STORE_SPLIT\"," }],
  },
  {
    name: "legacy-move-removes-its-lock-file",
    why: "leave the lock file once the move is done. It then sits beside the store for good, one per repository that ever had a legacy store",
    test: "test/init-store.test.mjs",
    expectRed: "nothing but the store is left beside it once the move is done",
    edits: [{ file: "src/paths.mjs",
              find: "    try { rmSync(lockPath, { force: true }); } catch { /* left for the next move */ }\n",
              replace: "" }],
  },
  {
    name: "cli-refuses-an-unusable-store",
    why: "let a store that can't be used escape as an exception. Every command then dies on a stack trace, and a script reading --json gets no answer at all",
    test: "test/init-store.test.mjs",
    expectRed: "a command given a store it can't use refuses it with a reason, not a stack trace",
    edits: [{ file: "bin/reeve",
              find: "  try { return adoptLegacyStore(statePathFor(HOME, nwo), legacyStatePathFor(HOME, nwo)); }\n  catch (e) { fail(\"store_unusable\", `reeve ${cmd}: ${e.message}`, { retryable: e.code === \"STORE_BUSY\" }); }\n",
              replace: "  return adoptLegacyStore(statePathFor(HOME, nwo), legacyStatePathFor(HOME, nwo));\n" }],
  },
  {
    name: "legacy-move-clears-a-killed-movers-lock",
    why: "leave the lock file of a mover killed after its last rename. Nothing looks for it again once the store is in place, so it stays beside the store for good",
    test: "test/init-store.test.mjs",
    expectRed: "a mover killed after its last rename leaves nothing behind once the next run finds the store",
    edits: [{ file: "src/paths.mjs",
              find: "  if (existsSync(next)) { clearMoveLock(next); return next; }",
              replace: "  if (existsSync(next)) return next;" }],
  },
  {
    name: "legacy-move-lock-journal-in-memory",
    why: "keep the lock's journal on disk. A holder killed while holding the lock then leaves the journal beside the store for good",
    test: "test/init-store.test.mjs",
    expectRed: "a mover killed after its last rename leaves nothing behind once the next run finds the store",
    edits: [{ file: "src/paths.mjs",
              find: "    lock.exec(\"PRAGMA journal_mode=MEMORY\");\n",
              replace: "" }],
  },
  {
    name: "init-clears-a-killed-movers-lock",
    why: "return from init as soon as the store is in place. A lock a mover killed after its last rename left beside it then stays for good when init is what runs next",
    test: "test/init-store.test.mjs",
    expectRed: "and so does reeve init --write, which finds the store in place",
    edits: [{ file: "src/init.mjs",
              find: "{ clearMoveLock(status.path); return { changed: false, line: null }; }",
              replace: "return { changed: false, line: null };" }],
  },
  {
    name: "init-publish-never-overwrites",
    why: "publish the new store over whatever is at the path. An init that lost to another then replaces the store the first one made, and whatever was written to it since",
    test: "test/init-store.test.mjs",
    expectRed: "an init that loses to another publishes nothing over the store it made, and cleans up",
    edits: [{ file: "src/init.mjs",
              find: "    try { linkSync(tmp, status.path); }",
              replace: "    try { rmSync(status.path, { force: true }); linkSync(tmp, status.path); }" }],
  },
  {
    name: "init-folder-errors-are-failures",
    why: "prepare the state folder outside the handled path. A file where the folder belongs then crashes init with a stack trace, possibly after the profile was written",
    test: "test/init-store.test.mjs",
    expectRed: "a state folder that can't be made is a failure that says why, not a crash",
    edits: [{ file: "src/init.mjs",
              find: "  try {\n    mkdirSync(dir, { recursive: true });\n",
              replace: "  mkdirSync(dir, { recursive: true });\n  try {\n" }],
  },
  {
    name: "init-cleanup-never-throws",
    why: "let the cleanup throw. On the failure path the folder may be a file, and the cleanup's own error then replaces the failure it was cleaning up after",
    test: "test/init-store.test.mjs",
    expectRed: "a state folder that can't be made is a failure that says why, not a crash",
    edits: [{ file: "src/init.mjs",
              find: "  const clear = () => { for (const s of [\"\", \"-wal\", \"-shm\", \"-journal\"]) try { rmSync(tmp + s, { force: true }); } catch { /* nothing to remove */ } };",
              replace: "  const clear = () => { for (const s of [\"\", \"-wal\", \"-shm\", \"-journal\"]) rmSync(tmp + s, { force: true }); };" }],
  },
  {
    name: "board-closed-is-done",
    why: "let a closed task keep another column. Finished work then shows as blocked or in progress, and the board overstates what is left",
    test: "test/resume-work-board.test.mjs",
    expectRed: "a closed task is Done, whatever else is true of it",
    edits: [{ file: ".agents/skills/resume-work/scripts/lib.mjs",
              find: "  if (!open) return \"Done\";\n",
              replace: "" }],
  },
  {
    name: "board-blocker-is-blocked",
    why: "ignore an open blocker. A task that can't proceed then reads as ready or under way",
    test: "test/resume-work-board.test.mjs",
    expectRed: "an open task with an open blocker is Blocked, even with a pull request",
    edits: [{ file: ".agents/skills/resume-work/scripts/lib.mjs",
              find: "  if (blocked) return \"Blocked\";\n",
              replace: "" }],
  },
  {
    name: "board-ready-pr-is-in-review",
    why: "put every task with a pull request In progress. A pull request waiting only on a person's merge then looks like work still being done",
    test: "test/resume-work-board.test.mjs",
    expectRed: "a task whose pull request is ready and waiting on a person is In review",
    edits: [{ file: ".agents/skills/resume-work/scripts/lib.mjs",
              find: "  if (closers.length) return closers.every((pr) => { const t = triagePullRequest(pr, null); return !t.reasons.length && t.settled; })\n    ? \"In review\" : \"In progress\";\n",
              replace: "  if (closers.length) return \"In progress\";\n" }],
  },
  {
    name: "board-pr-needing-author-is-in-progress",
    why: "put every task with a pull request In review. A draft, or one with open findings, then looks like it waits on a person, and In progress reads empty while work is being done",
    test: "test/resume-work-board.test.mjs",
    expectRed: "a task whose pull request is still a draft is In progress",
    edits: [{ file: ".agents/skills/resume-work/scripts/lib.mjs",
              find: "  if (closers.length) return closers.every((pr) => { const t = triagePullRequest(pr, null); return !t.reasons.length && t.settled; })\n    ? \"In review\" : \"In progress\";\n",
              replace: "  if (closers.length) return \"In review\";\n" }],
  },
  {
    name: "board-one-author-pr-is-enough",
    why: "require every pull request to need its author before the task counts as in progress. A task with one ready and one draft pull request then reads as waiting on a person",
    test: "test/resume-work-board.test.mjs",
    expectRed: "one pull request still needing its author keeps the task In progress",
    edits: [{ file: ".agents/skills/resume-work/scripts/lib.mjs",
              find: "closers.every((pr) =>",
              replace: "closers.some((pr) =>" }],
  },
  {
    name: "board-assigned-is-in-progress",
    why: "ignore who holds a task. Claimed work with no pull request yet then reads as ready for anyone to take",
    test: "test/resume-work-board.test.mjs",
    expectRed: "an assigned task with no pull request is In progress",
    edits: [{ file: ".agents/skills/resume-work/scripts/lib.mjs",
              find: "  if (assigned) return \"In progress\";\n",
              replace: "" }],
  },
  {
    name: "board-open-task-is-ready",
    why: "put unclaimed work anywhere but Ready. The column people pick work from then misses it",
    test: "test/resume-work-board.test.mjs",
    expectRed: "an open, unblocked, unassigned task is Ready",
    edits: [{ file: ".agents/skills/resume-work/scripts/lib.mjs",
              find: "  return \"Ready\";\n}\n",
              replace: "  return \"In progress\";\n}\n" }],
  },
  {
    name: "board-picks-exact-columns",
    why: "accept a project that has the five columns among others. A board with a column the rules never fill then gets written as if it were the plan board",
    test: "test/resume-work-board.test.mjs",
    expectRed: "a project with other columns, or extra ones, is not taken for the board",
    edits: [{ file: ".agents/skills/resume-work/scripts/lib.mjs",
              find: "return !p.closed && names.length === BOARD_COLUMNS.length && BOARD_COLUMNS.every",
              replace: "return !p.closed && BOARD_COLUMNS.every" }],
  },
  {
    name: "board-two-candidates-named",
    why: "take the first of two projects that both look like the board. The sync then writes to one of them by accident of order",
    test: "test/resume-work-board.test.mjs",
    expectRed: "two projects that both look like the board are named, not guessed between",
    edits: [{ file: ".agents/skills/resume-work/scripts/lib.mjs",
              find: "  if (boards.length === 1) return { board: boards[0], why: null };",
              replace: "  if (boards.length >= 1) return { board: boards[0], why: null };" }],
  },
  {
    name: "board-reads-every-page",
    why: "stop after the first page of a connection and call it whole. A board past the first twenty linked projects then reads as absent, and cards past the first hundred are never seen",
    test: "test/resume-work-board.test.mjs",
    expectRed: "every page of a connection is read, and a list cut short says so",
    edits: [{ file: ".agents/skills/resume-work/scripts/lib.mjs",
              find: "    if (!conn.pageInfo?.hasNextPage) return { nodes, complete: true };",
              replace: "    return { nodes, complete: true };" }],
  },
  {
    name: "board-refuses-a-partial-read",
    why: "write the board from lists read in part. A task whose pull request went unread then leaves In review, and every such card moves the wrong way",
    test: "test/resume-work-board.test.mjs",
    expectRed: "a list read in part stops the sync before any card moves",
    edits: [{ file: ".agents/skills/resume-work/scripts/lib.mjs",
              find: "  return missing.length ? `not every one of the ${missing.join(\", \")} could be read, so no card was moved` : null;",
              replace: "  return null;" }],
  },
  {
    name: "board-finishes-closed-cards",
    why: "visit only the open phases' tasks. A phase closed with its last tasks is then never read again, and their cards stay In review or In progress",
    test: "test/resume-work-board.test.mjs",
    expectRed: "a closed task no open phase lists any more still goes to Done",
    edits: [{ file: ".agents/skills/resume-work/scripts/lib.mjs",
              find: "!visited.has(n) && c.state === \"CLOSED\" && (c.phase || c.parent != null))",
              replace: "!visited.has(n) && false && (c.phase || c.parent != null))" }],
  },
  {
    name: "board-reads-closing-issues-whole",
    why: "read only the first page of the issues a pull request will close. A task named on a later page then looks as if nothing will close it, and leaves In review",
    test: "test/resume-work-board.test.mjs",
    expectRed: "every issue a pull request will close is read, past the first page, so neither task looks ready",
    edits: [{ file: ".agents/skills/resume-work/scripts/lib.mjs",
              find: "  const closing = whole(pr.closingIssuesReferences, \"closing\");",
              replace: "  const closing = { conn: pr.closingIssuesReferences, complete: true };" }],
  },
  {
    name: "board-reads-head-checks-whole",
    why: "read only the first page of the checks at a pull request's head. A failing check on a later page then goes unseen, and its task moves to In review",
    test: "test/resume-work-board.test.mjs",
    expectRed: "every check at its head is read, so a failing one past the first page keeps its task In progress",
    edits: [{ file: ".agents/skills/resume-work/scripts/lib.mjs",
              find: "  const contexts = whole(commit?.statusCheckRollup?.contexts, \"contexts\");",
              replace: "  const contexts = { conn: commit?.statusCheckRollup?.contexts, complete: true };" }],
  },
  {
    name: "board-partial-pull-request-is-partial",
    why: "call a pull request whole when one of its lists couldn't be read to the end. The board then moves cards from part of a list",
    test: "test/resume-work-board.test.mjs",
    expectRed: "a pull request whose lists still couldn't be read whole is partial, and the board moves no card from it",
    edits: [{ file: ".agents/skills/resume-work/scripts/lib.mjs",
              find: "           partial: !(closing.complete && threads.complete && contexts.complete) };",
              replace: "           partial: false };" }],
  },
  {
    name: "board-plan-completes-pull-requests",
    why: "leave each pull request's lists at their first page in the plan. The board and the snapshot then judge tasks from part of them",
    test: "test/resume-work-board.test.mjs",
    expectRed: "every check at its head is read, so a failing one past the first page keeps its task In progress",
    edits: [{ file: ".agents/skills/resume-work/scripts/plan.mjs",
              find: "  prs.nodes = prs.nodes.map((pr) => completePullRequest(pr, (field, after) => nextPage(pr.number, field, after)));\n",
              replace: "" }],
  },
  {
    name: "board-blockers-every-page",
    why: "count a task's open blockers from the first page only. An open blocker on a later page then leaves the task Ready or In progress",
    test: "test/resume-work-board.test.mjs",
    expectRed: "a task's open blockers are counted from every page",
    edits: [{ file: ".agents/skills/resume-work/scripts/plan.mjs",
              find: "  return run([\"api\", \"--paginate\", `repos/${repo}/issues/${n}/dependencies/blocked_by?per_page=100`,",
              replace: "  return run([\"api\", `repos/${repo}/issues/${n}/dependencies/blocked_by?per_page=100`," }],
  },
  {
    name: "board-running-checks-stay-in-progress",
    why: "move a task to In review while its pull request's checks are still running. Nobody can act on it yet, and the column says a person can",
    test: "test/resume-work-board.test.mjs",
    expectRed: "a task whose pull request's checks are still running is In progress, not yet In review",
    edits: [{ file: ".agents/skills/resume-work/scripts/lib.mjs",
              find: "return !t.reasons.length && t.settled; })",
              replace: "return !t.reasons.length; })" }],
  },
  {
    name: "board-reads-review-threads-whole",
    why: "read only the first page of a pull request's review threads. One with more than a hundred then stays In progress for good, or an unresolved thread on a later page goes unseen",
    test: "test/resume-work-board.test.mjs",
    expectRed: "every review thread is read past the first page: all resolved reaches In review, and one open on a later page keeps it In progress",
    edits: [{ file: ".agents/skills/resume-work/scripts/lib.mjs",
              find: "  const threads = whole(pr.reviewThreads, \"threads\");",
              replace: "  const threads = { conn: pr.reviewThreads, complete: true };" }],
  },
  {
    name: "board-syncs-open-strays",
    why: "leave an open task alone once its phase has closed. Its card then keeps whatever column it had, whatever happens to the task",
    test: "test/resume-work-board.test.mjs",
    expectRed: "an open task no open phase lists is still set from its own issue",
    edits: [{ file: ".agents/skills/resume-work/scripts/lib.mjs",
              find: "  return [...cards].filter(([n, c]) => !visited.has(n) && c.state === \"OPEN\" && c.parent != null).map(([n, c]) => ({ number: n, ...c }));",
              replace: "  return [...cards].filter(([n, c]) => !visited.has(n) && false && c.parent != null).map(([n, c]) => ({ number: n, ...c }));" }],
  },
  {
    name: "board-skips-closed-projects",
    why: "take a closed project for the board. An old one kept with the same five columns is then written to, or stops the sync as a second candidate",
    test: "test/resume-work-board.test.mjs",
    expectRed: "a closed project is never the board, beside the open one or alone",
    edits: [{ file: ".agents/skills/resume-work/scripts/lib.mjs",
              find: "return !p.closed && names.length",
              replace: "return names.length" }],
  },
  {
    name: "board-reads-closed-phases",
    why: "leave a closed phase's tasks unread. A task left open under it keeps its column for good, and one closed without a card never appears in Done",
    test: "test/resume-work-board.test.mjs",
    expectRed: "a closed phase on the board is read, so its tasks are synced, and one without a card gets one",
    edits: [{ file: ".agents/skills/resume-work/scripts/lib.mjs",
              find: "filter(([, c]) => c.state === \"CLOSED\" && c.phase)",
              replace: "filter(([, c]) => false)" }],
  },
  {
    name: "board-strays-must-be-sub-issues",
    why: "sync every open card the plan didn't visit. An issue that is no one's sub-issue, on the board for another reason, then has its column overwritten",
    test: "test/resume-work-board.test.mjs",
    expectRed: "and an open card that is no one's sub-issue is no task, and is left as it is",
    edits: [{ file: ".agents/skills/resume-work/scripts/lib.mjs",
              find: "c.state === \"OPEN\" && c.parent != null)",
              replace: "c.state === \"OPEN\")" }],
  },
  {
    name: "board-reads-closed-tasks-sub-tasks",
    why: "read only closed phases, not a task closed with sub-tasks of its own inside an open phase. Its sub-tasks then keep their columns, or never get a card",
    test: "test/resume-work-board.test.mjs",
    expectRed: "a closed task with sub-tasks of its own is read even after its open phase visited it",
    edits: [{ file: ".agents/skills/resume-work/scripts/lib.mjs",
              find: "c.state === \"CLOSED\" && c.phase)",
              replace: "c.state === \"CLOSED\" && c.phase && c.parent == null)" }],
  },
  {
    name: "board-closed-cards-only-plan",
    why: "move every closed card the plan didn't visit to Done. A closed issue on the board for another reason then has its column overwritten",
    test: "test/resume-work-board.test.mjs",
    expectRed: "and a closed issue on the board for another reason, no sub-issue and no phase, is left as it is",
    edits: [{ file: ".agents/skills/resume-work/scripts/lib.mjs",
              find: " && c.state === \"CLOSED\" && (c.phase || c.parent != null))",
              replace: " && c.state === \"CLOSED\")" }],
  },
  {
    name: "board-unarchives-active-cards",
    why: "set an archived card's column and leave it archived. An active task then stays hidden from every view, whatever its column says",
    test: "test/resume-work-board.test.mjs",
    expectRed: "an active task's archived card comes back before its column is set, and a finished one stays archived",
    edits: [{ file: ".agents/skills/resume-work/scripts/lib.mjs",
              find: "Boolean(archived) && column !== \"Done\"",
              replace: "false" }],
  },
  {
    name: "board-leaves-finished-cards-archived",
    why: "bring every archived card back, finished ones included. An auto-archive for finished work is then undone on each sync",
    test: "test/resume-work-board.test.mjs",
    expectRed: "an active task's archived card comes back before its column is set, and a finished one stays archived",
    edits: [{ file: ".agents/skills/resume-work/scripts/lib.mjs",
              find: "Boolean(archived) && column !== \"Done\"",
              replace: "Boolean(archived)" }],
  },
  {
    name: "board-reads-found-closed-parents",
    why: "read only the closed parents on the board when syncing starts. A closed task with sub-tasks met along the way, with no card yet, then leaves its sub-tasks unread",
    test: "test/resume-work-board.test.mjs",
    expectRed: "a closed task with sub-tasks met while syncing is read in its turn, card or no card, however deep",
    edits: [{ file: ".agents/skills/resume-work/scripts/lib.mjs",
              find: "  for (const [n] of parents) for",
              replace: "  for (const n of [...parents.keys()]) for" }],
  },
  {
    name: "board-notes-closed-parents",
    why: "never note a closed task with sub-tasks as a parent to read. Its sub-tasks are then read only if its card was on the board from the start",
    test: "test/resume-work-board.test.mjs",
    expectRed: "a closed task with sub-tasks met while syncing is read in its turn, card or no card, however deep",
    edits: [{ file: ".agents/skills/resume-work/scripts/lib.mjs",
              find: "issue?.state === \"CLOSED\" && (issue.subIssues?.totalCount ?? 0) > 0",
              replace: "false" }],
  },
  {
    name: "detect-shell-words-name-no-program",
    why: "take a shell keyword for the program a script runs. A `for` loop over the test files is then reported as running a missing program called `for`, and the warning is false",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "a script that starts with a shell keyword is not reported broken",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "      if (leading && !t.quoted && (t.text === \"{\" || ctx.shell?.keywords.has(t.text)) && t.text !== \"!\" && t.text !== \"time\") {",
              replace: "      if (false) {" },
            { file: "src/profile/shellscript.mjs",
              find: "  if (!name.quoted && shell?.keywords.has(name.text)) {",
              replace: "  if (false) {" }],
  },
  {
    name: "detect-assignments-are-skipped",
    why: "take a leading variable assignment for the program. `NODE_ENV=test jest` is then reported as running a missing program called `NODE_ENV=test`, and a runner that really is missing behind one goes unjudged",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "a leading variable assignment is skipped, and the program behind it judged",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "  for (let a; k < words.length && (a = assignment(words[k], ctx.shell)); k++) if (a.name === \"PATH\") path = literalPath(a.value);",
              replace: "  void assignment;" }],
  },
  {
    name: "detect-quotes-keep-words-whole",
    why: "split a quoted value on its spaces. The rest of the value is then read as the program, and the runner behind it goes unjudged",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "a quoted assignment value that spans words is skipped whole, and the program behind it judged",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "      if (c === '\"') {\n        quote();",
              replace: "      if (false) {\n        quote();" }],
  },
  {
    name: "detect-escapes-keep-words-whole",
    why: "split an escaped space as if it were a separator. `MSG=hello\\ node no-such-runner` is then read as running node, which is allowed, and a missing runner reads as present",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "an escaped space keeps an assignment's value whole, so the program behind it is judged, not the word after the space",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "      if (c === \"\\\\\") {\n        if (i + 1 >= s.length) return undefined;",
              replace: "      if (false) {\n        if (i + 1 >= s.length) return undefined;" }],
  },
  {
    name: "detect-wrappers-run-the-next-command",
    why: "take env, nohup or time for the program. The runner they start goes unjudged, and `env NODE_ENV=test jest` reads as a program called env",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "a program run through command, exec, time, nohup or env is judged",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "function wrapper(name, rest, path, state, ctx) {\n  let i = 0, p = path, cwd = state.cwd;",
              replace: "function wrapper(name, rest, path, state, ctx) {\n  if (name) return null;\n  let i = 0, p = path, cwd = state.cwd;" }],
  },
  {
    name: "detect-wrapper-options-not-judged",
    why: "read past command's lookup options as if they ran the program. `command -v jest` then reports a missing program, where it only looks one up",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "a wrapper given options isn't judged, since command -v only looks a program up",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "        if (/[vV]/.test(args[k].text)) return UNKNOWN;   // only looks a program up",
              replace: "        if (/[vV]/.test(args[k].text)) continue;" }],
  },
  {
    name: "detect-expansion-names-no-program",
    why: "judge a program named by an expansion. `$RUNNER --ci` is then reported as running a missing program called $RUNNER, which is whatever the variable holds at run time",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "a program named by an expansion isn't judged",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "  const [name, ...args] = words;\n  if (name.expansion || name.glob) return { o: \"?\", mutates: true };",
              replace: "  const [name, ...args] = words;\n  if (name.glob) return { o: \"?\", mutates: true };" },
            { file: "src/profile/shellscript.mjs",
              find: "  if (!name) return OK;\n  if (name.expansion || name.glob) return { o: \"?\", mutates: true };",
              replace: "  if (!name) return OK;\n  if (name.glob) return { o: \"?\", mutates: true };" }],
  },
  {
    name: "detect-missing-program-still-broken",
    why: "find every program. Every script then reads as fine, including one whose runner really is missing, which is the case the warning exists for",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "a script whose program is really missing is still reported broken",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "  return unsure ? null : false;",
              replace: "  return true;" }],
  },
  {
    name: "detect-only-env-takes-assignments",
    why: "skip assignments after command, as env does. `command MODE=test jest` then reads as running jest, where the shell runs a program called MODE=test and fails",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "an assignment after command, exec or nohup is a program's name, which fails",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "      return args.length > k ? command(args.slice(k), p, state, ctx, true) : OK;",
              replace: "      while (args[k] && ASSIGNMENT.test(args[k].bare)) k++;\n      return args.length > k ? command(args.slice(k), p, state, ctx, true) : OK;" }],
  },
  {
    name: "detect-command-p-runs-the-command",
    why: "stop judging at any option of command. `command -p no-such-runner` then reads as present, though it runs the missing program",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "options that still run the command, command -p, env -i and -u, exec -a, don't stop it being judged",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "        if (/[vV]/.test(args[k].text)) return UNKNOWN;   // only looks a program up",
              replace: "        return UNKNOWN;" }],
  },
  {
    name: "detect-env-options-run-the-command",
    why: "stop judging at env's own options. `env -i no-such-runner` then reads as present, though it runs the missing program",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "options that still run the command, command -p, env -i and -u, exec -a, don't stop it being judged",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "      if (/^-[iv0]+$/.test(o) || o === \"--ignore-environment\") {",
              replace: "      if (/^-[iv0]+$/.test(o) || o === \"--ignore-environment\") { return UNKNOWN;" }],
  },
  {
    name: "detect-env-i-empties-path",
    why: "keep the PATH through env -i. A dependency's runner then reads as found, where env -i empties PATH and the shell can't find it",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "env -i empties the environment, PATH with it, so a dependency's runner isn't found",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "        if (o.includes(\"i\")) p = STANDARD_PATH;",
              replace: "        void p;" }],
  },
  {
    name: "detect-shell-words-asked-of-the-shell",
    why: "take every candidate word for the shell's own. Under dash, `source` and `[[` then read as builtins, where dash has neither and the script fails",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "a word is the shell's own exactly when the shell npm runs scripts with says so",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "      else if (/ is a (special )?shell builtin$/.test(said)) shell.builtins.add(word);",
              replace: "      else if (said) shell.builtins.add(word);" }],
  },
  {
    name: "detect-false-always-fails",
    why: "take false for a command that may pass. A script that is `false` then reads as present, though it can never pass",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "a script that always fails is broken, and one that always passes isn't",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "    case \"false\": return failing(\"always fails: 'false' never succeeds\");",
              replace: "    case \"false\": return OK;" }],
  },
  {
    name: "detect-exit-status-counts",
    why: "take any exit for a pass. `exit 1` then reads as present, though it can never pass",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "a script that always fails is broken, and one that always passes isn't",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "      return n % 256 === 0 ? { o: \"ok\", stop: true } : { ...failing(`always fails: it exits ${n}`), stop: true };",
              replace: "      return { o: \"ok\", stop: true };" }],
  },
  {
    name: "detect-redirections-skipped",
    why: "read a redirection's target as the program. `>out no-such-runner` then reports a missing program called out, and `>out jest` reads as broken",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "a redirection before the command is skipped, and the program behind it judged",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "      const token = { t: \"redir\", writes, subst: target.subst, expansion: target.expansion, mayFail };",
              replace: "      const token = target;" }],
  },
  {
    name: "detect-later-commands-judged",
    why: "judge only the first command. `echo setup && no-such-runner` then reads as present, though it always reaches the missing program",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "a missing program after a first command is judged when it decides the result: after &&, under set -e, at a pipeline's end",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "    const skips = op === \"&&\" ? acc.o === \"fail\" : acc.o === \"ok\";",
              replace: "    const skips = true;" }],
  },
  {
    name: "detect-set-e-stops",
    why: "ignore set -e. A failure it stops at then reads as survivable, and `set -e; no-such-runner; true` reads as present",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "a missing program after a first command is judged when it decides the result: after &&, under set -e, at a pipeline's end",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "  if (name === \"errexit\") state.errexit = on;",
              replace: "  if (name === \"errexit\") void on;" }],
  },
  {
    name: "detect-pipeline-last-decides",
    why: "judge a pipeline by its first command. `cat input | no-such-runner` then reads as present, and `no-such-runner | cat`, which passes, as broken",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "a missing program after a first command is judged when it decides the result: after &&, under set -e, at a pipeline's end",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "  const r = sure(j, run(j));",
              replace: "  const r = sure(i, run(i));" }],
  },
  {
    name: "detect-quoted-reserved-words",
    why: "take a quoted reserved word for the keyword. `\"for\" x` then reads as a loop, where the shell runs a program called for and fails",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "a quoted reserved word is a program's name, and a quoted builtin still runs",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "      if (leading && !t.quoted && (t.text === \"{\" || ctx.shell?.keywords.has(t.text)) && t.text !== \"!\" && t.text !== \"time\") {",
              replace: "      if (leading && (t.text === \"{\" || ctx.shell?.keywords.has(t.text)) && t.text !== \"!\" && t.text !== \"time\") {" },
            { file: "src/profile/shellscript.mjs",
              find: "  if (!name.quoted && shell?.keywords.has(name.text)) {",
              replace: "  if (shell?.keywords.has(name.text)) {" }],
  },
  {
    name: "detect-path-before-program",
    why: "ignore a PATH given with the command. `PATH=/nowhere node` then reads as running node, where the shell looks only in /nowhere",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "a literal PATH before the program is where it's looked up, and a PATH built from expansions isn't judged",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "k < words.length && (a = assignment(words[k], ctx.shell)); k++) if (a.name === \"PATH\") path = literalPath(a.value);",
              replace: "k < words.length && (a = assignment(words[k], ctx.shell)); k++) void a;" }],
  },
  {
    name: "detect-path-expansion-unknown",
    why: "take a PATH built from expansions literally. `PATH=$HOME/bin:$PATH tool` then reads as broken, though the tool may be in either",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "a literal PATH before the program is where it's looked up, and a PATH built from expansions isn't judged",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "const literalPath = (value) => (!value || value.expansion ? null : value.text.split(\":\"));",
              replace: "const literalPath = (value) => (!value ? null : value.text.split(\":\"));" }],
  },
  {
    name: "detect-exit-stops-the-script",
    why: "carry on past exit. `exit 0; no-such-runner` then reads as broken, though the script ends before the missing program",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "and not where the script can still pass: before ; or ||, earlier in a pipeline, after exit, or before commands that aren't read",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "return n % 256 === 0 ? { o: \"ok\", stop: true } :",
              replace: "return n % 256 === 0 ? { o: \"ok\" } :" }],
  },
  {
    name: "detect-unread-tail-decides",
    why: "let a failure stand before commands that aren't read. After a loop, `; true` may still pass the script, and it reads as broken",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "and not where the script can still pass: before ; or ||, earlier in a pipeline, after exit, or before commands that aren't read",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "    if (opaque) { last = UNKNOWN; break; }",
              replace: "    if (opaque) { last = acc; break; }" }],
  },
  {
    name: "detect-closer-is-a-syntax-error",
    why: "take a keyword that closes nothing for more of a compound. Under dash, `function t { jest; }` then reads as present, where dash stops at a syntax error",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "a word is the shell's own exactly when the shell npm runs scripts with says so",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "      if (leading && !t.quoted && ctx.shell?.keywords.has(t.text) && CLOSERS.has(t.text))",
              replace: "      if (false)" }],
  },
  {
    name: "detect-script-shell-from-npm-config",
    why: "read script-shell from the environment alone. A project that sets `script-shell=bash` in its .npmrc then has its scripts judged as dash runs them, and bash's own words read as missing programs",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "npm's script-shell is read as npm reads it: the environment, then the project's .npmrc, a workspace root's for its workspaces, the user's, the global one",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "  return setting(key, fromEnv, own, user, global, builtin);",
              replace: "  return setting(key, fromEnv);" }],
  },
  {
    name: "detect-script-shell-workspace-root",
    why: "read a workspace's own .npmrc. npm ignores it for the workspace root's, so a root that sets bash has its workspaces judged by another shell",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "npm's script-shell is read as npm reads it: the environment, then the project's .npmrc, a workspace root's for its workspaces, the user's, the global one",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "    if (isListed === true) return [p];",
              replace: "    if (isListed === true) return [dir];" }],
  },
  {
    name: "detect-script-shell-is-asked",
    why: "ask `sh` whatever npm's script-shell says. Under a configured bash, `[[` then reads as a missing program, and a script that works as broken",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "the shell asked is the one npm's script-shell names, so a word of bash's is no missing program under bash",
    edits: [{ file: "src/profile/detect.mjs",
              find: "  const shells = given ? null : runnerShells(dir, packageManager);",
              replace: "  const shells = given ? null : [\"sh\"];" }],
  },
  {
    name: "detect-bang-inverts-pipeline",
    why: "leave `!` unread. `! false | true`, which always fails, then reads as present",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "! inverts a whole pipeline, set -e never stops at one, and an exit inside it ends the script first",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "  if (bang && !bang.quoted && bang.text === \"!\" && ctx.shell?.keywords.has(\"!\")) {",
              replace: "  if (false) {" }],
  },
  {
    name: "detect-bang-exempt-from-set-e",
    why: "stop at a failure `!` made under set -e. The shell never does, so `set -e; ! true; echo reached` reads as broken though it passes",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "! inverts a whole pipeline, set -e never stops at one, and an exit inside it ends the script first",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "    const errexit = fromLast === false || acc.negated ? false : state.errexit;",
              replace: "    const errexit = fromLast === false ? false : state.errexit;" }],
  },
  {
    name: "detect-exit-ends-before-bang",
    why: "invert an exit's status. The shell has ended by then, so `! exit 1`, which fails, reads as present",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "! inverts a whole pipeline, set -e never stops at one, and an exit inside it ends the script first",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "    return p.r.stop === true ? p : { ...p, r: inverted(p.r) };",
              replace: "    return { ...p, r: inverted(p.r) };" }],
  },
  {
    name: "detect-line-continuation-no-word",
    why: "read a line continuation before indentation as an empty word. A line ending `NODE_ENV=test \\` and an indented `jest` on the next then reads as running a program called ''",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "a line continuation, or a newline after an operator, joins the lines and makes no empty command; an operator with nothing after it is a syntax error",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "    if (c === \"\\\\\" && s[i + 1] === \"\\n\") { i += 2; continue; }",
              replace: "    if (false) { i += 2; continue; }" }],
  },
  {
    name: "detect-newline-after-operator-joins",
    why: "end a command at a newline after `||`. `jest ||` with a missing program on the next line then reads as a syntax error, where the shell reads one list",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "a line continuation, or a newline after an operator, joins the lines and makes no empty command; an operator with nothing after it is a syntax error",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "      if (![\"&&\", \"||\", \"|\"].includes(tokens.at(-1)?.op)) tokens.push({ t: \"op\", op: \";\", newline: true });",
              replace: "      tokens.push({ t: \"op\", op: \";\", newline: true });" }],
  },
  {
    name: "detect-dangling-operator-is-a-syntax-error",
    why: "let `&&` end the script. The shell stops at the syntax error, so `jest --ci &&` fails, and it reads as present",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "a line continuation, or a newline after an operator, joins the lines and makes no empty command; an operator with nothing after it is a syntax error",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "  else if (JOINS.has(list.at(-1)?.op)) return syntaxError(`'${list.at(-1).op}' has no command after it`);",
              replace: "  else if (list.at(-1)?.op === \"|\") return syntaxError(`'${list.at(-1).op}' has no command after it`);" }],
  },
  {
    name: "detect-trailing-newline-no-command",
    why: "take a trailing newline for an empty command that passes. `no-such-runner --ci` and a newline then reads as present",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "a line continuation, or a newline after an operator, joins the lines and makes no empty command; an operator with nothing after it is a syntax error",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "  if (cmd.words.length || cmd.redirs) list.push({ ...cmd, op: null });",
              replace: "  if (true) list.push({ ...cmd, op: null });" }],
  },
  {
    name: "detect-earlier-commands-may-install",
    why: "call a program missing after a command that may have made it. `npm ci && jest`, where the install brings jest, then reads as broken",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "a missing program after one that may change files, an install say, may be there by then; one beside it in a pipeline can't be",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "  if (state.mutated) return UNKNOWN;",
              replace: "  if (false) return UNKNOWN;" }],
  },
  {
    name: "detect-pipeline-runs-at-once",
    why: "count a command beside it in a pipeline as having run first. `npm ci | no-such-runner` then reads as present, though the two start together and the runner is looked up at once",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "a missing program after one that may change files, an install say, may be there by then; one beside it in a pipeline can't be",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "  const run = (k) => redirected(list[k], simple(list[k].words, { ...state, mutated: state.mutated || list[k].subst }, ctx));",
              replace: "  const run = (k) => redirected(list[k], simple(list[k].words, { ...state, mutated: true }, ctx));" }],
  },
  {
    name: "detect-substitution-runs-first",
    why: "look a program up before its command's substitutions run. `no-such-runner $(npm ci)`, where the install brings it, then reads as broken",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "a missing program after one that may change files, an install say, may be there by then; one beside it in a pipeline can't be",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "    if (first.subst) state.mutated = true;",
              replace: "    void first;" }],
  },
  {
    name: "detect-written-files-count",
    why: "take a redirection's file for no change. `echo x > out.txt && no-such-runner` then reads as broken, though what it writes may be the program",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "a missing program after one that may change files, an install say, may be there by then; one beside it in a pipeline can't be",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "    const mutates = r.mutates || first.writes || first.subst;",
              replace: "    const mutates = r.mutates || first.subst;" }],
  },
  {
    name: "detect-cd-moves-lookups",
    why: "look relative names up from the package whatever cd did. `cd tools && ./runner` then reads as broken though the runner is there",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "a relative program or PATH entry is found from the folder cd or env -C moved to",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "      state.cwd = target !== null && isDir(target) ? target : null;",
              replace: "      void target;" }],
  },
  {
    name: "detect-env-chdir-moves-lookups",
    why: "ignore env -C. `env -C tools ./runner` then reads as broken though env runs the runner from tools",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "a relative program or PATH entry is found from the folder cd or env -C moved to",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "        cwd = !to || to.expansion || to.glob || cwd === null ? null : resolve(cwd, to.text);",
              replace: "        void to;" }],
  },
  {
    name: "detect-cdpath-unsure",
    why: "take cd's bare folder name to be the package's. With CDPATH set, cd can go to another folder of that name, and a runner there reads as missing",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "a relative program or PATH entry is found from the folder cd or env -C moved to",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "(process.env.CDPATH && !/^\\.{0,2}\\//.test(to.text))",
              replace: "false" }],
  },
  {
    name: "detect-command-p-standard-path",
    why: "look up command -p's program in PATH. `command -p` with a dependency's runner then reads as present, though the standard path has no node_modules/.bin",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "command -p and env -u PATH look only in the standard path",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "        p = STANDARD_PATH;   // the standard path, whatever PATH says",
              replace: "        void p;" }],
  },
  {
    name: "detect-env-unset-path",
    why: "keep PATH through `env -u PATH`. A dependency's runner then reads as found, where env looks only in the standard path",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "command -p and env -u PATH look only in the standard path",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "        if ((m[1] ?? rest[++i]?.text) === \"PATH\") p = STANDARD_PATH;",
              replace: "        if (m[1] === undefined) i++;" }],
  },
  {
    name: "detect-alias-leaves-the-rest-unread",
    why: "read past an alias definition. `alias no-such-runner=true` and then `no-such-runner` reads as broken, though dash runs the alias",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "an alias definition leaves what follows it unread",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "      return args.some((a) => a.text.includes(\"=\")) ? { o: \"?\", opaque: true } : args.length ? UNKNOWN : OK;",
              replace: "      return OK;" }],
  },
  {
    name: "detect-pathname-patterns-unjudged",
    why: "take a pathname pattern literally. `./scripts/run-*.sh` then reads as a missing program, though it runs the file it matches",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "a command word holding a pathname pattern isn't judged: it names whatever files match",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "      if (c === \"*\" || c === \"?\" || (c === \"[\" && /^\\[[^\\s\\]]*\\]/.test(s.slice(i)))) glob = true;",
              replace: "      void glob;" }],
  },
  {
    name: "detect-set-options-end",
    why: "read set's operands as options. `set -- -e; false; true` then reads as stopping at false, though -e there is a positional parameter",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "set reads options only until --, - or its first operand, and one that may not have run leaves set -e unsure",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "        if (!m) break;",
              replace: "        if (!m) continue;" }],
  },
  {
    name: "detect-set-e-may-change",
    why: "keep set -e through a set that may not run. `set -e; jest --ci && set +e; false; true` then reads as broken, though it passes when jest does",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "set reads options only until --, - or its first operand, and one that may not have run leaves set -e unsure",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "    if (maybeState.errexit !== state.errexit) state.errexit = \"maybe\";",
              replace: "    void maybeState.errexit;" }],
  },
  {
    name: "detect-set-expansion-unsure",
    why: "take set's expanded options for none. `set -e; set $FLAGS; false; true` then reads as broken, though FLAGS may turn -e off",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "set reads options only until --, - or its first operand, and one that may not have run leaves set -e unsure",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "        if (args[k].expansion || args[k].glob) return { o: \"?\", opaque: true };",
              replace: "        if (args[k].expansion || args[k].glob) break;" }],
  },
  {
    name: "detect-exec-options-per-shell",
    why: "read exec's options in every shell. Under dash, whose exec takes none, `exec -a label jest` then reads as present, though it runs a program called -a",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "exec takes options only in a shell whose exec has them: under dash, exec -a runs a program called -a",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "      if (ctx.shell?.execOptions) {",
              replace: "      if (true) {" }],
  },
  {
    name: "detect-trap-unjudged",
    why: "ignore traps. `trap 'exit 0' EXIT; no-such-runner` then reads as broken, though the trap makes it pass",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "a trap can decide how the script ends, so a script that sets one isn't judged",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "      if (args.length) state.trapped = true;",
              replace: "      void args;" }],
  },
  {
    name: "detect-bare-exit-status",
    why: "give a bare exit the status of the list before. `false; true && exit` then reads as failing, though it exits with true's status",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "a bare exit exits with the status just before it, inside an and-or list too",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "    state.status = statusOf(acc);\n    if (runs) {",
              replace: "    if (runs) {" }],
  },
  {
    name: "detect-builtins-may-change-lookups",
    why: "take any builtin for one that changes nothing. `read PATH < pathfile; runner` then reads as broken, though read set the PATH it's found in",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "a builtin that may set PATH, printf -v or read, leaves a later missing program unjudged",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "    return PURE.has(name.text) ? r : { ...r, mutates: true };",
              replace: "    return r;" }],
  },
  {
    name: "detect-printf-v-may-set-path",
    why: "take `printf -v` for output only. Under bash it sets a variable, and `printf -v PATH` then a runner in the new PATH reads as missing",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "a builtin that may set PATH, printf -v or read, leaves a later missing program unjudged",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "      return /^-v/.test(text[0] ?? \"\") ? { o: \"?\", mutates: true } : UNKNOWN;",
              replace: "      return UNKNOWN;" }],
  },
  {
    name: "detect-found-up-the-tree",
    why: "look only in the package's own node_modules/.bin. A workspace's runner, hoisted to the root or declared there, then reads as missing",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "a program is found as npm finds it: in any node_modules/.bin from the package up, or as a dependency of a folder above it, such as a workspace root",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "      if (runnable(join(d, \"node_modules\", \".bin\", name)) || (d !== pkg.dir && declares(d, name))) return true;",
              replace: "      if (d === pkg.dir && runnable(join(d, \"node_modules\", \".bin\", name))) return true;" }],
  },
  {
    name: "detect-own-word-run-as-program",
    why: "say the shell lacks a word it has. A quoted `for` then reads as a word dash doesn't have, where dash has it and the quotes made it a program's name",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "a quoted reserved word is a program's name, and a quoted builtin still runs",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "    return failing(shell.keywords.has(name.text) || shell.builtins.has(name.text)",
              replace: "    return failing(false" }],
  },
  {
    name: "detect-exec-ends-options-per-shell",
    why: "take `--` after exec for its program in every shell. Where exec takes `--` and ends its options there, `exec -- jest` then reads as running a missing program called --",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "exec takes options only in a shell whose exec has them: under dash, exec -a runs a program called -a",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "      } else if (ctx.shell?.execEndsOptions && text[0] === \"--\") k = 1;",
              replace: "      }" }],
  },
  {
    name: "detect-runner-picks-the-shell",
    why: "ask npm's shell whatever runs the script. bun runs scripts with bash where it can, so `[[ ... ]]` under bun then reads as a missing program",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "a script bun runs is read as bash reads it, and one yarn 2 runs isn't judged",
    edits: [{ file: "src/profile/detect.mjs",
              find: "  const shells = given ? null : runnerShells(dir, packageManager);",
              replace: "  const shells = given ? null : runnerShells(dir, \"npm\");" }],
  },
  {
    name: "detect-bun-prefers-bash",
    why: "take sh for bun's shell where bash is there too. bun looks for bash first, so bash's words then read as missing under bun",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "the runner picks the shell: bun the first of bash, sh and zsh, yarn 1 and pnpm their settings, and yarn 2, an emulator or bun's own shell none the reader models",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "      const shell = [\"bash\", \"sh\", \"zsh\"].map((name) => onPath(name, env)).find(Boolean);",
              replace: "      const shell = [\"sh\", \"bash\", \"zsh\"].map((name) => onPath(name, env)).find(Boolean);" }],
  },
  {
    name: "detect-yarn-berry-unjudged",
    why: "take yarn 2 and later for yarn 1. They run scripts in a shell of their own, and a script that works there then reads as broken under sh",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "the runner picks the shell: bun the first of bash, sh and zsh, yarn 1 and pnpm their settings, and yarn 2, an emulator or bun's own shell none the reader models",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "      if (ancestorWith(dir, \".yarnrc.yml\") || /^yarn@([2-9]|\\d{2,})/.test(String(pkg?.packageManager ?? \"\"))",
              replace: "      if (/^yarn@([2-9]|\\d{2,})/.test(String(pkg?.packageManager ?? \"\"))" }],
  },
  {
    name: "detect-pnpm-emulator-unjudged",
    why: "read a script that pnpm's shell emulator runs as sh reads it. The emulator is a shell of its own, and a script that works there then reads as broken",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "the runner picks the shell: bun the first of bash, sh and zsh, yarn 1 and pnpm their settings, and yarn 2, an emulator or bun's own shell none the reader models",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "      if (/^\\s*shellEmulator:\\s*true\\b/m.test(yaml)) return null;\n",
              replace: "" }],
  },
  {
    name: "detect-unmodelled-runner-skips-scripts",
    why: "judge scripts whose runner's shell isn't modelled. A yarn 2 script is then read as sh would read it, and one that works under yarn reads as broken",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "a script bun runs is read as bash reads it, and one yarn 2 runs isn't judged",
    edits: [{ file: "src/profile/detect.mjs",
              find: "  const judged = given || shell !== null;",
              replace: "  const judged = true;" }],
  },
  {
    name: "detect-background-runs-apart",
    why: "run a list ended by & in the foreground. `exit 1 & true` then reads as stopping at the exit, though the exit ends only a background job",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "a list ended by & runs apart: its exit ends only itself, and its cd moves nothing after it",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "    if (list[listEnd(list, i)].op === \"&\") {",
              replace: "    if (false) {" }],
  },
  {
    name: "detect-background-state-apart",
    why: "let a background list change the script's own state. `cd tools & ./runner` then finds the runner from tools, though the cd moved only the background job",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "a list ended by & runs apart: its exit ends only itself, and its cd moves nothing after it",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "      const r = andOr(list, i, apart, ctx);",
              replace: "      const r = andOr(list, i, state, ctx);" }],
  },
  {
    name: "detect-tilde-is-home",
    why: "take a leading ~ literally. `~/bin/tool` is then looked for in a folder named ~ in the package, and a tool in the home folder reads as missing",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "a word or a PATH that starts from ~ isn't judged: it's a home folder",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "      if (c === \"~\" && bare === null && (text === \"\" || /[=:]$/.test(text))) expansion = true;",
              replace: "      void bare;" }],
  },
  {
    name: "detect-time-ends-options",
    why: "stop at -- after the shell's own time. bash's `time -- jest` then reads as running a program called --",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "the shell's own time takes -- before the command it times",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "    while (args[k]?.text === \"-p\") k++;\n    if (args[k]?.text === \"--\") k++;",
              replace: "    while (args[k]?.text === \"-p\") k++;" }],
  },
  {
    name: "detect-unknown-words-asked",
    why: "call a word missing without asking the shell. A builtin no list holds, from a newer version or a module, then reads as a missing program",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "a builtin no list holds is asked of the shell before it's called a missing program",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "  if (own === \"builtin\" || own === \"unknown\") return { o: \"?\", mutates: true };",
              replace: "  if (false) return { o: \"?\", mutates: true };" }],
  },
  {
    name: "detect-workspace-braces-expanded",
    why: "match a workspace pattern's braces literally. `{packages,apps}/*` then lists no workspace, and a member's own .npmrc names the shell where npm reads the root's",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "a workspace pattern's braces are expanded as npm's are, and one the reader can't match leaves both folders' shells in play",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "  if (!m || !m[1].includes(\",\")) return [glob];",
              replace: "  return [glob];" }],
  },
  {
    name: "detect-unmatched-pattern-keeps-both",
    why: "take a workspace pattern the reader can't match for one that doesn't list the folder. The member's own .npmrc then names the shell alone, where npm may read the root's",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "a workspace pattern's braces are expanded as npm's are, and one the reader can't match leaves both folders' shells in play",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "    if (isListed === null) return [p, dir];",
              replace: "    if (isListed === null) return [dir];" }],
  },
  {
    name: "detect-shells-merged",
    why: "judge a script by one of the shells it may run under. Under a root's bash or a member's sh, `[[ ... ]]` then reads as missing, though bash has it",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "a workspace pattern's braces are expanded as npm's are, and one the reader can't match leaves both folders' shells in play",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "  if (shells.length === 1) return shells[0];",
              replace: "  if (shells.length) return shells.at(-1);" }],
  },
  {
    name: "detect-append-assigns",
    why: "take NAME+=value for a program's name in every shell. Under bash it assigns, and `PATH+=:/tmp` then reads as a missing program",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "NAME+=value assigns where the shell has it, and adds to PATH what can't be known; where it doesn't, it's a program's name",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "  if (shell?.appendAssign && APPEND.test(w.bare)) return",
              replace: "  if (false && APPEND.test(w.bare)) return" }],
  },
  {
    name: "detect-append-asked-of-the-shell",
    why: "leave += out of what the shell is asked. Real bash then reads as a shell where `PATH+=:/tmp` runs a program",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "NAME+=value assigns where the shell has it, and adds to PATH what can't be known; where it doesn't, it's a program's name",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "    'if (x=a; x+=b; test \"$x\" = ab) >/dev/null 2>&1; then printf \"append-assign\\\\tyes\\\\n\"; fi; ' +\n",
              replace: "" }],
  },
  {
    name: "detect-env-takes-any-equals",
    why: "read env's assignments as the shell reads its own. A quoted `\"MSG=two words\"` after env then reads as a program's name, though env takes it for an assignment",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "env takes any word with an = in it for an assignment, quoted or not",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "    for (; i < rest.length && rest[i].text.indexOf(\"=\") > 0; i++) {",
              replace: "    for (; i < rest.length && ASSIGNMENT.test(rest[i].bare); i++) {" }],
  },
  {
    name: "temp-dirs-removed-however-the-test-ends",
    why: "remove a test's temporary folders only when it finishes on its own. A test that exits with a failure or throws, which is when a folder is likeliest to matter, then leaves every one behind",
    test: "test/temp-teardown.test.mjs",
    expectRed: "and once it exits with a failure, or on an uncaught error",
    edits: [{ file: "test/fixtures/temp.mjs",
              find: "process.on(\"exit\", () => {",
              replace: "process.on(\"beforeExit\", () => {" }],
  },
  {
    name: "temp-dirs-remembered",
    why: "hand out folders without remembering them. Nothing is then removed at exit, and every run of the suite leaves them in the temp directory",
    test: "test/temp-teardown.test.mjs",
    expectRed: "a test file's temporary folders are gone once it finishes",
    edits: [{ file: "test/fixtures/temp.mjs",
              find: "    home = dir;\n",
              replace: "" }],
  },
  {
    name: "ci-skipped-required-never-passed",
    why: "count a skipped required check as passing, as GitHub does. A test job that a broken `if:` skipped, or whose `needs:` failed, then passes the gate",
    test: "test/ci-read-completely.test.mjs",
    expectRed: "a required check whose every result is skipped or neutral never passed, and a skipped job nothing requires still passes",
    edits: [{ file: "src/github/reconciler.mjs",
              find: "  if (skipped.length) return { verdict: \"SKIPPED_REQUIRED\",",
              replace: "  if (false) return { verdict: \"SKIPPED_REQUIRED\"," }],
  },
  {
    name: "ci-neutral-required-never-passed",
    why: "count a neutral required check as passing. A check that ran and judged nothing then stands in for one that passed",
    test: "test/ci-read-completely.test.mjs",
    expectRed: "a required check whose every result is skipped or neutral never passed, and a skipped job nothing requires still passes",
    edits: [{ file: "src/github/reconciler.mjs",
              find: "const NOT_RUN = new Set([\"skipped\", \"neutral\"]);",
              replace: "const NOT_RUN = new Set([\"skipped\"]);" }],
  },
  {
    name: "ci-failure-before-skipped-required",
    why: "put a skipped required check before a failure. The job it needs failed, and the escalation then asks a person about the skip instead of sending a fixer to the failure",
    test: "test/ci-read-completely.test.mjs",
    expectRed: "a failure, or a check in flight, comes first: the job a skipped one needs is the one to fix",
    edits: [{ file: "src/github/reconciler.mjs",
              find: "  if (failing.length) return { verdict: \"RED\",",
              replace: "  if (failing.length && !required.some(c => notRun(meeting(c)))) return { verdict: \"RED\"," }],
  },
  {
    name: "ci-nothing-ran-is-unknown",
    why: "call a head green where every check was skipped or neutral. Nothing ran, and the evidence is only rows saying so",
    test: "test/ci-read-completely.test.mjs",
    expectRed: "a head where every check was skipped, neutral or superseded is unknown, not green: nothing ran",
    edits: [{ file: "src/github/reconciler.mjs",
              find: "  if (evidence && rows.every(r => NOT_RUN.has(String(r.conclusion)) || UNINFORMATIVE.has(String(r.conclusion))))",
              replace: "  if (false)" }],
  },
  {
    name: "ci-unplaced-skip-is-unknown",
    why: "let a head read green while the base's requirements couldn't be read. A requirement unread, reeve's own shadow check say, may be one no row meets, and GitHub passes it on reeve's neutral",
    test: "test/ci-read-completely.test.mjs",
    expectRed: "where the base's requirements couldn't be read, nothing reads green: a requirement unread may be one no row meets",
    edits: [{ file: "src/github/reconciler.mjs",
              find: "  const green = (result) => (evidence && !requiredKnown ? {",
              replace: "  const green = (result) => (false ? {" }],
  },
  {
    name: "ci-required-names-from-rules-and-branch",
    why: "know the base's required checks only when the rest of protection could be read too. A token that can't read protection then leaves every required check unknown, though the rules and the branch name them",
    test: "test/ci-read-completely.test.mjs",
    expectRed: "the base's required checks are read from its rules and its branch, even where the rest of protection can't be, and are unknown where those can't",
    edits: [{ file: "src/pr.mjs",
              find: "  const checks = Array.isArray(list) && b\n",
              replace: "  const checks = whole\n" }],
  },
  {
    name: "ci-required-set-includes-the-base",
    why: "take the required set from the profile alone. A check the base's rules require is then just another check, and skipping it passes",
    test: "test/ci-read-completely.test.mjs",
    expectRed: "a pull request's required checks are the profile's and the base's, reeve's own and reviewers' statuses aside",
    edits: [{ file: "src/pr.mjs",
              find: "origin: \"profile\" })),\n               ...(base ?? []).filter((c) => !aside(c)).map((c) => ({ ...c, origin: \"base\" }))];",
              replace: "origin: \"profile\" }))];" }],
  },
  {
    name: "ci-partial-read-classifies-unknown",
    why: "classify whatever part of a read arrived. With statuses unread, a failing status beside passing check runs then reads green",
    test: "test/ci-read-completely.test.mjs",
    expectRed: "a read that isn't whole passes nothing, but a failure it did read stays a failure",
    edits: [{ file: "src/pr.mjs",
              find: "  if (read?.ok || c.verdict === \"RED\") return c;",
              replace: "  return c;" }],
  },
  {
    name: "ci-head-read-must-be-whole",
    why: "classify the head's rows without asking whether the read was whole. evaluatePr then passes CI on check runs alone when the statuses couldn't be read",
    test: "test/ci-read-completely.test.mjs",
    expectRed: "through evaluatePr, a head whose statuses couldn't be read has no CI pass, though its check runs pass",
    edits: [{ file: "src/pr.mjs",
              find: "  const c = classifyRead(read, req);",
              replace: "  const c = classify(rows, req.required, { requiredKnown: req.known });" }],
  },
  {
    name: "ci-base-read-must-be-whole",
    why: "classify the base's rows without asking whether the read was whole. A base whose statuses couldn't be read then reads healthy",
    test: "test/ci-read-completely.test.mjs",
    expectRed: "through evaluatePr, a base whose statuses couldn't be read has unknown health, though its check runs pass",
    edits: [{ file: "src/pr.mjs",
              find: "    ? classifyRead(readChecks(nwo, baseHead.sha, { reviewerContexts }), { required: profile.ci?.requiredChecks ?? [] }, { evidence: false })",
              replace: "    ? classify(readChecks(nwo, baseHead.sha, { reviewerContexts }).rows, profile.ci?.requiredChecks ?? [], { evidence: false })" }],
  },
  {
    name: "ci-evaluate-reads-the-base-requirements",
    why: "have evaluatePr judge CI against the profile's list only. A check the base requires, skipped at the head, then passes",
    test: "test/ci-read-completely.test.mjs",
    expectRed: "through evaluatePr, a check the base requires that was skipped blocks, though the profile names none",
    edits: [{ file: "src/pr.mjs",
              find: "  const req = requiredChecksOf({ nwo, baseRef, profile });",
              replace: "  const req = { required: profile.ci?.requiredChecks ?? [], known: true };" }],
  },
  {
    name: "ci-impostor-blocks",
    why: "let the CI clause ignore another App's check under reeve's own name. Something speaking for the gate then goes unsaid, and CI passes beside it",
    test: "test/ci-read-completely.test.mjs",
    expectRed: "another App's check under reeve's own name blocks CI, settled or not, and goes to a person, not a fixer",
    edits: [{ file: "src/verdict.mjs",
              find: "  if (impostors.length) {",
              replace: "  if (false) {" }],
  },
  {
    name: "ci-impostor-reaches-the-verdict",
    why: "leave the impostors out of what evaluatePr hands the verdict. The clause that blocks on them then never sees one",
    test: "test/ci-read-completely.test.mjs",
    expectRed: "through evaluatePr, another App's check under reeve's own name blocks",
    edits: [{ file: "src/pr.mjs",
              find: "              impostors: read.impostors ?? [], shadowRequired: req.shadowRequired },",
              replace: "              impostors: [], shadowRequired: req.shadowRequired }," }],
  },
  {
    name: "ci-impostor-escalates",
    why: "send an impostor's block down the failing-check path. A fixer is then dispatched at a check it can't fix, or the founder is told reeve couldn't name a failure",
    test: "test/ci-read-completely.test.mjs",
    expectRed: "another App's check under reeve's own name blocks CI, settled or not, and goes to a person, not a fixer",
    edits: [{ file: "src/watcher.mjs",
              find: "    if (/under reeve's own name/i.test(ci.detail)) return act(ACTIONS.ESCALATE, ESCALATIONS.IMPOSTOR);\n",
              replace: "" }],
  },
  {
    name: "ci-skipped-required-escalates",
    why: "report a skipped required check as one that never reported. The founder is then told it never ran at all, where its workflow skipped it",
    test: "test/ci-read-completely.test.mjs",
    expectRed: "a skipped required check goes to a person too: no fixer can make a job run that its workflow skips",
    edits: [{ file: "src/watcher.mjs",
              find: "    if (/skipped or neutral/i.test(ci.detail)) return act(ACTIONS.ESCALATE, ESCALATIONS.SKIPPED_REQUIRED);\n",
              replace: "" }],
  },
  {
    name: "ci-base-judged-for-health",
    why: "judge the base for evidence of a pass, as a head is judged. A push whose path filters skipped every check then reads unknown, and every open pull request waits on the base",
    test: "test/ci-read-completely.test.mjs",
    expectRed: "through evaluatePr, a base whose push skipped every check, the one it requires among them, is healthy: the base is judged by its failures",
    edits: [{ file: "src/pr.mjs",
              find: "{ required: profile.ci?.requiredChecks ?? [] }, { evidence: false })",
              replace: "{ required: profile.ci?.requiredChecks ?? [] }, { evidence: true })" }],
  },
  {
    name: "ci-health-ignores-skips",
    why: "apply the head's rules about skipped checks to a base. A base whose push ran nothing but skipped jobs then reads unknown, and holds every open pull request",
    test: "test/ci-read-completely.test.mjs",
    expectRed: "a base is judged for health, which only its failures decide: a skipped check, or a push that ran nothing, leaves it green",
    edits: [{ file: "src/github/reconciler.mjs",
              find: "  const skipped = evidence ? required.filter(c => notRun(meeting(c))) : [];",
              replace: "  const skipped = required.filter(c => notRun(meeting(c)));" }],
  },
  {
    name: "ci-required-set-leaves-reviewers-aside",
    why: "take a reviewer's status the base requires into CI's required set. The review clauses read it, and CI's rows never hold it, so every pull request reads as missing a required check",
    test: "test/ci-read-completely.test.mjs",
    expectRed: "a pull request's required checks are the profile's and the base's, reeve's own and reviewers' statuses aside",
    edits: [{ file: "src/pr.mjs",
              find: "  const aside = (c) => c.context === POLICY_CONTEXT || reeveMeets(c) || (c.app == null && reviewers.has(c.context));",
              replace: "  const aside = (c) => c.context === POLICY_CONTEXT || reeveMeets(c);" }],
  },
  {
    name: "ci-bound-check-met-by-its-app",
    why: "let any App's result under a bound check's name meet it. The bound App's run can then be skipped while another App's pass stands in for it",
    test: "test/ci-read-completely.test.mjs",
    expectRed: "a required check bound to an App is met only by that App's runs: another App's pass isn't it, its cancelled run holds nothing, and a status can't be told",
    edits: [{ file: "src/github/reconciler.mjs",
              find: "  const meeting = (c) => (c.app == null ? named(c) : named(c).filter(r => r.source === \"check_run\" && String(r.appId) === c.app));",
              replace: "  const meeting = (c) => named(c);" }],
  },
  {
    name: "ci-bound-status-unknown",
    why: "take a commit status for a bound check's result. A status names no App reeve can read, so a bound check met only by one reads as never reported, where it can't be told",
    test: "test/ci-read-completely.test.mjs",
    expectRed: "a required check bound to an App is met only by that App's runs: another App's pass isn't it, its cancelled run holds nothing, and a status can't be told",
    edits: [{ file: "src/github/reconciler.mjs",
              find: "  if (unreadable.length) return { verdict: \"UNKNOWN\",",
              replace: "  if (false) return { verdict: \"UNKNOWN\"," }],
  },
  {
    name: "ci-requirement-keeps-its-binding",
    why: "drop the App a required check is bound to. Another App's pass under the name then meets it, as it wouldn't for GitHub",
    test: "test/ci-read-completely.test.mjs",
    expectRed: "a required check bound to an App is met only by that App's runs: another App's pass isn't it, its cancelled run holds nothing, and a status can't be told",
    edits: [{ file: "src/pr.mjs",
              find: "    if (typeof c === \"string\" && c) every.push({ context: c, app: bound == null || Number(bound) === -1 ? null : String(bound) });",
              replace: "    if (typeof c === \"string\" && c) every.push({ context: c, app: null });" }],
  },
  {
    name: "ci-failure-before-missing",
    why: "report a required check yet to report before a failure. The failure then waits on an escalation, where it is the fixer's to fix",
    test: "test/ci-read-completely.test.mjs",
    expectRed: "a failure or a running check comes before a required check yet to report, and a skipped required check settles at once",
    edits: [{ file: "src/github/reconciler.mjs",
              find: "  if (running.length) return { verdict: \"RUNNING\",",
              replace: "  if (running.length && required.every(c => meeting(c).length)) return { verdict: \"RUNNING\"," }],
  },
  {
    name: "ci-skipped-settles-at-once",
    why: "settle a skipped required check only when the provider's suites finish. With no suite of that provider's, it never settles, and the block never comes",
    test: "test/ci-read-completely.test.mjs",
    expectRed: "a failure or a running check comes before a required check yet to report, and a skipped required check settles at once",
    edits: [{ file: "src/github/reconciler.mjs",
              find: "  if (reading.verdict === \"RED\" || reading.verdict === \"SKIPPED_REQUIRED\")",
              replace: "  if (reading.verdict === \"RED\")" }],
  },
  {
    name: "ci-skipped-required-blocks",
    why: "leave the verdict's CI clause without a branch for a skipped required check. It then reads as an unknown verdict, and waits instead of blocking",
    test: "test/ci-read-completely.test.mjs",
    expectRed: "through evaluatePr, a skipped required check from another CI blocks at once, though no GitHub Actions suite exists to finish",
    edits: [{ file: "src/verdict.mjs",
              find: "  else if (i.checks.verdict === \"MISSING_REQUIRED\" || i.checks.verdict === \"SKIPPED_REQUIRED\") add(\"ci\", BLOCK, i.checks.why);",
              replace: "  else if (i.checks.verdict === \"MISSING_REQUIRED\") add(\"ci\", BLOCK, i.checks.why);" }],
  },
  {
    name: "ci-shadow-required-flagged",
    why: "set a base's requirement of reeve's shadow check aside, like reeve's own. The shadow result, which never fails, then gates the merge unsaid",
    test: "test/ci-read-completely.test.mjs",
    expectRed: "a base that requires reeve's shadow check blocks CI and goes to a person: the shadow result passes the rule whatever reeve finds",
    edits: [{ file: "src/pr.mjs",
              find: "  return { required, known: Array.isArray(base), shadowRequired: (base ?? []).some(reeveMeets) };",
              replace: "  return { required, known: Array.isArray(base), shadowRequired: false };" }],
  },
  {
    name: "ci-shadow-required-blocks",
    why: "let the CI clause pass where the base requires reeve's shadow check. The shadow result passes the rule whatever reeve found",
    test: "test/ci-read-completely.test.mjs",
    expectRed: "a base that requires reeve's shadow check blocks CI and goes to a person: the shadow result passes the rule whatever reeve finds",
    edits: [{ file: "src/verdict.mjs",
              find: "  else if (i.checks?.shadowRequired) add(\"ci\", BLOCK,",
              replace: "  else if (false) add(\"ci\", BLOCK," }],
  },
  {
    name: "ci-shadow-required-escalates",
    why: "send a required shadow check down the failing-check path. A fixer is then dispatched at a base rule it can't change",
    test: "test/ci-read-completely.test.mjs",
    expectRed: "a base that requires reeve's shadow check blocks CI and goes to a person: the shadow result passes the rule whatever reeve finds",
    edits: [{ file: "src/watcher.mjs",
              find: "    if (/requires reeve's shadow check/i.test(ci.detail)) return act(ACTIONS.ESCALATE, ESCALATIONS.SHADOW_REQUIRED);\n",
              replace: "" }],
  },
  {
    name: "ci-shadow-reaches-the-verdict",
    why: "leave the shadow requirement out of what evaluatePr hands the verdict. The clause that blocks on it then never sees it",
    test: "test/ci-read-completely.test.mjs",
    expectRed: "through evaluatePr, a base that requires reeve's shadow check blocks",
    edits: [{ file: "src/pr.mjs",
              find: "              impostors: read.impostors ?? [], shadowRequired: req.shadowRequired },",
              replace: "              impostors: read.impostors ?? [], shadowRequired: false }," }],
  },
  {
    name: "ci-required-checks-cached",
    why: "read the base's required checks afresh for every pull request. Every open pull request then reads the rules and the branch on every tick",
    test: "test/ci-read-completely.test.mjs",
    expectRed: "the base's required checks are read from its rules, every page, and its branch, never protection's admin-only read, and kept for a minute",
    edits: [{ file: "src/pr.mjs",
              find: "  if (checks !== null) REQUIRED_CHECKS.set(key, { at: now, checks });\n",
              replace: "" }],
  },
  {
    name: "ci-required-checks-without-protection",
    why: "read the rest of protection for the required checks too. A token that can't read it, an App's without administration say, then pays an admin-only read that tells nothing about checks",
    test: "test/ci-read-completely.test.mjs",
    expectRed: "the base's required checks are read from its rules, every page, and its branch, never protection's admin-only read, and kept for a minute",
    edits: [{ file: "src/pr.mjs",
              find: "    branch: gh([`repos/${nwo}/branches/${encodeURIComponent(base)}`]),\n  }, POLICY_CONTEXT);",
              replace: "    branch: gh([`repos/${nwo}/branches/${encodeURIComponent(base)}`]),\n    protection: gh([`repos/${nwo}/branches/${encodeURIComponent(base)}/protection`]),\n  }, POLICY_CONTEXT);" }],
  },
  {
    name: "ci-required-checks-every-page",
    why: "read only the first page of a branch's rules for its required checks. A rule on a later page is then missed, and so is the check it requires",
    test: "test/ci-read-completely.test.mjs",
    expectRed: "the base's required checks are read from its rules, every page, and its branch, never protection's admin-only read, and kept for a minute",
    edits: [{ file: "src/pr.mjs",
              find: "  const { checks } = requirementsOn({\n    rules: gh([\"--paginate\", `repos/${nwo}/rules/branches/${encodeURIComponent(base)}`, \"--jq\", \".[]\"]),",
              replace: "  const { checks } = requirementsOn({\n    rules: gh([`repos/${nwo}/rules/branches/${encodeURIComponent(base)}`, \"--jq\", \".[]\"])," }],
  },
  {
    name: "ci-seen-failure-stays-red",
    why: "call a partial read unknown though it holds a failure. The failure then waits for the unread surface, where it is the fixer's to fix now",
    test: "test/ci-read-completely.test.mjs",
    expectRed: "a read that isn't whole passes nothing, but a failure it did read stays a failure",
    edits: [{ file: "src/pr.mjs",
              find: "  if (read?.ok || c.verdict === \"RED\") return c;",
              replace: "  if (read?.ok) return c;" }],
  },
  {
    name: "ci-superseded-needs-the-bound-app",
    why: "let another App's cancelled run hold a check bound to an App that passed. The only run that could meet the requirement passed, and the head stays unknown for good",
    test: "test/ci-read-completely.test.mjs",
    expectRed: "a required check bound to an App is met only by that App's runs: another App's pass isn't it, its cancelled run holds nothing, and a status can't be told",
    edits: [{ file: "src/github/reconciler.mjs",
              find: "      ? uninformative.filter(r => required.some(c => c.context === r.name && (c.app == null || (r.source === \"check_run\" && String(r.appId) === c.app))))",
              replace: "      ? uninformative.filter(r => requiredNames.has(r.name))" }],
  },
  {
    name: "ci-missing-waits-on-its-app",
    why: "settle a bound requirement's absence on the CI provider's suites. A third-party App that hasn't scheduled its run yet then reads as never reporting, and escalates early",
    test: "test/ci-read-completely.test.mjs",
    expectRed: "a required check yet to report is settled by its own App's suites, not by the CI provider's",
    edits: [{ file: "src/pr.mjs",
              find: "    const done = app == null ? suites(nwo, sha, { app: profile.ci?.appSlug ?? \"github-actions\" }) : suites(nwo, sha, { appId: app });",
              replace: "    const done = suites(nwo, sha, { app: profile.ci?.appSlug ?? \"github-actions\" });" }],
  },
  {
    name: "ci-suites-matched-by-app-id",
    why: "match an App's suites by the provider's name whatever App is asked for. A bound App's unfinished suite then goes unseen, and GitHub Actions' finished ones settle for it",
    test: "test/ci-read-completely.test.mjs",
    expectRed: "through evaluatePr, a required check from a third-party App waits for that App's suite, not GitHub Actions'",
    edits: [{ file: "src/github/reconciler.mjs",
              find: "  const mine = suites.filter(s => (appId != null ? String(s.app?.id) === String(appId) : (s.app?.slug ?? null) === app));",
              replace: "  const mine = suites.filter(s => (s.app?.slug ?? null) === app);" }],
  },
  {
    name: "ci-evaluate-settles-missing-by-app",
    why: "have evaluatePr settle a missing requirement on the CI provider's suites alone. A third-party App's check then escalates as never reported before its App has run",
    test: "test/ci-read-completely.test.mjs",
    expectRed: "through evaluatePr, a required check from a third-party App waits for that App's suite, not GitHub Actions'",
    edits: [{ file: "src/pr.mjs",
              find: "    suitesComplete: c.verdict === \"MISSING_REQUIRED\" ? missingSettled(nwo, pin.sha, c.missingChecks, profile) : null };",
              replace: "    suitesComplete: c.verdict === \"MISSING_REQUIRED\" ? suitesComplete(nwo, pin.sha, { app: profile.ci?.appSlug ?? \"github-actions\" }) : null };" }],
  },
  {
    name: "ci-read-carries-known-requirements",
    why: "classify a read as though the base's requirements were read, whether or not they were. A head then reads green with its base's rules unread",
    test: "test/ci-read-completely.test.mjs",
    expectRed: "through evaluatePr, a head whose base's rules couldn't be read has no CI pass, though every check it has passes",
    edits: [{ file: "src/pr.mjs",
              find: "  const c = classify(read?.rows ?? [], required, { requiredKnown: known, evidence });",
              replace: "  const c = classify(read?.rows ?? [], required, { requiredKnown: true, evidence });" }],
  },
  {
    name: "ci-streak-counts-green-only",
    why: "count every reading of the same checks toward the three green looks. Two skipped readings then let a rerun's first green reading settle, before the rerun has finished scheduling",
    test: "test/ci-read-completely.test.mjs",
    expectRed: "a pass needs three green readings in a row: skipped or red readings before a rerun's green count for none",
    edits: [{ file: "src/github/reconciler.mjs",
              find: "  const counted = reading.verdict === \"GREEN\" && names.length >= floor;",
              replace: "  const counted = true;" }],
  },
  {
    name: "ci-unbound-imports-unsettled",
    why: "settle an unbound check the base requires on the CI provider's suites. A third party's status, Vercel's say, then escalates as never reported before its provider has run",
    test: "test/ci-read-completely.test.mjs",
    expectRed: "an unbound check the base requires stays unsettled while it hasn't reported: no suite can say its provider has finished",
    edits: [{ file: "src/pr.mjs",
              find: "  if (missing.some((c) => c?.app == null && c?.origin === \"base\")) return false;\n",
              replace: "" }],
  },
  {
    name: "ci-requirements-keep-their-origin",
    why: "drop where a required check came from. An unbound check the base requires then looks like the profile's own, and settles on the CI provider's suites",
    test: "test/ci-read-completely.test.mjs",
    expectRed: "an unbound check the base requires stays unsettled while it hasn't reported: no suite can say its provider has finished",
    edits: [{ file: "src/pr.mjs",
              find: "...(base ?? []).filter((c) => !aside(c)).map((c) => ({ ...c, origin: \"base\" }))];",
              replace: "...(base ?? []).filter((c) => !aside(c))];" }],
  },
  {
    name: "ci-classify-keeps-origin",
    why: "lose a requirement's origin in classify. The missing check evaluatePr settles then can't be told from the profile's, and an imported one settles early",
    test: "test/ci-read-completely.test.mjs",
    expectRed: "through evaluatePr, an unbound check the base requires waits while it hasn't reported, though GitHub Actions has finished",
    edits: [{ file: "src/github/reconciler.mjs",
              find: "String(c.app), ...(c.origin ? { origin: c.origin } : {}) }));",
              replace: "String(c.app) }));" }],
  },
  {
    name: "ci-partial-base-shows-failures",
    why: "give up on a partial read of the base. A head failure the base shows too is then neither inherited nor caused, and the watcher can't name it",
    test: "test/ci-read-completely.test.mjs",
    expectRed: "a partial read of the base still shows its failures: one matched there is inherited, and one with no match stays unverified",
    edits: [{ file: "src/github/reconciler.mjs",
              find: "  if (!read.ok && !(read.rows ?? []).length) return { verdict: \"UNKNOWN\", why: \"could not read base checks\", dropped };",
              replace: "  if (!read.ok) return { verdict: \"UNKNOWN\", why: \"could not read base checks\", dropped };" }],
  },
  {
    name: "ci-partial-base-absence-unverified",
    why: "call a head failure caused because a partial read of the base shows no match. The match may be on the surface that went unread, and a fixer is then sent at the base's failure",
    test: "test/ci-read-completely.test.mjs",
    expectRed: "a partial read of the base still shows its failures: one matched there is inherited, and one with no match stays unverified",
    edits: [{ file: "src/github/reconciler.mjs",
              find: "    if (!twin) { (partial ? unverified : caused).push(row.name); continue; }",
              replace: "    if (!twin) { caused.push(row.name); continue; }" }],
  },
  {
    name: "ci-reviewer-exclusion-statuses-only",
    why: "set aside every row under a reviewer's context name, check runs too. A check run bound to its App under that name then goes unread, and its skip passes",
    test: "test/ci-read-completely.test.mjs",
    expectRed: "through evaluatePr, a check bound to an App that was skipped blocks, though a reviewer's status shares its name",
    edits: [{ file: "src/github/reconciler.mjs",
              find: "  for (const r of rows) (set.has(r.name) && r.source !== \"check_run\" ? reviewerRows : rest).push(r);",
              replace: "  for (const r of rows) (set.has(r.name) ? reviewerRows : rest).push(r);" }],
  },
  {
    name: "ci-bound-reviewer-named-required",
    why: "drop a base requirement bound to an App because its name is a reviewer's status. The App's own check run is then required by GitHub and by no one in reeve",
    test: "test/ci-read-completely.test.mjs",
    expectRed: "a check bound to an App stays required under a reviewer's status name, and a check both the profile and the base name settles as the base's",
    edits: [{ file: "src/pr.mjs",
              find: "(c.app == null && reviewers.has(c.context));",
              replace: "reviewers.has(c.context);" }],
  },
  {
    name: "ci-base-origin-wins",
    why: "keep the profile's entry where the profile and the base name the same check. Its absence then settles on the CI provider's suites, though the base's may come from any App",
    test: "test/ci-read-completely.test.mjs",
    expectRed: "a check bound to an App stays required under a reviewer's status name, and a check both the profile and the base name settles as the base's",
    edits: [{ file: "src/pr.mjs",
              find: "    .map((c) => (all.some((d) => d.context === c.context && d.app === c.app && d.origin === \"base\") ? { ...c, origin: \"base\" } : c));",
              replace: "    .map((c) => c);" }],
  },
  {
    name: "ci-suites-every-page",
    why: "read only the first page of a head's check suites. An App's suite past the first hundred then goes unseen, and its requirement never settles, or settles early",
    test: "test/ci-read-completely.test.mjs",
    expectRed: "an App's suites are read past the first page before its absence settles",
    edits: [{ file: "src/github/reconciler.mjs",
              find: "  const r = gh(`repos/${nwo}/commits/${sha}/check-suites?per_page=100`, \".check_suites[]\", { paginate: true });",
              replace: "  const r = gh(`repos/${nwo}/commits/${sha}/check-suites?per_page=100`, \".check_suites\");" }],
  },
  {
    name: "detect-unasked-shell-unjudged",
    why: "judge a script whose shell can't be asked. Under script-shell=true npm runs the program true, which passes whatever the script says, and a missing program in it is still reported",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "a script under a shell that can't be asked isn't judged: script-shell=true runs nothing of it",
    edits: [{ file: "src/profile/detect.mjs",
              find: "  const judged = given || shell !== null;",
              replace: "  const judged = given || shells !== null;" }],
  },
  {
    name: "detect-shell-must-answer",
    why: "take a shell that ran without answering for one. script-shell=echo prints the script and passes, and a missing program in it is still reported",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "a script under a shell that can't be asked isn't judged: script-shell=true runs nothing of it",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "  if (!answered) shell = null;",
              replace: "  void answered;" }],
  },
  {
    name: "detect-npmrc-true-is-a-program",
    why: "read script-shell=true in an .npmrc as no setting, where npm runs the program true. The script is then judged under sh, and reported",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "a script under a shell that can't be asked isn't judged: script-shell=true runs nothing of it",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "    const shell = setting === true ? \"true\" : setting;",
              replace: "    const shell = setting;" }],
  },
  {
    name: "detect-local-wrapper-unknown",
    why: "read env, nohup and time as the system's wherever they are. A local env first on npm's PATH, which does whatever it does, is then read as running the command after it",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "env, nohup or time is read as the system's only where the system's runs: a local one first on the PATH does whatever it does",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "  if ([\"env\", \"nohup\", \"time\"].includes(name) && ![\"system\", \"missing\"].includes(locate(name, path, ctx.pkg, state.cwd)))",
              replace: "  if (false)" }],
  },
  {
    name: "detect-local-bin-shadows",
    why: "look for a wrapper in node_modules/.bin only as a dependency. The local env there, which npm finds first, is then taken for the system's",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "env, nohup or time is read as the system's only where the system's runs: a local one first on the PATH does whatever it does",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "      if (runnable(join(d, \"node_modules\", \".bin\", name)) || (d !== pkg.dir && declares(d, name))) return \"local\";",
              replace: "      if (d !== pkg.dir && declares(d, name)) return \"local\";" }],
  },
  {
    name: "detect-dependency-shadows",
    why: "take no notice of a dependency named env. Its program, first on npm's PATH, is then taken for the system's env",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "env, nohup or time is read as the system's only where the system's runs: a local one first on the PATH does whatever it does",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "    if (pkg.deps[name]) return \"local\";",
              replace: "" }],
  },
  {
    name: "detect-literal-path-wrapper-local",
    why: "take any env on a PATH given for the system's. PATH=./tools env, the script's own env, is then read as running the command after it",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "env, nohup or time is read as the system's only where the system's runs: a local one first on the PATH does whatever it does",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "  let system = (d) => STANDARD_PATH.includes(d);",
              replace: "  let system = () => true;" }],
  },
  {
    name: "detect-readonly-assignment-fails",
    why: "let an assignment to a read-only PATH take effect. PATH then reads as the new value, which bash never takes, and a program the old one finds is reported missing",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "an assignment to a read-only PATH fails, and leaves PATH as it was",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "  if (state.pathReadonly === true) return { ...failing(`${what} after making it read-only, which fails`), stop: \"maybe\" };",
              replace: "  void what;" }],
  },
  {
    name: "detect-readonly-marks-path",
    why: "not note that readonly made PATH read-only. A later assignment then reads as taking effect, and a program the kept PATH finds is reported missing",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "an assignment to a read-only PATH fails, and leaves PATH as it was",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "        if (name === \"readonly\" && (a?.name ?? w.text) === \"PATH\") state.pathReadonly = true;",
              replace: "" }],
  },
  {
    name: "detect-readonly-prefix-runs-with-old-path",
    why: "look a command up in a PATH assigned before it while PATH is read-only. bash runs it with the old PATH, where it's found, and it's reported missing",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "an assignment to a read-only PATH fails, and leaves PATH as it was",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "  if (assigned && state.pathReadonly !== false) {",
              replace: "  if (false) {" }],
  },
  {
    name: "detect-readonly-maybe-after-andor",
    why: "forget a readonly that may or may not have run. A later assignment then reads as taking effect for sure, and a program the kept PATH finds is reported missing",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "an assignment to a read-only PATH fails, and leaves PATH as it was",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "    if (maybeState.pathReadonly !== state.pathReadonly) state.pathReadonly = \"maybe\";",
              replace: "" }],
  },
  {
    name: "detect-pipefail-any-fails",
    why: "read a pipeline under pipefail by its last command. `set -o pipefail; false | true` exits 1, and reads as passing",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "pipefail fails a pipeline when any command in it fails, and set -o pipefail is an error in a shell without it",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "  if (state.pipefail !== false) {",
              replace: "  if (false) {" }],
  },
  {
    name: "detect-pipefail-set",
    why: "never turn pipefail on. A pipeline that fails under it reads as passing",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "pipefail fails a pipeline when any command in it fails, and set -o pipefail is an error in a shell without it",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "    if (shell?.pipefail === true) state.pipefail = on;",
              replace: "    if (shell?.pipefail === true) void on;" }],
  },
  {
    name: "detect-pipefail-missing-fails",
    why: "not read set -o pipefail as an error in a shell without it. On Debian 12's dash the script exits 2 there, and reads as passing",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "pipefail fails a pipeline when any command in it fails, and set -o pipefail is an error in a shell without it",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "    else if (shell?.pipefail === false)",
              replace: "    else if (shell?.pipefail === \"never\")" }],
  },
  {
    name: "detect-pipefail-missing-ends-shell",
    why: "let a shell without pipefail go on after set -o pipefail, where dash ends. The script then reads by what follows, and passes",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "pipefail fails a pipeline when any command in it fails, and set -o pipefail is an error in a shell without it",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "      return { ...failing(`uses 'set -o pipefail', which ${shell.name}, the shell that runs the script, doesn't have`), stop: shell.badOptionEnds ?? \"maybe\" };",
              replace: "      return { ...failing(`uses 'set -o pipefail', which ${shell.name}, the shell that runs the script, doesn't have`), stop: false };" }],
  },
  {
    name: "detect-pipefail-unsure-either",
    why: "read a pipeline under a pipefail that may not be set as if it were. `jest && set -o pipefail; false | true` passes when jest fails, and is reported",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "pipefail fails a pipeline when any command in it fails, and set -o pipefail is an error in a shell without it",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "    status = state.pipefail === true ? withIt : either(withIt, r);",
              replace: "    status = withIt;" }],
  },
  {
    name: "detect-pipefail-maybe-after-andor",
    why: "forget a set -o pipefail that may or may not have run. `! false | true` after it may pass, and is reported",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "pipefail fails a pipeline when any command in it fails, and set -o pipefail is an error in a shell without it",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "    if (maybeState.pipefail !== state.pipefail) state.pipefail = \"maybe\";",
              replace: "" }],
  },
  {
    name: "detect-pipefail-writer-unsure",
    why: "take a command before the last in a pipeline to succeed. Under pipefail, echo can die when the next command closes the pipe first, and `! echo hi | true` then passes",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "pipefail fails a pipeline when any command in it fails, and set -o pipefail is an error in a shell without it",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "    const each = [...list.slice(i, j).map((_, n) => { const e = sure(i + n, run(i + n)); return e.o === \"ok\" && !writesNothing(list[i + n], ctx.shell) ? UNKNOWN : e; }), r];",
              replace: "    const each = [...list.slice(i, j).map((_, n) => run(i + n)), r];" }],
  },
  {
    name: "detect-unmodelled-option-stops",
    why: "pass over a set option the reader doesn't model. `set -n; false` runs nothing after set, passes, and is reported",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "after a set option the reader doesn't model nothing is judged, and one that changes nothing it judges is passed over",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "  } else if (!UNJUDGED_OPTIONS.has(name)) return { o: \"?\", opaque: true };",
              replace: "  }" }],
  },
  {
    name: "detect-unjudged-options-pass",
    why: "stop at options that change nothing judged, such as set -u or set -x. A script that fails after one is then never reported",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "after a set option the reader doesn't model nothing is judged, and one that changes nothing it judges is passed over",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "  \"monitor\", \"privileged\", \"functrace\", \"nounset\", \"verbose\", \"vi\", \"emacs\", \"xtrace\", \"history\", \"interactive-comments\", \"nolog\"]);",
              replace: "  ]);" }],
  },
  {
    name: "detect-unset-path-current-folder",
    why: "take no notice of unset PATH. A program only npm's PATH has then reads as found, where dash and bash look in the current folder only",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "with PATH unset, a program is looked up in the current folder only",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "          const r = assignPath(state, [\"\"], \"unsets PATH\");",
              replace: "          const r = null;" }],
  },
  {
    name: "detect-unset-functions-apart",
    why: "read unset -f PATH as unsetting the variable. It unsets a function, PATH stays, and a program it finds is reported missing",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "with PATH unset, a program is looked up in the current folder only",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "      if (fn && (!vars || last === \"f\")) return outcome;",
              replace: "      if (false) return outcome;" }],
  },
  {
    name: "detect-unset-expansion-unknown",
    why: "keep PATH after unset of a name an expansion gives. It may be PATH, and a program the current folder has is reported missing",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "with PATH unset, a program is looked up in the current folder only",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "          state.path = null;\n        } else if (w.text === \"PATH\") {",
              replace: "        } else if (w.text === \"PATH\") {" }],
  },
  {
    name: "detect-syntax-per-shell",
    why: "read bash's operators the same under every shell. dash's syntax error on |& reads as passing, and bash's &> as & there",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "bash's own operators are read as the shell that runs the script reads them, and not judged where the shells differ",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "    return has === true ? [t] : has === false ? t.otherwise : [{ t: \"unsure\" }];",
              replace: "    return [t];" }],
  },
  {
    name: "detect-syntax-unsure-where-shells-differ",
    why: "read bash's operators as bash does where the shell may be dash too. A line dash backgrounds, and passes, is reported",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "bash's own operators are read as the shell that runs the script reads them, and not judged where the shells differ",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "    return has === true ? [t] : has === false ? t.otherwise : [{ t: \"unsure\" }];",
              replace: "    return has === false ? t.otherwise : [t];" }],
  },
  {
    name: "detect-and-redirect-read",
    why: "read &> as & and a redirection in bash too. bash runs the command in the foreground, and its missing program reads as passing",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "bash's own operators are read as the shell that runs the script reads them, and not judged where the shells differ",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "    const r = /^(\\d*)(<<<|<<-?|<>|>>|>\\||>&|<&|<|>)/.exec(s.slice(i)) ?? /^()(&>>?)/.exec(s.slice(i));",
              replace: "    const r = /^(\\d*)(<<<|<<-?|<>|>>|>\\||>&|<&|<|>)/.exec(s.slice(i));" }],
  },
  {
    name: "detect-here-string-read",
    why: "read <<< as a here-document. The script is then left unread, and dash's syntax error on it reads as passing",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "bash's own operators are read as the shell that runs the script reads them, and not judged where the shells differ",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "    const r = /^(\\d*)(<<<|<<-?|<>|>>|>\\||>&|<&|<|>)/.exec(s.slice(i)) ?? /^()(&>>?)/.exec(s.slice(i));",
              replace: "    const r = /^(\\d*)(<<-?|<<<|<>|>>|>\\||>&|<&|<|>)/.exec(s.slice(i)) ?? /^()(&>>?)/.exec(s.slice(i));" }],
  },
  {
    name: "detect-dup-to-file-bash-only",
    why: "read >& before a file as every shell's. dash exits 2 on it, and it reads as passing",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "bash's own operators are read as the shell that runs the script reads them, and not judged where the shells differ",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "      else if (r[2] === \">&\" && writes && !target.expansion) tokens.push({ ...token, syntax: r[2], otherwise: [{ t: \"error\", what: \"'>&' before a file\" }] });",
              replace: "" }],
  },
  {
    name: "detect-pipe-all-bash-only",
    why: "read |& as every shell's pipe. dash exits 2 on it, and it reads as passing",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "bash's own operators are read as the shell that runs the script reads them, and not judged where the shells differ",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "      tokens.push(op === \"|&\" ? { t: \"op\", op: \"|\", syntax: op, otherwise: [{ t: \"error\", what: \"'|&'\" }] } : { t: \"op\", op });",
              replace: "      tokens.push({ t: \"op\", op: op === \"|&\" ? \"|\" : op });" }],
  },
  {
    name: "detect-here-string-bash-only",
    why: "read <<< as every shell's redirection. dash exits 2 on it, and it reads as passing",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "bash's own operators are read as the shell that runs the script reads them, and not judged where the shells differ",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "      else if (r[2] === \"<<<\") tokens.push({ ...token, syntax: r[2], otherwise: [{ t: \"error\", what: \"'<<<'\" }] });",
              replace: "" }],
  },
  {
    name: "detect-command-names-not-keywords",
    why: "read a reserved word after command as the keyword. `command if` runs a program called if, exits 127, and reads as passing",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "a reserved word after command or exec is a program's name",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "  if (named && !shell?.builtins.has(name.text)) return program(words, path, state, ctx, true);",
              replace: "" }],
  },
  {
    name: "detect-asked-keyword-is-a-name",
    why: "stop reading at a keyword the shell was asked about, even after exec or command. `exec if` exits 127, and reads as passing",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "a reserved word after command or exec is a program's name",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "  if (own === \"keyword\" && !name.quoted && !named) return { o: \"?\", opaque: true };",
              replace: "  if (own === \"keyword\" && !name.quoted) return { o: \"?\", opaque: true };" }],
  },
  {
    name: "detect-redirection-may-fail",
    why: "take a command with a redirection to run. `true < missing || exit 0; false` passes when the file isn't there, and is reported",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "a command with a redirection that may fail never surely succeeds, and one to the null device or a standard descriptor can't fail",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "const redirected = (cmd, r) => (cmd.mayFail ? { ...r, o: r.o === \"ok\" ? \"?\" : r.o, stop: r.stop === true ? \"maybe\" : r.stop } : r);",
              replace: "const redirected = (cmd, r) => r;" }],
  },
  {
    name: "detect-exit-redirect-may-go-on",
    why: "take exit to end the shell when its redirection may fail. bash goes on after the failure, and `exit 3 > missing/f; echo after` passes there",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "a command with a redirection that may fail never surely succeeds, and one to the null device or a standard descriptor can't fail",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "const redirected = (cmd, r) => (cmd.mayFail ? { ...r, o: r.o === \"ok\" ? \"?\" : r.o, stop: r.stop === true ? \"maybe\" : r.stop } : r);",
              replace: "const redirected = (cmd, r) => (cmd.mayFail ? { ...r, o: r.o === \"ok\" ? \"?\" : r.o } : r);" }],
  },
  {
    name: "detect-safe-redirections",
    why: "count a redirection to /dev/null or a standard descriptor as one that may fail. `! true 2>/dev/null` always fails, and reads as passing",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "a command with a redirection that may fail never surely succeeds, and one to the null device or a standard descriptor can't fail",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "      const mayFail = !(r[2] === \"<<<\" || (target.text === \"/dev/null\" && !target.expansion)",
              replace: "      const mayFail = true || !(r[2] === \"<<<\" || (target.text === \"/dev/null\" && !target.expansion)" }],
  },
  {
    name: "detect-pipefail-asked",
    why: "never ask the shell about pipefail. bash's then reads as missing, and every set -o pipefail as an error",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "the shell is asked whether it has pipefail and bash's operators, and whether a bad set option ends it",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "    'if (set -o pipefail) >/dev/null 2>&1; then printf \"pipefail\\\\tyes\\\\n\"; fi; ' +\n",
              replace: "" }],
  },
  {
    name: "detect-bad-option-ends-asked",
    why: "never ask whether a bad set option ends the shell. dash's then reads as going on after set -o pipefail, where it exits 2",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "the shell is asked whether it has pipefail and bash's operators, and whether a bad set option ends it",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "    'if ! (set -o reeve-no-such-option; exit 0) >/dev/null 2>&1; then printf \"bad-option-ends\\\\tyes\\\\n\"; fi; ' +\n",
              replace: "" }],
  },
  {
    name: "detect-syntax-asked",
    why: "never ask the shell about bash's operators. bash's |& then reads as a syntax error",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "the shell is asked whether it has pipefail and bash's operators, and whether a bad set option ends it",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "    'if (eval \"true |& true\") >/dev/null 2>&1; then printf \"syntax\\\\t|&\\\\n\"; fi; ' +\n",
              replace: "" }],
  },
  {
    name: "detect-shells-differ-maybe",
    why: "read shells asked together as having what any of them has. dash's syntax error on |& then reads as bash's pipe",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "shells that may run a script, asked together, differ where one has an operator or ends on a bad option and another doesn't",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "const allHave = (values) => (values.every((v) => v === true) ? true : values.every((v) => v === false) ? false : \"maybe\");",
              replace: "const allHave = (values) => values.some((v) => v === true);" }],
  },
  {
    name: "board-closed-roots-every-page",
    why: "read only the search's first page. A closed phase past its first hundred then goes unfound, and a phase that never had a card never gets one",
    test: "test/resume-work-board.test.mjs",
    expectRed: "every page of the search is read, and more phases than it returns are a partial read",
    edits: [{ file: ".agents/skills/resume-work/scripts/plan.mjs",
              find: "    if (!conn.pageInfo.hasNextPage) return { nodes, complete: seen >= conn.issueCount };\n    after = conn.pageInfo.endCursor;",
              replace: "    return { nodes, complete: seen >= conn.issueCount };\n    after = conn.pageInfo.endCursor;" }],
  },
  {
    name: "board-closed-roots-are-phases",
    why: "take a closed issue the search found for a phase though it has a parent by now. A task is then synced as a phase of its own, beside its parent",
    test: "test/resume-work-board.test.mjs",
    expectRed: "what the search returns is checked against the issue as it is now",
    edits: [{ file: ".agents/skills/resume-work/scripts/plan.mjs",
              find: "i.state === \"CLOSED\" && !i.parent && isPhase(i)",
              replace: "i.state === \"CLOSED\" && isPhase(i)" }],
  },
  {
    name: "board-closed-roots-join-parents",
    why: "find closed phases from GitHub and leave them out of the parents synced. A phase that never had a card is then found, and its tasks still never appear in Done",
    test: "test/resume-work-board.test.mjs",
    expectRed: "a closed phase that never had a card is found, its tasks are synced, and it gets a card of its own in Done",
    edits: [{ file: ".agents/skills/resume-work/scripts/lib.mjs",
              find: "  for (const root of roots) if (!parents.has(root.number)) parents.set(root.number, root);\n",
              replace: "" }],
  },
  {
    name: "board-uncarded-roots-get-cards",
    why: "give no card to a closed phase that never had one. Its tasks reach Done, and the phase itself never appears on the board",
    test: "test/resume-work-board.test.mjs",
    expectRed: "a closed phase that never had a card is found, its tasks are synced, and it gets a card of its own in Done",
    edits: [{ file: ".agents/skills/resume-work/scripts/lib.mjs",
              find: "export const uncardedRoots = (roots, cards) => roots.filter((root) => !cards.has(root.number));",
              replace: "export const uncardedRoots = () => [];" }],
  },
  {
    name: "board-closed-roots-state-now",
    why: "take an issue the search found for closed though it was opened again. Its open tasks are then synced as a closed phase's",
    test: "test/resume-work-board.test.mjs",
    expectRed: "what the search returns is checked against the issue as it is now",
    edits: [{ file: ".agents/skills/resume-work/scripts/plan.mjs",
              find: "i.state === \"CLOSED\" && !i.parent && isPhase(i)",
              replace: "!i.parent && isPhase(i)" }],
  },
  {
    name: "board-closed-roots-search-features",
    why: "search only for closed issues with sub-issues. A Feature that closed with none never gets a card",
    test: "test/resume-work-board.test.mjs",
    expectRed: "closed phases are read from GitHub: closed issues with no parent that have sub-issues or are Features",
    edits: [{ file: ".agents/skills/resume-work/scripts/plan.mjs",
              find: "is:closed no:parent-issue (has:sub-issue OR type:Feature)",
              replace: "is:closed no:parent-issue has:sub-issue" }],
  },
  {
    name: "board-closed-roots-searched-narrowly",
    why: "search for every closed issue, not only the closed phases. A sync then reads a page for each hundred closed issues, and gives up past a thousand",
    test: "test/resume-work-board.test.mjs",
    expectRed: "reading them takes one request, however many issues are closed",
    edits: [{ file: ".agents/skills/resume-work/scripts/plan.mjs",
              find: "is:closed no:parent-issue (has:sub-issue OR type:Feature)",
              replace: "is:closed" }],
  },
  {
    name: "board-closed-roots-capped-search-partial",
    why: "take the search's last page for the end of the phases. Past its first thousand results the rest go unread, and the sync moves cards from part of the plan",
    test: "test/resume-work-board.test.mjs",
    expectRed: "every page of the search is read, and more phases than it returns are a partial read",
    edits: [{ file: ".agents/skills/resume-work/scripts/plan.mjs",
              find: "    if (!conn.pageInfo.hasNextPage) return { nodes, complete: seen >= conn.issueCount };",
              replace: "    if (!conn.pageInfo.hasNextPage) return { nodes, complete: true };" }],
  },
  {
    name: "board-closed-roots-advanced-search",
    why: "use GitHub's older issue search, which reads none of these qualifiers. It finds no closed phase at all",
    test: "test/resume-work-board.test.mjs",
    expectRed: "closed phases are read from GitHub: closed issues with no parent that have sub-issues or are Features",
    edits: [{ file: ".agents/skills/resume-work/scripts/plan.mjs",
              find: "search(query:$q, type:ISSUE_ADVANCED,",
              replace: "search(query:$q, type:ISSUE," }],
  },
  {
    name: "board-phase-counts-features",
    why: "count only issues with sub-issues as phases. A Feature that closed with none is then never found, and never gets a card",
    test: "test/resume-work-board.test.mjs",
    expectRed: "closed phases are read from GitHub: closed issues with no parent that have sub-issues or are Features",
    edits: [{ file: ".agents/skills/resume-work/scripts/lib.mjs",
              find: " || issue?.issueType?.name === \"Feature\";",
              replace: ";" }],
  },
  {
    name: "board-card-phase-by-type",
    why: "mark a card as a phase's only when its issue has sub-issues. A closed Feature's card is then never moved to Done",
    test: "test/resume-work-board.test.mjs",
    expectRed: "a card is a phase's when its issue has sub-issues or is a Feature, as the plan counts a phase",
    edits: [{ file: ".agents/skills/resume-work/scripts/lib.mjs",
              find: "                                 phase: isPhase(it.content) });",
              replace: "                                 phase: (it.content.subIssues?.totalCount ?? 0) > 0 });" }],
  },
  {
    name: "ci-shadow-bound-elsewhere-required",
    why: "refuse the shadow check whatever App a rule binds it to. Bound to another App, which alone can meet it, every pull request on the base then blocks for good, and that App's run is never required",
    test: "test/ci-read-completely.test.mjs",
    expectRed: "a shadow check bound to another App is required like any other, and one reeve's result could meet is refused",
    edits: [{ file: "src/pr.mjs",
              find: "  const reeveMeets = (c) => c.context === shadow && (c.app == null || appId == null || String(c.app) === String(appId));",
              replace: "  const reeveMeets = (c) => c.context === shadow;" }],
  },
  {
    name: "ci-shadow-untold-app-refused",
    why: "take a binding for another App's where reeve's own App can't be told. It may be reeve's, whose shadow result then passes the rule unrefused",
    test: "test/ci-read-completely.test.mjs",
    expectRed: "a shadow check bound to another App is required like any other, and one reeve's result could meet is refused",
    edits: [{ file: "src/pr.mjs",
              find: "  const reeveMeets = (c) => c.context === shadow && (c.app == null || appId == null || String(c.app) === String(appId));",
              replace: "  const reeveMeets = (c) => c.context === shadow && (c.app == null || String(c.app) === String(appId));" }],
  },
  {
    name: "ci-shadow-own-app-refused",
    why: "take a binding to reeve's own App for another App's. reeve's shadow result then meets the rule, and passes it whatever reeve found",
    test: "test/ci-read-completely.test.mjs",
    expectRed: "a shadow check bound to another App is required like any other, and one reeve's result could meet is refused",
    edits: [{ file: "src/pr.mjs",
              find: "  const reeveMeets = (c) => c.context === shadow && (c.app == null || appId == null || String(c.app) === String(appId));",
              replace: "  const reeveMeets = (c) => c.context === shadow && (c.app == null || appId == null);" }],
  },
  {
    name: "ci-shadow-unbound-refused",
    why: "refuse only a bound shadow requirement. Unbound, any result under the name meets it, reeve's shadow result among them",
    test: "test/ci-read-completely.test.mjs",
    expectRed: "a shadow check bound to another App is required like any other, and one reeve's result could meet is refused",
    edits: [{ file: "src/pr.mjs",
              find: "  const reeveMeets = (c) => c.context === shadow && (c.app == null || appId == null || String(c.app) === String(appId));",
              replace: "  const reeveMeets = (c) => c.context === shadow && (appId == null || String(c.app) === String(appId));" }],
  },
  {
    name: "typecheck-core-modules-marked",
    why: "drop `// @ts-check` from the verdict module. It is then parsed and never checked, and a missing import or a variable used too early there passes CI",
    test: "test/typecheck.test.mjs",
    expectRed: "the core verdict and evidence modules are type-checked: each starts with // @ts-check and is in the project",
    edits: [{ file: "src/verdict.mjs",
              find: "// @ts-check\n// verdict — the single answer",
              replace: "// verdict — the single answer" }],
  },
  {
    name: "typecheck-src-in-project",
    why: "type-check tools alone, leaving src, scripts and test out of the project. Nothing in tools imports a core module, so every one goes unchecked, though each still says @ts-check",
    test: "test/typecheck.test.mjs",
    expectRed: "the core verdict and evidence modules are type-checked: each starts with // @ts-check and is in the project",
    edits: [{ file: "tsconfig.json",
              find: "    \"src/**/*\",\n    \"scripts/**/*\",\n    \"tools/**/*\",\n    \"test/**/*\"\n",
              replace: "    \"tools/**/*\"\n" }],
  },
  {
    name: "typecheck-errors-fail",
    why: "call a name nothing defines in the verdict module. The type check must fail on it, as it would on a missing import",
    test: "test/typecheck.test.mjs",
    expectRed: "the project type-checks",
    edits: [{ file: "src/verdict.mjs",
              find: "export function computeVerdict(i) {\n",
              replace: "export function computeVerdict(i) {\n  noSuchName();\n" }],
  },
  {
    name: "lint-no-undef",
    why: "turn no-undef off. A name nothing defines, a missing import say, then reaches a test run before anything reports it",
    test: "test/typecheck.test.mjs",
    expectRed: "lint flags a name nothing defines and a variable read before its definition, and not a hoisted function, a later read or a Node global",
    edits: [{ file: "eslint.config.js",
              find: "\"no-undef\": \"error\", ",
              replace: "" }],
  },
  {
    name: "lint-no-use-before-define",
    why: "turn no-use-before-define off. A variable read before its definition in the same scope, the temporal dead zone, then throws only when that line runs",
    test: "test/typecheck.test.mjs",
    expectRed: "lint flags a name nothing defines and a variable read before its definition, and not a hoisted function, a later read or a Node global",
    edits: [{ file: "eslint.config.js",
              find: "\"no-use-before-define\": [\"error\", { functions: false, classes: false, variables: false }]",
              replace: "\"no-use-before-define\": \"off\"" }],
  },
  {
    name: "lint-hoisted-functions-pass",
    why: "flag functions called above their declaration. They are hoisted and run fine, and the rule then buries the one use that throws",
    test: "test/typecheck.test.mjs",
    expectRed: "lint flags a name nothing defines and a variable read before its definition, and not a hoisted function, a later read or a Node global",
    edits: [{ file: "eslint.config.js",
              find: "\"no-use-before-define\": [\"error\", { functions: false, classes: false, variables: false }]",
              replace: "\"no-use-before-define\": [\"error\", { functions: true, classes: false, variables: false }]" }],
  },
  {
    name: "lint-later-reads-pass",
    why: "flag a variable a function reads from a scope that defines it below. The function runs later, when it is defined, and the flag is noise",
    test: "test/typecheck.test.mjs",
    expectRed: "lint flags a name nothing defines and a variable read before its definition, and not a hoisted function, a later read or a Node global",
    edits: [{ file: "eslint.config.js",
              find: "\"no-use-before-define\": [\"error\", { functions: false, classes: false, variables: false }]",
              replace: "\"no-use-before-define\": [\"error\", { functions: false, classes: false, variables: true }]" }],
  },
  {
    name: "lint-node-globals",
    why: "lint without Node's globals. process, Buffer and the rest then read as names nothing defines",
    test: "test/typecheck.test.mjs",
    expectRed: "lint flags a name nothing defines and a variable read before its definition, and not a hoisted function, a later read or a Node global",
    edits: [{ file: "eslint.config.js",
              find: "globals: globals.nodeBuiltin",
              replace: "globals: {}" }],
  },
  {
    name: "temp-helper-leaves-signals-alone",
    why: "listen for SIGINT and SIGTERM in the temp helper. A listener switches the default action off and runs only between tasks, so a test busy in synchronous code runs on after Ctrl-C or a runner's SIGTERM, and then exits 0",
    test: "test/temp-teardown.test.mjs",
    expectRed: "a test busy in synchronous code is ended at once by SIGTERM or SIGINT, and by that signal",
    edits: [{ file: "test/fixtures/temp.mjs",
              find: "let home = null;\n",
              replace: "let home = null;\nprocess.on(\"SIGTERM\", () => {});\nprocess.on(\"SIGINT\", () => {});\n" }],
  },
  {
    name: "runner-passes-the-signal-on",
    why: "stop the run without passing the signal to the test that is running. A test busy in synchronous code then runs until the grace period kills it",
    test: "test/test-runner.test.mjs",
    expectRed: "stopped by SIGTERM or SIGINT, the runner ends the busy test at once, removes the run's folder, and ends by that signal",
    edits: [{ file: "scripts/test.mjs",
              find: "  current.kill(signal);\n",
              replace: "" }],
  },
  {
    name: "runner-removes-its-folder-on-a-signal",
    why: "end a stopped run without removing its folder. Every folder the stopped test made stays in the temp directory",
    test: "test/test-runner.test.mjs",
    expectRed: "stopped by SIGTERM or SIGINT, the runner ends the busy test at once, removes the run's folder, and ends by that signal",
    edits: [{ file: "scripts/test.mjs",
              find: "  remove();\n  for (const s of [\"SIGINT\", \"SIGTERM\"]) process.removeListener(s, onSignal);",
              replace: "  for (const s of [\"SIGINT\", \"SIGTERM\"]) process.removeListener(s, onSignal);" }],
  },
  {
    name: "runner-ends-by-the-signal",
    why: "end a stopped run with status 0. Whatever stopped it, a terminal or a runner's timeout, then reads the run as having passed",
    test: "test/test-runner.test.mjs",
    expectRed: "stopped by SIGTERM or SIGINT, the runner ends the busy test at once, removes the run's folder, and ends by that signal",
    edits: [{ file: "scripts/test.mjs",
              find: "  process.kill(process.pid, signal);\n};",
              replace: "  process.exit(0);\n};" }],
  },
  {
    name: "runner-stops-at-the-first-failure",
    why: "go on past a test that fails. The run then ends 0 whenever the last test passes, and a failure scrolls past",
    test: "test/test-runner.test.mjs",
    expectRed: "the runner runs the test files in order and stops at the first that fails",
    edits: [{ file: "scripts/test.mjs",
              find: "  if (code !== 0) { remove(); process.exit(1); }",
              replace: "  if (code !== 0) continue;" }],
  },
  {
    name: "runner-skips-the-escape-probe",
    why: "run the escape probe with the rest. It writes decoys into ~/.reeve/canary/, which a running daemon reads, so it is run deliberately and alone",
    test: "test/test-runner.test.mjs",
    expectRed: "and passes when every one does, running only test files and never the escape probe",
    edits: [{ file: "scripts/test.mjs",
              find: "f.endsWith(\".test.mjs\") && f !== \"escape.test.mjs\"",
              replace: "f.endsWith(\".test.mjs\")" }],
  },
  {
    name: "runner-removes-its-folder-at-the-end",
    why: "leave the run's folder after a run that passes. Every run then adds one to the temp directory",
    test: "test/test-runner.test.mjs",
    expectRed: "what the tests leave in the temp directory is gone once the run ends, passed or failed",
    edits: [{ file: "scripts/test.mjs",
              find: "}\nremove();\n",
              replace: "}\n" }],
  },
  {
    name: "runner-gives-tests-its-folder",
    why: "run the tests under the temp directory the runner was given. What they leave there is no run's to remove",
    test: "test/test-runner.test.mjs",
    expectRed: "what the tests leave in the temp directory is gone once the run ends, passed or failed",
    edits: [{ file: "scripts/test.mjs",
              find: "const env = { ...process.env, TMPDIR: run, TEMP: run, TMP: run };",
              replace: "const env = { ...process.env };" }],
  },
  {
    name: "detect-unset-last-option-wins",
    why: "read unset -fv as unsetting functions only, wherever -f is given. dash takes the last of the two, so -fv unsets the variable, and a program only npm's PATH has is then missing",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "unset reads -f and -v as the shell does: dash takes the last, and bash refuses both at once",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "      if (fn && (!vars || last === \"f\")) return outcome;",
              replace: "      if (fn) return outcome;" }],
  },
  {
    name: "detect-unset-options-per-shell",
    why: "read unset's -f and -v as dash does in every shell. bash refuses them together and keeps PATH, so a program npm's PATH has reads as missing",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "unset reads -f and -v as the shell does: dash takes the last, and bash refuses both at once",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "      const lastWins = fn && vars ? ctx.shell?.unsetLastOptionWins : null;",
              replace: "      const lastWins = fn && vars ? true : null;" }],
  },
  {
    name: "detect-unset-refused-keeps-path",
    why: "leave PATH unknown after an unset bash refuses. PATH is intact there, and a program missing from it is then never reported",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "unset reads -f and -v as the shell does: dash takes the last, and bash refuses both at once",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "      if (lastWins === false) return UNKNOWN;",
              replace: "" }],
  },
  {
    name: "detect-p-dash-takes-no-operands",
    why: "take readonly -p's operands in dash too. dash only lists, so PATH stays writable, and the assignment after it takes effect",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "readonly and export with -p take their operands only in a shell that does: dash only lists",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "      if (listing && ctx.shell?.pTakesOperands === false) return OK;",
              replace: "" }],
  },
  {
    name: "detect-p-bash-takes-operands",
    why: "read -p's operands as unknown in bash too. bash assigns them, so export -p PATH=/nowhere leaves sh unfound, and that reads as present",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "readonly and export with -p take their operands only in a shell that does: dash only lists",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "      if (listing && ctx.shell?.pTakesOperands !== true) {",
              replace: "      if (listing) {" }],
  },
  {
    name: "detect-p-unsure-where-shells-differ",
    why: "take -p's operands where the shells that may run the script differ. dash ignores them and passes, and the script is reported",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "readonly and export with -p take their operands only in a shell that does: dash only lists",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "      if (listing && ctx.shell?.pTakesOperands !== true) {",
              replace: "      if (false) {" }],
  },
  {
    name: "detect-bare-readonly-assignment-ends",
    why: "let a bare assignment to a read-only PATH maybe go on. It ends dash and bash, so a script that fails there reads as present",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "a bare assignment to a read-only PATH ends the shell, where one before a command may not",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "      if (r) return r.o === \"fail\" ? { ...r, stop: ctx.shell?.readonlyAssignEnds ?? \"maybe\" } : r;",
              replace: "      if (r) return r;" }],
  },
  {
    name: "detect-pipefail-quiet-commands",
    why: "doubt every command before the last under pipefail. `true` writes nothing, the pipe closing can't stop it, and `! true | true` then reads as present",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "under pipefail, a command before the last that writes nothing surely succeeds",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "    const each = [...list.slice(i, j).map((_, n) => { const e = sure(i + n, run(i + n)); return e.o === \"ok\" && !writesNothing(list[i + n], ctx.shell) ? UNKNOWN : e; }), r];",
              replace: "    const each = [...list.slice(i, j).map((_, n) => { const e = sure(i + n, run(i + n)); return e.o === \"ok\" ? UNKNOWN : e; }), r];" }],
  },
  {
    name: "detect-unset-options-asked",
    why: "never ask how unset reads -f and -v together. dash's then reads as refusing them, as bash's does",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "the shell is asked how unset reads -f and -v, whether -p takes operands, and whether assigning a read-only variable ends it",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "    'if (x=1; unset -fv x; test -z \"${x+set}\") >/dev/null 2>&1 && (x=1; unset -vf x; test -n \"${x+set}\") >/dev/null 2>&1; ' +\n    'then printf \"unset-last-option-wins\\\\tyes\\\\n\"; fi; ' +\n",
              replace: "" }],
  },
  {
    name: "detect-p-operands-asked",
    why: "never ask whether -p takes operands. bash's then reads as dash's",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "the shell is asked how unset reads -f and -v, whether -p takes operands, and whether assigning a read-only variable ends it",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "    'if (unset x; readonly -p x=1 >/dev/null; test \"${x-}\" = 1) >/dev/null 2>&1; then printf \"p-takes-operands\\\\tyes\\\\n\"; fi; ' +\n",
              replace: "" }],
  },
  {
    name: "detect-readonly-ends-asked",
    why: "never ask whether assigning a read-only variable ends the shell. It then reads as going on in both",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "the shell is asked how unset reads -f and -v, whether -p takes operands, and whether assigning a read-only variable ends it",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "    'if ! (readonly x=1; x=2; exit 0) >/dev/null 2>&1; then printf \"readonly-assign-ends\\\\tyes\\\\n\"; fi; ' +\n",
              replace: "" }],
  },
  {
    name: "detect-pipeline-expansions-unsure",
    why: "take a command in a pipeline to succeed whatever its expansions do. $((1/0)) there fails that command in its subshell, and `! true | true $((1/0))` passes",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "a command in a pipeline with an expansion that may fail never surely succeeds",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "  const sure = (k, e) => (e.o === \"ok\" && (list[k].expansion || assigns(list[k], ctx.shell)) ? UNKNOWN : e);",
              replace: "  const sure = (k, e) => e;" }],
  },
  {
    name: "detect-p-bare-name-keeps-path",
    why: "forget PATH after `export -p PATH` where the shells may differ. A bare name gives PATH no value in either, and a program neither finds reads as present",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "readonly and export with -p take their operands only in a shell that does: dash only lists",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "        if (state.pathReadonly !== true && operands.some((w) => assignment(w, ctx.shell)?.name === \"PATH\" || w.expansion)) state.path = null;",
              replace: "        if (state.pathReadonly !== true && operands.some((w) => (assignment(w, ctx.shell)?.name ?? w.text) === \"PATH\" || w.expansion)) state.path = null;" }],
  },
  {
    name: "detect-pipeline-here-string-expansions",
    why: "take a command in a pipeline to succeed whatever its here-string expands to. $((1/0)) there fails that command in its subshell, and `! true | true <<< $((1/0))` passes in bash",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "a command in a pipeline with an expansion that may fail never surely succeeds",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "      const token = { t: \"redir\", writes, subst: target.subst, expansion: target.expansion, mayFail };",
              replace: "      const token = { t: \"redir\", writes, subst: target.subst, mayFail };" }],
  },
  {
    name: "detect-here-string-ends-script",
    why: "doubt every command whose here-string holds an expansion. Outside a pipeline one that fails ends the script, which fails either way, and `true <<< \"$HOME\" && no-such-runner` then reads as present",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "outside a pipeline, a here-string's expansion that fails ends the script, so its command is read as succeeding",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "      const mayFail = !(r[2] === \"<<<\" || (target.text === \"/dev/null\" && !target.expansion)",
              replace: "      const mayFail = target.expansion || !(r[2] === \"<<<\" || (target.text === \"/dev/null\" && !target.expansion)" }],
  },
  {
    name: "detect-unset-either-f-last-keeps-path",
    why: "forget PATH after unset -vf where either shell may run the script. dash takes the final -f and bash refuses both, so PATH is kept in each, and a program neither finds reads as present",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "where either shell may run the script, unset given -f and -v may fail, and with -f last keeps PATH",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "      if (fn && (!vars || last === \"f\")) return outcome;",
              replace: "      if (fn && (!vars || (lastWins === true && last === \"f\"))) return outcome;" }],
  },
  {
    name: "detect-unset-either-may-fail",
    why: "take unset given -f and -v to succeed where either shell may run the script. bash refuses it with status 1, so `! unset -vf x` passes there, and reads as broken",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "where either shell may run the script, unset given -f and -v may fail, and with -f last keeps PATH",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "      const outcome = unsure ? UNKNOWN : OK;",
              replace: "      const outcome = OK;" }],
  },
  {
    name: "detect-pipeline-assignments-unsure",
    why: "take a command in a pipeline with an assignment to succeed. An assignment to a read-only variable fails it in its subshell, bash's own EUID among them, and `set -o pipefail; ! EUID=1 | true` passes in bash",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "a command in a pipeline with an assignment never surely succeeds: a read-only variable fails it",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "  const sure = (k, e) => (e.o === \"ok\" && (list[k].expansion || assigns(list[k], ctx.shell)) ? UNKNOWN : e);",
              replace: "  const sure = (k, e) => (e.o === \"ok\" && list[k].expansion ? UNKNOWN : e);" }],
  },
  {
    name: "detect-p-probe-own-variable",
    why: "probe -p with whatever x the environment holds. With x=1 there, dash reads as taking -p's operands, and a script it passes reads as broken",
    test: "test/detect-shell-scripts.test.mjs",
    expectRed: "whether -p takes operands is asked with a variable of the probe's own, whatever the environment holds",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "    'if (unset x; readonly -p x=1 >/dev/null; test \"${x-}\" = 1) >/dev/null 2>&1; then printf \"p-takes-operands\\\\tyes\\\\n\"; fi; ' +\n",
              replace: "    'if (readonly -p x=1 >/dev/null; test \"${x-}\" = 1) >/dev/null 2>&1; then printf \"p-takes-operands\\\\tyes\\\\n\"; fi; ' +\n" }],
  },
  {
    name: "reporter-fails-assertions-only",
    why: "report every error a test throws as a failed assertion. A test that died with a TypeError where its assertion should have failed then reads as catching the stub",
    test: "test/stubsweep-node-test.test.mjs",
    expectRed: "a node:test test that throws where its assertion should fail is not read as caught",
    edits: [{ file: "scripts/test-reporter.mjs",
              find: "    yield cause?.code === \"ERR_ASSERTION\" ? `FAIL  ${name}\\n`",
              replace: "    yield true ? `FAIL  ${name}\\n`" }],
  },
  {
    name: "reporter-prints-passes",
    why: "print only failures. A passing test then counts no assertion, so a file that stops short of its control run can't be told from one that finished",
    test: "test/stubsweep-node-test.test.mjs",
    expectRed: "a node:test test that throws where its assertion should fail is not read as caught",
    edits: [{ file: "scripts/test-reporter.mjs",
              find: "    if (type === \"test:pass\") { yield `PASS  ${name}\\n`; continue; }",
              replace: "    if (type === \"test:pass\") continue;" }],
  },
  {
    name: "reporter-silent-for-suites",
    why: "print a describe block as a test. A group is not an assertion, and an empty one then counts as one that held",
    test: "test/test-reporter.test.mjs",
    expectRed: "a describe block prints nothing, and the tests in it print",
    edits: [{ file: "scripts/test-reporter.mjs",
              find: "    if (data.details?.type === \"suite\" || data.skip !== undefined || data.todo !== undefined) continue;",
              replace: "    if (data.skip !== undefined || data.todo !== undefined) continue;" }],
  },
  {
    name: "reporter-silent-for-skips",
    why: "print a skipped or todo test as passing. It ran nothing, and it then counts as an assertion that held",
    test: "test/test-reporter.test.mjs",
    expectRed: "a skipped or todo test prints nothing",
    edits: [{ file: "scripts/test-reporter.mjs",
              find: "    if (data.details?.type === \"suite\" || data.skip !== undefined || data.todo !== undefined) continue;",
              replace: "    if (data.details?.type === \"suite\") continue;" }],
  },
  {
    name: "reporter-groups-own-failures-only",
    why: "report a test with subtests as a test. Its subtests' failure then prints FAIL under its name too, and a stub that breaks one child reads as caught by a manifest entry naming the parent",
    test: "test/test-reporter.test.mjs",
    expectRed: "a test with subtests prints only a failure of its own: its subtests' failures are theirs",
    edits: [{ file: "scripts/test-reporter.mjs",
              find: "    if (hadSubtest[n] && (type === \"test:pass\" || error?.failureType === \"subtestsFailed\")) continue;\n",
              replace: "" }],
  },
  {
    name: "reporter-qualified-names",
    why: "name a test without the path to it. The same name under two groups then prints the same line, and a stub that breaks the wrong one reads as caught",
    test: "test/test-reporter.test.mjs",
    expectRed: "a test inside another is named with the path to it, so the same name in two places reads as two tests",
    edits: [{ file: "scripts/test-reporter.mjs",
              find: "    const name = [...path.slice(0, n), oneLine(data.name)].join(\" > \");",
              replace: "    const name = oneLine(data.name);" }],
  },
  {
    name: "reporter-one-line-names",
    why: "print a test's name as given. A name with a line break then splits across lines, and the sweep reads half of it",
    test: "test/test-reporter.test.mjs",
    expectRed: "a name on several lines prints on one",
    edits: [{ file: "scripts/test-reporter.mjs",
              find: "const oneLine = (name) => String(name).replace(/\\s+/g, \" \").trim();",
              replace: "const oneLine = (name) => String(name);" }],
  },
  {
    name: "typecheck-compile-cache-off",
    why: "run tsc and eslint with Node's compile cache on. Each writes a node-compile-cache folder into the temp directory, and CI fails the test file for what it left there",
    test: "test/typecheck.test.mjs",
    expectRed: "the type checker and the linter leave nothing in the temp directory",
    edits: [{ file: "test/typecheck.test.mjs",
              find: "const childEnv = { ...process.env, NODE_DISABLE_COMPILE_CACHE: \"1\", TMPDIR: childTmp, TEMP: childTmp, TMP: childTmp };",
              replace: "const childEnv = { ...process.env, TMPDIR: childTmp, TEMP: childTmp, TMP: childTmp };" }],
  },
  {
    name: "runner-refuses-an-empty-run",
    why: "pass a run that ran no test. A wrong folder, or tests renamed out of the pattern, then reads as a green suite",
    test: "test/test-runner.test.mjs",
    expectRed: "a run with no test file to run fails, rather than passing on nothing",
    edits: [{ file: "scripts/test.mjs",
              find: "if (!files.length) {",
              replace: "if (false) {" }],
  },
  {
    name: "runner-keeps-listening-until-cleanup",
    why: "drop the runner's listener on the first signal. A second one, while a test that caught the first runs on, then ends the runner before it removes the run's folder",
    test: "test/test-runner.test.mjs",
    expectRed: "a second SIGTERM while a test outlasts the first doesn't end the runner before it removes the run's folder",
    edits: [{ file: "scripts/test.mjs",
              find: "for (const signal of [\"SIGINT\", \"SIGTERM\"]) process.on(signal, onSignal);",
              replace: "for (const signal of [\"SIGINT\", \"SIGTERM\"]) process.once(signal, onSignal);" }],
  },
  {
    name: "temp-watcher-started",
    why: "make folders without telling a watcher. A test killed outright, or stopped by a signal, then leaves every one, since no exit listener runs",
    test: "test/temp-teardown.test.mjs",
    expectRed: "and its folders are gone once it has ended, by a signal, by Ctrl-C to its group, or killed outright, with no suite runner",
    edits: [{ file: "test/fixtures/temp.mjs",
              find: "    watch(dir);\n",
              replace: "" }],
  },
  {
    name: "temp-watcher-removes-at-end",
    why: "watch without removing anything when the test is gone. The folders of a killed test then stay",
    test: "test/temp-teardown.test.mjs",
    expectRed: "and its folders are gone once it has ended, by a signal, by Ctrl-C to its group, or killed outright, with no suite runner",
    edits: [{ file: "test/fixtures/temp-watcher.mjs",
              find: "process.stdin.on(\"end\", removeAll);",
              replace: "" }],
  },
  {
    name: "temp-watcher-in-its-own-group",
    why: "start the watcher in the test's own process group. Ctrl-C at a terminal then stops the watcher with the test, before it has removed anything",
    test: "test/temp-teardown.test.mjs",
    expectRed: "and its folders are gone once it has ended, by a signal, by Ctrl-C to its group, or killed outright, with no suite runner",
    edits: [{ file: "test/fixtures/temp.mjs",
              find: "{ detached: true, stdio:",
              replace: "{ detached: false, stdio:" }],
  },
  {
    name: "sweep-runs-node-test-reporter",
    why: "run each test file with node's own reporter. A node:test file then prints neither PASS nor FAIL, and every stub on it reads as CRASHED",
    test: "test/stubsweep-node-test.test.mjs",
    expectRed: "a node:test file's stub is caught by the test it names",
    edits: [{ file: "scripts/stub-sweep.mjs",
              find: "  const child = spawn(process.execPath, [`--test-reporter=${TEST_REPORTER}`, \"--test-reporter-destination=stdout\", join(ROOT, file)],",
              replace: "  const child = spawn(process.execPath, [join(ROOT, file)]," }],
  },
  {
    name: "round5-assignment-positions",
    why: "doubt every word shaped like an assignment. `true X=1` then reads as able to fail, and `set -o pipefail; ! true X=1 | true`, which fails in both shells, goes unjudged",
    test: "test/detect-shell-scripts-round5.test.mjs",
    expectRed: "an assignment-shaped argument isn't an assignment: only a leading one, or export's and readonly's operands, can fail",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "  const sure = (k, e) => (e.o === \"ok\" && (list[k].expansion || assigns(list[k], ctx.shell)) ? UNKNOWN : e);",
              replace: "  const sure = (k, e) => (e.o === \"ok\" && (list[k].expansion || list[k].words.some((w) => assignment(w, ctx.shell))) ? UNKNOWN : e);" }],
  },
  {
    name: "round5-export-operands-assign",
    why: "treat export's operands as arguments. An export to a read-only variable fails in its subshell, and `readonly x; ! true | export x=1`, which passes in both shells, reads as broken",
    test: "test/detect-shell-scripts-round5.test.mjs",
    expectRed: "an assignment-shaped argument isn't an assignment: only a leading one, or export's and readonly's operands, can fail",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "  return Boolean(name && !name.quoted && ASSIGNING.has(name.text) && cmd.words.slice(1).some((w) => assignment(w, shell)));",
              replace: "  return false;" }],
  },
  {
    name: "round5-exit-writes-nothing",
    why: "count exit among the commands that may write. The pipe closing can't stop it, and `set -o pipefail; ! exit 0 | true`, which fails in both shells, goes unjudged",
    test: "test/detect-shell-scripts-round5.test.mjs",
    expectRed: "under pipefail, exit before the last command writes nothing, so it surely succeeds as its status says",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "  return !word || (!word.expansion && [\"true\", \":\", \"exit\"].includes(word.text));",
              replace: "  return !word || (!word.expansion && [\"true\", \":\"].includes(word.text));" }],
  },
  {
    name: "round5-p-export-succeeds",
    why: "read export -p as unsure where either shell may run the script. It succeeds in both when it assigns only a writable PATH, and `! export -p PATH=/nowhere` goes unjudged",
    test: "test/detect-shell-scripts-round5.test.mjs",
    expectRed: "where either shell may run the script, export -p to a writable PATH succeeds in both, and a read-only PATH keeps its value",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "        return onlyPath && state.pathReadonly === false ? OK : UNKNOWN;",
              replace: "        return UNKNOWN;" }],
  },
  {
    name: "round5-p-readonly-path-kept",
    why: "forget a read-only PATH after export -p where either shell may run the script. Neither changes it, and a program neither finds reads as present",
    test: "test/detect-shell-scripts-round5.test.mjs",
    expectRed: "where either shell may run the script, export -p to a writable PATH succeeds in both, and a read-only PATH keeps its value",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "        if (state.pathReadonly !== true && operands.some((w) => assignment(w, ctx.shell)?.name === \"PATH\" || w.expansion)) state.path = null;",
              replace: "        if (operands.some((w) => assignment(w, ctx.shell)?.name === \"PATH\" || w.expansion)) state.path = null;" }],
  },
  {
    name: "round5-unset-readonly-fails",
    why: "read unset -fv of a read-only PATH as unsure where either shell may run the script. It fails in both, and the script that goes on reads as present",
    test: "test/detect-shell-scripts-round5.test.mjs",
    expectRed: "where either shell may run the script, unset -fv of a read-only PATH fails in both",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "        if (unsure && !w.expansion && !w.glob && w.text === \"PATH\" && state.pathReadonly === true)\n          return { ...failing(\"unsets PATH after making it read-only, which fails in every shell that may run it\"), stop: \"maybe\" };\n",
              replace: "" }],
  },
  {
    name: "round5-time-both-ways",
    why: "read time as the keyword wherever any shell has it. In dash a local time runs instead, and `time no-such-runner` it passes reads as broken",
    test: "test/detect-shell-scripts-round5.test.mjs",
    expectRed: "time where only some of the shells have it as a keyword is read both ways",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "    if (shell.allKeywords && !shell.allKeywords.has(\"time\")) {",
              replace: "    if (false) {" }],
  },
  {
    name: "round5-all-keywords",
    why: "merge shells without saying which keywords all of them have. time then reads as every shell's keyword, as bash's alone is",
    test: "test/detect-shell-scripts-round5.test.mjs",
    expectRed: "shells merged for a script say which keywords every one of them has",
    edits: [{ file: "src/profile/shellscript.mjs",
              find: "           allKeywords: new Set([...shells[0].keywords].filter((k) => shells.every((s) => s.keywords.has(k)))),\n",
              replace: "" }],
  },
  {
    name: "temp-watcher-told-before-the-folder",
    why: "make this process's folder before the watcher is told of it. A test killed in between leaves the folder, and nothing removes it",
    test: "test/temp-kill-anywhere.test.mjs",
    expectRed: "a test killed the moment its first folder exists leaves nothing",
    edits: [{ file: "test/fixtures/temp.mjs",
              find: "    watch(dir);\n    mkdirSync(dir, { mode: 0o700 });\n",
              replace: "    mkdirSync(dir, { mode: 0o700 });\n    watch(dir);\n" }],
  },
  {
    name: "temp-folders-inside-home",
    why: "make each folder straight in the temp directory, as before. The watcher knows only of this process's own folder, and a test killed the moment another exists leaves it",
    test: "test/temp-kill-anywhere.test.mjs",
    expectRed: "a test killed the moment a later folder exists leaves nothing",
    edits: [{ file: "test/fixtures/temp.mjs",
              find: "  return mkdtempSync(join(home, prefix));",
              replace: "  return mkdtempSync(join(tmpdir(), prefix));" }],
  },
  {
    name: "typecheck-erasable-syntax-only",
    why: "let the type check take TypeScript Node can't strip. An enum then checks clean, and throws ERR_UNSUPPORTED_TYPESCRIPT_SYNTAX when Node loads it",
    test: "test/typecheck-node-only.test.mjs",
    expectRed: "the type check refuses TypeScript Node can't strip: an enum, or a namespace with code in it",
    edits: [{ file: "tsconfig.json",
              find: "    \"erasableSyntaxOnly\": true,\n",
              replace: "" }],
  },
  {
    name: "typecheck-no-browser-types",
    why: "give the type check the browser's library, as target es2024 does by default. `document` then checks clean, and throws in Node",
    test: "test/typecheck-node-only.test.mjs",
    expectRed: "the type check has no browser types: document is no global, and Node's own are",
    edits: [{ file: "tsconfig.json",
              find: "    \"lib\": [\n      \"es2024\"\n    ],\n",
              replace: "" }],
  },
  {
    name: "lint-esm-globals-only",
    why: "give lint Node's CommonJS globals too. require, module and __dirname then pass lint in an .mjs file, where each throws",
    test: "test/typecheck-node-only.test.mjs",
    expectRed: "lint flags CommonJS's names in an ES module, where each throws, and not Node's own globals",
    edits: [{ file: "eslint.config.js",
              find: "globals: globals.nodeBuiltin",
              replace: "globals: globals.node" }],
  },
];
